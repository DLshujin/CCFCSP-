# 第13章 二分查找

!!! info "本章内容"
    二分查找是 CSP-S 高频考点，也是"二分答案"这一类最优化问题的基石。本章你将学习：

    - 二分查找的原理与前提
    - 整数二分两个模板（找左边界 / 找右边界）
    - 实数二分与 EPS 精度控制
    - 二分答案：把判定问题转为最优化问题

!!! warning "本章特点"
    二分是**初赛复赛都考**的重点。CSP-S 中"二分答案"几乎是每年必出的思想，掌握它能稳定拿下中难题。

---

## 📌 学习目标

学完本章，你应当能够：

1. **准确默写**整数二分的两个模板（找左边界 / 找右边界），并能说清 `l=mid` 与 `mid=l+(r-l+1)/2` 的配套关系
2. 在 **10 分钟内**完成有序数组的查找问题（如 P2249），复杂度控制在 $O(n \log n)$ 以内
3. 掌握实数二分的 **EPS 精度控制**，能在一元三次方程（P1024）中求出符合精度的根
4. 识别"**最大值最小化**""最小值最大化"两类信号，独立写出 `check` 函数完成二分答案
5. 独立 AC 跳石头（P2678）等二分答案经典题

---

## 💡 生活类比

> **二分查找像"猜数字游戏"**：我心里想一个 1~100 的数，你来猜。
> - 你猜 50，我说"大了"，你就知道答案在 1~49；
> - 你猜 25，我说"小了"，答案在 26~49；
> - 你猜 37，我说"大了"，答案在 26~36……
>
> 每次砍掉一半，**最多 7 次**（$\lceil\log_2 100\rceil = 7$）就能猜中。

这就是二分的精髓：**有序 + 折半**。把搜索范围每次缩小一半，效率极高。

---

## 核心概念

### 一、二分查找的原理

「二分查找 binary search」：在**有序序列**中，每次取中间值与目标比较，**砍掉不可能的一半**，直到找到目标或范围为空。

**两个前提**：

1. **序列有序**（升序或降序）
2. **能根据中间值判断目标在哪一半**（即具有"单调性"）

**复杂度**：$O(\log n)$。当 $n=10^6$ 时只需约 20 次比较，而顺序查找最坏要 $10^6$ 次。

**过程演示**：在有序数组 `[1, 3, 5, 7, 9, 11, 13]` 中找 `7`：

```
第1次：l=0, r=6, mid=0+(6-0)/2=3
       a[3]=7 == 7，找到了！
```

如果找 `8`：

```
第1次：l=0, r=6, mid=3
       a[3]=7 < 8 → 在右半边 → l = mid+1 = 4
第2次：l=4, r=6, mid=4+(6-4)/2=5
       a[5]=11 > 8 → 在左半边 → r = mid-1 = 4
第3次：l=4, r=4, mid=4
       a[4]=9 > 8 → r = mid-1 = 3
l > r，结束，没找到
```

!!! warning "核心思想迁移"
    二分不只能"在数组里找数"。只要答案空间具有**单调性**（越大越容易/越难满足），就能二分。这就是"二分答案"的来源。

### 二、整数二分两个模板

整数二分最大的坑在**边界**。下面两个模板是竞赛通用的"红蓝染色"写法，背下来终身受用。

**红蓝染色思想**：把数组想象成两段——左段"不满足条件"（红），右段"满足条件"（蓝）。二分就是要找到红蓝的分界点。
- 找左边界 = 找**第一个蓝**（第一个满足的）
- 找右边界 = 找**最后一个红**（最后一个不满足的反面）

这样理解，模板的 `r=mid` / `l=mid` 就有了直观含义：满足时把分界点压向左（找第一个），不满足时压向右。

| | 模板一：找左边界 | 模板二：找右边界 |
|---|---|---|
| **含义** | 找**第一个**满足条件的位置 | 找**最后一个**满足条件的位置 |
| **类比 STL** | `lower_bound` | `upper_bound - 1` |
| **循环条件** | `while (l < r)` | `while (l < r)` |
| **mid 取法** | `mid = l + (r-l)/2` | `mid = l + (r-l+1)/2` ← 注意 +1 |
| **满足时** | `r = mid` | `l = mid` |
| **不满足时** | `l = mid + 1` | `r = mid - 1` |

!!! tip "为什么模板二 mid 要 +1？"
    当 `l = mid`（左边界不缩）时，若 `mid = l + (r-l)/2`（向下取整），在 `r = l+1` 时 `mid` 会等于 `l`，于是 `l = mid` 不变 → **死循环**。
    `mid = l + (r-l+1)/2` 让 `mid` 向上取整，保证 `mid` 能突破 `l`，循环才能结束。

**记忆口诀**：

> **`r = mid` 配 `mid = l+(r-l)/2`**（向下取整，安全）  
> **`l = mid` 配 `mid = l+(r-l+1)/2`**（向上取整，防死循环）

**两模板手算对比**：数组 `[1, 2, 2, 2, 3]`，target=2。

```
模板一(找第一个≥2)：l=0,r=4
  mid=2, a[2]=2≥2 → r=2
  mid=1, a[1]=2≥2 → r=1
  mid=0, a[0]=1<2 → l=1
  l==r=1，答案=1（第一个2的位置）✓

模板二(找最后一个≤2)：l=0,r=4
  mid=2, a[2]=2≤2 → l=2   (mid向上取整=0+(4-0+1)/2=2)
  mid=3, a[3]=2≤2 → l=3   (mid=2+(4-2+1)/2=3)
  mid=4, a[4]=3>2 → r=3   (mid=3+(4-3+1)/2=4)
  l==r=3，答案=3（最后一个2的位置）✓
```

### 三、实数二分

实数二分没有"取整"问题，`mid = (l+r)/2` 即可，但要用 **EPS** 控制精度：

```cpp
while (r - l > EPS) {
    double mid = (l + r) / 2;
    if (check(mid)) l = mid;   // 或 r = mid，视题意
    else r = mid;
}
```

- 一般取 `EPS = 1e-6` 或 `1e-7`
- 也可固定循环次数（如 100 次），更稳妥，避免精度陷阱

**EPS 经验值**：题目要求保留 $k$ 位小数时，取 `EPS = 1e-(k+2)`。例如保留 2 位小数用 `1e-4`，保留 6 位用 `1e-8`。多 2 位是给二分收敛留余量，避免四舍五入时进位出错。

!!! tip "固定循环次数写法"
    ```cpp
    for (int i = 0; i < 100; i++) {   // 100次足够把区间缩小到 2^-100
        double mid = (l + r) / 2;
        if (check(mid)) l = mid; else r = mid;
    }
    ```
    这种写法不用纠结 EPS，循环 100 次后 `l` 与 `r` 几乎相等，输出 `l` 即可。

### 四、二分答案

「二分答案」：题目求"最大的最小值""最小的最大值"等最优化问题，若答案具有**单调性**，就把"求最优解"转为"判定某个值是否可行"，用二分逐步逼近。

**四步套路**：

| 步骤 | 内容 | 跳石头示例 |
|------|------|-----------|
| ① 找单调性 | 答案越大，越难/越易满足 | 最短跳跃距离越大，要移走的石头越多 |
| ② 定范围 | 答案落在 `[L, R]` | `[0, 终点距离]` |
| ③ 写 check | `bool check(mid)` 判断可行 | 贪心统计移走石头数 ≤ M |
| ④ 二分边界 | 满足→抬高，不满足→降低 | 可行→`l=mid+1`，不可行→`r=mid-1` |

**常见信号词**："最大值最小化""最小值最大化""最近的两个最远"……

### 五、二分查找 vs 二分答案对比

| 维度 | 二分查找 | 二分答案 |
|------|---------|---------|
| **对象** | 在**有序数组**中找元素 | 在**答案区间**中找最优值 |
| **前提** | 数组有序 | 答案单调 |
| **check** | `a[mid]` 与目标比较 | 自定义 `check(mid)` 判定可行性 |
| **典型问法** | "找第一个/最后一个 ≥ x" | "最大的最小值""最小的最大值" |
| **复杂度** | $O(\log n)$ | $O(\log V \cdot T_{check})$，$V$ 为答案范围 |
| **代表题** | P2249 查找 | P2678 跳石头 |

!!! tip "如何区分"
    - 题目给数组、问"找某值"→ 二分**查找**
    - 题目求"使某某最大/最小"且答案有范围 → 二分**答案**

### 六、二分解题流程清单

遇到一道题，按以下流程判断能否用二分：

1. **有没有单调性**？答案/序列是否随某变量单调变化？
2. **答案有没有范围**？能确定 `[L, R]` 吗？
3. **能不能写 check**？给定 mid，能否在多项式时间内判定可行？
4. **选模板**：找左边界用模板一，找右边界用模板二，二分答案通常用 `while(l<=r)` 配 `ans` 记录。
5. **查边界**：mid 防溢出、`l=mid` 配向上取整、check 方向正确。

---

## 代码实现

### 一、整数二分模板一：找左边界

在有序数组中找**第一个 ≥ target** 的位置（等价 `lower_bound`）：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, target;
    cin >> n >> target;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];

    int l = 0, r = n - 1;
    while (l < r) {
        int mid = l + (r - l) / 2;     // 防溢出 + 向下取整
        if (a[mid] >= target) r = mid; // 满足，缩右边界
        else l = mid + 1;              // 不满足，缩左边界
    }
    // l == r 即为答案位置
    if (a[l] >= target) cout << l;
    else cout << -1;                   // 所有元素都 < target
    return 0;
}
```

### 二、整数二分模板二：找右边界

在有序数组中找**最后一个 ≤ target** 的位置（等价 `upper_bound - 1`）：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, target;
    cin >> n >> target;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];

    int l = 0, r = n - 1;
    while (l < r) {
        int mid = l + (r - l + 1) / 2; // 向上取整，防死循环
        if (a[mid] <= target) l = mid; // 满足，缩左边界
        else r = mid - 1;              // 不满足，缩右边界
    }
    if (a[l] <= target) cout << l;
    else cout << -1;
    return 0;
}
```

!!! tip "统计某值出现次数"
    `upper_bound - lower_bound` = 该值出现次数。手写即：左边界(第一个≥x) 与 右边界(最后一个≤x) 配合，两位置之差即为个数。

### STL 函数：lower_bound 与 upper_bound

C++ 自带二分函数，竞赛中可直接调用，省去手写：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a[] = {1, 2, 2, 2, 3, 5, 7};
    int n = 7;

    // lower_bound：找第一个 >= 2 的位置
    int p1 = lower_bound(a, a + n, 2) - a;
    cout << p1 << endl;                    // 输出 1

    // upper_bound：找第一个 > 2 的位置
    int p2 = upper_bound(a, a + n, 2) - a;
    cout << p2 << endl;                    // 输出 4

    // 统计 2 出现的次数
    cout << p2 - p1;                       // 输出 3
    return 0;
}
```

| 函数 | 含义 | 等价模板 |
|------|------|---------|
| `lower_bound` | 第一个 ≥ x 的位置 | 模板一(找左边界) |
| `upper_bound` | 第一个 > x 的位置 | 模板一变形 |
| `upper_bound - 1` | 最后一个 ≤ x 的位置 | 模板二(找右边界) |

!!! tip "竞赛建议"
    简单查找直接用 STL；但二分答案、自定义 check 等场景必须**手写**，所以两模板都要熟练。

### 三、实数二分（EPS 精度控制）

求函数 $f(x)$ 在 $[L,R]$ 上的零点（已知该区间内单调且变号）：

```cpp
#include <bits/stdc++.h>
using namespace std;

double a, b, c, d;
double f(double x) { return a*x*x*x + b*x*x + c*x + d; }

int main() {
    cin >> a >> b >> c >> d;
    double L, R;
    cin >> L >> R;                      // 已确定 [L,R] 内有唯一零点
    const double EPS = 1e-7;
    while (R - L > EPS) {
        double mid = (L + R) / 2;
        if (fabs(f(mid)) < 1e-9) { L = R = mid; break; }
        if (f(L) * f(mid) < 0) R = mid; // 零点在 [L,mid]
        else L = mid;                   // 零点在 [mid,R]
    }
    printf("%.6f", L);
    return 0;
}
```

!!! warning "实数二分的精度细节"
    `f(L) * f(mid)` 可能因数值过大产生溢出或精度误差。稳妥做法是分别判断 `f(L)` 与 `f(mid)` 的正负号，避免相乘。EPS 一般比题目要求精度多 1~2 位。

### 四、二分答案示例（砍树）

$n$ 棵树，锯高 $H$ 时砍下 $\sum\max(0,h_i-H)$。要求至少砍 $M$ 米，求 $H$ 最大值。

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    long long M;
    cin >> n >> M;
    vector<int> h(n);
    int maxH = 0;
    for (int i = 0; i < n; i++) { cin >> h[i]; maxH = max(maxH, h[i]); }

    long long l = 0, r = maxH, ans = 0;
    while (l <= r) {
        long long mid = l + (r - l) / 2;  // 防溢出
        long long total = 0;
        for (int i = 0; i < n; i++)
            if (h[i] > mid) total += h[i] - mid;
        if (total >= M) { ans = mid; l = mid + 1; }  // 够了，再高一点
        else r = mid - 1;
    }
    cout << ans;
    return 0;
}
```

---

## ⚠️ 常见误区

!!! danger "误区一：边界条件写错导致死循环（最致命）"
    **`l = mid` 必须配 `mid = l + (r-l+1)/2`（向上取整）**，否则当 `r = l+1` 时 `mid` 向下取整等于 `l`，`l = mid` 永远不动 → **死循环**。

    错误写法：
    ```cpp
    while (l < r) {
        int mid = l + (r - l) / 2;     // ✗ 向下取整
        if (a[mid] <= target) l = mid; // ✗ 配套错误，死循环！
        else r = mid - 1;
    }
    ```
    正确配套关系：
    - `r = mid`  ←→  `mid = l + (r-l)/2`（向下取整）
    - `l = mid`  ←→  `mid = l + (r-l+1)/2`（向上取整）

    **一句话**：谁不动（`l=mid` 表示 l 不动），mid 就向谁那边取整（向上取整靠近 l→r 方向突破）。

!!! warning "误区二：忘了数组必须有序"
    二分查找的前提是**有序**。在无序数组上二分会得到错误结果。竞赛中先 `sort` 再二分，整体复杂度仍为 $O(n\log n)$。

!!! warning "误区三：mid 用 (l+r)/2 有溢出风险"
    当 `l`、`r` 接近 `int` 上界（约 $2.147\times10^9$）时，`l+r` 会溢出。**永远用 `mid = l + (r-l)/2`**。
    
    二分答案时答案范围常达 $10^9$，两端相加可能超过 `int` 上界。建议答案变量直接用 `long long`，并坚持防溢出写法。

!!! tip "误区四：实数二分精度不足或循环过多"
    - EPS 太大（如 $10^{-3}$）答案不准；
    - EPS 太小（如 $10^{-12}$）可能因浮点误差永远达不到，陷入死循环。
    - 经验值：题目要求保留 $k$ 位小数，取 `EPS = 1e-(k+2)`；或固定循环 100 次，稳定可靠。

!!! warning "误区五：二分答案 check 方向写反"
    求"最大化最小值"时，`check(mid)` 为真应让答案**更大**（`l = mid + 1`），反之则更小。初学者常把方向搞反，导致答案恒为边界值。
    
    **判断技巧**：问自己"mid 可行时，更优解在更大还是更小的一侧？"
    - 最大化问题：可行 → 往大找（`l = mid + 1`）
    - 最小化问题：可行 → 往小找（`r = mid - 1`）

---

## 典型例题

### 【P2678】跳石头（NOIP 2015 提高组，二分答案经典）

**题目**：赛道起点到终点相距 $L$，中间有 $n$ 块石头（不含起点终点）。组委会最多移走 $m$ 块石头，求**最短跳跃距离的最大值**。

**分析**：典型的"**最大化最小值**"——二分答案的信号词。设最短跳跃距离为 $x$，$x$ 越大需要移走的石头越多，具有单调性。

**单调性论证**：若最短距离 $x$ 可行（移走 ≤ $m$ 块即可达成），则 $x-1$ 也一定可行（要求更宽松）；反之 $x$ 不可行，则 $x+1$ 更不可行。满足单调性，可二分。

**check 函数**：贪心。从左到右走，若当前石头到上一块的距离 $< x$，就移走这块石头；统计移走总数，若 $\leq m$ 则 $x$ 可行。

**手算示例**：$L=25$，石头位置 `2 11 14 17 21`，$m=2$。

```
二分 x：l=1, r=25
试 x=13：2→0距离2<13 移走；11→0距离11<13 移走；
        14→0距离14≥13 保留last=14；17→14距离3<13 移走；
        21→14距离7<13 移走；终点25→14距离11<13 移走
        共移走5块 > 2，不可行 → r=12
试 x=4：2保留；11→2=9≥4保留；14→11=3<4移走；17→11=6≥4保留；
        21→17=4≥4保留；终点25→21=4≥4。共移走1块 ≤2，可行 → ans=4, l=5
继续二分... 最终 ans=4
```

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    long long L;                     // 起点到终点距离
    int n, m;                        // 石头数、最多移走数
    cin >> L >> n >> m;
    vector<long long> a(n + 2);      // a[0]=起点0, a[1..n]=石头, a[n+1]=终点L
    for (int i = 1; i <= n; i++) cin >> a[i];
    a[0] = 0; a[n + 1] = L;

    long long l = 1, r = L, ans = 0;
    while (l <= r) {
        long long mid = l + (r - l) / 2;  // 防溢出
        int cnt = 0;                      // 移走石头数
        long long last = 0;               // 上一块未移走石头位置
        for (int i = 1; i <= n + 1; i++) {
            if (a[i] - last < mid) cnt++; // 距离不够，移走当前石头
            else last = a[i];             // 保留，更新上一块
        }
        if (cnt <= m) { ans = mid; l = mid + 1; }  // 可行，尝试更大
        else r = mid - 1;                          // 不可行，缩小
    }
    cout << ans;
    return 0;
}
```

!!! tip "二分答案三件套"
    1. **单调性**：$x$ 越大越难满足（移走石头越多）
    2. **check**：贪心判定，$O(n)$
    3. **边界**：可行就 `l=mid+1` 抬高答案，不可行 `r=mid-1` 降低

总复杂度 $O(n \log L)$，$n\leq5\times10^4, L\leq10^9$ 时约 $5\times10^4 \times 30 \approx 1.5\times10^6$，轻松 AC。

---

## 📚 本节练习

### 🟢 基础题（CSP-J 难度，巩固模板）

**练习 1**：[P2249] 查找 — https://www.luogu.com.cn/problem/P2249
> 在递增序列中多次查找某个数**第一次**出现的位置。
**提示**：找"第一个 ≥ x 且等于 x"的位置，正是**模板一（找左边界）**的用武之地。也可直接 `lower_bound`。注意输出位置从 1 开始，找不到输出 -1。复杂度 $O(n + q\log n)$（先读入再 $q$ 次二分）。
**避坑**：用 `cin/cout` 关同步或 `scanf`，本题查询次数多，注意 I/O 速度；不要每次查询都顺序扫描，会超时。

**练习 2**：[P1102] A-B 数对 — https://www.luogu.com.cn/problem/P1102
> 统计数列中满足 $A - B = C$ 的数对 $(A,B)$ 数量。
**提示**：转化 $A = B + C$。排序后对每个 $B$，用 `upper_bound - lower_bound` 统计 $B+C$ 出现次数。也可用哈希。复杂度 $O(n\log n)$。
**避坑**：$C$ 可能为负或 0；当 $C=0$ 时数对 $(x,x)$ 也算，注意 `lower_bound` 与 `upper_bound` 配合正好统计重复元素个数。

### 🟡 拓展题（CSP-S 入门，需思考）

**练习 3**：[P1024] 一元三次方程求解 — https://www.luogu.com.cn/problem/P1024
> 求 $ax^3+bx^2+cx+d=0$ 的三个不同实根（保留 2 位小数）。
**提示**：题目保证相邻根差 $\geq 1$。枚举整数区间 $[i, i+1]$（$i$ 从 $-100$ 到 $99$），若 $f(i)\cdot f(i+1) \le 0$ 则该区间有根，用**实数二分（EPS=1e-4）**逼近。注意端点恰好为根的情况要单独判断。
**避坑**：$f(i)\cdot f(i+1)$ 用乘法判断变号时，若一端为 0 要直接输出该端点，避免漏根；区间步长用 1 而非更小，否则可能重复输出同一根。

**练习 4**：[P1873] 砍树 — https://www.luogu.com.cn/problem/P1873
> 锯高 $H$ 时砍下 $\sum\max(0,h_i-H)$，要求至少 $M$ 米，求 $H$ 最大值。
**提示**：典型二分答案。$H$ 越大砍得越少，单调。直接套本章"二分答案示例"模板，注意 $M$ 和总木材用 `long long` 防溢出。
**避坑**：$H$ 可以为 0（一棵都不砍完），故 `l` 从 0 开始；`total` 累加可能极大，务必 `long long`。

### 🔴 CSP-S 真题（进阶，综合应用）

**练习 5**：[P2678] 跳石头 — https://www.luogu.com.cn/problem/P2678
> 移走最多 $m$ 块石头，最大化最短跳跃距离。
**提示**：即本章典型例题，建议先自己写一遍再对照。关键在 `check` 的贪心：距离不够就移走当前块。注意终点也要参与距离判定。
**避坑**：起点 0 和终点 $L$ 都不可移走，但终点要参与距离比较（若最后一块到终点距离不够，需移走该块）；二分上界取 $L$，下界取 1。

**练习 6**：[P1182] 数列分段 Section II — https://www.luogu.com.cn/problem/P1182
> 将 $n$ 个正整数分成 $m$ 段，使各段和的最大值**最小**。
**提示**："最小化最大值"——二分答案。二分"段和上限 mid"，贪心 check：从左到右累加，超 mid 就新开一段，统计段数。段数 $\leq m$ 说明 mid 可行、尝试更小（`r=mid-1`）；段数 $> m$ 说明 mid 太小（`l=mid+1`）。注意段和可能很大，用 `long long`。
**避坑**：二分下界取 $\max(a_i)$（至少容纳最大元素），上界取 $\sum a_i$；段数统计时若某元素本身 > mid 则 mid 不可行，但本题数据保证不会出现。

---

## 📖 本章总结

### 核心要点回顾

- ✅ 二分前提：**有序 / 单调性**
- ✅ 复杂度 $O(\log n)$，高效
- ✅ 整数二分两模板：找左边界 `r=mid` 配向下取整；找右边界 `l=mid` 配向上取整
- ✅ 实数二分用 **EPS** 控制精度
- ✅ 二分答案四步：单调性→范围→check→边界
- ✅ 信号词："最大值最小化""最小值最大化"
- ✅ **永远用 `mid = l + (r-l)/2` 防溢出**

### 知识脉络

- **二分查找**
  - **整数二分**
    - 模板一（找左边界）：`r = mid` 配 `mid = l + (r-l)/2`（向下取整）
    - 模板二（找右边界）：`l = mid` 配 `mid = l + (r-l+1)/2`（向上取整，防死循环）
    - 防溢出写法：`mid = l + (r-l)/2`
    - 常见误区：`l = mid` 必须配向上取整，否则死循环
  - **实数二分**
    - EPS 精度控制：`while (r - l > EPS)`
    - 固定循环次数：`for (int i = 0; i < 100; i++)`（更稳妥）
    - EPS 经验值：保留 $k$ 位小数取 `1e-(k+2)`
  - **二分答案**
    - 单调性识别：答案越大越难/越易满足
    - `check` 函数：贪心判定，$O(n)$
    - 边界处理：可行 → 抬高答案，不可行 → 降低答案
    - 典型题：P2678 跳石头（最大化最小值）、P1873 砍树、P1182 数列分段（最小化最大值）

### 题目速查表

| 题目 | 难度 | 考点 | 模板 |
|------|------|------|------|
| P2249 查找 | 🟢 | 找第一个位置 | 模板一(找左边界) |
| P1102 A-B 数对 | 🟢 | lower/upper_bound | 两模板配合 |
| P1024 一元三次方程 | 🟡 | 实数二分 | EPS 精度 |
| P1873 砍树 | 🟡 | 二分答案 | check+边界 |
| P2678 跳石头 | 🔴 | 最大化最小值 | 二分答案经典 |
| P1182 数列分段 | 🔴 | 最小化最大值 | 贪心 check |

### 知识衔接

本章是CSP-J/S入门教材模块二（基础算法）的重要章节，与前后章节的联系如下：

- **前置知识**：第12章「排序算法」——排序使数据有序，二分查找的前提条件就是序列有序
- **本章定位**：O(log n)搜索的利器，掌握二分查找（在有序序列中快速定位）与二分答案（对答案空间进行二分），是CSP-S高频得分点
- **后续发展**：为第14章「前缀和与差分」铺路——继续学习区间操作的高效技巧，二者常配合使用解决区间问题

---

!!! tip "下一章预告"
    二分掌握了，下一步学**二分答案的进阶应用**与**三分法**（求单峰函数极值）。第14章我们将学习前缀和与差分——把O(n)的区间查询降到O(1)、把O(n)的区间修改降到O(1)！
