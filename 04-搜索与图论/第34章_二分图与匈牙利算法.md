# 第34章 二分图与匈牙利算法

> 前面几章我们学习了图的存储、遍历、最短路、拓扑排序等内容，它们处理的都是"一般图"上的问题。本章将聚焦一类结构特殊的图——**二分图**。二分图不仅判定方法简洁（染色法），其上的"最大匹配"问题还有经典而优雅的**匈牙利算法**。这套思想在 CSP-J/S 中既是模板题常客，也频繁以"矩阵游戏""假期的宿舍"等巧妙建模形式出现。

!!! info "本章内容"
    二分图的定义与判定（染色法 DFS/BFS）、奇环与二分图的等价关系、匹配与增广路的概念、**匈牙利算法**求最大匹配的完整实现、König 定理（最小点覆盖 = 最大匹配），并配有三道经典习题的建模与代码。

!!! warning "本章定位"
    二分图匹配是 CSP-S 的高频考点，CSP-J 偶有涉及。匈牙利算法代码不长（核心仅十几行），但"增广路"的思想理解门槛较高。学完本章后，你应当能把"两类事物配对"的问题自动转化为"二分图最大匹配"。

---

## 📌 学习目标

学完本章后，你应当能够：

1. **准确说出**二分图的定义，并解释"二分图当且仅当不含奇环"
2. **默写**染色法（DFS/BFS）判定二分图的完整代码
3. **解释**匹配、极大匹配、最大匹配、增广路、交替路的概念
4. **默写**匈牙利算法的递归实现（`match[]` + `vis[]`）
5. **避开**常见误区：`vis` 数组每轮重置的位置、递归深度过大、建图方向搞反
6. **陈述** König 定理：二分图最小点覆盖 = 最大匹配
7. **运用**匈牙利算法解决"矩阵游戏""假期的宿舍"等建模题

---

## 💡 生活类比

### 类比一：男女配对舞会

想象一场舞会，男生站左边、女生站右边，**只有男女之间才能结对跳舞**，男生之间、女生之间都不互相配对。把每个人看作顶点，"可以配对"的关系看作边——这就是一张**二分图**：顶点被分成两部分，边只跨两部分之间存在，**同一部分内部没有边**。

```
   左部（男生）              右部（女生）
   ┌─────┐                 ┌─────┐
   │  A  │─────────────────│  X  │
   │     │─────────────────│     │
   │  B  │──\              │  Y  │
   │     │   \─────────────│     │
   │  C  │─────────────────│  Z  │
   └─────┘                 └─────┘
   注意：A-B、A-C 之间没有边；X-Y、Y-Z 之间也没有边。
```

### 类比二：腾位置找新搭档

匈牙利算法像一场"**腾位置找新搭档**"的游戏。A 想和 X 配对，但 X 已经和 B 配对了。于是算法去问 B："你能不能换一个搭档？" B 去找还没被占用（或能让出位置）的 Y，若成功，B 改配 Y，X 就腾出来给 A。这条"A→X→B→Y"的路径，就是**增广路**——沿路径把配对状态全部翻转，匹配数 +1。

!!! tip "一句话记忆"
    二分图像"男女配对舞会"——边只在两性之间；匈牙利算法像"腾位置找新搭档"——沿增广路翻转配对，让总对数多一对。

---

## 第一节 二分图的定义

### 1.1 什么是二分图

「二分图 Bipartite Graph」是指顶点集合 $V$ 可以划分为两个**不相交**的子集 $L$ 和 $R$（$V = L \cup R$，$L \cap R = \emptyset$），使得图中每条边的两个端点**一个属于 $L$、一个属于 $R$**。换言之，$L$ 内部没有边，$R$ 内部也没有边，所有边都跨在 $L$ 与 $R$ 之间。

形式化地，图 $G=(V,E)$ 是二分图 $\Leftrightarrow$ 存在划分 $V=L \cup R$，$\forall (u,v) \in E$ 有 $u \in L, v \in R$ 或 $u \in R, v \in L$。

```
二分图示例：              非二分图示例（含三角形奇环）：
   L         R               1 ─── 2
   1 ─────── 4               │     │
   │  \      │               │     │
   │   \    │                3 ────┘
   2 ─────── 5               三角形 1-2-3-1 是长度为 3 的奇环
   │         │               → 不是二分图
   3 ─────── 6
```

### 1.2 二分图与奇环的等价关系

二分图有一个极其重要的等价刻画：

!!! info "核心定理：二分图 ⟺ 不含奇环"
    一个无向图是二分图，**当且仅当**它不包含长度为奇数的环（奇环）。

**直观理解**：在二分图中，从 $L$ 出发走一条边到 $R$，再走一条边回到 $L$……每走一步切换一次"部"。绕一圈回到起点，走的步数必须是偶数（才能回到原来的部），所以环长必为偶数。反过来，若图无奇环，可以用染色法（下一节）成功给所有点涂两色，从而证明它是二分图。

| 图的性质 | 是否二分图 | 原因 |
|----------|:----------:|------|
| 树（无环） | ✅ 是 | 没有任何环，自然无奇环 |
| 偶环（长度为偶数的环） | ✅ 是 | 环长为偶数，可两色交替染色 |
| 奇环（长度为奇数的环） | ❌ 否 | 绕一圈回到起点颜色矛盾 |
| 含三角形的图 | ❌ 否 | 三角形是长度 3 的奇环 |
| 完全二分图 $K_{m,n}$ | ✅ 是 | 定义本身就是二分图 |

---

## 第二节 染色法判定二分图

### 2.1 核心思想

**染色法**：用两种颜色（如 0 和 1）给所有顶点染色，使得相邻顶点颜色不同。若能成功染完所有点，则是二分图；若某一步发现"当前点与其邻居同色"，则存在奇环，不是二分图。

```
染色过程（DFS）：
   起点 1 染色 0
        1(0) ──── 2(1)
        │          │
       3(1) ──── 4(0)     成功！所有相邻点异色 → 二分图

   起点 1 染色 0
        1(0) ──── 2(1)
        │          │
       3(1) ──── 4(0)
        │          │
        └──────────┘       若 4-1 有边：1(0) 与 4(0) 同色 → 失败！有奇环
```

### 2.2 DFS 实现

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 100005;
vector<int> g[MAXN];
int color[MAXN];   // -1: 未染色; 0/1: 两种颜色
int n, m;

// 返回是否能成功染色 u 为 c
bool dfs(int u, int c) {
    color[u] = c;
    for (int v : g[u]) {
        if (color[v] == -1) {
            // 邻居未染色，染成相反颜色
            if (!dfs(v, c ^ 1)) return false;
        } else if (color[v] == c) {
            // 邻居已染色且同色 → 奇环
            return false;
        }
    }
    return true;
}

int main() {
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        g[u].push_back(v);
        g[v].push_back(u);
    }
    memset(color, -1, sizeof(color));

    bool ok = true;
    for (int i = 1; i <= n; i++) {
        if (color[i] == -1) {
            if (!dfs(i, 0)) { ok = false; break; }
        }
    }
    cout << (ok ? "Yes" : "No") << endl;
    return 0;
}
```

### 2.3 BFS 实现

BFS 写法用队列代替递归，**避免递归过深导致栈溢出**，在链状图（如一条长链）上更安全：

```cpp
bool bfs(int start) {
    queue<int> q;
    q.push(start);
    color[start] = 0;
    while (!q.empty()) {
        int u = q.front(); q.pop();
        for (int v : g[u]) {
            if (color[v] == -1) {
                color[v] = color[u] ^ 1;
                q.push(v);
            } else if (color[v] == color[u]) {
                return false;
            }
        }
    }
    return true;
}
```

!!! tip "DFS vs BFS 的选择"
    点数 $n$ 较大（如 $10^5$ 以上）且图可能呈长链状时，DFS 可能因递归深度过大而爆栈（RE），此时优先用 BFS。CSP 中若不确定，统一用 BFS 更稳妥。

### 2.4 复杂度

| 实现 | 时间复杂度 | 空间复杂度 | 备注 |
|------|:----------:|:----------:|------|
| DFS 染色 | $O(n+m)$ | $O(n+m)$ | 递归栈可能过深 |
| BFS 染色 | $O(n+m)$ | $O(n+m)$ | 队列实现，更安全 |

---

## 第三节 匹配与增广路

### 3.1 匹配的定义

在二分图 $G=(L \cup R, E)$ 中：

- **匹配 Matching**：边集 $M \subseteq E$，且 $M$ 中任意两条边**没有公共端点**（即每个顶点最多被一条匹配边覆盖）。
- **最大匹配 Maximum Matching**：边数最多的匹配。
- **完美匹配 Perfect Matching**：所有顶点都被匹配边覆盖的匹配（要求 $|L|=|R|$）。

```
二分图：        一个匹配（2条边）：      最大匹配（3条边）：
  L      R        L      R              L      R
  1 ─── 4         1 ═══ 4               1 ═══ 4
  │  \   │        │      │              │      │
  2 ─── 5         2      5               2 ═══ 5
  │      │        │      │              │      │
  3 ─── 6         3 ═══ 6               3 ═══ 6
  匹配数 = 3         匹配数 = 2              匹配数 = 3（最大）
```

### 3.2 增广路：匈牙利算法的灵魂

「**交错路**」：从未匹配点出发，依次经过"非匹配边、匹配边、非匹配边、匹配边……"的路径。
「**增广路 Augmenting Path**」：一条**以另一个未匹配点结尾**的交错路。

增广路的关键性质：**非匹配边比匹配边多一条**。把增广路上的边状态全部翻转（匹配边变非匹配边，非匹配边变匹配边），匹配数 **+1**。

```
增广路示例（A 未匹配，Y 未匹配）：
   A ---X===B ---Y
   非匹配 边 匹配 非匹配

翻转后：
   A ===X   B ===Y
   匹配     匹配

匹配数从 1（仅 X-B）→ 2（A-X 与 B-Y）
```

!!! info "匈牙利算法的本质"
    匈牙利算法就是**反复寻找增广路**：从左部每个未匹配点出发尝试找增广路，找到就翻转，匹配数 +1；找不到就跳过。当所有左部点都尝试过后，得到的就是最大匹配。

---

## 第四节 匈牙利算法

### 4.1 算法思想

**生活类比**：左部的 A 想找右部的搭档。它依次扫描自己能连的右部点 v：

- 若 v 还没被匹配 → 直接配对，成功。
- 若 v 已被左部的 u 匹配 → 试着让 u **另寻搭档**（递归调用 `dfs(u)`）。如果 u 能找到新的搭档，v 就腾出来给 A，成功。
- 若 u 找不到新搭档 → 这条路走不通，A 换下一个右部点试试。

这一过程就是"**腾位置找新搭档**"——每次增广都让匹配数 +1。

### 4.2 算法流程

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 枚举左部每个点 $u$ | 尝试为它找匹配 |
| 2 | 每轮重置 `vis[]` 为 false | 标记本轮右部点是否被尝试过 |
| 3 | 对 $u$ 调用 `dfs(u)` 寻找增广路 | 沿邻接边尝试 |
| 4 | `dfs` 内遍历 $u$ 能连到的右部点 $v$ | 若 $v$ 未 vis 则标记并尝试 |
| 5 | 若 $v$ 未匹配或 `dfs(match[v])` 成功 | 让出或抢占，`match[v]=u`，返回 true |
| 6 | 统计匹配数 | `match[v]!=0` 的 $v$ 个数 |

### 4.3 关键数据结构

```cpp
vector<int> g[MAXN];   // 邻接表：只存 左部→右部 的边
int match[MAXN];       // match[v] = 与右部点 v 匹配的左部点，0 表示未匹配
bool vis[MAXN];        // 本轮 dfs 中右部点 v 是否被访问过
```

!!! warning "vis 数组的作用与重置时机"
    `vis[]` 标记的是**一轮增广（为某个左部点 $u$ 找匹配）过程中**右部点的访问情况，作用是**防止同一个右部点在同一轮被反复尝试导致死循环**。因此必须在**每次为新的左部点调用 `dfs` 之前**重置 `vis[]` 为 false，而在 `dfs` 内部**不重置**。把 `memset(vis, ...)` 写到 `dfs` 内部是经典错误。

### 4.4 完整代码（递归实现）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1005;
vector<int> g[MAXN];   // 左部点 → 右部点 的边
int match[MAXN];       // match[v]: 右部点 v 匹配的左部点
bool vis[MAXN];        // 本轮右部点是否访问
int n, m, e;           // n 左部点数, m 右部点数, e 边数

// 尝试为左部点 u 找匹配，返回是否成功
bool dfs(int u) {
    for (int v : g[u]) {
        if (vis[v]) continue;       // 本轮已尝试过，跳过
        vis[v] = true;
        // 若 v 未匹配，或 v 的原配 match[v] 能另寻搭档
        if (match[v] == 0 || dfs(match[v])) {
            match[v] = u;           // 抢占/建立匹配
            return true;
        }
    }
    return false;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> m >> e;
    for (int i = 0; i < e; i++) {
        int u, v;
        cin >> u >> v;
        g[u].push_back(v);          // 只存左→右
    }

    int ans = 0;
    for (int i = 1; i <= n; i++) {
        memset(vis, false, sizeof(vis));   // ★ 每轮重置！
        if (dfs(i)) ans++;
    }
    cout << ans << endl;
    return 0;
}
```

### 4.5 增广过程图解

以左部 $\{1,2,3\}$、右部 $\{4,5,6\}$、边 $1\text{-}4, 1\text{-}5, 2\text{-}4, 2\text{-}5, 3\text{-}5, 3\text{-}6$ 为例：

```
初始：match 全空
第1轮 i=1：1 尝试 4，4 未匹配 → match[4]=1 ✓  ans=1
   L      R          L      R
   1 ═══ 4           1 ═══ 4
   2      5          2      5
   3      6          3      6

第2轮 i=2：2 尝试 5，5 未匹配 → match[5]=2 ✓  ans=2
   1 ═══ 4
   2 ═══ 5
   3      6

第3轮 i=3：3 尝试 5（vis[5]=1），5 已匹配 match[5]=2
   → 递归 dfs(2)：2 尝试 4（vis[4]=1），4 已匹配 match[4]=1
     → 递归 dfs(1)：1 尝试 5(已vis)、4(已vis)… 无路 → false
   → 2 找不到新搭档 → 5 这条路不通
   3 尝试 6，6 未匹配 → match[6]=3 ✓  ans=3

最终最大匹配 = 3（1-4, 2-5, 3-6）
```

### 4.6 复杂度

| 维度 | 复杂度 | 说明 |
|------|:------:|------|
| 时间 | $O(n \cdot m)$ | $n$ 个左部点，每次 dfs 最坏遍历 $m$ 条边 |
| 空间 | $O(n+m)$ | 邻接表 + match + vis |

实测中匈牙利算法远快于理论上界，CSP 数据规模（$n,m \le 500$）完全够用。

---

## 第五节 常见误区

### 5.1 vis 重置位置错误

```cpp
// ✗ 错误：把重置写在 dfs 里
bool dfs(int u) {
    memset(vis, false, sizeof(vis));   // 每次递归都清空，完全失去防重复作用
    ...
}

// ✓ 正确：在主循环里，每次为新的左部点调用前重置
for (int i = 1; i <= n; i++) {
    memset(vis, false, sizeof(vis));
    if (dfs(i)) ans++;
}
```

!!! danger "最典型的错误"
        把 `memset(vis, ...)` 写进 `dfs` 函数开头，会导致递归过程中 `vis` 被反复清空，**失去"本轮已访问"的标记意义**，轻则 TLE（反复尝试同一右部点），重则栈溢出/死循环。务必写在主循环里。

### 5.2 递归深度过大

当左部点数 $n$ 很大且匹配链很长时，递归深度可能触发栈溢出（RE）。例如 $n=10^5$ 的链状二分图，递归深度可达 $10^5$。

**应对方案**：
- 数据范围小（$n \le 10^3$）时无需担心。
- 数据范围大时，可改用**非递归（迭代）版本的匈牙利算法**，或开大栈空间。

### 5.3 建图方向搞反

匈牙利算法要求**只存"左部 → 右部"的单向边**，且枚举的是左部点。若把左右搞反（如把右部当左部枚举），虽然结果一样，但容易在 `match` 数组的下标含义上出错。建议固定：`match[v]` 中 $v$ 是**右部点**编号。

### 5.4 多组数据忘记清空

多组测试数据时，必须清空 `g[]`、`match[]`：

```cpp
for (int i = 1; i <= n; i++) g[i].clear();
memset(match, 0, sizeof(match));
```

### 5.5 误认为"完美匹配一定存在"

最大匹配不一定是完美匹配。只有当 $|L|=|R|$ 且最大匹配数等于 $|L|$ 时才是完美匹配。题目要求"全部配对"时，**必须检查答案是否等于 $|L|$**，不能想当然。

---

## 第六节 König 定理：最小点覆盖 = 最大匹配

### 6.1 点覆盖与最小点覆盖

「**点覆盖 Vertex Cover**」：顶点集 $S \subseteq V$，使得图中**每条边至少有一个端点在 $S$ 中**。
「**最小点覆盖**」：顶点数最少的点覆盖。

### 6.2 König 定理

!!! info "König 定理（ König's Theorem）"
    在**二分图**中，**最小点覆盖数 = 最大匹配数**。

这是二分图最重要的定理之一。它把"找最少的点覆盖所有边"这个 NP 难问题（在一般图上）在二分图上转化为"求最大匹配"，可用匈牙利算法在多项式时间内解决。

```
二分图（边：1-4, 1-5, 2-5, 2-6）：
                    最大匹配 = 2（如 1-4, 2-5）
  L      R          最小点覆盖 = 2：选 {1, 2}
  1 ═══ 4           1 覆盖 1-4、1-5；2 覆盖 2-5、2-6
  │  \              所有边都被覆盖，且只用 2 个点
  2 ═══ 5
  │  \
  │  ═══ 6
```

!!! tip "König 定理的构造性证明（直觉）"
    从最大匹配出发，对所有"未匹配的左部点"做交替路（非匹配边优先）搜索，标记经过的点。然后取：**未标记的左部点 ∪ 已标记的右部点**，这恰好是最小点覆盖，且点数 = 最大匹配数。CSP-J/S 一般只需记住结论并会应用即可。

### 6.3 相关结论速查

| 问题 | 在二分图中的结论 |
|------|------------------|
| 最大匹配 | 匈牙利算法求出 |
| 最小点覆盖 | = 最大匹配数（König 定理） |
| 最大独立集 | = 总点数 − 最小点覆盖 = 总点数 − 最大匹配 |
| 最小边覆盖 | = 总点数 − 最大匹配（要求无孤立点） |

---

## 第七节 典型例题

### 7.1 【P3386】二分图最大匹配模板

**题意**：给定二分图，左部 $n$ 个点、右部 $m$ 个点、$e$ 条边，求最大匹配数。

**分析**：直接套匈牙利算法模板即可。注意只存左→右的边，枚举左部点。

```cpp
#include <bits/stdc++.h>
using namespace std;
const int MAXN = 1005;
vector<int> g[MAXN];
int match[MAXN];
bool vis[MAXN];

bool dfs(int u) {
    for (int v : g[u]) {
        if (vis[v]) continue;
        vis[v] = true;
        if (match[v] == 0 || dfs(match[v])) {
            match[v] = u;
            return true;
        }
    }
    return false;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, m, e;
    cin >> n >> m >> e;
    for (int i = 0; i < e; i++) {
        int u, v;
        cin >> u >> v;
        g[u].push_back(v);
    }
    int ans = 0;
    for (int i = 1; i <= n; i++) {
        memset(vis, false, sizeof(vis));
        if (dfs(i)) ans++;
    }
    cout << ans << endl;
    return 0;
}
```

!!! tip "模板要点"
    这段代码就是匈牙利算法的标准模板，**务必能默写**。三道习题的建模各不相同，但匹配部分都复用这段核心代码。

### 7.2 【P1129】矩阵游戏

**题意**：$n \times n$ 矩阵，每个格子是 0 或 1。可以任意交换两行、任意交换两列。问能否通过若干次交换，使主对角线全为 1。

**分析**：行交换、列交换都不会改变"第 $i$ 行能和哪些列配对"的集合关系。问题转化为：能否为每一行选一个不同的、值为 1 的列。即行作为左部、列作为右部、$(i,j)$ 为 1 则连边，**求是否完美匹配**（匹配数 $=n$）。

```cpp
#include <bits/stdc++.h>
using namespace std;
const int MAXN = 405;
vector<int> g[MAXN];
int match[MAXN];
bool vis[MAXN];

bool dfs(int u) {
    for (int v : g[u]) {
        if (vis[v]) continue;
        vis[v] = true;
        if (match[v] == 0 || dfs(match[v])) {
            match[v] = u;
            return true;
        }
    }
    return false;
}

int main() {
    int T;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        for (int i = 1; i <= n; i++) g[i].clear();
        memset(match, 0, sizeof(match));
        for (int i = 1; i <= n; i++) {
            for (int j = 1; j <= n; j++) {
                int x;
                cin >> x;
                if (x == 1) g[i].push_back(j);
            }
        }
        int ans = 0;
        for (int i = 1; i <= n; i++) {
            memset(vis, false, sizeof(vis));
            if (dfs(i)) ans++;
        }
        cout << (ans == n ? "Yes" : "No") << "\n";
    }
    return 0;
}
```

!!! warning "多组数据务必清空"
        本题多组数据，每组开始前必须 `g[i].clear()` 和 `memset(match, 0, ...)`，否则上一组数据会污染当前结果。

### 7.3 【P2055】假期的宿舍

**题意**：学校放假，$n$ 个学生。有些学生留校需要床位，有些回家可提供床位。已知每个学生是否认识其他学生（认识则可以睡对方的床）。每人要么睡自己的床，要么睡认识的人的床。问能否安排所有留校学生都有床睡。

**分析建模**：
- 左部：需要床的学生（留校学生）。
- 右部：所有可提供的床（每个学生自己的床，无论是否留校都算一张床）。
- 连边：留校学生 $i$ 可以睡 $j$ 的床，当且仅当 $i=j$（自己的床）或 $i$ 认识 $j$。
- 答案：**最大匹配数是否等于留校学生数**。

```cpp
#include <bits/stdc++.h>
using namespace std;
const int MAXN = 55;
vector<int> g[MAXN];
int match[MAXN];
bool vis[MAXN];
int stay[MAXN];   // stay[i]=1 表示 i 留校需要床
int bed[MAXN];    // bed[i]=1 表示 i 的床可用（不回家的学生才有床）

bool dfs(int u) {
    for (int v : g[u]) {
        if (vis[v]) continue;
        vis[v] = true;
        if (match[v] == 0 || dfs(match[v])) {
            match[v] = u;
            return true;
        }
    }
    return false;
}

int main() {
    int T;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        for (int i = 1; i <= n; i++) g[i].clear();
        memset(match, 0, sizeof(match));
        int need = 0;
        for (int i = 1; i <= n; i++) { cin >> stay[i]; if (stay[i]) need++; }
        for (int i = 1; i <= n; i++) cin >> bed[i];
        // 注意：回家的学生没有自己的床可用（即床不可借出）
        // 但留校学生自己的床可用
        for (int i = 1; i <= n; i++) {
            if (!stay[i]) continue;        // 只为留校学生建边
            for (int j = 1; j <= n; j++) {
                if (!bed[j] && i != j) continue;  // j 回家且非自己 → 无床
                // i 能睡 j 的床：i==j 或 i 认识 j
                int know;
                // 读取认识矩阵（按行）
            }
        }
        // 为简化，重新组织读入：先读认识矩阵
        // —— 上面写法为示意，实际应先读矩阵再建边
        // 下面给出完整正确版本（请以本段为准）
        // （为避免歧义，本代码段示意思路，完整代码见下方）
        int ans = 0;
        for (int i = 1; i <= n; i++) {
            if (!stay[i]) continue;
            memset(vis, false, sizeof(vis));
            if (dfs(i)) ans++;
        }
        cout << (ans == need ? "^_^" : "T_T") << "\n";
    }
    return 0;
}
```

上面为思路示意，**完整正确版本**如下（注意先读认识矩阵再建边）：

```cpp
#include <bits/stdc++.h>
using namespace std;
const int MAXN = 55;
vector<int> g[MAXN];
int match[MAXN];
bool vis[MAXN];
int stay[MAXN], bed[MAXN], know[MAXN][MAXN];

bool dfs(int u) {
    for (int v : g[u]) {
        if (vis[v]) continue;
        vis[v] = true;
        if (match[v] == 0 || dfs(match[v])) {
            match[v] = u;
            return true;
        }
    }
    return false;
}

int main() {
    int T;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        for (int i = 1; i <= n; i++) g[i].clear();
        memset(match, 0, sizeof(match));
        int need = 0;
        for (int i = 1; i <= n; i++) { cin >> stay[i]; if (stay[i]) need++; }
        for (int i = 1; i <= n; i++) cin >> bed[i];
        for (int i = 1; i <= n; i++)
            for (int j = 1; j <= n; j++)
                cin >> know[i][j];
        // 建边：留校学生 i → 床 j（j 的床可用 且 (i==j 或 i 认识 j)）
        for (int i = 1; i <= n; i++) {
            if (!stay[i]) continue;
            for (int j = 1; j <= n; j++) {
                bool canBed = bed[j] || (i == j); // 自己的床总可用
                if (canBed && (i == j || know[i][j]))
                    g[i].push_back(j);
            }
        }
        int ans = 0;
        for (int i = 1; i <= n; i++) {
            if (!stay[i]) continue;
            memset(vis, false, sizeof(vis));
            if (dfs(i)) ans++;
        }
        cout << (ans == need ? "^_^" : "T_T") << "\n";
    }
    return 0;
}
```

!!! tip "建模是关键"
    本题难点不在算法，而在建模：识别"谁需要床"（左部）、"谁能提供床"（右部）、"谁能睡谁的床"（边）。**二分图匹配题 80% 的工作在建模**，代码反而是模板。

!!! warning "床可用性的细节"
        回家学生的床**不可用**（他带着床走/床锁了），留校学生自己的床可用。读题时务必区分"是不是留校"与"床是否可用"，这是本题最易错的点。

---

## 📖 本章总结

- **二分图定义**
  - 顶点集划分为 L、R 两部，边只跨部存在
  - 等价刻画：不含奇环 ⟺ 二分图
- **染色法判定**
  - DFS：`color[u]=c`，邻居染 `c^1`，遇同色则失败
  - BFS：队列实现，避免递归过深
  - 时间 O(n+m)，空间 O(n+m)
- **匹配与增广路**
  - 匹配：无边共享端点的边集
  - 增广路：非匹配边比匹配边多一条的交错路
  - 翻转增广路 → 匹配数 +1
- **匈牙利算法**
  - 核心：枚举左部点，dfs 找增广路
  - 数据：`g[]`（左→右）、`match[]`（右→左）、`vis[]`（本轮右部访问）
  - ★ `vis` 每轮（每个左部点）重置，不在 `dfs` 内重置
  - 时间 O(n·m)，空间 O(n+m)
  - 递归深度大时改 BFS/迭代或开大栈
- **König 定理**
  - 最小点覆盖 = 最大匹配（仅二分图成立）
  - 最大独立集 = 总点数 − 最大匹配
  - 最小边覆盖 = 总点数 − 最大匹配
- **常见误区**
  - `vis` 重置位置写错（写进 `dfs`）
  - 递归深度过大 RE
  - 建图方向搞反（`match` 下标含义混乱）
  - 多组数据忘记清空 `g[]` / `match[]`
  - 误以为最大匹配 = 完美匹配（需校验 `==|L|`）
- **配套习题**
  - P3386 模板（直接套用）
  - P1129 矩阵游戏（行→列，判完美匹配）
  - P2055 假期的宿舍（学生→床，判匹配==留校人数）

### 自测清单（合上书自检）

!!! tip "能做到以下几点，才算真正掌握本章"
    - [ ] 说出二分图的定义，解释"二分图 ⟺ 无奇环"
    - [ ] 默写染色法（DFS 与 BFS）判定二分图的代码
    - [ ] 解释匹配、增广路、交错路的概念，说明增广路翻转后匹配数为何 +1
    - [ ] 默写匈牙利算法递归实现（含 `match[]`、`vis[]`）
    - [ ] 说出 `vis[]` 数组的作用，以及为什么每轮重置而非 dfs 内重置
    - [ ] 说明递归深度过大的风险及应对方案
    - [ ] 陈述 König 定理：最小点覆盖 = 最大匹配
    - [ ] 用匈牙利算法解决 P3386、P1129、P2055 三道题的建模

### 核心速查表

| 操作 | 代码 | 说明 |
|------|------|------|
| 染色 DFS | `dfs(v, c^1)` 邻居异色 | 遇同色返回 false |
| 染色 BFS | `color[v]=color[u]^1; q.push(v)` | 防递归过深 |
| 建图（左→右） | `g[u].push_back(v)` | 只存单向 |
| 每轮重置 vis | `memset(vis,false,sizeof(vis))` | 写在主循环 |
| 匈牙利 dfs | `if(match[v]==0\|\|dfs(match[v])) match[v]=u` | 增广路核心 |
| 统计匹配数 | `if(dfs(i)) ans++` | 枚举左部点 |
| 判完美匹配 | `ans == n`（左部点数） | 题目要求全覆盖时 |
| 最小点覆盖 | = 最大匹配数 | König 定理 |
| 最大独立集 | = 总点数 − 最大匹配 | 二分图结论 |

### 知识衔接

本章是**图论匹配**的经典章节，也是图论卷的收官，与前后章节的联系如下：

- **前置知识**：第33章最小生成树（图论基础延续）、第28章BFS进阶（匈牙利算法的增广路基于BFS/DFS）、第13章二分查找（二分图判定与二分思想）
- **本章定位**：二分图匹配的经典算法，是二分图理论与匹配理论的入门
- **后续发展**：图论卷至此告一段落，为第35章质数与约数开启数学卷，匈牙利算法在网络流中是最大流的特例

!!! info "下一章预告"
    二分图匹配是图论匹配问题的入门。后续可进一步学习**网络流**（最大流最小割定理，二分图最大匹配可建模为最大流）、**KM 算法**（带权二分图最大权匹配）等更高级内容，它们共同构成 CSP-S 及更高阶段的图论匹配体系。
