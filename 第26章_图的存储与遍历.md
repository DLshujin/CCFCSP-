# 第26章 图的存储与遍历：从城市道路网到算法骨架

!!! info "本章内容"
    图是 CSP-S 复赛最重要的数据结构之一，几乎所有高级算法（最短路、最小生成树、拓扑排序、网络流）都建立在图之上。本章系统讲解：图的基本术语、三种存储方式（邻接矩阵/邻接表/链式前向星）及其对比、DFS 与 BFS 遍历、常见误区（无向图边数组开两倍、DFS 栈溢出）。

!!! warning "本章定位"
    图的存储与遍历是图论的"地基"。地基不牢，后面学最短路、最小生成树时一定会崩。本章要求**三种存储方式都能默写**，尤其**链式前向星**是 CSP-S 竞赛标配，必须练到形成肌肉记忆。

---

## 📌 学习目标

学完本章后，你应当能够：

1. **准确说出**顶点、边、度、有向图、无向图、带权图等术语含义
2. **默写**邻接矩阵、邻接表（vector）、链式前向星三种存储结构的建图代码
3. **独立写出**DFS 和 BFS 的遍历模板，能用于连通块计数和层序遍历
4. **解释**为什么无向图的边数组要开两倍、为什么 DFS 可能栈溢出
5. **根据题目特点**选择合适的存储方式（稠密图用矩阵，稀疏图用前向星）

---

## 💡 生活类比

### 类比一：城市道路网

把城市想象成一张图：**顶点（Vertex）** = 路口或地标，**边（Edge）** = 连接两个路口的道路。**有向图** = 单行道，**无向图** = 双向通行的道路；**带权图** = 道路有长度/用时，**无权图** = 只关心"通不通"；**度数** = 一个路口连了几条路。

```
        家 ──2km── 学校
        │           │
       3km         1km
        │           │
       商场 ──4km── 医院

这是一个无向带权图：
- 4 个顶点：家、学校、商场、医院；4 条边，每条边有权值（公里数）
- 家的度数 = 2（连了学校和商场）
```

### 类比二：微信好友关系

微信好友关系是典型的**无向图**：**顶点** = 每个人，**边** = 好友关系（A 是 B 的好友，B 也是 A 的好友），**连通块** = 一个朋友圈里的人互相都能通过好友链认识。而微博关注关系则是**有向图**：A 关注 B 不代表 B 关注 A。

!!! tip "一句话记忆"
    图 = 顶点的集合 + 边的集合。无向图边是"双向的"，有向图边是"单向的"。

---

## 第一节 图的定义与基本术语

### 1.1 图的形式化定义

「图 graph」由顶点集合 $V$（vertex）和边集合 $E$（edge）组成，记作 $G = (V, E)$。$|V| = n$ 表示有 $n$ 个顶点（编号 $1 \sim n$），$|E| = m$ 表示有 $m$ 条边。每条边连接两个顶点 $u$ 和 $v$，记作 $(u, v)$。

### 1.2 图的分类

| 分类维度 | 类型 | 说明 |
|----------|------|------|
| 方向 | **有向图** | 边有方向，$(u,v)$ 表示从 $u$ 指向 $v$ |
| 方向 | **无向图** | 边无方向，$(u,v)$ 等价于 $(v,u)$ |
| 权值 | **带权图** | 每条边有一个权值（距离、费用等） |
| 权值 | **无权图** | 边没有权值，只关心连通性 |
| 边数 | **稠密图** | $m$ 接近 $n^2$（边很多） |
| 边数 | **稀疏图** | $m$ 接近 $n$ 或 $n \log n$（边很少） |
| 自环 | **有自环** | 存在 $(u,u)$ 这样的边 |
| 重边 | **有重边** | 两个顶点间有多条边 |

### 1.3 度数与握手定理

| 概念 | 定义 |
|------|------|
| **度 degree**（无向图） | 与顶点相连的边数 |
| **出度 out-degree**（有向图） | 从该顶点**出发**的边数 |
| **入度 in-degree**（有向图） | **指向**该顶点的边数 |

!!! info "握手定理"
    无向图中所有顶点度数之和 = $2 \times$ 边数（每条边贡献两个度）。有向图中所有顶点出度之和 = 入度之和 = 边数。

### 1.4 路径与连通

| 术语 | 定义 |
|------|------|
| **路径 path** | 顶点序列 $v_1, v_2, \dots, v_k$，相邻顶点间有边 |
| **简单路径** | 顶点不重复的路径 |
| **回路/环 cycle** | 起点 = 终点的路径 |
| **连通**（无向图） | 顶点 $u$ 到 $v$ 存在路径 |
| **连通图 / 连通分量** | 任意两顶点都连通 / 极大连通子图 |
| **强连通**（有向图） | $u$ 到 $v$ 且 $v$ 到 $u$ 都有路径 |

```
非连通图示例（含 2 个连通分量）：
    1 ── 2    4 ── 5
    │         │
    3         6
连通分量1：{1,2,3}    连通分量2：{4,5,6}
```

---

## 第二节 存储方式一：邻接矩阵

### 2.1 基本思想

用一个 $n \times n$ 的二维数组 `g[i][j]` 存储图：`g[i][j] = 1`（或权值）表示顶点 $i$ 到 $j$ 有边，`g[i][j] = 0`（或 INF）表示无边。

### 2.2 ASCII 示意图

```
无向图：              邻接矩阵 g[1..4][1..4]：
    1 ─── 2              0  1  1  0
    │     │              1  0  0  1
    3 ─── 4              1  0  0  1
                          0  1  1  0
特点：无向图的邻接矩阵**沿主对角线对称**
```

### 2.3 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1010;
int g[N][N];                 // g[i][j] 存边 (i,j) 的权值，0 表示无边
int n, m;

int main() {
    cin >> n >> m;
    for (int i = 1; i <= m; i++) {
        int u, v, w; cin >> u >> v >> w;
        g[u][v] = w;         // 有向图只存一行
        // g[v][u] = w;      // 无向图要存两行（对称）
    }
    if (g[1][2]) cout << "1 和 2 之间有边" << endl;   // O(1) 判边
    for (int v = 1; v <= n; v++)                       // O(n) 遍历邻接点
        if (g[1][v]) cout << v << " ";
    return 0;
}
```

### 2.4 邻接矩阵的特性

| 操作 | 时间复杂度 | 说明 |
|------|:----------:|------|
| 判断边 $(u,v)$ 是否存在 | $O(1)$ | 直接查 `g[u][v]` |
| 遍历 $u$ 的所有邻接点 | $O(n)$ | 要扫一整行 |
| 建图空间 | $O(n^2)$ | 与边数无关 |
| 适合 | 稠密图 | 边数接近 $n^2$ 时高效 |

!!! warning "邻接矩阵的致命缺陷"
    当 $n = 10^5$ 时，$n^2 = 10^{10}$，数组直接爆内存（约 40GB）。CSP-S 中 $n$ 经常达到 $10^5$ 甚至 $10^6$，**邻接矩阵几乎一定 MLE**。所以邻接矩阵只用于 $n \leq 2000$ 的小图（如 Floyd 最短路）。

---

## 第三节 存储方式二：邻接表（vector）

### 3.1 基本思想

每个顶点 $i$ 维护一个 `vector`，存所有从 $i$ 出发的边。`g[i]` 就是顶点 $i$ 的邻接点列表。

### 3.2 ASCII 示意图

```
有向图：1 → 2 → 3, 1 → 4 → 5

邻接表（vector<int> g[N]）：
g[1]: [2, 4]      ← 从 1 出发能到 2 和 4
g[2]: [3]         ← 从 2 出发能到 3
g[3]: []          ← 3 没有出边
g[4]: [5]         ← 从 4 出发能到 5
g[5]: []          ← 5 没有出边
```

### 3.3 完整代码（无权图 + 带权图）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
vector<int> g[N];                  // 无权图
vector<pair<int,int>> gw[N];       // 带权图：first=终点，second=权值
int n, m;

int main() {
    cin >> n >> m;
    for (int i = 1; i <= m; i++) {
        int u, v, w; cin >> u >> v >> w;
        g[u].push_back(v);          // 无权有向图
        gw[u].push_back({v, w});    // 带权有向图
        // 无向图加一行：g[v].push_back(u); gw[v].push_back({u, w});
    }
    for (int v : g[1]) cout << v << " ";                     // O(度数) 遍历
    for (auto &e : gw[1])
        cout << "到 " << e.first << " 距离 " << e.second << endl;
    return 0;
}
```

### 3.4 邻接表的特性

| 操作 | 时间复杂度 | 说明 |
|------|:----------:|------|
| 判断边 $(u,v)$ 是否存在 | $O(\text{度数})$ | 要扫 `g[u]` |
| 遍历 $u$ 的所有邻接点 | $O(\text{度数})$ | 只扫邻接点 |
| 建图空间 | $O(n + m)$ | 与顶点数 + 边数成正比 |
| 适合 | 稀疏图 | 大多数竞赛题 |

!!! tip "vector 邻接表 vs 链式前向星"
    vector 版写法简洁、不易出错，适合平时练习。但 vector 动态扩容有拷贝开销、内存不连续缓存命中率低、极端数据下可能被卡常。所以 CSP-S 比赛中**链式前向星更稳**，是高分选手的首选。

---

## 第四节 存储方式三：链式前向星（竞赛标配）

### 4.1 基本思想

链式前向星本质是**数组模拟链表**的邻接表。用几个数组：

- `h[u]`：顶点 $u$ 的第一条边在 `e` 数组中的下标（头指针）
- `e[i]`：第 $i$ 条边的终点
- `ne[i]`：第 $i$ 条边的"下一条边"下标（类似链表的 next 指针）
- `w[i]`（可选）：第 $i$ 条边的权值
- `idx`：当前可用边下标（从 0 开始）

每加一条边 $(u, v)$：用"头插法"把边插入 `h[u]` 链表头部。

### 4.2 ASCII 示意图

```
有向图：1 → 2, 1 → 4, 2 → 3，按顺序加边，头插法：

h[1] → 边1(到4) → 边0(到2) → -1
h[2] → 边2(到3) → -1
h[3] = -1,  h[4] = -1

数组状态（idx 从 0 开始）：
idx:     0    1    2
e[]:     2    4    3       ← 终点
ne[]:   -1    0   -1       ← 同起点的下一条边
h[]:     1    2   -1  -1   ← h[1]=1, h[2]=2, h[3]=h[4]=-1

遍历顶点 1 的邻接点（从 h[1]=1 开始顺 ne 走）：
  i=1: e[1]=4 → i=ne[1]=0
  i=0: e[0]=2 → i=ne[0]=-1（结束）
输出顺序：4, 2（与加入顺序相反，因为头插法）
```

### 4.3 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;      // 最大顶点数
const int M = 1e5 + 10;      // 最大边数（无向图要开 2 倍！）

int h[N], e[M], ne[M], w[M], idx;
int n, m;

void init() { memset(h, -1, sizeof(h)); idx = 0; }   // -1 表示空指针

// 加一条从 u 到 v、权值为 weight 的有向边（头插法）
void add(int u, int v, int weight) {
    e[idx] = v; w[idx] = weight; ne[idx] = h[u]; h[u] = idx++;
}

int main() {
    cin >> n >> m;
    init();
    for (int i = 1; i <= m; i++) {
        int u, v, w; cin >> u >> v >> w;
        add(u, v, w);          // 有向图加一条
        // add(v, u, w);       // 无向图加两条（视为两条有向边）
    }
    for (int i = h[1]; i != -1; i = ne[i])   // 遍历顶点 1 的所有邻接边
        cout << "到 " << e[i] << " 权值 " << w[i] << endl;
    return 0;
}
```

无权图可省去 `w` 数组：`void add(int u, int v) { e[idx] = v; ne[idx] = h[u]; h[u] = idx++; }`

### 4.4 链式前向星的特性

| 操作 | 时间复杂度 | 说明 |
|------|:----------:|------|
| 加边 | $O(1)$ | 头插法 |
| 遍历 $u$ 的所有邻接点 | $O(\text{度数})$ | 顺 `ne` 链走 |
| 判断边 $(u,v)$ 是否存在 | $O(\text{度数})$ | 要遍历 `h[u]` 链 |
| 建图空间 | $O(n + m)$ | 数组大小固定 |
| 适合 | 所有竞赛场景 | 速度最快、内存最省 |

!!! tip "为什么链式前向星是竞赛标配"
    1. **数组连续存储**，缓存友好，遍历速度比 vector 快 30% 以上
    2. **内存预分配**，无动态扩容开销
    3. **支持高级算法**（网络流、Tarjan 等）的边操作。CSP-S 高分选手几乎 100% 使用链式前向星。

---

## 第五节 三种存储方式对比

| 对比维度 | 邻接矩阵 | 邻接表（vector） | 链式前向星 |
|----------|:--------:|:----------------:|:----------:|
| 空间复杂度 | $O(n^2)$ | $O(n+m)$ | $O(n+m)$ |
| 判断边存在 | $O(1)$ | $O(\text{度数})$ | $O(\text{度数})$ |
| 遍历邻接点 | $O(n)$ | $O(\text{度数})$ | $O(\text{度数})$ |
| 加边 | $O(1)$ | $O(1)$ 均摊 | $O(1)$ |
| 适用图类型 | 稠密图 | 稀疏图 | 所有 |
| 内存稳定性 | 固定 | 动态扩容 | 固定 |
| 缓存性能 | 好 | 一般 | 好 |
| 编码复杂度 | 低 | 低 | 中 |
| 竞赛推荐度 | 仅小图 | 一般 | 强烈推荐 |

!!! warning "无向图边数组开两倍（最常见误区）"
    无向图每条边要存两次（$u \to v$ 和 $v \to u$），所以 `M` 必须开**题目边数的 2 倍**！`const int M = 2e5 + 10;`（题目说 $m \leq 10^5$ 时开 $2 \times 10^5$）。这是 CSP-S 中**最经典的 RE/MLE 原因之一**，每年都有选手栽在这里。

---

## 第六节 DFS 深度优先遍历

### 6.1 基本思想

DFS（Depth-First Search）从起点出发，**一条路走到黑**，走到无路可走时回溯，再换一条路继续。借助**递归栈**实现。

```
图：1 ─── 2, 1 ─── 3, 2 ─── 4
从 1 开始 DFS（按邻接点从小到大）：
访问 1 → 2 → 4 → 回溯 → 3 → 回溯 → 回溯
输出顺序：1 2 4 3
```

### 6.2 DFS 模板（连通块计数）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
const int M = 2e5 + 10;                 // 边数上限
// 注意：边数应独立定义，不能用 N<<1 代替 M<<1
int h[N], e[M << 1], ne[M << 1], idx;   // 无向图边数开 2 倍
bool vis[N];                            // 标记是否访问过
int n, m;

void add(int u, int v) { e[idx] = v; ne[idx] = h[u]; h[u] = idx++; }

void dfs(int u) {                       // DFS 遍历从 u 出发的连通块
    vis[u] = true;                      // 标记已访问
    cout << u << " ";
    for (int i = h[u]; i != -1; i = ne[i]) {
        int v = e[i];
        if (!vis[v]) dfs(v);            // 递归访问未访问的邻接点
    }
}

int main() {
    cin >> n >> m;
    memset(h, -1, sizeof(h));
    for (int i = 1; i <= m; i++) {
        int u, v; cin >> u >> v;
        add(u, v); add(v, u);           // 无向图加两条边
    }
    int cnt = 0;                        // 连通块计数
    for (int i = 1; i <= n; i++)
        if (!vis[i]) { cnt++; dfs(i); }
    cout << "连通块总数：" << cnt << endl;
    return 0;
}
```

### 6.3 DFS 的特性与栈溢出

| 维度 | 内容 |
|------|------|
| 时间复杂度 | $O(n + m)$（每点每边访问一次） |
| 空间复杂度 | $O(n)$（vis 数组 + 递归栈） |
| 适用场景 | 连通性判断、连通块计数、路径搜索、二分图染色 |

!!! warning "DFS 递归深度过大导致栈溢出（致命误区）"
    当图退化为链（如 $1 \to 2 \to \dots \to 10^6$）时，DFS 递归深度可达 $10^6$，**系统栈默认只有 8MB（Windows 约 1MB）**，会直接栈溢出 RE！**解决方案**：1. 改用 BFS（队列在堆上，不受栈大小限制）；2. 手写栈模拟 DFS（用 `stack<int>`，`vis[v]` 在**入栈时**标记避免重复入栈）；3. 编译时加大栈（CSP-S 评测机不一定支持）。CSP-S 中**链状图、网格图**优先考虑 BFS。

---

## 第七节 BFS 广度优先遍历

### 7.1 基本思想

BFS（Breadth-First Search）从起点出发，**一层一层**向外扩展，先访问所有距离为 1 的顶点，再访问距离为 2 的……借助**队列**实现。

```
图：1 ─── 2 ─── 5, 1 ─── 3, 2 ─── 4
从 1 开始 BFS：层0：1 → 层1：2,3 → 层2：4,5
输出顺序：1 2 3 4 5
```

### 7.2 BFS 模板（层序遍历 + 求距离）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
const int M = 2e5 + 10;                 // 边数上限
// 注意：边数应独立定义，不能用 N<<1 代替 M<<1
int h[N], e[M << 1], ne[M << 1], idx;
int dist[N];                            // dist[u] = 起点 s 到 u 的距离
int n, m, s;

void add(int u, int v) { e[idx] = v; ne[idx] = h[u]; h[u] = idx++; }

void bfs(int s) {                        // BFS 从 s 出发，求到各顶点距离
    memset(dist, -1, sizeof(dist));     // -1 表示未访问
    queue<int> q;
    q.push(s); dist[s] = 0;
    while (!q.empty()) {
        int u = q.front(); q.pop();
        for (int i = h[u]; i != -1; i = ne[i]) {
            int v = e[i];
            if (dist[v] == -1) {        // 未访问
                dist[v] = dist[u] + 1;  // 距离 = 上一步 + 1
                q.push(v);
            }
        }
    }
}

int main() {
    cin >> n >> m >> s;
    memset(h, -1, sizeof(h));
    for (int i = 1; i <= m; i++) {
        int u, v; cin >> u >> v;
        add(u, v); add(v, u);
    }
    bfs(s);
    for (int i = 1; i <= n; i++)
        cout << "到 " << i << " 的距离：" << dist[i] << endl;
    return 0;
}
```

### 7.3 BFS 的特性

| 维度 | 内容 |
|------|------|
| 时间复杂度 | $O(n + m)$ |
| 空间复杂度 | $O(n)$（vis/dist 数组 + 队列） |
| 适用场景 | **最短路（无权图）**、层序遍历、最小步数 |

!!! tip "BFS 求无权图最短路"
    无权图（或所有边权相等）中，BFS 第一次访问到某顶点时的 `dist` 就是**最短距离**，这是 CSP-S 高频考点。层序遍历按层输出时，只需在 `dist[u] > curLevel` 时换行即可。

### 7.4 DFS 与 BFS 对比

| 维度 | DFS | BFS |
|------|-----|-----|
| 数据结构 | 栈（递归） | 队列 |
| 遍历顺序 | 一条路走到底 | 一层一层扩展 |
| 时间复杂度 | $O(n+m)$ | $O(n+m)$ |
| 空间复杂度 | $O(h)$（递归深度） | $O(w)$（队列最大宽度） |
| 求最短路 | 困难（要枚举所有路径） | **天然适合**（无权图） |
| 连通块计数 | 适合 | 适合 |
| 栈溢出风险 | 有（链状图） | 无 |
| 适用场景 | 路径搜索、连通性、回溯 | 最短路、层序、最小步数 |

---

## 第八节 常见误区

!!! warning "误区一：无向图边数组开两倍"
    最经典的错误。无向图每条边要加两次（$u \to v$ 和 $v \to u$），所以 `M` 必须开题目边数的 **2 倍**。漏开导致数组越界 RE。

!!! warning "误区二：DFS 递归深度过大栈溢出"
    链状图（$n = 10^6$）DFS 递归深度可达 $10^6$，远超系统栈大小（Windows 默认 1MB）。解决：改用 BFS 或手写栈 DFS。

!!! warning "误区三：忘记 vis 数组 / 标记时机错误"
    图中若有环，不标记 `vis` 会反复访问，无限递归/循环。**进入即标记**，且只在未访问时才递归/入队。特别注意标记时机：递归 DFS 在**进入函数时**标记，手写栈 DFS 在**入栈时**标记，BFS 在**入队时**赋值 `dist`。标记晚了会导致同一顶点重复入队/入栈，时间退化为 $O(n^2)$ 甚至死循环。

!!! warning "误区四：邻接矩阵用于大图导致 MLE"
    $n = 10^5$ 时邻接矩阵需 $10^{10}$ 个 int ≈ 40GB，直接 MLE。大图必须用邻接表或链式前向星。

!!! warning "误区五：重边和自环未处理"
    邻接矩阵：重边自动覆盖（只保留最后的权值）。邻接表/前向星：重边会存多条，遍历时都会访问；若题意要求去重，需额外处理。自环：DFS/BFS 中自环不影响（已 vis），但度数计算需注意题意。

---

## 第九节 典型例题

### 9.1 [P5318] 查找文献

**题意**：给定 $n$ 个点 $m$ 条边的有向图，分别输出从 1 出发的 DFS 序和 BFS 序。当多个邻接点可选时，按编号从小到大访问。

**思路**：用邻接表存图，**邻接点排序**（保证按编号访问），然后分别 DFS 和 BFS。注意有向图只加单向边。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
vector<int> g[N];
bool vis[N];
int n, m;

void dfs(int u) {
    vis[u] = true; cout << u << " ";
    for (int v : g[u]) if (!vis[v]) dfs(v);
}

void bfs(int s) {
    memset(vis, false, sizeof(vis));
    queue<int> q; q.push(s); vis[s] = true;
    while (!q.empty()) {
        int u = q.front(); q.pop(); cout << u << " ";
        for (int v : g[u]) if (!vis[v]) { vis[v] = true; q.push(v); }
    }
}

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    cin >> n >> m;
    for (int i = 1; i <= m; i++) {
        int u, v; cin >> u >> v;
        g[u].push_back(v);          // 有向图
    }
    for (int i = 1; i <= n; i++) sort(g[i].begin(), g[i].end());
    dfs(1); cout << endl;
    bfs(1); cout << endl;
    return 0;
}
```

!!! tip "为什么本题用 vector 而非链式前向星"
    链式前向星头插法导致邻接点顺序与加入顺序**相反**，要按编号排序较麻烦。本题 $n, m \leq 10^5$，vector 够用且排序方便。选择存储方式要看题目需求。

### 9.2 [P3916] 图的遍历

**题意**：给定 $n$ 个点 $m$ 条边的有向图，对每个点 $i$ 输出从 $i$ 出发能到达的**编号最大**的点。

**思路**：正着做每个点都要 DFS/BFS，$O(n(n+m))$ 超时。**反向建图**：从编号大的点往小 DFS，每个能反向到达的点都能到达它，取最大编号。倒序枚举起点，已访问的跳过。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
vector<int> g[N];            // 反向图
int ans[N];                  // ans[i] = 从 i 出发能到达的最大编号
int n, m;

void dfs(int u, int mx) {
    if (ans[u]) return;      // 已确定，跳过
    ans[u] = mx;
    for (int v : g[u])       // 反向边：v 能到 u，则 v 能到 mx
        if (!ans[v]) dfs(v, mx);
}

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    cin >> n >> m;
    for (int i = 1; i <= m; i++) {
        int u, v; cin >> u >> v;
        g[v].push_back(u);   // 反向建图：v → u 表示原图 u → v
    }
    for (int i = n; i >= 1; i--)   // 倒序枚举，大的先
        if (!ans[i]) dfs(i, i);
    for (int i = 1; i <= n; i++) cout << ans[i] << " ";
    return 0;
}
```

!!! tip "反向建图的巧妙思维"
    正向思考困难时，**反向建图**是图论经典技巧。本题反向后，"从 $i$ 能到哪"变成"谁能到 $i$"，倒序枚举起点自然得到最大编号。

### 9.3 [P1330] 封锁阳光大学

**题意**：给定无向图，选出若干顶点，使得每条边**至少有一个端点**被选中，且选中的顶点**两两不相邻**。求最少选多少个点，无解输出 `Impossible`。

**思路**：转化为**二分图判定 + 取少的一边**。选中点两两不相邻→独立集，每条边至少一个端点选中→补集是点覆盖。对每个连通分量做**二分图染色**，取黑白数量较少的一边累加；若某连通分量**非二分图**（有奇环），则无解。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
const int M = 2e5 + 10;                 // 边数上限
// 注意：边数应独立定义，不能用 N<<1 代替 M<<1
int h[N], e[M << 1], ne[M << 1], idx;   // 无向图边数开 2 倍
int color[N];               // 0 未染色，1 白，2 黑
int cnt[3];                 // 每个连通分量中白、黑数量
int n, m;

void add(int u, int v) { e[idx] = v; ne[idx] = h[u]; h[u] = idx++; }

bool dfs(int u, int c) {
    color[u] = c; cnt[c]++;
    for (int i = h[u]; i != -1; i = ne[i]) {
        int v = e[i];
        if (color[v] == 0) { if (!dfs(v, 3 - c)) return false; }
        else if (color[v] == c) return false; // 同色相邻，非二分图
    }
    return true;
}

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    cin >> n >> m;
    memset(h, -1, sizeof(h));
    for (int i = 1; i <= m; i++) {
        int u, v; cin >> u >> v;
        add(u, v); add(v, u);                  // 无向图，边开 2 倍
    }
    long long ans = 0; bool ok = true;
    for (int i = 1; i <= n && ok; i++) {
        if (color[i] == 0) {
            cnt[1] = cnt[2] = 0;
            if (!dfs(i, 1)) { ok = false; break; }
            ans += min(cnt[1], cnt[2]);        // 取较少的一边
        }
    }
    cout << (ok ? to_string(ans) : "Impossible") << endl;
    return 0;
}
```

!!! tip "二分图染色是 DFS 的重要应用"
    二分图判定 = 能否用两种颜色染色使相邻异色。DFS 染色模板必须熟记，CSP-S 图论高频考点。无向图边数组别忘了开 2 倍！

---

## 📖 本章总结

```
第26章 图的存储与遍历
├── 图的定义：V+E，有向/无向、带权/无权、度、连通分量、握手定理
├── 三种存储方式
│   ├── 邻接矩阵 g[N][N]：O(1) 判边，O(n²) 空间，仅小图
│   ├── 邻接表 vector：O(度数) 遍历，O(n+m) 空间，编码简单
│   └── 链式前向星 h/e/ne/idx：O(1) 加边，竞赛标配最快
├── DFS：递归 + vis 标记，O(n+m)；应用：连通块、二分图染色、路径搜索
├── BFS：队列 + dist 标记，O(n+m)；应用：无权图最短路、层序、最小步数
├── 常见误区
│   ├── 无向图边数组开 2 倍（最经典）
│   ├── DFS 递归深度过大栈溢出 → 改 BFS/手写栈
│   ├── 忘记 vis 导致死循环；vis 标记时机（入栈/入队时）
│   ├── 邻接矩阵用于大图 MLE；重边和自环未处理
└── 配套习题：P5318 模板+排序 / P3916 反向建图 / P1330 二分图染色
```

### 自测清单（合上书自检）

!!! tip "能做到以下几点，才算真正掌握本章"
    - [ ] 说出顶点、边、度、出度、入度、连通分量的定义
    - [ ] 默写邻接矩阵、邻接表（vector）、链式前向星三种建图代码
    - [ ] 解释为什么无向图边数组要开 2 倍
    - [ ] 默写 DFS（递归版）和 BFS（队列版）遍历模板
    - [ ] 解释 DFS 在链状图上为什么会栈溢出，如何解决
    - [ ] 说出 vis/dist 标记的正确时机（入栈/入队时，而非出栈/出队）
    - [ ] 用 BFS 求无权图最短路，用 DFS 做连通块计数和二分图染色
    - [ ] 根据题目特点选择存储方式（稠密用矩阵，稀疏用前向星）

### 知识衔接

本章是**搜索与图论卷**的起点章节，与前后章节的联系如下：

- **前置知识**：第25章二叉树（树形遍历思想）、第18章栈与第19章队列（DFS/BFS的底层数据结构）、第08章函数与递归入门（DFS的递归实现）
- **本章定位**：从树过渡到图，建立图论的基础框架（存储与遍历），是所有图论算法的地基
- **后续发展**：为第27章DFS进阶与剪枝、第28章BFS进阶提供遍历基础，也是第29章拓扑排序、最短路系列章节的前置

!!! info "下一章预告"
    后续章节将讲解**最短路算法**（Dijkstra、Floyd、SPFA）、**最小生成树**（Kruskal、Prim）、**拓扑排序**等 CSP-S 核心考点。这些算法的代码骨架都是"建图 + 遍历"，本章是它们共同的起跑线。
