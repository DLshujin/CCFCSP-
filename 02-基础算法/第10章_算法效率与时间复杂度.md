# 第10章 算法效率与时间复杂度

!!! info "本章定位"
    本章是 CSP-S 算法分析的**基石**。初赛必考代码复杂度判断，复赛必用"数据范围反推算法"技巧。
    学完本章，你将能在一分钟内判断任意代码片段的复杂度量级，并据此选择不会超时的算法。

!!! warning "本章难度"
    概念多、易混淆。**大 O 表示法的化简规则**和**嵌套循环的复杂度叠加**是两大难点，请务必动手算一遍例题。

---

## 📌 学习目标

> 💡 一句话概括：掌握时间复杂度的计算与数据范围反推，是 CSP 竞赛中"不超时"的最核心保障。

学完本章，你应能够：

1. **准确口述**算法、时间复杂度、大 O 表示法三者的定义，并能区分时间复杂度与"运行秒数"的差异。
2. **熟练计算**任意单层/嵌套/对数循环代码片段的复杂度，10 段代码正确率 ≥ 9 道。
3. **默写**常见复杂度层级链：$O(1) < O(\log n) < O(n) < O(n\log n) < O(n^2) < O(2^n)$，并能画出增长趋势对比图。
4. **根据数据范围 $n$ 反推**可接受的复杂度上界（如 $n \leq 10^5$ → 必须 $O(n\log n)$ 或更优），匹配正确率 100%。
5. **运用"1 秒 ≈ $10^8$ 次运算"**经验公式，预估程序运行时间，判断是否超时。

> 📌 要点
> - 时间复杂度衡量的是**增长趋势**，不是绝对秒数。
> - 初赛考"判断复杂度"，复赛考"根据数据范围选算法"。
> - "1 秒 ≈ $10^8$ 次运算"是贯穿竞赛始终的铁律。

---

## 💡 生活类比

> 💡 一句话概括：把时间复杂度想象成"比两个人的工作效率"——不看具体分钟数，只看任务量翻倍后用时怎么涨。

!!! tip "时间复杂度像「比较两个人谁干活快」"

    假设你要比较 A、B 两位工人谁干活更高效，你**不会**这样比：

    - ❌ "A 今天用了 30 分钟，B 用了 35 分钟" —— 因为今天的活儿可能本身就不一样多，且 A 的工具可能更好。
    - ❌ "A 处理 10 件用了 30 分钟，B 处理 10 件用了 35 分钟" —— 这只看了**一个固定点**，看不出"任务量变大后会怎样"。

    正确的比法是看**工作量随任务规模增大怎么涨**：

    | 任务量 $n$ | A 用时 | B 用时 |
    |-----------|--------|--------|
    | 10 | 30 分钟 | 35 分钟 |
    | 100 | 50 分钟 | 350 分钟 |
    | 1000 | 70 分钟 | 3500 分钟 |

    A 的用时增长很慢（随 $n$ 几乎不变 → $O(1)$ 趋势），B 的用时线性增长（→ $O(n)$ 趋势）。**任务量越大，A 越占优势**。

    时间复杂度就是这种"**忽略具体秒数、只看增长趋势**"的比较方法。它告诉我们：当 $n$ 趋于无穷大时，谁会被远远甩在后面。

> 📌 要点
> - 时间复杂度不看具体时间，看**增长趋势**。
> - 只有数据规模 $n$ 足够大时，复杂度差异才会体现出来。
> - 常数差异在**同阶**算法之间才值得比较。

---

## 核心概念

> 💡 一句话概括：算法是解决问题的步骤序列，大 O 表示法描述它随数据规模增长的最坏趋势，化简时只保留最高阶项。

### 一、什么是算法

「算法 algorithm」是**解决一类问题的明确、有限步骤序列**。它有五个重要特征：

| 特征 | 含义 | 举例 |
|------|------|------|
| **有穷性** | 步骤必须有限 | 不能死循环 |
| **确定性** | 每步含义明确 | 不能有歧义 |
| **可行性** | 每步都能在有限时间内完成 | 不能做做不到的操作 |
| **输入** | 有 0 个或多个输入 | 可以无输入 |
| **输出** | 有 1 个或多个输出 | 必须有结果 |

!!! info "算法 vs 程序"
    算法是**思想**，程序是**算法的具体实现**。同一个算法可以用 C++、Python 等不同语言实现，复杂度本质相同。

### 二、为什么需要时间复杂度

> 📖 通俗解释：同一段程序在不同电脑上跑的时间可能差几倍，因此不能直接用"秒数"评价算法好坏。需要一个与机器无关的衡量标准——看**运算次数**随数据量 $n$ 怎么增长。

!!! warning "为什么不直接测运行秒数"
    同一段程序：
    - 在高性能电脑上跑 0.5 秒
    - 在老旧电脑上跑 2 秒

    难道这段程序"在快电脑上是好算法、在慢电脑上是坏算法"？显然不合理。

    所以需要一个**与机器无关**的衡量标准——**用运算次数随数据规模 $n$ 的增长趋势**来衡量，这就是时间复杂度。

### 三、大 O 表示法

「大 O 表示法 Big O notation」描述：当 $n$ 足够大时，算法运算次数 $T(n)$ 的**渐近上界**。记作：

$$T(n) = O(f(n))$$

**化简三规则**（牢记）：

| 规则 | 说明 | 示例 |
|------|------|------|
| 1. 去常数系数 | 常数倍不影响增长阶 | $O(3n) = O(n)$，$O(100) = O(1)$ |
| 2. 去低阶项 | 低阶项在 $n$ 大时微不足道 | $O(n^2 + n) = O(n^2)$ |
| 3. 只留最高阶 | 最高阶决定增长趋势 | $O(2n^2 + 100n + 1000) = O(n^2)$ |

!!! tip "为什么能这样化简"
    当 $n \to \infty$ 时，常数项和低阶项对总量的贡献**微乎其微**。
    例如 $n=10^5$ 时，$n^2 = 10^{10}$，而 $100n$ 仅 $10^7$，差 1000 倍，可忽略。

### 四、常见时间复杂度层级表

| 复杂度 | 名称 | $n=10$ | $n=100$ | $n=1000$ | $n=10^5$ | 直观感受 |
|--------|------|--------|---------|----------|----------|----------|
| $O(1)$ | 常数 | 1 | 1 | 1 | 1 | 秒级，永远不超时 |
| $O(\log n)$ | 对数 | 3 | 7 | 10 | 17 | 极快，几乎无感 |
| $O(n)$ | 线性 | 10 | 100 | 1000 | $10^5$ | 快，遍历一遍 |
| $O(n\log n)$ | 线性对数 | 33 | 664 | 9966 | $1.7\times10^6$ | 较快，sort 级别 |
| $O(n^2)$ | 平方 | 100 | $10^4$ | $10^6$ | $10^{10}$ | 慢，$n\leq 1000$ 才行 |
| $O(n^3)$ | 立方 | 1000 | $10^6$ | $10^9$ | 爆炸 | 很慢，$n\leq 100$ |
| $O(2^n)$ | 指数 | 1024 | 天文 | 天文 | 天文 | 爆炸，$n\leq 25$ |
| $O(n!)$ | 阶乘 | $3.6\times10^6$ | 天文 | 天文 | 天文 | 灾难，$n\leq 12$ |

### 五、增长趋势对比

随着数据规模 $n$ 增大，各复杂度函数的增长趋势如下：

- **$O(1)$**：始终贴着 x 轴，几乎不变。
- **$O(\log n)$**：增长极其缓慢，$n$ 翻倍才多 1 次运算。
- **$O(n)$**：线性增长，数据翻倍、运算也翻倍。
- **$O(n\log n)$**：略高于线性，$n$ 翻倍时运算增约 2 倍多。
- **$O(n^2)$**：抛物线增长，$n$ 翻倍时运算增 4 倍。
- **$O(n^3)$**：更陡峭的抛物线。
- **$O(2^n)$**：指数爆炸，$n$ 每增加 1，运算量翻倍。
- **$O(n!)$**：阶乘爆炸，增长最快，$n$ 稍大即不可计算。

**增长快慢排序**：$O(1) < O(\log n) < O(n) < O(n\log n) < O(n^2) < O(n^3) < O(2^n) < O(n!)$

!!! tip "记忆口诀"
    **1 < log < n < nlog < n² < n³ < 2ⁿ < n!**

    简记："**一 log 线 线log 平 立 指 阶**"。

!!! info "建议用在线工具生成精美曲线图"
    可访问 [Desmos](https://www.desmos.com/calculator) 输入 $y=1, y=\log_2 x, y=x, y=x\log_2 x, y=x^2, y=2^x$ 对比曲线；或用 [GraphFunc](https://www.graphfunc.com/) 一键绘制。课件中建议替换为彩色矢量图。

> 📌 要点
> - 大 O 化简三规则：去常数、去低阶、留最高阶。
> - 记住复杂度层级链：$O(1) < O(\log n) < O(n) < O(n\log n) < O(n^2) < O(2^n)$。
> - 当 $n$ 达到 $10^5$ 时，$O(n^2)$ 已不可接受（$10^{10}$ 次运算远超 1 秒限制）。

---

## 代码实现

> 💡 一句话概括：下面给出每种复杂度的典型代码模式——看一遍循环结构就能秒判复杂度，是初赛阅读题的必考技能。

下面给出每种复杂度的**典型代码片段**，统一使用 `#include <bits/stdc++.h>`。

### 1. $O(1)$ —— 常数复杂度

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    // 交换两数：固定 3 次赋值，与 n 无关
    int t = a;
    a = b;
    b = t;
    cout << a << " " << b;   // 输出：交换后的两数
    return 0;
}
```

### 2. $O(\log n)$ —— 对数复杂度

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    long long n;
    cin >> n;
    // i 每次翻倍：1→2→4→8→...→n，共 log2(n) 次
    long long i = 1, cnt = 0;
    while (i < n) {
        i *= 2;
        cnt++;
    }
    cout << cnt;          // 输出：log2(n) 的近似值（如 n=1000 → 输出 10）
    return 0;
}
```

!!! tip "对数复杂度的识别特征"
    循环变量**乘/除**一个常数（`*= 2`、`/= 2`），就是 $O(\log n)$。二分查找也属此类。

### 3. $O(n)$ —— 线性复杂度

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    // 单层循环遍历一次
    long long sum = 0;
    for (int i = 1; i <= n; i++) {
        sum += i;
    }
    cout << sum;          // 输出：1+2+...+n 的和（如 n=5 → 输出 15）
    return 0;
}
```

### 4. $O(n\log n)$ —— 线性对数复杂度

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int a[100005];
    for (int i = 0; i < n; i++) cin >> a[i];
    // sort 内部是快排/内省排序，复杂度 O(n log n)
    sort(a, a + n);
    for (int i = 0; i < n; i++) cout << a[i] << " ";  // 输出：升序排列的数组
    return 0;
}
```

### 5. $O(n^2)$ —— 平方复杂度

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    // 双重嵌套循环：内层执行 n 次，外层也 n 次，共 n*n 次
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cout << i * j << " ";
        }
        cout << "\n";     // 输出：n×n 的乘法表
    }
    return 0;
}
```

### 6. $O(2^n)$ —— 指数复杂度

```cpp
#include <bits/stdc++.h>
using namespace std;

// 递归求斐波那契第 n 项：每层调两次自己，呈二叉树展开
long long fib(int n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);     // 两个递归调用 → O(2^n)
}

int main() {
    int n;
    cin >> n;
    cout << fib(n);     // 输出：斐波那契第 n 项（如 n=6 → 输出 8）
    return 0;
}
```

!!! warning "指数复杂度的恐怖"
    `fib(40)` 就要调用约 $10^8$ 次函数，普通电脑要跑好几秒。$n \geq 45$ 基本卡死。

### 关键代码模板

> 📖 通俗解释：初赛阅读题中，90% 的复杂度判断题都可以套用下面的对照表——

| 代码模式 | 复杂度 | 识别特征 | 典型算法 |
|---------|--------|---------|---------|
| 固定次数操作 | $O(1)$ | 无循环 / 循环次数与 $n$ 无关 | 交换、取模、数组下标访问 |
| `i *= 2` 或 `i /= 2` | $O(\log n)$ | 循环变量乘/除常数 | 二分查找、快速幂 |
| 单层 `for (i=0; i<n; i++)` | $O(n)$ | 遍历一遍 | 顺序查找、前缀和 |
| `sort` 调用 | $O(n\log n)$ | 标准库排序 | 快排、归并排序 |
| 两层独立嵌套 `for` | $O(n^2)$ | 内外层均从 0 到 n | 冒泡、选择排序、简单 DP |
| 三层嵌套 `for` | $O(n^3)$ | 三层均从 0 到 n | Floyd 最短路 |
| 两路递归无记忆化 | $O(2^n)$ | 每层分裂为两个子调用 | 朴素斐波那契 |
| 全排列枚举 | $O(n!)$ | 枚举所有排列 | 旅行商暴力 |

> 📌 要点
> - 看到 `i *= 2` 就想到 $O(\log n)$，看到两重循环就想到 $O(n^2)$。
> - 嵌套循环的复杂度是**内层总次数**的求和，不是简单"层数相乘"。
> - 递归复杂度取决于递归树形态，**重复计算**才是慢的根源。

---

## ⚠️ 常见误区

> 💡 一句话概括：以下四个误区是初赛复杂度判断题中最常见的失分点，务必逐一辨析清楚。

### 常见陷阱

#### 陷阱 1：误认为 $O(2n)$ 比 $O(n)$ 慢一档

```cpp
for (int i = 0; i < 2 * n; i++) cout << i;   // 2n 次
for (int i = 0; i < n; i++)     cout << i;   // n  次
```

- ❌ **错误理解**：第一段执行 $2n$ 次，比第二段慢一倍，所以是 $O(2n)$，比 $O(n)$ 慢一档。
- ✅ **正确理解**：大 O 表示法**忽略常数系数**，$O(2n) = O(n)$。它们**同属一个量级**，只是常数差异。真正影响算法好坏的是**阶**（$n$、$n\log n$、$n^2$……），不是常数。

!!! tip "什么时候常数才重要"
    只有当两个算法**同阶**（如都是 $O(n\log n)$）时，才比较常数大小。比如快排和归并都是 $O(n\log n)$，但快排常数更小，实际更快。

#### 陷阱 2：混淆最坏 / 平均 / 最好情况

```cpp
// 在数组 a 中顺序查找 x
for (int i = 0; i < n; i++) {
    if (a[i] == x) { cout << i; break; }
}
```

- ❌ **错误理解**：这段代码就是 $O(n)$。
- ✅ **正确理解**：要分三种情况讨论——
  - **最好情况**：$x$ 在第一个位置，循环 1 次 → $O(1)$
  - **最坏情况**：$x$ 在末尾或不存在，循环 $n$ 次 → $O(n)$
  - **平均情况**：$x$ 平均在中间，循环 $n/2$ 次 → $O(n)$

!!! warning "竞赛中默认讨论最坏情况"
    题目的时间限制是按**最坏情况**算的。所以平时说"这段代码是 $O(n)$"，默认指最坏复杂度。
    但分析题意时要警惕：若数据有特殊性质（如已排序），平均/最好情况可能更优。

#### 陷阱 3：忽略嵌套循环的内层变化

```cpp
for (int i = 0; i < n; i++) {
    for (int j = i; j < n; j++) {     // 注意：j 从 i 开始！
        sum++;
    }
}
```

- ❌ **错误理解**：双重循环，所以是 $O(n^2)$。（结论碰巧对，但理由不全）
- ✅ **正确理解**：内层次数随 $i$ 变化——
  - $i=0$：内层跑 $n$ 次
  - $i=1$：内层跑 $n-1$ 次
  - ...
  - $i=n-1$：内层跑 $1$ 次

  总次数 $= n + (n-1) + \dots + 1 = \dfrac{n(n+1)}{2} = \dfrac{1}{2}n^2 + \dfrac{1}{2}n$，去常数和低阶后仍是 $O(n^2)$。

!!! tip "判断嵌套循环的通法"
    不要简单"层数相乘"，要**算内层总次数**。若内层次数依赖外层变量，需用求和公式计算。

#### 陷阱 4：认为递归一定是 $O(2^n)$

- ❌ **错误理解**：递归 = 指数级，很慢。
- ✅ **正确理解**：递归复杂度取决于**递归树形态**——
  - 二分查找递归：每层只递归一个分支 → $O(\log n)$
  - 归并排序递归：每层递归两个分支但合并是 $O(n)$，共 $\log n$ 层 → $O(n\log n)$
  - 朴素斐波那契递归：两个分支且无重叠计算 → $O(2^n)$

  递归本身不慢，**重复计算**才慢。加记忆化后，$O(2^n)$ 的斐波那契可降到 $O(n)$。

> 📌 要点
> - 大 O 忽略常数系数，$O(2n) = O(n)$，别被系数迷惑。
> - 竞赛默认讨论**最坏情况复杂度**。
> - 嵌套循环不等于简单层数相乘，要算**内层总次数**。

---

## 典型例题

> 💡 一句话概括：通过一道综合例题，实战"拆→算→合"三步法，串联多段代码的复杂度分析。

### 例题：分析下面代码的时间复杂度

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;

    // 片段 A
    int s = 0;
    for (int i = 0; i < n; i++) {
        s += i;
    }

    // 片段 B
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < m; j++) {
            s += i * j;
        }
    }

    // 片段 C
    int x = 1;
    while (x < n) {
        for (int k = 0; k < 100; k++) {
            s++;
        }
        x *= 2;
    }

    cout << s;    // 输出：三段累加后的总和
    return 0;
}
```

**问题**：分别求片段 A、B、C 的复杂度，并求整段程序的总复杂度。

### 答案解析

**片段 A**：单层循环 $n$ 次 → $O(n)$。

**片段 B**：外层 $n$ 次，内层 $m$ 次，且 $m$ 与外层无关 → 总次数 $n \times m$ → $O(nm)$。

**片段 C**：外层 `while` 因 `x *= 2` 跑 $\log_2 n$ 次；内层固定 100 次（常数！）。
总次数 $= 100 \cdot \log_2 n$，去常数 → $O(\log n)$。

**总复杂度**：多段代码串联，取**最大项**。

| 条件 | 各段复杂度 | 总复杂度 |
|------|-----------|---------|
| $m$ 与 $n$ 同阶（$m = O(n)$） | $O(n) + O(n^2) + O(\log n)$ | $O(n^2)$ |
| $m \ll n$（如 $m$ 是常数） | $O(n) + O(n) + O(\log n)$ | $O(n)$ |

!!! tip "解题三步法"
    1. **拆**：把代码拆成独立片段。
    2. **算**：每个片段单独算复杂度，注意内层是否依赖外层变量、是否有常数。
    3. **合**：串联取最大，嵌套取乘积（或求和）。

!!! warning "警惕题目中的隐藏变量"
    题目若给出 $n, m$ 两个变量，要先问"$m$ 和 $n$ 同阶吗？"。CSP-S 阅读题常在此设坑。

> 📌 要点
> - 串联代码取**最大复杂度项**，嵌套代码取**乘积或求和**。
> - 内层循环次数为常数时，视为 $O(1)$，不影响外层复杂度。
> - 遇到多变量（$n, m$）时，先判断变量间关系再下结论。

---

## 📚 本节练习

> 💡 一句话概括：六道分层练习，从基础判断到 CSP-S 真题变式，覆盖复杂度分析的全部实战场景。

### 🟢 基础题（巩固概念）

**练习 1（复杂度判断）**：写出下列四段代码的时间复杂度，并判断 $n = 10^5$ 时哪些会在 1 秒内超时。

```cpp
① for (int i = 0; i < n; i++) sum += i;
② for (int i = 0; i < n; i++)
     for (int j = 0; j < n; j++) sum += i * j;
③ for (int i = 1; i < n; i *= 2) sum += i;
④ for (int i = 0; i < n; i++)
     for (int j = i; j < n; j++) sum++;
```

> **提示**：①单层循环 $O(n)$；②两层嵌套 $O(n^2)$；③每次翻倍 $O(\log n)$；④内层从 $i$ 起，总次数 $\dfrac{n(n+1)}{2}$ 仍 $O(n^2)$。
> 按 1 秒 $\approx 10^8$ 次：$n=10^5$ 时 $O(n^2) \approx 10^{10}$ 超时，其余不超时。

**练习 2（洛谷 P1424 小鱼的航程）**：小鱼平日每天游泳 250 公里，周末休息。给定起始星期 $x$ 和经过天数 $n$（$n \leq 10^6$），求累计游泳公里数。

> **提示**：模拟每周 7 天循环，用取模判断周末。复杂度 $O(n)$，$n=10^6$ 约 $10^6$ 次运算，秒过。体会线性复杂度的效率边界。

### 🟡 拓展题（CSP-S 入门）

**练习 3（洛谷 P1216 数字三角形）**：给定数字三角形，从顶向下走，每次走到下方相邻的两个数之一，求路径最大和。$n \leq 1000$。

> **提示**：朴素递归枚举所有路径是 $O(2^n)$，$n=1000$ 必然超时（回顾指数复杂度的恐怖）。
> 改用递推，每格只算一次，复杂度降为 $O(n^2)$。这正是"根据数据范围选算法"的典型：$n \leq 1000$ → 可接受 $O(n^2)$。

**练习 4（复杂度分析进阶）**：分析下面代码的复杂度（注意 $m$ 与 $n$ 的关系）。

```cpp
for (int i = 0; i < n; i++) {
    for (int j = 0; j < m; j++) {
        a[i][j] = i + j;
    }
}
for (int k = 0; k < 1000000; k++) {
    sum++;
}
```

> **提示**：第一部分 $O(nm)$；第二部分看似 100 万次很多，但与 $n$ 无关，是常数 $O(1)$。总复杂度 $O(nm)$。
> 若 $m = O(n)$ 则为 $O(n^2)$。本题训练"区分变量与常数"。

### 🔴 CSP-S 真题变式（进阶）

**练习 5（洛谷 P1115 最大子段和）**：给定 $n$ 个整数（$n \leq 2\times10^5$），求一段连续非空子序列和的最大值。

> **提示**：若用两重循环枚举所有子段是 $O(n^2)$，$n=2\times10^5$ 时约 $4\times10^{10}$ 次运算，必然超时。
> 需要 $O(n)$ 算法（在线处理：累加和为负则舍弃、从当前元素重新开始）。
> 训练核心：先看数据范围反推所需复杂度——$n \leq 2\times10^5$ 意味着必须优于 $O(n^2)$。

**练习 6（洛谷 P1226 快速幂）**：给定 $b, p, k$，求 $b^p \bmod k$。$0 \leq b, p < 2^{31}$。

> **提示**：朴素循环 $p$ 次相乘是 $O(p)$，$p$ 可达 $2^{31}$，必超时。
> 快速幂利用 $b^p = (b^2)^{p/2}$，每次把指数减半，复杂度 $O(\log p)$，$p=2^{31}$ 也只需 31 次运算。
> 本题是**对数复杂度的威力**的最佳例证。

> 📌 要点
> - 遇到 $n \leq 10^5$ 就想到 $O(n\log n)$ 或更优，$n \leq 10^6$ 必须是 $O(n)$。
> - 指数/阶乘复杂度在 $n > 25$ 时完全不可用。
> - 快速幂是对数复杂度威力的最佳例证：$p=2^{31}$ 只需 31 次运算。

---

## 📖 本章总结

> 💡 一句话概括：复杂度分析是竞赛的"元技能"——会算复杂度，才能选对算法、不超时。

### 一、核心要点回顾

- ✅ **算法**是解决问题的有限步骤序列，有有穷性、确定性、可行性、输入、输出五大特征。
- ✅ **时间复杂度**用运算次数随 $n$ 的增长趋势衡量算法快慢，与机器无关。
- ✅ **大 O 表示法**化简三规则：去常数系数、去低阶项、只留最高阶。
- ✅ **复杂度排序**：$O(1) < O(\log n) < O(n) < O(n\log n) < O(n^2) < O(2^n) < O(n!)$。
- ✅ **1 秒定律**：CSP 中 1 秒约能执行 $10^8$ 次简单运算。
- ✅ **数据范围反推算法**：$n \leq 20$ 用 $O(2^n)$，$n \leq 1000$ 用 $O(n^2)$，$n \leq 10^5$ 用 $O(n\log n)$，$n \leq 10^6$ 用 $O(n)$，$n \leq 10^9$ 用 $O(\log n)$。
- ✅ **嵌套循环**不要简单层数相乘，要算内层总次数；**多段串联**取最大项。
- ✅ 递归不一定慢，**重复计算**才慢，记忆化可大幅降阶。

### 二、复杂度速查表

| 代码模式 | 复杂度 | 典型算法 |
|---------|--------|---------|
| 单条语句 / 固定次操作 | $O(1)$ | 交换、取模 |
| 循环变量乘/除常数 | $O(\log n)$ | 二分查找、快速幂 |
| 单层循环 | $O(n)$ | 顺序查找、前缀和 |
| `sort` / 归并 | $O(n\log n)$ | 快排、归并排序 |
| 两层独立嵌套循环 | $O(n^2)$ | 冒泡、选择排序 |
| 三层嵌套循环 | $O(n^3)$ | Floyd 最短路 |
| 两路递归无记忆化 | $O(2^n)$ | 朴素斐波那契 |
| 全排列枚举 | $O(n!)$ | 旅行商暴力 |

### 三、数据范围 → 算法复杂度对照表

| 数据范围 $n$ | 可接受复杂度 | 时间（1秒限制） |
|-------------|-------------|----------------|
| $\leq 20$ | $O(2^n)$ | $\approx 10^6$ 次 |
| $\leq 100$ | $O(n^3)$ | $\approx 10^6$ 次 |
| $\leq 1000$ | $O(n^2)$ | $\approx 10^6$ 次 |
| $\leq 10^5$ | $O(n\log n)$ | $\approx 1.7\times10^6$ 次 |
| $\leq 10^6$ | $O(n)$ | $\approx 10^6$ 次 |
| $\leq 10^9$ | $O(\log n)$ | $\approx 30$ 次 |

### 四、知识脉络

- **算法定义**
  - 五大特征：有穷性、确定性、可行性、输入、输出
  - 算法 vs 程序：算法是思想，程序是实现
- **大 O 表示法**
  - 化简三规则：去常数系数、去低阶项、只留最高阶
  - 复杂度层级链：$O(1) < O(\log n) < O(n) < O(n\log n) < O(n^2) < O(2^n) < O(n!)$
- **实战估算**
  - 1 秒定律：1 秒 ≈ $10^8$ 次运算
  - 数据范围反推算法：根据 $n$ 上限选择合适复杂度的算法
  - 空间限制：同样需关注，但本章重点在时间
- **常见代码模式识别**
  - 单层循环 → $O(n)$
  - 双重独立嵌套 → $O(n^2)$
  - 循环变量 `*=2` → $O(\log n)$
  - `sort` 调用 → $O(n\log n)$
  - 两路无记忆化递归 → $O(2^n)$

### 五、判断复杂度的"三步法"口诀

> **拆 → 算 → 合**
>
> - **拆**：把代码拆成独立片段。
> - **算**：每段单独算，注意内层是否依赖外层、是否有常数。
> - **合**：串联取最大，嵌套取乘积（或求和）。

> 📌 要点
> - 复杂度的核心是"增长趋势"，不是绝对数值。
> - 数据范围反推算法是**复赛不超时**的最关键技巧。
> - 初赛阅读题中，嵌套循环和隐藏变量是最常见的陷阱点。

### 知识衔接

本章是CSP-J/S入门教材模块二（基础算法）的开篇，与前后章节的联系如下：

- **前置知识**：第01~09章C++基础——变量、输入输出、分支循环、数组、字符串、函数、结构体与文件操作（全部编程语言筑基知识）
- **本章定位**：从"写对代码"到"写快代码"的转折点，掌握时间复杂度分析（O记号）与数据范围估算法，是初赛必考、复赛必用的核心技能
- **后续发展**：为第11章「枚举与模拟算法」铺路——进入具体算法学习，用复杂度分析指导算法选择与优化

---

!!! tip "下一章预告"
    下一章我们将学习**枚举与模拟算法**——这是最基础的算法思想，也是竞赛中"骗分"的利器。学会它，你就能解决一半以上的 CSP-J/S 题！届时请结合本章的复杂度分析，判断你的枚举是否会超时。