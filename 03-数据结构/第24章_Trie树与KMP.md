# 第24章 Trie树与KMP：字符串的高效存储与匹配

`[CSP-S 必考]` 字符串算法是 CSP-S 的重要板块，Trie 与 KMP 是其中最基础的两块基石

!!! info "本章内容"
    本章将带你认识两种处理字符串的经典数据结构与算法。你将学习：

    - Trie（字典树）的核心思想与数组实现
    - Trie 的两大操作：`insert`（插入）与 `query`（查询）
    - Trie 的经典应用：字符串统计、最大异或对
    - KMP 算法解决的问题与 $O(n+m)$ 复杂度
    - `next` 数组（前缀函数）的定义与递推求法
    - KMP 匹配过程的完整实现

!!! warning "本章特点"
    Trie 和 KMP 都是"思想简单、代码短、但极易写错"的典型。Trie 的难点在数组下标的含义，KMP 的难点在 `next` 数组的递推与匹配时的回退。CSP-S 中 Trie 常与异或、贪心结合，KMP 常考 `next` 数组的性质应用。建议学完第7章字符串与第10章时间复杂度后再学本章。

---

## 📌 学习目标

完成本章学习后，你应当能够：

1. **独立默写** Trie 树的 `insert` 与 `query` 操作，理解 `son[N][26]`、`cnt[N]`、`idx` 三件套的含义
2. **准确说出** Trie 树插入与查询的时间复杂度均为 $O(|s|)$，与单词总数无关
3. **掌握** 01-Trie 的构建与贪心查询，能独立解决"最大异或对"类问题
4. **手算** KMP 的 `next` 数组（前缀函数），理解 `next[i]` 表示最长相等前后缀长度的含义
5. **独立写出** KMP 的完整匹配代码（`next` 递推 + 匹配），理解为何复杂度为 $O(n+m)$
6. **理解** border 链的性质（`ne[i] → ne[ne[i]] → ...`），能解决动物园（P2375）等进阶问题

---

## 第一节 问题引入：如何快速查找一个单词

!!! abstract "一个生活中的问题"
    查字典时，我们不会把字典从头到尾翻一遍来找 "apple"。我们会先翻到 "a" 开头的页，再找第二个字母 "p"，再找 "p"，再找 "l"，再找 "e"——**沿着字母一步步定位**。

    这种"按字符逐层深入"的查找方式，正是 **Trie 树**（字典树）的核心思想。

    假设要存储这 5 个单词：`cat`、`car`、`care`、`dog`、`do`，并支持：

    - 插入一个单词
    - 查询某个单词出现过几次

    **朴素做法**：用数组存所有字符串，每次查询线性扫描，单次 $O(L \cdot n)$，$n$ 个单词总成本 $O(n^2 L)$，单词数多时慢。

    **目标**：插入和查询都只需 $O(L)$（$L$ 为单词长度），与单词总数无关。这就是 Trie 树。

---

## 第二节 Trie树的核心思想

### 2.1 什么是Trie树

## 💡 生活类比：字典查单词

Trie 树就像一本"按字母分层"的字典：根结点是"起点"，第一层是单词首字母，第二层是第二个字母……走到某结点时，从根到该结点的路径就拼出了一个字符串前缀。

公共前缀的单词**共享同一条路径**，所以存储很省空间，查找也只需按字母逐层往下走。

把 `cat`、`car`、`care`、`dog`、`do` 插入 Trie 后，结构如下：

```
            (根 0)
           /      \
         c         d
        /           \
       a             o*
      / \           /
     t*  r         g*
         \
          e*
    （* 表示该结点是一个单词的结尾）
```

- `cat`：根 → c → a → t（t 标记结尾）
- `car`：根 → c → a → r（r 标记结尾）
- `care`：根 → c → a → r → e（e 标记结尾，r 同时也是 car 的结尾）
- `dog`：根 → d → o → g（g 标记结尾）
- `do`：根 → d → o（o 标记结尾）

可以看到 `car` 和 `care` 共享了 `ca` 与 `car` 前缀路径，节省了存储。

### 2.2 数组模拟实现

Trie 用**二维数组**模拟一棵多叉树：

```cpp
const int N = 100010;          // 总结点数上限
int son[N][26];                // son[p][c] = 结点 p 的第 c 个儿子（c=0..25 对应 a..z）的下标
int cnt[N];                    // cnt[p] = 以结点 p 为结尾的单词个数
int idx;                       // 当前已分配的结点编号，0 号是根结点（空结点）
```

**三个数组的含义**：

| 数组 | 含义 | 说明 |
|------|------|------|
| `son[p][c]` | 结点 `p` 通过字符 `c` 指向的子结点编号 | 为 0 表示"没有这个儿子" |
| `cnt[p]` | 以结点 `p` 为结尾的单词出现次数 | 区分"经过"与"结束" |
| `idx` | 已分配结点数 | 0 是根（空），新结点从 1 开始 |

!!! warning "为什么 0 既表示根又表示"空"？"
    这是一个巧妙的约定：根结点 0 不存任何字符，而 `son[p][c] == 0` 表示"该儿子不存在"。由于我们**永远不会把根当作某个结点的儿子**（新建结点用 `++idx`，编号从 1 开始），所以 0 的双重含义不会冲突。但要注意：**全局数组默认全 0**，这正好满足"初始时所有儿子都不存在"。如果 `son` 是局部数组未初始化，里面是随机值，会误指向不存在的结点——这是初学者常踩的坑。

### 2.3 两个核心操作

**插入 insert**：沿单词的每个字符往下走，没有对应儿子就新建。

```cpp
void insert(char *s) {
    int p = 0;                       // 从根结点出发
    for (int i = 0; s[i]; i++) {     // 逐字符处理（s[i] 为 '\0' 时结束）
        int u = s[i] - 'a';          // 把字符映射到 0..25
        if (!son[p][u]) {            // 没有这个儿子
            son[p][u] = ++idx;       // 新建一个结点
        }
        p = son[p][u];               // 走到儿子
    }
    cnt[p]++;                        // 标记单词结尾，计数 +1
}
```

**查询 query**：沿字符往下走，走不通说明没这个单词，走通了返回结尾计数。

```cpp
int query(char *s) {
    int p = 0;
    for (int i = 0; s[i]; i++) {
        int u = s[i] - 'a';
        if (!son[p][u]) return 0;    // 走不通，单词不存在
        p = son[p][u];
    }
    return cnt[p];                   // 返回该单词出现次数
}
```

**手动模拟**（依次插入 `cat`、`car`、`care` 后查询 `car`）：

```
初始：idx=0，所有 son[0][*]=0

insert("cat"):
  p=0, c='a'.. 'c'→u=2, son[0][2]=0→新建 idx=1, p=1
  p=1, 'a'→u=0, son[1][0]=0→新建 idx=2, p=2
  p=2, 't'→u=19, son[2][19]=0→新建 idx=3, p=3
  cnt[3]++ → cnt[3]=1

insert("car"):
  p=0,'c'→son[0][2]=1 已存在,p=1
  p=1,'a'→son[1][0]=2 已存在,p=2
  p=2,'r'→son[2][17]=0→新建 idx=4,p=4
  cnt[4]++ → cnt[4]=1

insert("care"):
  p=0,'c'→1; p=1,'a'→2; p=2,'r'→4 已存在
  p=4,'e'→新建 idx=5,p=5
  cnt[5]++ → cnt[5]=1

query("car"): 走 0→1→2→4，返回 cnt[4]=1 ✓
```

### 2.4 完整模板

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100010;
int son[N][26], cnt[N], idx;

void insert(char *s) {
    int p = 0;
    for (int i = 0; s[i]; i++) {
        int u = s[i] - 'a';
        if (!son[p][u]) son[p][u] = ++idx;
        p = son[p][u];
    }
    cnt[p]++;
}

int query(char *s) {
    int p = 0;
    for (int i = 0; s[i]; i++) {
        int u = s[i] - 'a';
        if (!son[p][u]) return 0;
        p = son[p][u];
    }
    return cnt[p];
}

int main() {
    int n;
    cin >> n;
    while (n--) {
        char op[2], s[N];
        cin >> op >> s;
        if (op[0] == 'I') insert(s);
        else cout << query(s) << "\n";
    }
    return 0;
}
```

!!! tip "数组大小怎么开"
    结点总数上限 = 所有字符串长度之和 + 1（根）。例如 $n$ 个单词每个最长 $L$，则 $N = n \cdot L + 5$。开小会越界 RE，开大浪费空间。`son[N][26]` 是 `int` 数组，$N=10^5$ 时约 $10\text{MB}$，注意内存限制。

---

## 第三节 Trie的复杂度与常见误区

### 3.1 复杂度分析

| 操作 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| `insert(s)` | $O(|s|)$ | $O(\sum |s_i| \cdot 26)$ |
| `query(s)` | $O(|s|)$ | — |

插入和查询都只与单词长度有关，与单词总数无关，这是 Trie 的核心优势。

### 3.2 常见误区

!!! warning "误区1：忘记 `son` 数组是全局变量（未初始化）"
    `son` 必须是**全局数组**（默认初始化为 0），或在 `main` 内用 `memset` 清 0。如果声明为局部数组且未初始化，里面是随机值，`son[p][u]` 会指向一个垃圾下标，导致越界或错误。

    ```cpp
    // ❌ 错误：局部数组未初始化
    int main() {
        int son[N][26];          // 随机值！
        ...
    }
    // ✅ 正确：全局数组
    int son[N][26];              // 自动全 0
    int main() { ... }
    ```

!!! warning "误区2：混淆"经过"与"结尾""
    `cnt[p]` 只在单词**最后一个字符对应的结点**上 +1。查询时必须走到结尾结点才返回 `cnt[p]`，不能在中间结点返回。例如插入 `care` 后，查询 `car` 应返回 1（`car` 也是单独插入的），但查询 `ca` 应返回 0（`ca` 不是完整单词）。判断"是否为完整单词"靠的是 `cnt[p]`，不是"能否走到这一步"。

!!! warning "误区3：新建结点用 `idx++` 而非 `++idx`"
    根结点是 0 号，新结点必须从 1 号开始。若写成 `son[p][u] = idx++`，第一个新结点会变成 0 号——和根冲突！正确写法是 `++idx`（先自增再赋值），保证新结点编号 $\ge 1$。

!!! warning "误区4：字符集范围搞错"
    `son[p][c]` 的第二维大小取决于字符集。小写字母用 26，数字用 10，0/1 用 2（最大异或问题）。字符到下标的映射：小写字母 `s[i]-'a'`，数字 `s[i]-'0'`，二进制位直接用 0/1。

---

## 第四节 Trie的经典应用

### 4.1 应用一：字符串统计（洛谷 P2580）

**题目**：给定 $n$ 个名字（单词）组成字典，再有 $m$ 次点名。每次点名输出：名字不在字典中输出 `WRONG`；第一次点到输出 `OK`；之后重复点到输出 `REPEAT`。

**思路**：用 Trie 存字典。`cnt[p]` 记录该名字被点到的次数：0 表示不在字典（`WRONG`），1 表示第一次（`OK`），>1 表示重复（`REPEAT`）。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 500010;
int son[N][26], cnt[N], idx;

void insert(const string &s) {
    int p = 0;
    for (char ch : s) {
        int u = ch - 'a';
        if (!son[p][u]) son[p][u] = ++idx;
        p = son[p][u];
    }
    cnt[p] = 1;                 // 初始标记为"在字典中，尚未被点过"
}

int query(const string &s) {
    int p = 0;
    for (char ch : s) {
        int u = ch - 'a';
        if (!son[p][u]) return 0;   // 不在字典
        p = son[p][u];
    }
    if (cnt[p] == 0) return 0;      // 不在字典
    if (cnt[p] == 1) { cnt[p]++; return 1; }  // 第一次点到
    return 2;                       // 重复
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n; cin >> n;
    while (n--) { string s; cin >> s; insert(s); }
    int m; cin >> m;
    while (m--) {
        string s; cin >> s;
        int r = query(s);
        cout << (r == 0 ? "WRONG" : (r == 1 ? "OK" : "REPEAT")) << "\n";
    }
    return 0;
}
```

### 4.2 应用二：最大异或对（整数二进制插入Trie）

**问题**：给定 $n$ 个整数 $a_1, \dots, a_n$，选出两个数使它们的异或值最大。

**思路**：把每个整数看成 31 位二进制串（从高位到低位），插入 01-Trie。对每个数 $a_i$，在 Trie 上"贪心"地走**相反位**——高位能取到 1 就尽量取 1，这样异或值最大。

## 💡 生活类比：选相反的走法

想让两个二进制数的异或尽量大，就要让高位尽量不同。就像两个人走岔路，**每个路口都选相反方向**，差距就最大。Trie 上从高位往低位走，每步优先选与当前位相反的儿子。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100010, M = 31 * N;   // 每个数最多 31 位
int son[M][2], idx;
int a[N];

void insert(int x) {
    int p = 0;
    for (int i = 30; i >= 0; i--) {  // 从高位到低位
        int u = (x >> i) & 1;
        if (!son[p][u]) son[p][u] = ++idx;
        p = son[p][u];
    }
}

int query(int x) {                   // 返回 x 与 Trie 中某数的最大异或值
    int p = 0, res = 0;
    for (int i = 30; i >= 0; i--) {
        int u = (x >> i) & 1;
        if (son[p][!u]) {            // 优先走相反位
            res += 1 << i;           // 该位异或为 1
            p = son[p][!u];
        } else {
            p = son[p][u];           // 只能走相同位
        }
    }
    return res;
}

int main() {
    int n; cin >> n;
    for (int i = 0; i < n; i++) cin >> a[i];
    int ans = 0;
    for (int i = 0; i < n; i++) {
        insert(a[i]);                // 先插入，再与前面对比（避免空 Trie）
        ans = max(ans, query(a[i]));
    }
    cout << ans << "\n";
    return 0;
}
```

!!! warning "高位到低位 vs 低位到高位"
    01-Trie 插入时**必须从高位到低位**处理。因为高位的权值更大（第 $i$ 位代表 $2^i$），贪心要优先让高位异或为 1。如果从低位到高位，低位先确定，高位反而被忽略，结果不是最优。这是 01-Trie 与普通字符串 Trie 的关键区别——字符串 Trie 从左到右（高位到低位）天然满足，而整数要手动控制位序。

---

## 第五节 KMP要解决的问题

### 5.1 字符串匹配问题

!!! abstract "问题定义"
    给定主串 $s$（长度 $n$）和模式串 $p$（长度 $m$），求 $p$ 在 $s$ 中所有出现位置。

    **朴素做法**：枚举 $s$ 的每个起点 $i$，逐字符比较 $p$，最坏 $O(nm)$。

    ```
    主串 s: a b a b a b c a
    模式 p: a b a b c
    起点 i=0: a b a b a → 第5个字符 a≠c，失败
    起点 i=1: b a b a b → 第1个字符 b≠a，失败
    起点 i=2: a b a b c → 全部匹配！✓
    ```

    朴素算法的问题：每次失败后，主串指针只回退 1 位，之前匹配的信息全部浪费。

## 💡 生活类比：避免重复比较的聪明匹配

你在长文本里找 "ababc"，已经匹配到 "abab" 时发现下一个不是 'c'。朴素做法会从头重新比，但你其实知道已匹配的 "abab" 里有规律——它的**前缀 "ab" 和后缀 "ab" 相同**！所以下次不必从起点重比，直接从 "ab" 之后继续即可。KMP 就是利用这个"已匹配部分的前后缀信息"来跳过重复比较的聪明算法。

### 5.2 KMP的核心思想

KMP 利用模式串自身的结构，**预处理出 `next` 数组**，使主串指针**永不回退**，从而把匹配复杂度从 $O(nm)$ 降到 $O(n+m)$。

| 算法 | 预处理 | 匹配 | 总复杂度 |
|------|--------|------|---------|
| 朴素 | 无 | $O(nm)$ | $O(nm)$ |
| **KMP** | $O(m)$ | $O(n)$ | $O(n+m)$ |

---

## 第六节 next数组（前缀函数）

### 6.1 next数组的定义

!!! info "前缀函数定义"
    对模式串 $p[1..m]$，定义 `next[i]`（也叫 `ne[i]`、`π[i]`）为：**$p[1..i]$ 的最长相等前后缀长度**（前后缀均不为整个串本身）。

    即：`next[i]` = 最大的 $k$（$0 \le k < i$），使得 $p[1..k] = p[i-k+1..i]$。

**举例**（模式串 `p = ababc`，1 下标）：

```
i=1, p[1..1]=a:       无非平凡前后缀      next[1]=0
i=2, p[1..2]=ab:      前缀a≠后缀b         next[2]=0
i=3, p[1..3]=aba:     前缀a=后缀a         next[3]=1
i=4, p[1..4]=abab:    前缀ab=后缀ab       next[4]=2
i=5, p[1..5]=ababc:   前缀...≠后缀...     next[5]=0
```

```
i=4 时 p[1..4]=abab：
  前缀 a  ─┐
  前缀 ab ─┼─→ 与后缀比较：ab == ab ✓ (长度2)
  前缀 aba─┘  与后缀比较：aba ≠ bab ✗
  所以 next[4]=2
```

!!! tip "next[i] 的直觉理解"
    `next[i]` 回答了这样一个问题："如果匹配到 $p[i]$ 时失败，模式串指针应该跳到哪里继续比，而不必从头开始？" 答案就是跳到 `next[i-1]` 处——因为 $p[1..i-1]$ 的前 `next[i-1]` 个字符和后 `next[i-1]` 个字符相同，主串那一段已经匹配过了。

### 6.2 next数组的递推求法

`next` 数组用**自己匹配自己**的方式递推求解，思路与 KMP 匹配几乎一样。

```cpp
// p[1..m]，1 下标
for (int i = 2, j = 0; i <= m; i++) {
    while (j && p[i] != p[j+1]) j = ne[j];   // 失配就回退
    if (p[i] == p[j+1]) j++;                 // 匹配则扩展
    ne[i] = j;                               // 记录
}
```

**手动模拟**（`p = abab`，1 下标，`ne[1]=0` 已知）：

```
i=2, j=0: p[2]=b, p[j+1]=p[1]=a, b≠a, while 不进
          p[2]==p[1]? 否 → j 不变, ne[2]=0
i=3, j=0: p[3]=a, p[1]=a, a==a → j=1, ne[3]=1
i=4, j=1: p[4]=b, p[j+1]=p[2]=b, b==b → j=2, ne[4]=2
```

!!! warning "误区1：next 数组的边界"
    - `ne[1] = 0`（单个字符没有非平凡前后缀），**不需要计算**，循环从 `i=2` 开始。
    - 比较的是 `p[i]` 和 `p[j+1]`（即"下一个要比的字符"），不是 `p[j]`。混用 `j` 和 `j+1` 是最常见的 bug。
    - `j` 初始为 0，`while (j && ...)` 中的 `j` 判空是为了防止越界（`j=0` 时不能再回退）。

!!! warning "误区2：while 循环不能写成 if"
    失配时可能要**连续回退多次**（`j = ne[j]` 后仍可能失配），所以必须用 `while` 而不是 `if`。写成 `if` 会导致 `next` 数组算错，是 KMP 最隐蔽的错误之一。

---

## 第七节 KMP匹配过程

### 7.1 匹配算法

主串指针 $i$ 只增不减，模式串指针 $j$ 利用 `next` 数组回退。

```cpp
// s[1..n] 主串, p[1..m] 模式串, ne[1..m] 已预处理
for (int i = 1, j = 0; i <= n; i++) {
    while (j && s[i] != p[j+1]) j = ne[j];   // 失配回退
    if (s[i] == p[j+1]) j++;                 // 匹配则推进
    if (j == m) {                            // 完整匹配
        cout << i - m + 1 << "\n";           // 输出起点（1 下标）
        j = ne[j];                           // 继续找下一处
    }
}
```

**手动模拟**（`s = ababab`，`p = abab`，`ne = [_,0,0,1,2]`）：

```
i=1, j=0: s[1]=a, p[1]=a → j=1
i=2, j=1: s[2]=b, p[2]=b → j=2
i=3, j=2: s[3]=a, p[3]=a → j=3
i=4, j=3: s[4]=b, p[4]=b → j=4 == m → 匹配！输出 4-4+1=1, j=ne[4]=2
i=5, j=2: s[5]=a, p[3]=a → j=3
i=6, j=3: s[6]=b, p[4]=b → j=4 == m → 匹配！输出 6-4+1=3, j=ne[4]=2
```

主串指针 $i$ 从 1 走到 6，**从未回退**，完美体现 KMP 的 $O(n)$ 特性。

!!! warning "误区3：匹配完成后未回退 j"
    当 `j == m`（完整匹配一次）后，若要继续找后续匹配，**必须执行 `j = ne[j]`**。因为当前 `p[1..m]` 已全部匹配，相当于在 `p[m+1]` 处"失配"，应回退到 `ne[m]`。如果忘记回退，会导致漏掉重叠出现的匹配（如上例中第二个 `abab` 就和第一个重叠）。

### 7.2 完整模板（洛谷 P3375）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1000010;
char s[N], p[N];
int ne[N];
int n, m;

int main() {
    cin >> s + 1 >> p + 1;          // 从下标 1 开始存
    n = strlen(s + 1);
    m = strlen(p + 1);

    // 求 next 数组（p 自己匹配自己）
    for (int i = 2, j = 0; i <= m; i++) {
        while (j && p[i] != p[j+1]) j = ne[j];
        if (p[i] == p[j+1]) j++;
        ne[i] = j;
    }

    // KMP 匹配
    for (int i = 1, j = 0; i <= n; i++) {
        while (j && s[i] != p[j+1]) j = ne[j];
        if (s[i] == p[j+1]) j++;
        if (j == m) {
            cout << i - m + 1 << "\n";
            j = ne[j];              // 继续匹配
        }
    }

    // 输出 next 数组（P3375 要求）
    for (int i = 1; i <= m; i++) cout << ne[i] << " ";
    return 0;
}
```

!!! tip "1 下标 vs 0 下标"
    KMP 在 OI 中通常用 **1 下标**写法（如上），因为 `j+1` 的比较和 `ne[1]=0` 的边界非常自然。若用 0 下标，需要把 `ne[0]` 设为 `-1` 并小心处理，容易出错。CSP-S 中推荐 1 下标写法，背熟一套模板即可。

---

## 第八节 KMP的复杂度与常见误区

### 8.1 复杂度分析

| 步骤 | 时间 | 证明直觉 |
|------|------|---------|
| 求 `next` | $O(m)$ | `j` 最多 +1 共 $m$ 次，每次 `while` 使 `j` 减 $\ge 1$，总减少不超过总增加 |
| 匹配 | $O(n)$ | 同理，`i` 每步 +1，`j` 增减均摊 |
| **总计** | $O(n+m)$ | — |

### 8.2 常见误区汇总

!!! warning "误区1：next 数组从 i=1 开始算"
    `ne[1]` 必为 0（单字符无非平凡前后缀），循环从 `i=2` 开始。若从 `i=1` 开始，`ne[1]` 会被错误覆盖。

!!! warning "误区2：比较时用 p[j] 而非 p[j+1]"
    `j` 表示"已匹配长度"，下一个要比的字符是 `p[j+1]`。误写成 `p[j]` 会比较错位置，导致 `next` 和匹配全错。这是 KMP 最容易写错的地方，**牢记：比的是下一个字符 `j+1`**。

!!! warning "误区3：用 if 代替 while 回退"
    失配时需要连续 `j = ne[j]` 直到能匹配或 `j=0`，必须用 `while`。用 `if` 只回退一次，遇到需要多次回退的情况就出错。

!!! warning "误区4：匹配成功后忘记 j = ne[j]"
    找到一处匹配后，若要继续找，必须 `j = ne[j]`。忘记这步会漏掉重叠匹配。

!!! warning "误区5：主串指针回退"
    KMP 的核心是**主串指针 $i$ 永不回退**。如果你发现自己在匹配循环里回退 $i$，那一定是写成了朴素算法，不是 KMP。

---

## 第九节 KMP的进阶应用

### 9.1 应用一：KMP模板（洛谷 P3375）

见 7.2 节完整模板。注意 P3375 要求：先输出所有匹配起点（1 下标，升序），再输出 `next[1..m]`。

### 9.2 应用二：动物园（洛谷 P2375）

**题目**：对模式串的每个前缀 $p[1..i]$，求有多少个"能同时作为前缀和后缀的真前后缀"，且其长度不超过 $i/2$。

**思路**：利用 `next` 数组的链式性质——`next[i]` 是最长 border，`next[next[i]]` 是次长 border，……，形成一条 border 链。预处理 `num[i]` 表示 $p[1..i]$ 的 border 链长度（递推：`num[i] = num[ne[i]] + 1`），再匹配时统计满足长度 $\le i/2$ 的 border 个数。

!!! tip "border 链：next 数组的递归结构"
    `next[i]` 给出最长 border，那"次长 border"是什么？它必然也是最长 border 的 border（因为相等前后缀的相等前后缀还是原串的相等前后缀）。所以从 `ne[i]` 出发再取 `ne[ne[i]]`，依次缩短，就得到所有 border。这条链的长度就是 `num[i]`。

    ```
    p = ababab, i=6:
      ne[6]=4 (abab), ne[4]=2 (ab), ne[2]=0
      border 链: 4→2→0，长度 num[6]=2（不算 0）
    ```

**核心代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1000010;
char p[N];
int ne[N], num[N];              // num[i] = p[1..i] 的 border 链长度
int m;

int main() {
    int T; scanf("%d", &T);
    while (T--) {
        scanf("%s", p + 1);
        m = strlen(p + 1);
        ne[1] = 0; num[1] = 0;
        for (int i = 2, j = 0; i <= m; i++) {
            while (j && p[i] != p[j+1]) j = ne[j];
            if (p[i] == p[j+1]) j++;
            ne[i] = j;
            num[i] = num[j] + 1;        // j 的 border 链 + 自己
        }
        long long ans = 1;
        for (int i = 2, j = 0; i <= m; i++) {
            while (j && p[i] != p[j+1]) j = ne[j];
            if (p[i] == p[j+1]) j++;
            int k = j;  // 使用临时变量，不修改 j（j 需在下一轮 KMP 匹配中保持状态）
            while (k * 2 > i) k = ne[k]; // 限制 border 长度 <= i/2
            ans = ans * (num[k] + 1) % 1000000007;
        }
        printf("%lld\n", ans);
    }
    return 0;
}
```

!!! warning "动物园题的关键技巧"
    匹配过程中维护的 `j` 表示当前匹配长度，但要求 border 长度 $\le i/2$，所以要用 `while (j*2 > i) j = ne[j]` 把超长的 border 缩短。注意这里是**对每个 $i$ 单独处理**，不能影响后续 `j` 的值（因为缩小后的 `j` 用于统计，下一轮 `i` 还要用真实的匹配长度）。这种"对 border 链做限制"的思路是 `next` 数组应用的常见套路。

---

## 第十节 最长异或路径（洛谷 P4551）

**题目**：给定一棵 $n$ 个结点的树，边有权值。求路径权值异或和最大的简单路径的异或值。

**思路**：树上任意路径 $u \to v$ 的边权异或和 = `xor(u,root) ^ xor(v,root)`（其中 `xor(u,root)` 是根到 $u$ 的路径异或和），因为重叠部分异或两次抵消。所以问题转化为：给 $n$ 个数（每个点到根的异或和），求两个数的最大异或值——正是 4.2 节的 01-Trie！

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100010, M = 31 * N;
int head[N], nxt[N<<1], to[N<<1], w[N<<1], tot;
int X[N];                         // X[i] = 根到 i 的异或和
int son[M][2], idx;
int n, ans;

void add(int u, int v, int c) {
    to[++tot]=v; w[tot]=c; nxt[tot]=head[u]; head[u]=tot;
}

void dfs(int u, int fa, int x) {
    X[u] = x;
    for (int i = head[u]; i; i = nxt[i]) {
        int v = to[i];
        if (v != fa) dfs(v, u, x ^ w[i]);
    }
}

void insert(int x) {
    int p = 0;
    for (int i = 30; i >= 0; i--) {
        int u = (x >> i) & 1;
        if (!son[p][u]) son[p][u] = ++idx;
        p = son[p][u];
    }
}

int query(int x) {
    int p = 0, res = 0;
    for (int i = 30; i >= 0; i--) {
        int u = (x >> i) & 1;
        if (son[p][!u]) { res += 1 << i; p = son[p][!u]; }
        else p = son[p][u];
    }
    return res;
}

int main() {
    cin >> n;
    for (int i = 1; i < n; i++) {
        int u, v, c; cin >> u >> v >> c;
        add(u, v, c); add(v, u, c);
    }
    dfs(1, 0, 0);                  // 以 1 为根求异或和
    for (int i = 1; i <= n; i++) {
        insert(X[i]);
        ans = max(ans, query(X[i]));
    }
    cout << ans << "\n";
    return 0;
}
```

!!! tip "Trie + 树/异或 的通用套路"
    凡是"树上路径异或"问题，先想到：路径异或和 = 两端点到根异或和的异或。然后转化为"选两个数使异或最大/最小"，用 01-Trie 解决。这是 CSP-S 中 Trie 最常见的综合考法。

---

## Trie与KMP对比

| 维度 | Trie | KMP |
|------|------|-----|
| 解决问题 | 字符串集合存储与查询 | 单模式串匹配 |
| 核心结构 | 多叉树（`son[p][c]`） | `next` 数组（前缀函数） |
| 预处理 | 边插边建 | $O(m)$ 求 `next` |
| 查询/匹配 | $O(L)$ 单串查询 | $O(n)$ 主串匹配 |
| 总复杂度 | $O(\sum L)$ | $O(n+m)$ |
| 常见考法 | 字符串统计、01-Trie 求最大异或 | 匹配位置、`next` 性质、border 链 |
| 易错点 | `son` 未初始化、`++idx` vs `idx++` | `j+1` 比较、`while` 非 `if`、匹配后回退 |

---

## 可视化构思：Trie与KMP交互演示

> 以下文字描述供后续前端开发使用，可独立实现为 HTML 交互页面。

### 设计目标
- Trie 演示：动态展示插入过程，结点编号、`cnt` 标记、共享前缀路径一目了然。
- KMP 演示：展示 `next` 数组求解与匹配过程中 `j` 的回退，主串指针永不回退。

### 界面布局

```
┌──────────────────────────────────────────────┐
│          Trie 树交互演示器                     │
├────────────────────┬─────────────────────────┤
│                    │  控制面板                │
│  Trie 可视化区     │  插入单词: [apple   ]    │
│  (SVG 树形布局)    │  [插入] [查询] [清空]    │
│                    │  当前 idx: 12           │
│  结点显示编号      │  单词列表:               │
│  结尾结点高亮      │  cat(1) car(1) care(1)  │
│  共享前缀加粗      │  dog(1) do(1)           │
├────────────────────┴─────────────────────────┤
│  操作日志:                                   │
│  > insert("cat"): 新建结点 1,2,3, cnt[3]=1   │
│  > query("car"): 走 0→1→2→4, 返回 1          │
└──────────────────────────────────────────────┘
```

### 核心动画效果

1. **Trie 插入动画**：每输入一个字符，从当前结点画出一条新边到新结点（结点编号递增显示），已存在的边则高亮闪烁。单词结尾结点变绿并显示 `cnt`。

2. **Trie 查询动画**：沿字符逐层走，每步高亮当前边。走不通时在缺失处显示红叉并返回 0；走到结尾显示 `cnt` 值。

3. **KMP `next` 求解动画**：双指针 $i$、$j$，失配时 `j` 沿 `ne[j]` 回退（用弧线箭头表示回退路径），匹配时 $j$ 前进。每步在表格中填入 `ne[i]`。

4. **KMP 匹配动画**：主串与模式串上下对齐，匹配的字符变绿，失配字符变红，`j` 回退时模式串"向右滑动"到新位置（体现 `next` 的作用），主串指针 $i$ 始终只向右移动。

5. **border 链可视化**：对模式串每个前缀，用不同颜色标注所有 border，直观展示 `ne[i] → ne[ne[i]] → ... → 0` 的链条。

---

## 本章小结

### 核心要点回顾

- ✅ **Trie** 用多叉树存储字符串集合，公共前缀共享路径，插入与查询均 $O(L)$
- ✅ Trie 数组实现三件套：`son[N][26]`、`cnt[N]`、`idx`，根为 0，新结点 `++idx` 从 1 起
- ✅ `son` 必须初始化为 0（全局数组天然满足），0 既表根又表"无儿子"不冲突
- ✅ **01-Trie**：整数按二进制高位到低位插入，贪心走相反位求最大异或
- ✅ **KMP** 利用模式串自身结构，主串指针不回退，匹配 $O(n)$
- ✅ `next[i]` = $p[1..i]$ 的最长相等前后缀长度，`ne[1]=0`，从 $i=2$ 递推
- ✅ 失配用 `while` 连续回退 `j = ne[j]`，比的是 `p[j+1]`
- ✅ 完整匹配后必须 `j = ne[j]` 才能继续找重叠匹配
- ✅ **border 链**：`ne[i] → ne[ne[i]] → ...`，是 `next` 数组进阶应用的基础

### 速查表

| 操作 | 代码 | 说明 |
|------|------|------|
| Trie 插入 | `if(!son[p][u]) son[p][u]=++idx; p=son[p][u];` | 逐字符建树 |
| Trie 查询 | `if(!son[p][u]) return 0; p=son[p][u];` | 走不通返回 0 |
| 01-Trie 查询 | `if(son[p][!u]){res+=1<<i; p=son[p][!u];}` | 贪心走相反位 |
| 求 `next` | `while(j&&p[i]!=p[j+1]) j=ne[j]; if(p[i]==p[j+1]) j++; ne[i]=j;` | 从 $i=2$ 起 |
| KMP 匹配 | `while(j&&s[i]!=p[j+1]) j=ne[j]; if(s[i]==p[j+1]) j++;` | 主串指针不回退 |
| 匹配后继续 | `j=ne[j];` | 找重叠匹配 |

### 知识脉络思维导图

- **字符串算法**
  - **Trie 字典树**
    - 核心实现：`son[N][26]` + `cnt[N]` + `idx`，根为 0
    - `insert`：逐字符建树，`++idx` 分配新结点
    - `query`：逐字符查找，走不通返回 0
    - **01-Trie**（异或）：二进制高位到低位插入，贪心走相反位求最大异或
    - 应用：字符串统计（P2580）、最长异或路径（P4551）
  - **KMP 匹配**
    - `next` 数组（前缀函数）：`ne[i]` = $p[1..i]$ 的最长相等前后缀长度
    - 递推求法：`while(j&&p[i]!=p[j+1]) j=ne[j];` 从 $i=2$ 起
    - 匹配过程：主串指针 $i$ 永不回退，$O(n)$ 复杂度
    - **border 链**：`ne[i] → ne[ne[i]] → ...`，进阶应用（P2375 动物园）

### 相关题目推荐

| 题目 | 来源 | 难度 | 考点 | 组别 |
|------|------|------|------|------|
| Trie 字符串统计 | AcWing 835 | ★ | Trie 模板 | J-S |
| 于是他错误的点名开始了 | 洛谷 P2580 | ★★ | Trie + 状态标记 | J-S |
| 最长异或路径 | 洛谷 P4551 | ★★★ | 01-Trie + 树上异或 | S |
| KMP 字符串匹配 | 洛谷 P3375 | ★★ | KMP 模板 | J-S |
| 动物园 | 洛谷 P2375 | ★★★★ | border 链 + `next` 性质 | S |
| 最大异或对 | AcWing 143 | ★★★ | 01-Trie | S |

---

### 知识衔接

本章是**字符串高效处理**的核心章节，与前后章节的联系如下：

- **前置知识**：第20章链表（节点与指针思想）、第07章字符串（`string`与字符数组基础）、第21章哈希表（字符串哈希对比）
- **本章定位**：用树形结构与状态机思想实现字符串集合存储与模式匹配，是字符串算法的基础与起点
- **后续发展**：为第25章二叉树的树形遍历打基础，Trie与KMP思想在AC自动机、后缀自动机等进阶字符串算法中深度融合

!!! tip "下一章预告"
    本章我们用树形结构（Trie）解决了"字符串集合存储"，用 KMP 解决了"单模式匹配"。下一章我们将学习**二叉树**——树形结构的代表，掌握二叉树的遍历与性质，为后续学习二叉搜索树、堆、线段树、平衡树等基于二叉树的高级结构打下根基。
