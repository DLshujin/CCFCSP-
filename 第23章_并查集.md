# 第23章 并查集

`[CSP-S 必考]` 并查集是处理"集合合并与查询"的利器，CSP-S 几乎每年必考

!!! info "本章内容"
    本章将带你认识一种专为"连通性与集合关系"而生的高效数据结构。你将学习：

    - 并查集的定义与核心思想（森林表示不相交集合）
    - 数组模拟实现：`int p[N]` 存储父节点
    - 两大核心操作：`find`（路径压缩）与 `merge`（按秩合并可选）
    - 路径压缩的递归与非递归实现
    - 常见误区与避坑指南
    - 典型应用：连通块计数、集合大小维护
    - 进阶应用：带权并查集、扩展域并查集（食物链）
    - 与最小生成树 Kruskal 算法的关联

!!! warning "本章特点"
    并查集代码极短（核心只有 5 行），但威力巨大。它能在**近乎 $O(1)$** 的时间内回答"两个元素是否属于同一集合"。CSP-S 中，凡涉及"连通""分组""关系传递"的问题，第一反应就该是并查集。建议学完第12章排序后学本章。

---

## 📌 学习目标

完成本章学习后，你应当能够：

1. **独立默写**并查集的 `find`（路径压缩）与 `merge` 两大核心操作，代码无 bug
2. **准确说出**路径压缩的原理与均摊复杂度 $O(\alpha(n))$，并能用非递归写法防爆栈
3. **熟练运用**并查集解决连通块计数、集合大小维护等基础问题
4. **掌握**按秩合并与启发式合并（小挂大）两种优化策略
5. **理解**带权并查集（维护到根的距离）与扩展域并查集（补集思想）的原理与写法
6. **独立解决**关押罪犯（P1525）、食物链（P2024）等 CSP-S 经典并查集题目

---

## 第一节 问题引入：亲戚的亲戚还是亲戚吗

!!! abstract "一个生活中的问题"
    春节家族聚会，来了 $n$ 位亲戚。已知若干对关系："1号和2号是亲戚""3号和4号是亲戚""2号和3号是亲戚"。

    现在问你：**1号和4号是不是亲戚？**

    ```
    1 —— 2 —— 3 —— 4
    ```

    答案是"是"——因为亲戚关系可以传递：1的亲戚2是3的亲戚，3又是4的亲戚，所以1和4也是亲戚。

    **朴素做法**：用邻接表存关系，每次查询做一遍 BFS/DFS 判断连通，单次查询 $O(n+m)$。如果有 $q$ 次查询，总时间 $O(q(n+m))$，当 $n=10^5, q=10^5$ 时会 TLE。

    **目标**：初始化 $O(n)$，每次"合并两人"和"查询两人是否同组"都**近乎 $O(1)$**。这就要请出「并查集」。

## 💡 生活类比：家族族谱查亲戚

把并查集想象成一棵**家族族谱**：每个人只记得自己的"父亲"是谁，而家族的"族长"（根）就是这一家族的代表。查两个人是不是亲戚，只要看他们的族长是不是同一个人；两个家族联姻合并，就让一个族长"认"另一个族长当父亲。族谱越扁平，查得越快。

### 1.1 把问题抽象一下

亲戚问题的本质是：维护若干个**互不相交的集合**，支持两种操作——

| 操作 | 含义 | 亲戚问题中的对应 |
|------|------|----------------|
| **合并 merge(x, y)** | 把 x 和 y 所在的两个集合合为一个 | "x 和 y 是亲戚" |
| **查找 find(x)** | 返回 x 所属集合的代表元（根） | "x 的家族族长是谁" |
| **查询 same(x, y)** | 判断 x、y 是否同集合 | "x 和 y 是不是亲戚" |

只要 `find(x) == find(y)`，就说明 x、y 同属一个集合（同一家族）。

---

## 第二节 并查集的定义与核心思想

### 2.1 什么是并查集

**并查集**（Disjoint Set Union, DSU）是一种用于管理**不相交集合**的数据结构，支持高效的"合并集合"与"查询元素所属集合"两种操作。

!!! info "并查集的三要素"
    - **表示**：用一片森林（多棵树）表示多个不相交集合，每棵树代表一个集合。
    - **代表元**：每棵树的根结点作为该集合的"代表"（族长）。
    - **存储**：只需一个数组 `p[x]`（或写作 `fa[x]`），表示 x 的父节点。根结点的父节点指向自己，即 `p[root] == root`。

### 2.2 用森林表示集合

```
集合 {1,2,4} 和集合 {3,5,6} 用两棵树表示：

      1            3            ← 根 = 代表元
     / \          / \
    2   4        5   6         ← 叶子只记住父亲

p[1]=1, p[2]=1, p[4]=1, p[3]=3, p[5]=3, p[6]=3
```

合并两个集合时，把一棵树的根挂到另一棵树的根下：

```
合并前：                  合并后（1的根挂到3的根下）：
      1        3                1         3
     / \      / \              / \       /|\
    2   4    5   6            2   4    5 6 1
                                        ↑
                              p[1] = 3（1的父亲变成3）
```

!!! tip "为什么只需存父节点"
    并查集只关心"谁能当代表"和"谁和谁同组"，不关心树的具体形状。所以每个结点**只需记住父亲**，无需存子节点列表。这也是并查集空间只需 $O(n)$ 的原因。

### 2.3 三个基本操作（朴素版）

```cpp
const int N = 10005;
int p[N];                     // p[x] = x 的父节点，根的 p 是自己

// 初始化：每个元素自成一棵树（自己是根）
void init(int n) {
    for (int i = 1; i <= n; i++) p[i] = i;
}

// 查找：沿父节点指针一路向上找根
int find(int x) {
    while (p[x] != x) x = p[x];   // 不是根就继续往上
    return x;                     // 找到根
}

// 合并：把 x 和 y 所在的两棵树连起来
void merge(int x, int y) {
    int rx = find(x), ry = find(y);   // 先各自找到根
    if (rx != ry) p[rx] = ry;         // 把 x 的根挂在 y 的根下
}

// 查询：x 和 y 是否同集合
bool same(int x, int y) {
    return find(x) == find(y);
}
```

!!! warning "为什么不直接 p[x] = y？"
    合并时必须**先找到各自的根**，再把"根和根"连起来。如果直接 `p[x] = y`，会把 x 从原来的树里"挖出来"，导致 x 原来的子树与 x 断开——这是初学者最常犯的错误。

    ✅ 正确：`p[find(x)] = find(y)`（根挂根）
    ❌ 错误：`p[x] = y`（节点挂节点，破坏原树）

### 2.4 朴素并查集的隐患：树会变高

如果一直把"大树挂在小树下"或按固定方向合并，树可能退化成一条长链：

```
最坏情况（链状）：
6 → 5 → 4 → 3 → 2 → 1
```

此时 `find(6)` 要走 6 步才能到根，单次查找退化为 $O(n)$。$n$ 次操作总成本 $O(n^2)$，和朴素 BFS 一样慢。

**解决方法**：两大优化——路径压缩 + 按秩合并。

---

## 第三节 核心操作：find 与 merge

并查集的所有功能都围绕 `find` 和 `merge` 展开。下表汇总了核心操作：

### 3.1 核心操作一览表

| 操作 | 函数签名 | 代码核心 | 复杂度（优化后） | 说明 |
|------|---------|---------|----------------|------|
| 初始化 | `init(n)` | `p[i] = i` | $O(n)$ | 每个元素自成一棵树 |
| 查找根 | `find(x)` | 递归 + 路径压缩 | 均摊 $O(\alpha(n))$ | 返回 x 所在集合的代表元 |
| 合并 | `merge(x,y)` | `p[find(x)] = find(y)` | 均摊 $O(\alpha(n))$ | 合并两集合 |
| 查询 | `same(x,y)` | `find(x) == find(y)` | 均摊 $O(\alpha(n))$ | 判断是否同集合 |
| 按秩合并 | `merge(x,y)` | 矮树挂高树 | 控制树高 $\le \log n$ | 可选优化 |

### 3.2 find 操作详解

`find(x)` 的任务是找到 x 所在集合的根（代表元）。朴素版只需一个 while 循环，但为避免树退化为链，需配合**路径压缩**。

```cpp
// 路径压缩版 find（递归写法，最常用）
int find(int x) {
    if (p[x] != x) {
        p[x] = find(p[x]);   // 递归找根，顺便把 x 的父亲直接改成根
    }
    return p[x];
}
```

**手动模拟**（对链 `6→5→4→3→2→1` 调用 `find(6)`）：

```
调用 find(6):
  6≠根 → find(5)
    5≠根 → find(4)
      4≠根 → find(3)
        3≠根 → find(2)
          2≠根 → find(1)
            1是根 → 返回 1
          回溯：p[2]=1，返回 1
        回溯：p[3]=1，返回 1
      回溯：p[4]=1，返回 1
    回溯：p[5]=1，返回 1
  回溯：p[6]=1，返回 1

结果：树被"压扁"成
       1
     /|\\\
    2 3 4 5 6   ← 所有结点直接指向根
```

下次再查任何一个结点，都只需 1 步！

### 3.3 merge 操作详解

`merge(x, y)` 把 x 和 y 所在的两个集合合并。关键：**先 find 找根，再根挂根**。

```cpp
// 朴素合并（配合路径压缩已够用）
void merge(int x, int y) {
    int rx = find(x), ry = find(y);
    if (rx != ry) p[rx] = ry;     // 把 x 的根挂到 y 的根下
}
```

如需进一步优化树高，可加入**按秩合并**（见第五节）。

!!! tip "竞赛速记版"
    竞赛中，**只用路径压缩的递归 find + 朴素 merge** 就能应付绝大多数题目。但面对卡常的题目（如 $10^6$ 次操作），建议同时使用按秩合并。

---

## 第四节 路径压缩：递归与非递归实现

## 💡 生活类比：直接拜见族长

每次你大老远跑去找族长，路上经过好几个"中间人"。办完事后你想：**下次别绕路了，直接记住族长在哪**，把中间人跳过。

路径压缩就是这个思路：`find` 的过程中，把路径上所有结点的父亲**直接改成根**，下次再查就是 $O(1)$。

### 4.1 递归实现（最常用）

```cpp
int find(int x) {
    return p[x] == x ? x : p[x] = find(p[x]);
    // 等价于：
    // if (p[x] != x) p[x] = find(p[x]);
    // return p[x];
}
```

**优点**：代码极短，一行搞定。
**缺点**：递归有栈开销，极端情况下（$10^6$ 层链）可能爆栈。

### 4.2 非递归实现（两遍法）

为避免爆栈，可用非递归写法：**第一遍找根，第二遍把路径上所有点直接挂到根**。

```cpp
int find(int x) {
    int r = x;
    while (p[r] != r) r = p[r];      // 第一遍：找到根 r
    while (p[x] != r) {              // 第二遍：路径压缩
        int t = p[x];                // 暂存 x 原来的父亲
        p[x] = r;                    // x 直接指向根
        x = t;                       // 继续处理原父亲
    }
    return r;
}
```

**手动模拟**（对链 `6→5→4→3→2→1` 调用 `find(6)`）：

```
第一遍找根：r 从 6 走到 1，根 r = 1
第二遍压缩：
  x=6: 暂存 t=p[6]=5, 令 p[6]=1, x=5
  x=5: 暂存 t=p[5]=4, 令 p[5]=1, x=4
  x=4: 暂存 t=p[4]=3, 令 p[4]=1, x=3
  x=3: 暂存 t=p[3]=2, 令 p[3]=1, x=2
  x=2: p[2]=1 已等于 r，结束

结果：6,5,4,3,2 全部直接指向根 1
```

!!! info "两种写法的选择"
    - 一般题目：用递归版（短、好记）。
    - 数据量极大（$n \ge 10^6$）或链状退化严重时：用非递归版，避免栈溢出。

### 4.3 路径压缩的效果

```
压缩前（链状）：              压缩后（扁平）：
6 → 5 → 4 → 3 → 2 → 1              1
                                 /|\\\
                                2 3 4 5 6
```

路径压缩后，树高接近 1，后续查询几乎都是 $O(1)$。

---

## 第五节 按秩合并（启发式合并）

## 💡 生活类比：小家族并入大家族

两个家族合并时，让**小的家族认大的家族当族长**更合理——这样树的高度不会增加。

"秩"（rank）可以理解为**树的高度**。合并时，始终把矮树挂在高树下，保证树高增长缓慢。

```cpp
int p[N], rnk[N];            // rnk[x] = 以 x 为根的树的高度

void init(int n) {
    for (int i = 1; i <= n; i++) {
        p[i] = i;
        rnk[i] = 1;          // 初始高度为 1
    }
}

void merge(int x, int y) {
    int rx = find(x), ry = find(y);
    if (rx == ry) return;     // 已同集合，无需合并
    if (rnk[rx] < rnk[ry]) {  // 矮树挂高树
        p[rx] = ry;
    } else if (rnk[rx] > rnk[ry]) {
        p[ry] = rx;
    } else {                  // 等高：随便挂，但新根高度 +1
        p[rx] = ry;
        rnk[ry]++;
    }
}
```

!!! warning "等高合并要 +1"
    当两棵树高度相同时，无论谁挂谁，新树的高度都会比原来多 1（因为新根下多了一层）。这是 `rnk[ry]++` 的来源，容易漏写。

!!! info "路径压缩 vs 按秩合并"
    - **路径压缩**改变树的结构（压扁），会破坏 `rnk` 的准确性，但 `rnk` 仍可用作"高度上界"的估计，不影响正确性。
    - 实际中，**单用路径压缩**已经够快（均摊 $O(\log n)$）；**两者结合**可达均摊 $O(\alpha(n))$，理论最优。
    - 竞赛中常省略按秩合并，只用路径压缩，代码更短。

### 5.1 复杂度小结

| 写法 | 单次操作 | 说明 |
|------|---------|------|
| 朴素 | $O(n)$ | 树可能退化为链 |
| 只路径压缩 | 均摊 $O(\log n)$ | 实际极快 |
| 只按秩合并 | $O(\log n)$ | 树高不超过 $\log n$ |
| **两者结合** | **均摊 $O(\alpha(n))$** | $\alpha$ 是反阿克曼函数 |

$\alpha(n)$ 增长极慢，对 $n < 10^{80}$（宇宙原子总数级别）都有 $\alpha(n) < 5$，实际可视为常数。

---

## 第六节 常见误区与避坑指南

并查集代码虽短，但初学者常踩坑。以下列出高频错误。

### 6.1 误区一：合并时未找到根直接合并

```cpp
// ❌ 错误写法
void merge(int x, int y) {
    p[x] = y;                 // 直接让 x 的父亲变成 y
}
```

**问题**：这会把 x 从原来的树里"挖出来"，x 的子树与 x 断开，集合关系被破坏。

```
原集合：{1,2,4}（1是根，2、4挂在1下）
执行 merge(2, 3)：
  错误写法 p[2]=3 后：
      1            3
     / \          /
    4   2        2      ← 2 被挖走，4 还挂在 1 下，4 和 2 分家了！
```

**正确写法**：必须先 `find` 找根，再根挂根。

```cpp
// ✅ 正确写法
void merge(int x, int y) {
    p[find(x)] = find(y);     // 根挂根
}
```

### 6.2 误区二：路径压缩写错导致死递归

```cpp
// ❌ 错误写法 1：忘记判断根，无限递归
int find(int x) {
    return find(p[x]);        // 没有 p[x]==x 的终止条件！
}
```

**问题**：当 `p[x]==x`（x 是根）时，仍会 `find(p[x]) = find(x)`，无限递归直至爆栈。

```cpp
// ❌ 错误写法 2：压缩方向反了 / 忘记赋值
int find(int x) {
    if (p[x] != x) {
        find(p[x]);           // 忘了赋值，没压缩
        // 或写成 p[p[x]] = x，方向反
    }
    return p[x];
}
```

**正确写法**：

```cpp
// ✅ 正确写法
int find(int x) {
    if (p[x] != x) p[x] = find(p[x]);   // 递归找根 + 把 x 直接挂到根
    return p[x];
}
```

!!! warning "路径压缩的要点"
    - 必须有终止条件 `p[x] == x`。
    - 必须把 `find(p[x])` 的返回值**赋回 `p[x]`**，否则没有压缩效果。
    - `return p[x]` 而不是 `return x`（因为 x 的父亲可能已被更新）。

### 6.3 误区三：忘记初始化

```cpp
// ❌ 错误
int main() {
    int n; cin >> n;
    // 忘了 init(n)，p[] 全是 0，find 会死循环或越界
    merge(1, 2);
}
```

**正确**：使用前必须 `init(n)`，让每个 `p[i] = i`。

### 6.4 误区四：统计连通块用 p[i]==i 而非 find(i)==i

```cpp
// ❌ 错误（可能漏统计）
int cnt = 0;
for (int i = 1; i <= n; i++)
    if (p[i] == i) cnt++;      // 路径压缩未覆盖所有点时漏统计
```

**问题**：路径压缩只压缩查询过的路径，有些点的 `p[i]` 可能还指向中间结点而非根。

**正确**：用 `find(i) == i` 统计，确保每个点都被压到根。

```cpp
// ✅ 正确
int cnt = 0;
for (int i = 1; i <= n; i++)
    if (find(i) == i) cnt++;
```

### 6.5 误区五：合并前忘记判同集合

```cpp
// ⚠️ 不规范
void merge(int x, int y) {
    p[find(x)] = find(y);     // 若 x,y 已同集合，相当于 p[根]=根，无害但冗余
}
```

虽然无害，但配合按秩合并时若忘记判 `rx == ry` 会让 `rnk` 错误 +1。建议养成判同集合的习惯。

---

## 第七节 典型应用

### 7.1 连通块计数

**问题**：给定 $n$ 个点 $m$ 条边的无向图，求连通分量个数。

**思路**：每条边就是一次合并。合并完所有边后，统计有多少个不同的根（即 `find(i) == i` 的点）。

```cpp
// 在输入所有边并 merge 后：
int cnt = 0;
for (int i = 1; i <= n; i++) {
    if (find(i) == i) cnt++;    // 统计根的个数
}
cout << cnt << "\n";
```

### 7.2 维护集合大小（size 数组）

**问题**：除合并与查询外，还要随时知道某元素所在集合的大小。

**思路**：额外维护 `sz[x]` 数组，**只在根结点处维护有效值**。合并时把大小累加到新根。

```cpp
int p[N], sz[N];                // sz[x] 仅当 x 是根时有效

void init(int n) {
    for (int i = 1; i <= n; i++) {
        p[i] = i;
        sz[i] = 1;              // 初始每个集合大小为 1
    }
}

int find(int x) {
    return p[x] == x ? x : p[x] = find(p[x]);
}

void merge(int x, int y) {
    int rx = find(x), ry = find(y);
    if (rx == ry) return;
    p[rx] = ry;                 // rx 挂到 ry 下
    sz[ry] += sz[rx];           // ry 的大小 += rx 的大小
    // 注意：sz[rx] 不再有效，因为 rx 不再是根
}

// 查询 x 所在集合的大小
int sizeOf(int x) {
    return sz[find(x)];         // 必须先 find 找根，再取 sz
}
```

!!! warning "size 数组只在根处有效"
    `sz[x]` 仅当 x 是根时才是真实大小。查询时必须 `sz[find(x)]`，不能直接 `sz[x]`。合并时只能把被挂掉的根的 size 累加到新根上。

!!! tip "启发式合并：小挂大"
    若配合"小集合挂大集合"的启发式合并（用 `sz` 代替 `rnk`），可保证树高 $\le \log n$，且无需维护 `rnk`。这是竞赛中常见的简化写法：
    ```cpp
    void merge(int x, int y) {
        int rx = find(x), ry = find(y);
        if (rx == ry) return;
        if (sz[rx] > sz[ry]) swap(rx, ry);  // 小挂大
        p[rx] = ry;
        sz[ry] += sz[rx];
    }
    ```

---

## 第八节 进阶应用

### 8.1 带权并查集（维护到根的距离）

!!! abstract "问题"
    并查集不仅能维护"是否同集合"，还能维护元素之间的**相对关系**（如距离、差值）。在 `p[x]` 之外再加一个 `d[x]`，表示 x 到其父节点的"权值"（距离/差值），路径压缩时同步更新。

**核心思想**：`d[x]` 记录 x 到父节点的权值。`find` 时通过路径压缩，把 `d[x]` 累加成 x 到根的权值。

```cpp
int p[N], d[N];                 // d[x] = x 到父节点 p[x] 的权值

int find(int x) {
    if (p[x] != x) {
        int t = find(p[x]);      // 先递归找根（这一步会更新 p[p[x]] 和 d[p[x]]）
        d[x] += d[p[x]];         // x 到根的权值 = x 到原父的权值 + 原父到根的权值
        p[x] = t;                // 路径压缩：x 直接挂到根
    }
    return p[x];
}
```

**手动模拟**（链 `3 -(d=2)-> 2 -(d=5)-> 1`，即 d[3]=2, d[2]=5, d[1]=0，find(3)）：

```
调用 find(3):
  3≠根 → find(2)
    2≠根 → find(1)
      1是根 → 返回 1
    回溯：d[2] += d[1](=0) → d[2]=5, p[2]=1, 返回 1
  回溯：d[3] += d[2](=5) → d[3]=7, p[3]=1, 返回 1

结果：3 直接到 1，d[3]=7（3 到根的距离）
```

!!! info "带权并查集的典型场景"
    - 维护数列差值：已知 $a_y - a_x = k$，查询 $a_y - a_x = ?$
    - 食物链（关系用模 3 的权值表示，见 8.3）
    - 区间和（前缀和差分转带权并查集）

### 8.2 扩展域并查集（补集思想）

!!! abstract "问题"
    当关系有"敌/友""对/错""A/B 两类"等对立面时，把每个元素**复制一份**（用 $i+n$ 表示其对立面），就能把对立关系转化为普通的同集合关系。

**经典场景**：朋友的朋友是朋友，敌人的敌人是朋友（洛谷 P1892 团伙）。

```cpp
// i 是本人，i+n 是 i 的"敌人代表"
// 朋友关系：merge(i, j)
// 敌人关系：merge(i+n, j) 和 merge(j+n, i)
//   （i 的敌人代表 和 j 是朋友；j 的敌人代表 和 i 是朋友）
```

!!! tip "扩展域的核心"
    把"对立"关系转化为"同集合"关系。开 $k$ 倍空间（$k$ 类对立关系），第 $i+n, i+2n, \ldots$ 分别表示 $i$ 的各类"对立面"。

### 8.3 食物链（带权并查集 / 扩展域三域）

**题目**（洛谷 P2024）：$n$ 只动物，A 吃 B、B 吃 C、C 吃 A。$k$ 句话，判断真假。

**方法一：带权并查集**（用 `d[x]` 记录 x 与根的关系，0=同类，1=x 吃根，2=x 被根吃）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 50005;
int p[N], d[N];                 // d[x] = x 到 p[x] 的关系（0/1/2）

int find(int x) {
    if (p[x] != x) {
        int t = find(p[x]);
        d[x] = (d[x] + d[p[x]]) % 3;   // 关系叠加，模 3
        p[x] = t;
    }
    return p[x];
}

int main() {
    int n, k, ans = 0;
    scanf("%d %d", &n, &k);
    for (int i = 1; i <= n; i++) p[i] = i;
    while (k--) {
        int op, x, y;
        scanf("%d %d %d", &op, &x, &y);
        if (x > n || y > n) { ans++; continue; }           // 越界，假话
        if (op == 2 && x == y) { ans++; continue; }        // 自己吃自己，假话
        int rx = find(x), ry = find(y);
        if (op == 1) {                                       // 说 x、y 同类
            if (rx == ry && (d[x] - d[y] + 3) % 3 != 0) ans++;   // 已不同类，假话
            else if (rx != ry) {                            // 还没关系，建立关系
                p[rx] = ry;
                d[rx] = (d[y] - d[x] + 3) % 3;              // 让 d[x]+d[rx] ≡ d[y]
            }
        } else {                                             // 说 x 吃 y
            if (rx == ry && (d[x] - d[y] + 3) % 3 != 1) ans++;   // 不满足 x 吃 y，假话
            else if (rx != ry) {
                p[rx] = ry;
                d[rx] = (d[y] - d[x] + 3 + 1) % 3;          // 让 (d[x]+d[rx]-d[y]) ≡ 1
            }
        }
    }
    printf("%d\n", ans);
    return 0;
}
```

**方法二：扩展域并查集**（开三倍空间，$i$ 同类域、$i+n$ 猎物域、$i+2n$ 天敌域）

思路：x 吃 y 等价于 x 的猎物域 = y 的同类域，即 `merge(x+n, y)`、`merge(y+2n, x)`、`merge(x, y+2n)` 等。代码核心是把"吃/被吃/同类"三种关系拆到三个域上。

!!! warning "食物链的难点"
    关系叠加的公式（`(d[x] + d[p[x]]) % 3`）和建立关系时的方向容易写反。建议先手画几个例子验证，再写代码。竞赛中带权并查集写法更紧凑，扩展域写法更直观但代码长，按个人习惯选择。

---

## 第九节 与最小生成树 Kruskal 的关联

!!! info "并查集是 Kruskal 算法的核心组件"
    Kruskal 算法求最小生成树（MST）时，需要判断加入一条边是否形成环。并查集完美胜任：若边的两端点已同集合，则加边会成环，跳过；否则合并两集合并加入该边。

**Kruskal 算法流程**：

1. 将所有边按权值从小到大排序。
2. 依次考察每条边 $(u, v, w)$：若 `find(u) != find(v)`（不成环），则合并 `merge(u, v)` 并把边加入 MST。
3. 直到加入 $n-1$ 条边为止。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 5005, M = 200005;
int p[N];

int find(int x) {
    return p[x] == x ? x : p[x] = find(p[x]);
}

struct Edge {
    int u, v, w;
    bool operator<(const Edge &o) const { return w < o.w; }
} e[M];

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = 1; i <= n; i++) p[i] = i;
    for (int i = 0; i < m; i++) scanf("%d %d %d", &e[i].u, &e[i].v, &e[i].w);
    sort(e, e + m);

    long long sum = 0, cnt = 0;
    for (int i = 0; i < m && cnt < n - 1; i++) {
        int ru = find(e[i].u), rv = find(e[i].v);
        if (ru != rv) {            // 不成环，加入 MST
            p[ru] = rv;
            sum += e[i].w;
            cnt++;
        }
    }
    if (cnt == n - 1) printf("%lld\n", sum);
    else printf("orz\n");          // 图不连通，无 MST
    return 0;
}
```

!!! tip "并查集 + 排序 = Kruskal"
    Kruskal 的"判环"本质就是并查集的 `same(u, v)`。并查集让 Kruskal 的时间复杂度达到 $O(m \log m)$（排序为主，并查集近乎 $O(1)$）。例如边按权排序后依次加入 `(1,2,1)`、`(2,3,2)`、`(1,3,3)`、`(3,4,4)`：前两条合并，第三条 `1==3` 成环跳过，第四条合并，共加入 $n-1=3$ 条边完成 MST。这是并查集在图论中最经典的应用之一。

---

## 配套习题

### 🟢 基础题（模板，巩固操作）

**[P3367] 并查集模板**
> 如题，给定 $n$ 个元素，支持合并（操作 1）和查询（操作 2）操作。
**考点**：并查集基础操作。
**提示**：直接套用第三、四节模板。注意路径压缩的递归写法，以及合并时先 `find` 再挂根。注意输出格式（Y/N）。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 10005;
int p[N];

int find(int x) {
    return p[x] == x ? x : p[x] = find(p[x]);
}

void merge(int x, int y) {
    p[find(x)] = find(y);
}

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = 1; i <= n; i++) p[i] = i;
    while (m--) {
        int op, x, y;
        scanf("%d %d %d", &op, &x, &y);
        if (op == 1) merge(x, y);
        else printf(find(x) == find(y) ? "Y\n" : "N\n");
    }
    return 0;
}
```

### 🟢 基础题（经典应用）

**[P1551] 亲戚**
> $n$ 个人，$m$ 对亲戚关系（关系可传递），$p$ 次询问，每次问两人是否亲戚。
**考点**：并查集连通性判断。
**提示**：并查集最经典应用。每对关系 `merge`，每次询问判 `find` 是否相等。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 5005;
int p[N];

int find(int x) {
    return p[x] == x ? x : p[x] = find(p[x]);
}

int main() {
    int n, m, q;
    scanf("%d %d %d", &n, &m, &q);
    for (int i = 1; i <= n; i++) p[i] = i;
    while (m--) {
        int x, y; scanf("%d %d", &x, &y);
        p[find(x)] = find(y);
    }
    while (q--) {
        int x, y; scanf("%d %d", &x, &y);
        printf(find(x) == find(y) ? "Yes\n" : "No\n");
    }
    return 0;
}
```

### 🟡 拓展题（二分 + 并查集）

**[P1525] 关押罪犯**（NOIP2010 提高组）
> $n$ 个罪犯分两个监狱，$m$ 对冲突有影响值。求一种分配方案使**最大影响值最小**。
**考点**：二分答案 + 并查集判二分图 / 按权值排序 + 贪心并查集。
**提示**：两种思路——
- **二分 + 并查集（补集）**：二分最大影响值 $mid$，把影响值 $> mid$ 的冲突对用并查集判断能否分到两个监狱。冲突两人必须在对方监狱，即 `merge(x, y+n)` 和 `merge(y, x+n)`；若发现冲突两人已同集合（`find(x)==find(y)`）则当前 $mid$ 不可行。
- **按权值排序 + 贪心并查集**：把冲突从大到小排序，依次处理。能分（不同集合）就分到对方监狱；第一个分不出去（已同集合）的就是答案（最大影响值）。

```cpp
// 按权值排序 + 贪心并查集（补集）写法
#include <bits/stdc++.h>
using namespace std;

const int N = 40005;
int p[N];

int find(int x) {
    return p[x] == x ? x : p[x] = find(p[x]);
}

void merge(int x, int y) {
    p[find(x)] = find(y);
}

struct Edge { int u, v, w; bool operator<(const Edge &o) const { return w > o.w; } } e[100005];

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = 0; i < m; i++) scanf("%d %d %d", &e[i].u, &e[i].v, &e[i].w);
    sort(e, e + m);
    for (int i = 1; i <= 2 * n; i++) p[i] = i;       // 开两倍：i+n 是 i 的"敌人监狱"
    for (int i = 0; i < m; i++) {
        int x = e[i].u, y = e[i].v;
        if (find(x) == find(y)) {                     // 已同集合，分不开
            printf("%d\n", e[i].w);
            return 0;
        }
        merge(x, y + n);                              // x 和 y 的敌人同侧
        merge(y, x + n);                              // y 和 x 的敌人同侧
    }
    printf("0\n");                                    // 所有冲突都能分开
    return 0;
}
```

### 🔴 进阶题（带权并查集 / 扩展域）

**[P2024] 食物链**（NOI2001）
> $n$ 只动物组成 A吃B、B吃C、C吃A 的环形食物链。$k$ 句话，判断真假。
**考点**：带权并查集（关系模 3）或扩展域并查集（三域）。
**提示**：见第八节 8.3。带权并查集用 `d[x]` 记录 x 与根的关系（0 同类、1 吃、2 被吃），关系叠加模 3。扩展域并查集开三倍空间，$i$ 同类、$i+n$ 猎物、$i+2n$ 天敌。注意判断假话的三种情况：越界、自吃、与已知关系矛盾。

---

## 本章小结

### 核心要点回顾

- ✅ 并查集用**森林**表示互不相交的集合，每个集合用一棵树，根是代表元
- ✅ 存储只需 `int p[N]`，`p[x]` 记录 x 的父节点，根的 `p[root] == root`
- ✅ 两大操作：`find(x)` 找根、`merge(x,y)` 合并两集合
- ✅ **路径压缩**：`find` 时把路径上的点直接挂到根，树被"压扁"，递归与非递归两种写法
- ✅ **按秩合并**：矮树挂高树，等高时根的秩 +1（可选优化）
- ✅ 两者结合，单次操作均摊 $O(\alpha(n))$，**实际近 $O(1)$**
- ✅ **维护集合大小**：`sz[x]` 只在根处有效，合并时累加到新根
- ✅ **带权并查集**：`d[x]` 维护到父节点的权值，路径压缩时累加
- ✅ **扩展域并查集**：开 $k$ 倍空间，处理"对立"关系
- ✅ **Kruskal** 最小生成树用并查集判环

### 并查集速查表

| 操作 | 代码 | 含义 |
|------|------|------|
| 初始化 | `p[i] = i` | 每个元素自成一棵树 |
| 查找根 | `find(x)` | 返回 x 所在集合的根 |
| 路径压缩递归 | `p[x] = find(p[x])` | find 时直接指向根 |
| 路径压缩非递归 | 两遍法：找根 + 压扁 | 防爆栈 |
| 朴素合并 | `p[find(x)] = find(y)` | 根挂根 |
| 按秩合并 | 矮树挂高树，等高 +1 | 控制树高 |
| 启发式合并 | `sz` 小挂大 | 用 size 代替 rank |
| 查询同集合 | `find(x) == find(y)` | x、y 是否同集合 |
| 连通块计数 | `find(i) == i` 的个数 | 统计根的数量 |
| 集合大小 | `sz[find(x)]` | 必须先 find 找根 |

### 常见误区速查

| 误区 | 后果 | 正确做法 |
|------|------|---------|
| `p[x] = y` 直接合并 | 破坏原树，子树断开 | `p[find(x)] = find(y)` 根挂根 |
| find 忘记终止条件 | 死递归爆栈 | `if (p[x] == x) return x;` |
| find 忘记赋值回 p[x] | 无压缩效果 | `p[x] = find(p[x])` |
| 忘记 init | p[] 全 0，死循环/越界 | 使用前 `init(n)` |
| 统计用 `p[i]==i` | 漏统计未压缩点 | 用 `find(i) == i` |
| `sz[x]` 直接取大小 | x 非根时值无效 | `sz[find(x)]` |

### 知识脉络思维导图

- **并查集 DSU**
  - **核心思想**
    - 森林表示：每棵树代表一个集合
    - 根 = 代表元（族长）
    - 父指针：`p[N]` 存储父节点
  - **实现**
    - `find` 查找：路径压缩（递归 + 非递归）
    - `merge` 合并：按秩合并 / 启发式合并（小挂大）
  - **应用**
    - 连通块计数：统计 `find(i) == i` 的个数
    - 集合大小 `sz`：只在根处有效，合并时累加
    - 带权并查集：`d[x]` 维护到父节点的权值
    - 扩展域并查集：开 $k$ 倍空间处理对立关系
    - Kruskal 判环：`find(u) != find(v)` 则不成环

### 相关题目推荐

| 题目 | 来源 | 难度 | 考点 | 组别 |
|------|------|------|------|------|
| 并查集模板 | 洛谷 P3367 | ★ | 基础操作 | J-S |
| 亲戚 | 洛谷 P1551 | ★★ | 连通性判断 | J-S |
| 关押罪犯 | 洛谷 P1525 | ★★★★ | 二分+并查集/贪心 | S |
| 食物链 | 洛谷 P2024 | ★★★★★ | 带权并查集/三域 | S+ |

---

### 知识衔接

本章是**集合运算与图论前置**的关键章节，与前后章节的联系如下：

- **前置知识**：第21章哈希表（映射与查找思想）、第12章排序算法（Kruskal算法的排序前提）、第16章位运算与离散化（离散化映射）
- **本章定位**：以近乎 $O(1)$ 的复杂度实现集合合并与查询，是图论连通性问题的基础工具，也是Kruskal最小生成树的核心组件
- **后续发展**：为第33章最小生成树（Kruskal算法）提供核心数据结构，带权并查集在图论进阶中广泛应用

!!! tip "下一章预告"
    并查集解决了"连通性"问题。下一章我们将学习 **Trie 树与 KMP 算法**——字符串处理的两大经典数据结构与算法。Trie 用树形结构高效存储字符串集合，KMP 用 `next` 数组实现 $O(n+m)$ 的单模式匹配，两者都是 CSP-S 必考内容！
