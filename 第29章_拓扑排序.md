# 第29章 拓扑排序：让有先后依赖的事排成一条线

!!! info "本章内容"
    拓扑排序是图论中处理"依赖关系"的核心算法。本章系统讲解：拓扑排序的定义与 DAG 的关系、BFS 实现拓扑排序（Kahn 算法）、拓扑序与动态规划的结合（DAG 上最长路/最短路/路径计数）、判断图中是否存在环、任务调度等典型应用。所有代码统一使用 `#include <bits/stdc++.h>` 与链式前向星建图。

!!! warning "本章定位"
    拓扑排序是 CSP-S 提高级的高频考点，常与动态规划结合出题。它的代码不长，但"在拓扑序上做 DP"这一思想是 DAG 类问题的通用套路。学完本章后，你应当能把"有依赖关系的任务"自动转化为"DAG + 拓扑序 DP"。

---

## 📌 学习目标

学完本章后，你应当能够：

1. **准确说出**拓扑排序的定义，以及为什么只有 DAG 才能进行拓扑排序
2. **默写** Kahn 算法（队列 + 入度数组）的完整代码
3. **解释**拓扑排序判断图中是否有环的原理
4. **熟练运用**"拓扑序 + DP"解决 DAG 上最长路、最短路、路径计数问题
5. **避开**常见误区：图中存在环导致拓扑排序不完整、入度更新漏减、DP 转移顺序错误

---

## 💡 生活类比

### 类比一：课程先修关系排课

大学课程有"先修关系"：学《数据结构》之前必须先学《程序设计》，学《算法设计》之前必须先学《数据结构》。把这些课程画成一张有向图——**课程是顶点，"先修"关系是有向边**——那么任何一个合理的上课顺序，都是这张图的一个**拓扑排序**。

```
程序设计 ──→ 数据结构 ──→ 算法设计
                              │
                              ▼
                           操作系统

合理的上课顺序：程序设计 → 数据结构 → 算法设计 → 操作系统
不合理的顺序：数据结构 → 程序设计 → ...  ✗（程序设计还没学，怎能学数据结构？）
```

!!! tip "一句话记忆"
    拓扑排序就是把"有先后依赖的事"排成一条线，让每件事排在它依赖的所有事之后。又如做菜：切菜依赖洗菜、下锅依赖切菜和烧油，拓扑排序给出的就是一条合法的做菜流水线。

---

## 第一节 拓扑排序的定义

### 1.1 什么是拓扑排序

「拓扑排序 Topological Sort」是对**有向无环图**（DAG, Directed Acyclic Graph）的所有顶点的一种线性排序，使得对图中任意一条有向边 $(u, v)$，$u$ 在排序中都出现在 $v$ **之前**。

形式化地说，给定 DAG $G = (V, E)$，拓扑排序得到顶点序列 $v_1, v_2, \dots, v_n$，满足：若 $(v_i, v_j) \in E$，则 $i < j$。

### 1.2 为什么必须是 DAG

| 图的类型 | 能否拓扑排序 | 原因 |
|----------|:----------:|------|
| **DAG**（有向无环图） | ✅ 可以 | 没有环，总能找到合法的线性顺序 |
| **有环有向图** | ❌ 不可以 | 环上任意两点互相依赖，无法排出先后 |

```
有环图：1 → 2 → 3 → 1
1 要在 2 前（因 1→2），2 要在 3 前（因 2→3），3 要在 1 前（因 3→1）
→ 1 在 1 前？矛盾！所以有环图没有拓扑排序。
```

!!! info "DAG 与拓扑排序的等价性"
    一个有向图存在拓扑排序 **当且仅当** 它是 DAG。这是判断"图中是否有环"的重要方法：如果拓扑排序能排出全部 $n$ 个顶点，则无环；如果排不完（队列提前空了），则有环。

### 1.3 拓扑排序的性质

| 性质 | 说明 |
|------|------|
| **不唯一** | 同一个 DAG 可能有多种合法的拓扑序 |
| **起点** | 拓扑序的第一个顶点入度必为 0（没有边指向它） |
| **终点** | 拓扑序的最后一个顶点出度必为 0（没有边从它出发） |
| **与 BFS/DFS 的关系** | Kahn 算法本质是 BFS；DFS 也能求拓扑序（按离开时间逆序） |

```
DAG 示例：               合法的拓扑序（不止一种）：
    1 ──→ 2 ──→ 4        ① 1 2 3 4 5
    │     │              ② 1 3 2 4 5
    └──→ 3 ──→ 5         ③ 1 2 3 5 4  ✗（5 在 4 前，但 4→5，错误！）
（边：1→2, 1→3, 2→4, 3→5, 4→5）
```

---

## 第二节 BFS 实现拓扑排序（Kahn 算法）

### 2.1 算法思想

Kahn 算法的核心是**反复删除入度为 0 的顶点**：

1. 计算所有顶点的**入度**（有多少条边指向它）
2. 把所有入度为 0 的顶点**入队**
3. 每次从队列取出一个顶点 $u$，将它加入拓扑序；然后**删除 $u$ 的所有出边** $(u, v)$——即把 $v$ 的入度减 1
4. 若 $v$ 的入度减到 0，就把 $v$ 入队
5. 重复直到队列为空

!!! tip "生活类比：先修课排课"
    入度为 0 的课 = 没有任何先修要求的课，可以直接排上。排完一门课后，依赖它的课少了一门先修要求；当某门课的所有先修都排完了（入度归零），它就可以排上了。

### 2.2 ASCII 示意图

```
DAG：1→2, 1→3, 2→4, 3→4, 4→5    初始入度：1:0  2:1  3:1  4:2  5:1

取出 1 → 删 1→2,1→3 → 入度 2:0,3:0 → 2,3 入队   拓扑序：(1)
取出 2 → 删 2→4     → 入度 4:1            →        拓扑序：(1,2)
取出 3 → 删 3→4     → 入度 4:0 → 4 入队   →        拓扑序：(1,2,3)
取出 4 → 删 4→5     → 入度 5:0 → 5 入队   →        拓扑序：(1,2,3,4)
取出 5 → 无出边                              →        拓扑序：(1,2,3,4,5) ✓
```

### 2.3 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
int h[N], e[N], ne[N], idx;
int in_deg[N];                  // 入度数组
int n, m;

void add(int u, int v) { e[idx] = v; ne[idx] = h[u]; h[u] = idx++; }

void topo_sort() {
    queue<int> q;
    for (int i = 1; i <= n; i++)
        if (in_deg[i] == 0) q.push(i);     // 入度为 0 的先入队
    vector<int> order;
    while (!q.empty()) {
        int u = q.front(); q.pop();
        order.push_back(u);                // 加入拓扑序
        for (int i = h[u]; i != -1; i = ne[i]) {
            int v = e[i];
            if (--in_deg[v] == 0)          // 删边后入度归零
                q.push(v);
        }
    }
    if ((int)order.size() == n) {          // 全部排出，无环
        for (int x : order) cout << x << " ";
        cout << endl;
    } else {
        cout << "图中存在环，无法拓扑排序" << endl;
    }
}

int main() {
    cin >> n >> m;
    memset(h, -1, sizeof(h));
    for (int i = 1; i <= m; i++) {
        int u, v; cin >> u >> v;
        add(u, v);
        in_deg[v]++;                       // 建图时同步累加入度
    }
    topo_sort();
    return 0;
}
```

### 2.4 核心操作一览表

| 操作 | 代码 | 说明 |
|------|------|------|
| 建图时累加入度 | `in_deg[v]++` | 每加一条边 $u \to v$，`in_deg[v]` 加 1 |
| 初始入队 | `if (in_deg[i] == 0) q.push(i)` | 入度为 0 的顶点先入队 |
| 删边（减入度） | `--in_deg[v]` | 取出 $u$ 后，其所有出边的终点入度减 1 |
| 入度归零入队 | `if (--in_deg[v] == 0) q.push(v)` | 减完后若为 0 则入队 |
| 判环 | `order.size() == n` | 排出的顶点数等于 $n$ 则无环 |

### 2.5 复杂度分析

| 维度 | 复杂度 | 说明 |
|------|:------:|------|
| 时间复杂度 | $O(n + m)$ | 每个顶点入队出队各一次，每条边删一次 |
| 空间复杂度 | $O(n + m)$ | 链式前向星 + 入度数组 + 队列 |

!!! tip "要输出字典序最小的拓扑序怎么办"
    把 `queue` 换成 `priority_queue<int, vector<int>, greater<int>>`（小根堆），每次取出编号最小的入度为 0 的顶点。注意：这**不保证**所有拓扑序中字典序最小，而是"贪心地每步取最小"得到的序，恰为字典序最小的拓扑序。

---

## 第三节 拓扑排序的典型例题

### 3.1 判断图中是否存在环

拓扑排序的副产品就是**判环**：如果排出的顶点数小于 $n$，说明图中存在环（环上的顶点入度永远降不到 0，无法入队）。

```cpp
// 在 topo_sort 中：
if (order.size() < n) {
    cout << "有环！" << endl;   // 队列空了但还有顶点没排出
}
```

!!! info "判环的多种方法对比"
    - **拓扑排序判环**：$O(n+m)$，适合有向图，能顺便给出拓扑序
    - **DFS 三色标记法**：$O(n+m)$，白色未访问/灰色在栈中/黑色已结束，遇到灰色即有环
    - **并查集判环**：仅适合**无向图**（同集合内再加边即成环）

### 3.2 任务调度（先修课程）

给定若干任务和它们的依赖关系，求一个合法的执行顺序，或判断是否存在"循环依赖"导致无法完成。这是拓扑排序最直接的应用：

- 把任务建成 DAG（任务 = 顶点，依赖 = 有向边）
- 跑一遍拓扑排序
- 若能排完，输出顺序；若排不完，说明存在循环依赖

---

## 第四节 拓扑排序与 DP 的结合

这是本章**最重要**的进阶内容。DAG 上的很多问题（最长路、最短路、路径计数）都可以用"拓扑序 + DP"高效解决。

### 4.1 为什么拓扑序适合做 DP

动态规划要求**无后效性**——计算某状态时，它依赖的所有状态都已算完。在 DAG 上，如果按照**拓扑序**从小到大计算，那么对任意边 $u \to v$，$u$ 一定在 $v$ 之前算完。于是以 $v$ 为终点的转移（来自所有 $u$）都能正确完成。

!!! tip "核心套路"
    **DAG 上的 DP = 拓扑排序 + 按拓扑序转移**。拓扑序保证了"先算的不会依赖后算的"。

### 4.2 DAG 上的最长路

求 DAG 上从某个起点（或所有入度为 0 的点）到终点的**最长路径**长度。

设 `f[v]` 表示到顶点 $v$ 的最长路长度，则：

$$f[v] = \max_{(u, v) \in E} \{f[u] + w(u, v)\}$$

入度为 0 的顶点初始化为 0（或自身权值）。按拓扑序计算即可。

```
DAG 带权：1 →(2)→ 2 →(3)→ 4
          1 →(5)→ 3 →(1)→ 4
求 1 到 4 的最长路：
f[1]=0, f[2]=2, f[3]=5, f[4]=max(f[2]+3, f[3]+1)=max(5, 6)=6
最长路：1→3→4，长度 6
```

### 4.3 DAG 上的路径计数

求 DAG 上从起点到终点的路径总数。设 `f[v]` 表示到 $v$ 的路径数：

$$f[v] = \sum_{(u, v) \in E} f[u]$$

入度为 0 的起点 `f = 1`。按拓扑序累加。注意结果可能很大，通常要取模。

### 4.4 拓扑序 DP 模板

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
int h[N], e[N], ne[N], w[N], idx;
int in_deg[N];
int n, m;

void add(int u, int v, int weight) {
    e[idx] = v; w[idx] = weight; ne[idx] = h[u]; h[u] = idx++;
}

int f[N];      // f[v] = 到 v 的最长路（或路径数，按题意改转移）

void topo_dp() {
    queue<int> q;
    for (int i = 1; i <= n; i++)
        if (in_deg[i] == 0) { q.push(i); f[i] = 0; }  // 起点初始化
    while (!q.empty()) {
        int u = q.front(); q.pop();
        for (int i = h[u]; i != -1; i = ne[i]) {
            int v = e[i];
            f[v] = max(f[v], f[u] + w[i]);    // 最长路转移
            // 若求路径数：f[v] = (f[v] + f[u]) % MOD;
            if (--in_deg[v] == 0) q.push(v);
        }
    }
}

int main() {
    cin >> n >> m;
    memset(h, -1, sizeof(h));
    for (int i = 1; i <= m; i++) {
        int u, v, weight; cin >> u >> v >> weight;
        add(u, v, weight);
        in_deg[v]++;
    }
    topo_dp();
    int ans = 0;
    for (int i = 1; i <= n; i++) ans = max(ans, f[i]);
    cout << ans << endl;
    return 0;
}
```

!!! warning "为什么不能在删边前转移"
    必须在"删边"动作中**一并完成转移**——即处理 $u \to v$ 时立即用 `f[u]` 更新 `f[v]`。当 `in_deg[v]` 归零时，所有指向 $v$ 的边都已处理完，`f[v]` 已是最终值，此时才能把 $v$ 入队继续向后转移。若忘记在删边时转移，DP 结果会错。

### 4.5 DP 转移方式对比

| 问题类型 | 转移方程 | 初始化 | 说明 |
|----------|---------|--------|------|
| 最长路 | `f[v] = max(f[v], f[u] + w)` | 起点 `f = 0` | 关键路径、任务最短工期 |
| 最短路 | `f[v] = min(f[v], f[u] + w)` | 起点 `f = 0`，其余 `INF` | 类似 Dijkstra 但 DAG 上更简单 |
| 路径计数 | `f[v] = (f[v] + f[u]) % MOD` | 起点 `f = 1` | 食物链计数等 |

---

## 第五节 常见误区

!!! warning "误区一：图中存在环时拓扑排序不完整"
    有环时，环上顶点的入度永远降不到 0，队列会提前变空，`order.size() < n`。**务必检查排出的顶点数**是否等于 $n$，否则会误以为得到了完整拓扑序。这是判环的关键，也是初学者最易忽略的检查。

!!! warning "误区二：入度更新漏减"
    删边时必须对**每一条出边**的终点入度减 1。若漏减某条边，该终点的入度永远不为 0，导致它和它的后继都无法入队，拓扑序不完整。常见原因：建图时某条边忘记 `in_deg[v]++`，或遍历出边时写错循环条件。

!!! warning "误区三：建图方向搞反"
    "A 依赖 B"应建边 `B → A`（B 在前，A 在后）。若建反成 `A → B`，拓扑序恰好颠倒，任务顺序全错。**先理清依赖方向再建图**：被依赖的在前，依赖者在后。

!!! warning "误区四：拓扑序 DP 的转移时机错误"
    必须在"删边 $u \to v$"时立即用 `f[u]` 更新 `f[v]`，而不是等 $v$ 出队时才遍历前驱。因为前驱在 $v$ 出队时已经出队完毕，若没在删边时记录转移，信息就丢了。正确做法：**边删边转移**。

!!! warning "误区五：用 priority_queue 时忘了 greater"
    `priority_queue<int>` 默认是大根堆，取出的是**最大**编号。要字典序最小的拓扑序，必须用 `priority_queue<int, vector<int>, greater<int>>`（小根堆）。

!!! warning "误区六：多组数据忘记清空"
    多组测试数据时，`h`、`in_deg`、`idx`、`f` 都要重新初始化，否则上一组的数据会污染当前组。`memset(h, -1, sizeof(h))`、`memset(in_deg, 0, sizeof(in_deg))`、`idx = 0` 一个都不能少。

---

## 第六节 📚 本节练习

### 6.1 [B3644] 拓扑排序模板

**题意**：给定 $n$ 个点的有向图，输出一个拓扑排序。要求每次选择当前可输出的点中**编号最小**的一个。

**思路**：Kahn 算法，但把队列换成**小根堆**（`priority_queue` + `greater`），每次取出编号最小的入度为 0 的顶点。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 105;
int h[N], e[N * N], ne[N * N], idx;
int in_deg[N];
int n;

void add(int u, int v) { e[idx] = v; ne[idx] = h[u]; h[u] = idx++; }

int main() {
    cin >> n;
    memset(h, -1, sizeof(h));
    for (int u = 1; u <= n; u++) {
        int v;
        while (cin >> v && v) {       // 以 0 结束
            add(u, v);
            in_deg[v]++;
        }
    }
    priority_queue<int, vector<int>, greater<int>> q;   // 小根堆
    for (int i = 1; i <= n; i++)
        if (in_deg[i] == 0) q.push(i);
    while (!q.empty()) {
        int u = q.top(); q.pop();
        cout << u << " ";
        for (int i = h[u]; i != -1; i = ne[i]) {
            int v = e[i];
            if (--in_deg[v] == 0) q.push(v);
        }
    }
    cout << endl;
    return 0;
}
```

!!! tip "为什么用小根堆"
    题目要求编号最小的先输出。普通队列只保证"先入先出"，不保证编号最小。小根堆每次弹出最小值，正好满足要求。注意题目数据范围 $n \le 100$，数组开 `N = 105` 即可。

### 6.2 [P1113] 杂务

**题意**：John 有 $n$ 项杂务，第 $i$ 项耗时 $T_i$。某些杂务有前置杂务（必须全部完成才能开始）。多项无依赖关系的杂务可以**同时进行**。求完成所有杂务的最少时间。

**思路**：这是经典的 **DAG 最长路**问题。设 `f[i]` 表示完成杂务 $i$ 的最早时间：

$$f[i] = T_i + \max_{j \to i} f[j]$$

即 $i$ 的完成时间 = 自身耗时 + 所有前置杂务中最晚完成的那个的时间。没有前置的杂务 `f[i] = T_i`。答案为所有 `f[i]` 的最大值。按拓扑序计算即可。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 10;
int h[N], e[N], ne[N], idx;
int in_deg[N], T[N], f[N];
int n;

void add(int u, int v) { e[idx] = v; ne[idx] = h[u]; h[u] = idx++; }

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    cin >> n;
    memset(h, -1, sizeof(h));
    for (int i = 1; i <= n; i++) {
        int id, t; cin >> id >> t;
        T[id] = t;
        int pre;
        while (cin >> pre && pre) {
            add(pre, id);            // pre → id（pre 是 id 的前置）
            in_deg[id]++;
        }
    }
    queue<int> q;
    for (int i = 1; i <= n; i++)
        if (in_deg[i] == 0) { q.push(i); f[i] = T[i]; }   // 无前置，f = 自身耗时
    while (!q.empty()) {
        int u = q.front(); q.pop();
        for (int i = h[u]; i != -1; i = ne[i]) {
            int v = e[i];
            f[v] = max(f[v], f[u] + T[v]);   // 转移：u 完成后 v 才能开始
            if (--in_deg[v] == 0) q.push(v);
        }
    }
    int ans = 0;
    for (int i = 1; i <= n; i++) ans = max(ans, f[i]);
    cout << ans << endl;
    return 0;
}
```

!!! tip "关键路径思想"
    本题本质是求 DAG 的**关键路径**——决定总工期的最长链。`f[i]` 是事件 $i$ 的"最早发生时间"。若还需"最迟发生时间"，则是另一套逆拓扑序 DP。本题只需最早时间。

!!! warning "建图方向"
    "杂务 $i$ 的前置是 $pre$"意味着 $pre$ 要先完成，所以建边 $pre \to i$（$pre$ 在拓扑序中靠前）。建反了会导致拓扑序颠倒、DP 转移错误。

### 6.3 [P4017] 最大食物链计数

**题意**：给定一个食物网（DAG），$A \to B$ 表示 $A$ 被 $B$ 吃（$A$ 是 $B$ 的猎物）。求**最大食物链**的数量。最大食物链指从**生产者**（入度为 0，不被任何生物吃）到**顶级捕食者**（出度为 0，不吃任何生物）的路径。答案对 $80112002$ 取模。

**思路**：DAG 上的**路径计数 DP**。设 `f[i]` 表示以 $i$ 为终点的食物链数量：

$$f[v] = \sum_{u \to v} f[u]$$

生产者（入度为 0）的 `f = 1`。按拓扑序累加，最终答案为所有顶级捕食者（出度为 0）的 `f` 之和。

```
食物网示例：1 → 3 → 5, 2 → 3 → 5, 3 → 4 → 5
生产者：1, 2（入度 0）    顶级捕食者：5（出度 0）
f[1]=1, f[2]=1, f[3]=f[1]+f[2]=2, f[4]=f[3]=2, f[5]=f[3]+f[4]=2+2=4
答案 = f[5] = 4
```

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 5010, M = 500010;       // M 为最大边数，避免 N*N 导致 MLE
const int MOD = 80112002;
int h[N], e[M], ne[M], idx;
int in_deg[N], out_deg[N], f[N];
int n, m;

void add(int u, int v) { e[idx] = v; ne[idx] = h[u]; h[u] = idx++; }

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    cin >> n >> m;
    memset(h, -1, sizeof(h));
    for (int i = 1; i <= m; i++) {
        int u, v; cin >> u >> v;
        add(u, v);                  // u 被 v 吃：u → v
        in_deg[v]++;
        out_deg[u]++;
    }
    queue<int> q;
    for (int i = 1; i <= n; i++) {
        if (in_deg[i] == 0) {       // 生产者
            q.push(i);
            f[i] = 1;               // 生产者起点 f = 1
        }
    }
    while (!q.empty()) {
        int u = q.front(); q.pop();
        for (int i = h[u]; i != -1; i = ne[i]) {
            int v = e[i];
            f[v] = (f[v] + f[u]) % MOD;     // 路径累加取模
            if (--in_deg[v] == 0) q.push(v);
        }
    }
    int ans = 0;
    for (int i = 1; i <= n; i++)
        if (out_deg[i] == 0)               // 顶级捕食者
            ans = (ans + f[i]) % MOD;
    cout << ans << endl;
    return 0;
}
```

!!! tip "路径计数的核心"
    路径计数 DP 的转移是**累加**（`f[v] += f[u]`），而最长路是**取最大**（`f[v] = max(f[v], f[u] + w)`）。两者都在拓扑序上进行，区别只在转移运算符。务必记得**取模**，否则会溢出。

!!! warning "注意出度数组的维护"
    本题需要区分"顶级捕食者"（出度为 0），所以建图时要同时维护 `out_deg`。很多同学只记着维护入度，忘了出度，导致最后求和时无法判断哪些是终点。

---

## 📖 本章总结

```
第29章 拓扑排序
├── 定义：DAG 的线性排序，每条边 u→v 中 u 在 v 之前
│   └── 只有 DAG 才能拓扑排序；有环图不行
├── Kahn 算法（BFS 实现）
│   ├── 入度数组 in_deg[]：建图时累加
│   ├── 入度为 0 的顶点入队
│   ├── 取出顶点，删其出边，终点入度 -1，归零则入队
│   ├── 时间 O(n+m)，空间 O(n+m)
│   └── 字典序最小：queue 换 priority_queue + greater
├── 典型应用
│   ├── 判环：order.size() < n 则有环
│   └── 任务调度 / 先修课程：依赖关系建 DAG
├── 拓扑序 + DP（核心套路）
│   ├── 最长路：f[v] = max(f[v], f[u] + w)   ← P1113 杂务
│   ├── 路径计数：f[v] = (f[v] + f[u]) % MOD ← P4017 食物链
│   └── 转移时机：删边时立即转移，in_deg 归零时入队
├── 常见误区
│   ├── 有环时未检查 order.size()，误以为排完
│   ├── 入度更新漏减（漏 in_deg[v]++ 或漏 --）
│   ├── 建图方向搞反（"A 依赖 B"应建 B→A）
│   ├── DP 转移时机错误（应在删边时转移）
│   └── 多组数据忘记清空
└── 配套习题：B3644 模板 / P1113 最长路 / P4017 路径计数
```

### 自测清单（合上书自检）

!!! tip "能做到以下几点，才算真正掌握本章"
    - [ ] 说出拓扑排序的定义，解释为什么有环图不能拓扑排序
    - [ ] 默写 Kahn 算法（队列 + 入度数组）的完整代码
    - [ ] 用拓扑排序判断图中是否有环
    - [ ] 写出"字典序最小的拓扑序"的代码（小根堆）
    - [ ] 解释为什么拓扑序适合做 DP（无后效性）
    - [ ] 用拓扑序 DP 求 DAG 最长路（P1113）
    - [ ] 用拓扑序 DP 求路径计数并取模（P4017）
    - [ ] 说出建图方向："A 依赖 B"应建 B→A
    - [ ] 说出 DP 转移的正确时机：删边时立即转移

### 核心速查表

| 操作 | 代码 | 复杂度 |
|------|------|:------:|
| 建图累加入度 | `in_deg[v]++` | $O(1)$ |
| 入度为 0 入队 | `if (in_deg[i]==0) q.push(i)` | $O(n)$ |
| 取顶点 + 删出边 | `u=q.front(); q.pop();` 遍历 `h[u]` | $O(度数)$ |
| 减入度 + 入队 | `if (--in_deg[v]==0) q.push(v)` | $O(1)$ |
| 判环 | `order.size() == n` | $O(1)$ |
| 最长路转移 | `f[v] = max(f[v], f[u] + w)` | $O(1)$ |
| 路径计数转移 | `f[v] = (f[v] + f[u]) % MOD` | $O(1)$ |

### 知识衔接

本章是**有向无环图（DAG）上的核心算法**，与前后章节的联系如下：

- **前置知识**：第28章BFS进阶（Kahn算法基于入度队列）、第26章图的存储与遍历（入度与邻接表）、第12章排序算法（线性序的对比）
- **本章定位**：在DAG上实现线性排序，是差分约束系统与关键路径算法的前置工具
- **后续发展**：为第53章强连通分量与差分约束铺路，拓扑序在DP求最长/最短路中也是关键预处理步骤

!!! info "下一章预告"
    拓扑排序解决了"有依赖关系的顺序"问题。后续将学习**最短路算法**（Dijkstra、SPFA、Floyd）与**最小生成树**（Kruskal、Prim），它们同样建立在图的存储与遍历之上，与拓扑排序共同构成 CSP 图论的核心知识体系。
