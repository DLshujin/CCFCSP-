# 第42章 背包DP

> 给你一个容量有限的背包和一堆物品，每个物品有体积和价值，怎样装才能让总价值最大？这就是"背包问题"——动态规划最经典、最常考的模型家族。CSP-S 中 01 背包、完全背包、多重背包、分组背包四种基本形态必须熟练默写，它们之间的差异往往只在一行代码、一个循环方向上。

!!! info "本章内容"
    - 01 背包：每件物品选或不选，二维转一维的空间优化（倒序遍历）
    - 完全背包：每件物品无限件，正序遍历的原理
    - 多重背包：每件物品有限件，二进制拆分优化
    - 分组背包：每组最多选一件，组内枚举转移
    - 四种背包对比与初始化技巧（求最大值 vs 恰好装满）
    - 常见误区：遍历方向、long long、体积为 0 的物品

---

## 📌 学习目标

学完本章后，你应当能够：

1. **默写** 01 背包的二维与一维转移方程，并解释一维化时"倒序遍历"的原因
2. **说清楚**完全背包与 01 背包在转移上的唯一区别——遍历方向，并解释正序遍历为何允许重复选取
3. **演示**多重背包的二进制拆分过程（如 $s=13$ 拆成 $1,2,4,6$），并写出优化代码
4. **默写**分组背包的转移框架（组外倒序、组内枚举）
5. **区分**"求最大价值"与"恰好装满"两种初始化方式，并能识别需要开 long long 的方案数问题

---

## 💡 生活类比

打个比方：你要出门旅行，行李箱容量是固定的（背包容量 $V$），面前有一堆东西想带（物品）。每样东西占据一定空间（体积 $v_i$），又给你带来不同的"幸福感"（价值 $w_i$）。你的目标是：**在箱子装得下的前提下，让幸福感总和最大**。

- 如果每样东西只能带一件 → **01 背包**
- 如果每样东西可以带任意多件 → **完全背包**
- 如果每样东西只有几件（如袜子 5 双）→ **多重背包**
- 如果东西分成几组，每组最多带一件（上衣、裤子、鞋子各选一件）→ **分组背包**

四种背包的本质都是在"容量约束"下做"选/不选"的决策，区别只在于物品的可选次数和组合方式。本章统一符号：$v_i$ 表示物品体积（消耗的容量），$w_i$ 表示物品价值。

---

## 第1节 01背包

### 1.1 问题模型

有 $n$ 件物品和一个容量为 $V$ 的背包。第 $i$ 件物品体积为 $v_i$、价值为 $w_i$。每件物品**至多选一次**，求装入背包物品的总价值最大值。

### 1.2 状态设计与转移

- `f[i][j]`：从前 $i$ 件物品中选，**总体积不超过 $j$** 的最大价值

对第 $i$ 件物品，只有"不选"和"选"两种决策：

$$f[i][j] = \max\bigl(f[i-1][j],\; f[i-1][j-v_i] + w_i\bigr)$$

- `f[i-1][j]`：不选第 $i$ 件，价值不变
- `f[i-1][j-v_i] + w_i`：选第 $i$ 件，腾出 $v_i$ 体积，加上 $w_i$ 价值（需 $j \ge v_i$）

**复杂度**：时间 $O(nV)$，空间 $O(nV)$。

### 1.3 空间优化：二维转一维

观察转移方程：`f[i][...]` 只依赖 `f[i-1][...]`（上一层）。可以把第一维压掉，只用 `f[j]` 表示"体积不超过 $j$ 的最大价值"：

$$f[j] = \max\bigl(f[j],\; f[j-v_i] + w_i\bigr)$$

!!! warning "关键：j 必须倒序遍历"
    一维化后，**j 必须从大到小遍历**（`for j = V downto v[i]`）。

    **原因**：转移需要用到"上一层"的 `f[j-v_i]`（还没考虑第 $i$ 件物品时的旧值）。若 j 正序（从小到大），计算 `f[j]` 时 `f[j-v_i]`（$j-v_i < j$）可能已在本次循环中被更新成"考虑了第 $i$ 件物品的新值"，导致第 $i$ 件被**重复选取**——这就退化成完全背包了！

    **倒序遍历**保证计算 `f[j]` 时，`f[j-v_i]`（$j-v_i < j$，尚未被本轮更新）仍是上一层的旧值，每件物品只选一次。

**图解（容量 $V=4$，物品 $(v,w)=(2,3),(3,4)$）**：

```
初始:        f[0]=0  f[1]=0  f[2]=0  f[3]=0  f[4]=0
处理物品1(v=2,w=3), j 倒序 4→2:
  j=4: f[4]=max(f[4], f[2]+3)=3    (f[2]是旧值0)
  j=3: f[3]=max(f[3], f[1]+3)=3
  j=2: f[2]=max(f[2], f[0]+3)=3
处理后:       f[0]=0  f[1]=0  f[2]=3  f[3]=3  f[4]=3
处理物品2(v=3,w=4), j 倒序 4→3:
  j=4: f[4]=max(f[4], f[1]+4)=4    (f[1]是旧值0)
  j=3: f[3]=max(f[3], f[0]+4)=4
最终:         f[0]=0  f[1]=0  f[2]=3  f[3]=4  f[4]=4
答案 f[4]=4（选物品2，体积3价值4）
```

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXV = 1005;
int v[105], w[105];
int f[MAXV];                  // 一维：f[j]=体积不超过j的最大价值

int main() {
    int n, V;
    scanf("%d %d", &n, &V);
    for (int i = 1; i <= n; i++) scanf("%d %d", &v[i], &w[i]);
    for (int i = 1; i <= n; i++)
        for (int j = V; j >= v[i]; j--)   // 倒序！保证每件只选一次
            f[j] = max(f[j], f[j - v[i]] + w[i]);
    printf("%d\n", f[V]);
    return 0;
}
```

!!! tip "口诀"
    **01 背包一维化，j 从大往小扫；完全背包 j 从小往大扫。** 方向写错，01 背包就"退化"成完全背包。

---

## 第2节 完全背包

### 2.1 问题模型

有 $n$ 件物品和容量为 $V$ 的背包，第 $i$ 件物品体积 $v_i$、价值 $w_i$。每件物品**可选无限件**，求最大价值。

### 2.2 状态转移与正序遍历

与 01 背包几乎一样，区别在于"选了第 $i$ 件后还能继续选第 $i$ 件"：

$$f[i][j] = \max\bigl(f[i-1][j],\; f[i][j-v_i] + w_i\bigr)$$

注意第二项是 `f[i][j-v_i]`（本层），而非 `f[i-1][j-v_i]`（上层）——因为选了一件后还可以再选同一件。

压成一维后：

$$f[j] = \max\bigl(f[j],\; f[j-v_i] + w_i\bigr)$$

这里 **j 正序遍历**（`for j = v[i] to V`）。因为计算 `f[j]` 时，`f[j-v_i]`（$j-v_i < j$，已在本轮被更新）恰好表示"已经选过若干件第 $i$ 件物品"的状态，从而允许重复选取。

例如 $V=4$、物品 $(v,w)=(2,3)$ 正序遍历：`j=2` 时 `f[2]=3`（选 1 件），`j=4` 时 `f[4]=f[2]+3=6`（`f[2]` 已是新值，等于选 2 件）。可见正序遍历时 `f[j-v_i]` 用的是"已选过本物品"的新值，从而实现重复选取。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXV = 1005;
int v[105], w[105];
int f[MAXV];

int main() {
    int n, V;
    scanf("%d %d", &n, &V);
    for (int i = 1; i <= n; i++) scanf("%d %d", &v[i], &w[i]);
    for (int i = 1; i <= n; i++)
        for (int j = v[i]; j <= V; j++)    // 正序！允许重复选
            f[j] = max(f[j], f[j - v[i]] + w[i]);
    printf("%d\n", f[V]);
    return 0;
}
```

!!! info "01 与完全的唯一区别"
    两种背包的一维转移方程**完全一样**，都是 `f[j]=max(f[j], f[j-v]+w)`。唯一区别是 **j 的遍历方向**：01 倒序（用旧值，每件选一次），完全正序（用新值，可重复选）。一维化后，"用旧值还是新值"决定了能否重复选取，这正是背包问题最精妙的细节。

---

## 第3节 多重背包

### 3.1 问题模型

有 $n$ 种物品和容量为 $V$ 的背包，第 $i$ 种物品体积 $v_i$、价值 $w_i$、**数量 $s_i$ 件**。求最大价值。

### 3.2 朴素拆分 O(nVs)

最直观的做法：把第 $i$ 种物品的 $s_i$ 件**拆成 $s_i$ 个独立的单件物品**，然后当 01 背包做。复杂度 $O(nVs)$，当 $s_i$ 较大时太慢，需要优化。

### 3.3 二进制拆分优化 O(nV log s)

**核心思想**：用 $O(\log s)$ 个"打包物品"等价替换 $s$ 件单品，使得任意选取件数 $k \in [0, s]$ 都能由这些包"选或不选"组合出来。

**拆分方法**：依次取出 $1, 2, 4, 8, \dots, 2^k$（每个是上一个的两倍），直到剩余不足下一个 $2$ 的幂，把**剩余部分**单独打包。这样得到的若干个包，每个"选或不选"，就能凑出 $0$ 到 $s$ 的任何整数。

**演示（$s = 13$）**：

| 步骤 | 取出 | 剩余 | 说明 |
|------|------|------|------|
| 1 | 1 | 12 | $2^0$ |
| 2 | 2 | 10 | $2^1$ |
| 3 | 4 | 6 | $2^2$ |
| 4 | 6 | 0 | 余数 $r=13-1-2-4=6$，单独打包 |

拆成 4 个包，体积价值分别为 $(1v, 1w),\; (2v, 2w),\; (4v, 4w),\; (6v, 6w)$。验证：用这 4 个包（每个选或不选）能凑出 $0$ 到 $13$ 的任意件数，例如 $11 = 1+4+6$，$13 = 1+2+4+6$。

!!! tip "为什么正确？"
    $1,2,4,\dots,2^k$ 能凑出 $0$ 到 $2^{k+1}-1$ 的所有数（二进制原理）。剩余部分 $r < 2^{k+1}$，加上 $r$ 后能凑出的范围从 $[0, 2^{k+1}-1]$ 平移扩展到 $[r, 2^{k+1}-1+r] = [r, s]$。两段 $[0, 2^{k+1}-1]$ 与 $[r, s]$ 相交重叠（$r \le 2^{k+1}-1$），合起来恰好覆盖 $[0, s]$，不重不漏。

**完整模板**：

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 100005;           // 拆分后物品数可能很多
int v0[105], w0[105], s0[105];     // 原始物品：体积、价值、数量
int V2[MAXN], W2[MAXN];            // 拆分后的包：体积、价值
int f[40005];

int main() {
    int n, V;
    scanf("%d %d", &n, &V);
    for (int i = 1; i <= n; i++) scanf("%d %d %d", &v0[i], &w0[i], &s0[i]);
    int cnt = 0;                   // 拆分后的包数
    for (int i = 1; i <= n; i++) {
        int s = s0[i];
        for (int k = 1; k <= s; k <<= 1) {   // 依次取 1,2,4,...
            cnt++;
            V2[cnt] = v0[i] * k;             // 一个包 = k 件原物品
            W2[cnt] = w0[i] * k;
            s -= k;
        }
        if (s > 0) {                         // 剩余部分单独打包
            cnt++;
            V2[cnt] = v0[i] * s;
            W2[cnt] = w0[i] * s;
        }
    }
    // 对拆分后的 cnt 个包做 01 背包
    for (int i = 1; i <= cnt; i++)
        for (int j = V; j >= V2[i]; j--)
            f[j] = max(f[j], f[j - V2[i]] + W2[i]);
    printf("%d\n", f[V]);
    return 0;
}
```

**复杂度**：拆分后物品数 $O(\sum \log s_i)$，总时间 $O(V \sum \log s_i)$。

---

## 第4节 分组背包

### 4.1 问题模型

有 $n$ 组物品和容量为 $V$ 的背包。第 $i$ 组有若干件物品，每组**最多选一件**（可选 0 件或 1 件）。求最大价值。

### 4.2 状态转移

`f[j]`：体积不超过 $j$ 的最大价值。对每一组，先倒序遍历容量 j，再在组内枚举"选哪一件或不选"：

```
for 每一组 i:
    for j = V downto 0:                // 倒序，保证每组只选一件
        for 组内每件物品 k:
            if j >= v[k]:
                f[j] = max(f[j], f[j - v[k]] + w[k])
```

!!! warning "循环顺序是核心"
    - **j 必须在组内枚举的外层**（倒序遍历），保证更新 `f[j]` 时 `f[j-v[k]]` 用的是本组未选任何物品的旧值，从而每组最多选一件。
    - 若把组内枚举放外层、j 放内层，会导致同一组被选多件。

**举例**：第 1 组有 A($v=1,w=2$)、B($v=2,w=3$) 两件，容量 $V=2$。j 倒序：`j=2` 时比较"不选""选 A""选 B"；`j=1` 时比较"不选""选 A"。每组在同一个 j 上最多更新一次有效选取，保证组内单选。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXV = 1005;
vector<int> v[105], w[105];         // 第 i 组的物品列表
int f[MAXV];

int main() {
    int n, V;
    scanf("%d %d", &n, &V);
    for (int i = 1; i <= n; i++) {
        int c; scanf("%d", &c);     // 第 i 组物品数
        for (int j = 0; j < c; j++) {
            int a, b; scanf("%d %d", &a, &b);
            v[i].push_back(a);
            w[i].push_back(b);
        }
    }
    for (int i = 1; i <= n; i++)
        for (int j = V; j >= 0; j--)              // 倒序遍历容量
            for (int k = 0; k < (int)v[i].size(); k++)   // 组内枚举
                if (j >= v[i][k])
                    f[j] = max(f[j], f[j - v[i][k]] + w[i][k]);
    printf("%d\n", f[V]);
    return 0;
}
```

!!! tip "与树形背包的联系"
    第 45 章的树形背包本质就是分组背包：每个子树看作一组，给子树分配不同名额数对应不同的"物品"，组内选一个分配方案。理解了分组背包，树形背包的循环顺序就迎刃而解。

---

## 第5节 四种背包对比

| 维度 | 01 背包 | 完全背包 | 多重背包 | 分组背包 |
|------|---------|---------|---------|---------|
| 物品数量 | 每件 1 件 | 每件无限件 | 每件 $s_i$ 件 | 每组若干件，最多选 1 件 |
| 决策 | 选/不选 | 选 $0\sim\infty$ 件 | 选 $0\sim s_i$ 件 | 组内选 0 或 1 件 |
| 转移（一维） | `f[j]=max(f[j],f[j-v]+w)` | 同左 | 同左（拆成 01） | `f[j]=max(f[j],f[j-v[k]]+w[k])` |
| j 遍历方向 | **倒序** | **正序** | 倒序（拆成 01） | **倒序**，组内枚举在内层 |
| 时间复杂度 | $O(nV)$ | $O(nV)$ | $O(nV\log s)$ | $O(nV\cdot\bar c)$ |
| 典型题目 | P1048 | P1616 | P1776 | P1757 |

!!! info "记忆口诀"
    **01 倒、完全正、多重拆、分组组内枚举。** 四种背包的转移方程长得几乎一样，区别全在"遍历方向"和"物品组织方式"上。只要把 01 背包的一维模板吃透，其余三种都是在它基础上变形。

---

## 第6节 初始化技巧

背包问题的初始化方式取决于"问什么"。

### 6.1 求最大价值（体积不超过 V）

直接把 `f[0..V]` 全部初始化为 **0**。含义：没有任何物品时，无论容量多大，价值都是 0。最终答案在 `f[V]`。全局数组默认全 0，无需额外处理。

### 6.2 恰好装满（体积正好等于 V）

初始化 `f[0] = 0`，其余 `f[1..V] = -\infty`。含义：容量为 0 时价值 0（合法），其余容量"无物品时装不满"是非法状态。转移后 `f[V]` 表示恰好装满容量 $V$ 的最大价值；若 `f[V] < 0` 说明无法恰好装满。

```cpp
memset(f, 0xcf, sizeof(f));          // 全部设为很小的负数（约 -inf）
f[0] = 0;                            // 只有容量 0 合法
```

!!! tip "原理"
    $-\infty$ 表示"不可达"。转移时 `max(f[j], f[j-v]+w)` 中，若 `f[j-v]` 是 $-\infty$（不可达），加上 $w$ 后仍是很小的负数，不会赢得 `max`，从而保证只有合法状态能传递。

### 6.3 求方案数

把 `max` 改成 `+`，初始化 `f[0] = 1`（容量 0 有 1 种方案：什么都不选），其余 0。注意方案数增长很快，**通常需要开 long long** 或对大数取模。

---

## 第7节 常见误区

### 误区1：01背包一维化后正序遍历

!!! warning "问题"
    一维 01 背包若 j 正序遍历（`for j = v[i] to V`），会导致同一物品被重复选取，实际跑成了完全背包。

    ```cpp
    // ❌ 错误：正序，物品被重复选取
    for (int j = v[i]; j <= V; j++)
        f[j] = max(f[j], f[j - v[i]] + w[i]);

    // ✅ 正确：倒序，每件只选一次
    for (int j = V; j >= v[i]; j--)
        f[j] = max(f[j], f[j - v[i]] + w[i]);
    ```

    **记忆**：01 背包一维化一定要倒序！这是背包问题最经典的坑。

### 误区2：多重背包二进制拆分遗漏剩余部分

!!! warning "问题"
    二进制拆分时，循环 `for (k=1; k<=s; k<<=1)` 结束后，若 $s$ 还有剩余（不能被 $1+2+4+\dots$ 完整取尽），**必须单独打包剩余部分**，否则凑不出所有件数。

    例如 $s=13$：取出 $1,2,4$ 后剩 $6$，必须把 $6$ 单独打包，否则无法凑出 $6\sim 13$ 这些件数。

    ```cpp
    // ✅ 正确：循环结束后处理剩余
    for (int k = 1; k <= s; k <<= 1) { /* 打包 k 件 */ s -= k; }
    if (s > 0) { /* 打包剩余 s 件 */ }
    ```

### 误区3：求方案数忘记开 long long

!!! warning "问题"
    方案数增长极快（指数级）。当 $n, V$ 稍大时，方案数会溢出 `int` 范围（约 $2.1\times 10^9$）。

    ```cpp
    // ❌ 错误：方案数溢出 int
    int f[MAXV]; f[0] = 1;

    // ✅ 正确：开 long long
    long long f[MAXV]; f[0] = 1;
    ```

    若题目要求对某数取模，转移时及时 `% MOD`。"十年 OI 一场空，不开 long long 见祖宗。"

### 误区4：体积为 0 的物品

!!! warning "问题"
    当某物品 $v_i = 0$ 且 $w_i > 0$ 时：
    - **01 背包**：倒序遍历会出现 `f[j] = max(f[j], f[j]+w[i])` 自引用，需特殊处理
    - **完全背包**：可无限选取，价值无穷大！必须特殊判断或题目保证 $v_i > 0$

    **建议**：读入时若 $v_i = 0$，直接把 $w_i$ 累加到答案（不占容量），不进入背包循环。

---

## 第8节 配套习题精讲

### 例题一：[P1048] 采药（NOIP2005 普及组）

**题意**：山洞里有 $M$ 株草药（$1 \le M \le 100$），采每株需时间 $t_i$、价值 $v_i$（$1 \le t_i, v_i \le 100$）。给定总时间 $T$（$1 \le T \le 1000$），每株最多采一次，求能采到的最大价值。

**思路**：标准 01 背包。时间即"容量"，草药即"物品"。一维化，j 倒序。数据范围小，朴素 $O(MT)$ 轻松通过。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXT = 1005;
int t[105], val[105];              // t=采集时间(体积)，val=价值
int f[MAXT];

int main() {
    int T, M;
    scanf("%d %d", &T, &M);
    for (int i = 1; i <= M; i++) scanf("%d %d", &t[i], &val[i]);
    for (int i = 1; i <= M; i++)
        for (int j = T; j >= t[i]; j--)      // 倒序
            f[j] = max(f[j], f[j - t[i]] + val[i]);
    printf("%d\n", f[T]);
    return 0;
}
```

**复杂度**：时间 $O(MT) = 10^5$，空间 $O(T)$。

---

### 例题二：[P1616] 疯狂的采药

**题意**：与采药类似，但每种草药**可采无限株**。$1 \le M \le 10^4$，$1 \le T \le 10^7$，且 $1 \le M \times T \le 10^7$，$1 \le a_i, b_i \le 10^4$。求最大价值。

**思路**：完全背包。注意两个数据范围坑点：
1. **$T$ 可达 $10^7$**，`f` 数组要开到 $10^7$；
2. **极限价值 $T \times b_i \approx 10^{11}$**，远超 `int`（约 $2.1\times 10^9$），必须开 `long long`。

得益于 $M \times T \le 10^7$，$O(MT)$ 的完全背包恰好能在 1s 内通过。注意大数组 `f` 必须开在**全局区**（栈区放不下）。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXT = 10000005;         // T 最大 1e7
int t[10005], val[10005];
long long f[MAXT];                 // 注意 long long！开在全局区

int main() {
    int T, M;
    scanf("%d %d", &T, &M);
    for (int i = 1; i <= M; i++) scanf("%d %d", &t[i], &val[i]);
    for (int i = 1; i <= M; i++)
        for (int j = t[i]; j <= T; j++)      // 正序！完全背包
            f[j] = max(f[j], f[j - t[i]] + val[i]);
    printf("%lld\n", f[T]);
    return 0;
}
```

**复杂度**：时间 $O(MT) \le 10^7$，空间 $O(T)$（`f` 数组约 80MB）。

---

### 例题三：[P1776] 宝物筛选（SDOI2009）

**题意**：$n$ 种宝物（$1 \le n \le 100$），采集车最大载重 $W$（$0 \le W \le 4\times 10^4$），每种宝物有价值 $v_i$、重量 $w_i$、数量 $m_i$（$\sum m_i \le 10^5$）。在不超过载重的前提下，求收集宝物的最大价值和。

**思路**：多重背包，二进制拆分优化。把每种宝物的 $m_i$ 件按 $1,2,4,\dots$ 拆成 $O(\log m_i)$ 个包，再对拆分后的包做 01 背包。

!!! warning "注意输入顺序"
    本题输入每行为「价值 重量 数量」($v_i, w_i, m_i$)，与本章模板的「体积 价值 数量」顺序不同，读入时要对应清楚。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1000005;          // 拆分后物品数上界
const int MAXW = 40005;
int V2[MAXN], W2[MAXN];            // 拆分后包的价值、重量
int f[MAXW];

int main() {
    int n, W;
    scanf("%d %d", &n, &W);
    int cnt = 0;
    for (int i = 1; i <= n; i++) {
        int v, w, s;
        scanf("%d %d %d", &v, &w, &s);   // 输入顺序：价值、重量、数量
        for (int k = 1; k <= s; k <<= 1) {
            cnt++;
            V2[cnt] = v * k;             // 一个包 = k 件原宝物
            W2[cnt] = w * k;
            s -= k;
        }
        if (s > 0) {                     // 剩余部分单独打包
            cnt++;
            V2[cnt] = v * s;
            W2[cnt] = w * s;
        }
    }
    for (int i = 1; i <= cnt; i++)       // 01 背包
        for (int j = W; j >= W2[i]; j--)
            f[j] = max(f[j], f[j - W2[i]] + V2[i]);
    printf("%d\n", f[W]);
    return 0;
}
```

**复杂度**：拆分后包数 $O(\sum \log m_i) \le 1700$，时间 $O(W\cdot\sum\log m_i) \approx 6.8\times 10^7$，可 AC。

---

## 📚 本节练习

### 🟢 基础题

**练习1**：[P1049] 装箱问题 - https://www.luogu.com.cn/problem/P1049
> 01 背包变式，求剩余空间最小。把体积同时当作"体积"和"价值"，转化为"体积不超过 V 的最大价值"，再用 $V - f[V]$ 求剩余空间。

**练习2**：[P1060] 开心的金明 - https://www.luogu.com.cn/problem/P1060
> 01 背包模板，物品有价格和重要度，价值 = 价格 × 重要度。注意预算就是容量。

### 🟡 提高题

**练习3**：[P1757] 通天之分组背包 - https://www.luogu.com.cn/problem/P1757
> 分组背包模板题。每组最多选一件，注意组编号可能不连续，需按组读入处理，j 倒序、组内枚举在内层。

**练习4**：[P1941] 飞扬的小鸟 - https://www.luogu.com.cn/problem/P1941
> 完全背包（上升）与 01 背包（下降）结合的二维 DP，CSP-S 经典真题，考查背包模型在游戏场景中的灵活运用。

### 🔴 CSP-S 真题变式

**练习5**：[P1806] 跑步 - https://www.luogu.com.cn/problem/P1806
> 完全背包求方案数，注意开 long long。求用若干正整数之和表示 $n$ 的方案数。

**练习6**：[P2347] 砝码称重 - https://www.luogu.com.cn/problem/P2347
> 多重背包求"能表示多少种重量"（方案存在性）。可用 bitset 优化，把多重背包转 01 背包后做位运算。

---

## 📖 本章总结

### 思维导图

- 背包 DP
  - 01 背包
    - 每件物品选或不选，一维化后**倒序**遍历
    - $O(nV)$ 时间、$O(V)$ 空间
  - 完全背包
    - 每件物品无限件，一维化后**正序**遍历
    - $O(nV)$ 时间、$O(V)$ 空间
  - 多重背包
    - 每件物品有限件，**二进制拆分**转 01 背包
    - $O(nV \log s)$ 时间
  - 分组背包
    - 每组至多选一件，组内枚举 + j 倒序
    - $O(nV)$ 时间
  - 初始化技巧
    - 求最大价值：全 0
    - 恰好装满：`f[0]=0`，其余 $-\infty$
    - 求方案数：`f[0]=1`，其余 0

### 速查表

| 模型 | 状态设计 | 转移方程 | 遍历方向 | 复杂度 | 典型题 |
|------|---------|---------|---------|--------|--------|
| 01 背包 | `f[j]` | `f[j]=max(f[j], f[j-v]+w)` | j 倒序 | $O(nV)$ | P1048 |
| 完全背包 | `f[j]` | `f[j]=max(f[j], f[j-v]+w)` | j 正序 | $O(nV)$ | P1616 |
| 多重背包 | `f[j]` | 二进制拆分转 01 背包 | j 倒序 | $O(nV\log s)$ | P1776 |
| 分组背包 | `f[j]` | `f[j]=max(f[j], f[j-v[k]]+w[k])` | j 倒序、组内正序 | $O(nV)$ | P1757 |
| 恰好装满 | `f[j]` | 同上，初始化 `f[0]=0` 其余 $-\infty$ | — | $O(nV)$ | P1164 |
| 方案数 | `f[j]` | `f[j]+=f[j-v]` | 视模型 | $O(nV)$ | P1832 |

### 核心口诀

| 口诀 | 含义 |
|------|------|
| **01 倒序，完全正序** | 一维化后遍历方向决定能否重复选取 |
| **多重二进制拆** | 把 $s$ 件拆成 $1,2,4,\dots,r$，转 01 背包 |
| **分组组内枚举** | j 倒序在外，组内选哪件在内 |
| **求最大全 0，恰好负无穷** | 初始化看问"不超过"还是"正好装满" |
| **方案数开 long long** | 方案数指数增长，警惕溢出 |

### 自测清单

!!! tip "能做到以下几点，才算真正掌握本章"
    - [ ] 默写 01 背包一维转移并解释倒序遍历的原因
    - [ ] 默写完全背包转移并解释正序遍历的原因
    - [ ] 用 $s=13$ 演示二进制拆分过程
    - [ ] 默写分组背包的三层循环顺序
    - [ ] 区分"求最大价值"与"恰好装满"的初始化
    - [ ] 独立 AC P1048、P1616、P1776 三道题

### 知识衔接

本章是动态规划卷的第二章，与前后章节的联系如下：

- **前置知识**：第41章DP入门（DP三要素、记忆化搜索与递推、无后效性原理）是本章的直接基础
- **本章定位**：系统讲解DP最经典的模型家族——01背包、完全背包、多重背包、分组背包，是CSP-S最高频的DP考点，也是理解"阶段—状态—决策"框架的最佳载体
- **后续发展**：背包模型是后续各类DP的基础组件，第43章线性DP将DP从"容量选择"推广到"序列递推"，第45章树形DP中的树形背包更是背包模型的直接延伸

!!! info "下一章预告"
    背包 DP 是动态规划最基础的模型家族。下一章将进入**线性 DP**——最长上升子序列（LIS）、最长公共子序列（LCS）、编辑距离、最大子段和等经典序列模型，它们是 DP 家族中最高频的基础成员。
