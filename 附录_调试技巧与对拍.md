# 附录：调试技巧与对拍

> 竞赛中"写出代码"只完成了一半，"调对代码"才是另一半。本附录系统讲解 CSP-J/S 竞赛中最实用的调试方法：从读懂编译错误、定位运行时错误，到竞赛终极武器——**对拍**。掌握这些技巧，能让你在赛场上少交 5 次罚时。

---

## 📌 本附录目标

学完后你应当能够：

1. **快速读懂**编译器的报错信息，定位到出错的行号和原因
2. **区分** RE / TLE / WA / MLE 四种常见评测状态，并知道各自的排查方向
3. **熟练使用** `cerr`、`assert`、中间输出等调试手段
4. **独立完成对拍**：编写数据生成器、暴力程序、检查脚本，实现自动找错
5. **养成**边界测试的习惯：$n=1$、$n=0$、最大值、最小值、全相同等

---

## 💡 生活类比

!!! tip "生活类比：做饭与试菜"
    **写代码**像"做菜"——按菜谱（算法）把食材（数据）加工成菜品（输出）。
    
    **调试**像"试菜"——菜做出来不对味（答案错误），你得尝一口看看是盐放多了（逻辑错误）还是火候不够（边界没处理）。
    
    **对拍**像"请两个人同时做同一道菜"——一个用你的秘方（待测程序），一个用最朴素的笨办法（暴力程序），做出来一对比就知道谁的秘方有问题。只要两个结果不一样，就说明你的程序在**这组数据**上出错了。

---

## 第一节 读懂编译错误

### 1.1 常见编译错误类型

| 错误关键词 | 含义 | 常见原因 |
|---------|------|---------|
| `expected ';'` | 缺少分号 | 上一行末尾忘了 `;` |
| `expected ')'` / `expected ']'` | 括号不匹配 | 漏写右括号，或多写了左括号 |
| `undeclared identifier` | 变量未声明 | 拼写错误、作用域问题、忘记 `#include` |
| `redefinition` | 重复定义 | 同名变量/函数定义了两次 |
| `no matching function` | 函数不匹配 | 参数类型/个数不对 |
| `invalid conversion` | 类型不匹配 | `int` 赋给 `char*` 等 |
| `array bound is not an integer` | 数组大小非整数 | 用了变量做数组大小（非 `const`） |

### 1.2 如何看报错

```
test.cpp:15:23: error: expected ';' before '}' token
```

- `test.cpp`：出错的文件
- `15:23`：第 **15** 行，第 **23** 列
- `error: expected ';'`：缺少分号
- `before '}' token`：在 `}` 之前缺分号 → **第 14 行末尾**漏了 `;`

!!! tip "第一个报错最重要"
    编译器看到第一个错误后可能"连锁反应"产生几十个后续报错。**只看第一个**，修完再重新编译。不要被满屏红色吓到。

### 1.3 警告也要看

```bash
g++ -Wall -o test test.cpp   # 加 -Wall 显示所有警告
```

常见警告：
- `warning: unused variable`：变量定义了没用（可能是拼写错误导致用了别的变量）
- `warning: comparison between signed and unsigned`：有符号无符号比较（可能导致死循环）
- `warning: control reaches end of non-void function`：函数没有 return（可能漏了返回值）

!!! warning "把警告当错误"
    竞赛中建议 `g++ -Wall -Wextra -o test test.cpp`，把警告都消除。很多 WA 的根源是警告里提示的问题。

---

## 第二节 四种评测状态与排查

### 2.1 AC（Accepted）

答案正确，恭喜。

### 2.2 WA（Wrong Answer）

**答案错误**，最常见也最难调。排查思路：

1. **手算小数据**：用样例或 $n \leq 5$ 的小数据手算，对比程序输出
2. **检查特殊边界**：$n=1$、$n=0$、全相同元素、最大值、最小值、负数
3. **检查数据类型**：`int` 是否溢出？该用 `long long` 的地方用了 `int`？
4. **检查输出格式**：多了空格、少了换行、多输出了调试信息
5. **检查数组大小**：数组开小了导致越界，可能不 RE 但结果错误
6. **对拍**：终极手段，见第四节

### 2.3 RE（Runtime Error）

**运行时错误**，程序崩溃。常见原因：

| 原因 | 典型场景 | 排查方法 |
|------|---------|---------|
| **数组越界** | 访问 `a[-1]`、`a[n]`（数组大小只有 n） | 检查所有数组访问的下标范围 |
| **空指针/野指针** | 解引用 `nullptr` | 检查指针是否初始化 |
| **除以零** | `a / b` 中 `b = 0` | 检查除数是否为 0 |
| **栈溢出** | 递归太深（如 $10^6$ 层递归） | 改为迭代或手动开大栈 |
| **未定义行为** | 有符号整数溢出、未初始化变量 | 开 `-Wall`，检查初始化 |

!!! tip "本地开栈"
    Linux 下可用 `ulimit -s unlimited` 开无限栈空间。CSP 复赛评测机通常已设置 256MB 栈，但本地默认只有 8MB，递归题本地可能 RE 但评测机 AC。

### 2.4 TLE（Time Limit Exceeded）

**超时**。排查思路：

1. **检查复杂度**：$O(n^2)$ 能否承受 $n = 10^5$？（$10^{10}$ 次运算，必然超时）
2. **检查常数**：`cin/cout` 是否解绑？是否用了 `endl`（频繁刷新缓冲区）？
3. **检查死循环**：`while` 条件恒为真、循环变量没更新
4. **检查输入输出量**：$10^6$ 个数用 `cin` 不解绑会慢 5-10 倍
5. **优化**：换更优算法、加剪枝、用位运算优化

```cpp
// 必加的提速三件套
ios::sync_with_stdio(false);
cin.tie(0);
// cout 用 "\n" 而非 endl
```

### 2.5 MLE（Memory Limit Exceeded）

**内存超限**。常见原因：

- 数组开太大：`int a[100000000]` = 400MB，远超限制
- 多维数组：`int dp[5000][5000]` = 100MB
- 没用滚动数组：能滚动降维的 DP 开了二维

!!! tip "内存估算公式"
    - `int` 数组：元素数 × 4 字节
    - `long long` 数组：元素数 × 8 字节
    - CSP-S 通常限制 256MB ≈ $6.4 \times 10^7$ 个 `int`

---

## 第三节 调试工具与技巧

### 3.1 cerr 调试法

`cerr` 输出到标准错误流，**不经过缓冲**，程序崩溃前一定能输出。更重要的是，评测机**不检查 stderr**，所以即使忘记删调试输出也不会 WA。

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    
    // 调试：输出中间状态
    cerr << "n=" << n << "\n";
    for (int i = 0; i < n; i++) cerr << a[i] << " ";
    cerr << "\n";
    
    // ... 算法逻辑 ...
    
    cout << "answer\n";
    return 0;
}
```

!!! tip "cerr 的优势"
    1. 不影响评测（stderr 不被检查）
    2. 不缓冲，崩溃前一定输出
    3. 可以用条件控制：`#ifdef DEBUG` 在提交时关闭

### 3.2 条件编译调试

```cpp
#include <bits/stdc++.h>
using namespace std;
#define DEBUG   // 本地调试时定义，提交时注释掉

int main() {
    int n; cin >> n;
    #ifdef DEBUG
    cerr << "输入 n=" << n << "\n";
    #endif
    
    // ... 算法 ...
    
    #ifdef DEBUG
    cerr << "中间结果已计算\n";
    #endif
    
    cout << "answer\n";
    return 0;
}
```

编译时加 `-DDEBUG` 也可启用：`g++ -DDEBUG -o test test.cpp`

### 3.3 assert 断言

`assert(条件)` 在条件为假时立即终止程序并报错，适合检查"不该发生"的情况。

```cpp
#include <cassert>   // assert 的头文件

int binarySearch(int a[], int n, int x) {
    int l = 0, r = n - 1;
    while (l <= r) {
        int mid = (l + r) / 2;
        assert(mid >= 0 && mid < n);   // 确保下标合法
        if (a[mid] == x) return mid;
        else if (a[mid] < x) l = mid + 1;
        else r = mid - 1;
    }
    return -1;
}
```

!!! warning "assert 注意事项"
    1. `assert` 在 `NDEBUG` 宏定义后会被禁用（`#define NDEBUG`），提交前确认
    2. 不要用 `assert` 检查输入数据的合法性（输入可能不满足），那是 `if` 的工作
    3. `assert` 用于检查"程序员自己的逻辑假设"，如"这里 mid 一定在 [0,n) 内"

### 3.4 打印中间状态

对于 DP、搜索等问题，在关键位置打印状态：

```cpp
// DP 调试：打印 DP 表
for (int i = 0; i <= n; i++) {
    for (int j = 0; j <= m; j++)
        cerr << dp[i][j] << " ";
    cerr << "\n";
}
```

```cpp
// DFS 调试：打印搜索路径
void dfs(int u, int depth) {
    for (int i = 0; i < depth; i++) cerr << "  ";  // 缩进表示深度
    cerr << "dfs(" << u << ")\n";
    // ...
}
```

### 3.5 二分法定位错误

如果代码很长，不知道哪里出错，可以用**注释法二分定位**：

1. 把代码后半段注释掉，看前半段是否正常
2. 如果前半段正常，错误在后半段；反之在前半段
3. 对出错的那一半再二分，直到找到具体行

---

## 第四节 对拍：竞赛终极调试法

### 4.1 什么是对拍

**对拍** = 写一个**暴力程序**（保证正确但慢）+ 一个**数据生成器**（随机生成测试数据）+ 一个**对比脚本**，自动大量测试你的程序，找出第一组让两个程序输出不一致的数据。

!!! info "为什么对拍最有效"
    - 样例只有几组，覆盖不了所有边界
    - 手算太慢，算不了大数据
    - 对拍可以**一秒钟测几百组**，自动找到出错数据
    - 这是竞赛选手的标配技能，CSP 赛场上能救命

### 4.2 对拍四件套

| 文件 | 作用 |
|------|------|
| `sol.cpp` | 你的正解程序（待测） |
| `brute.cpp` | 暴力程序（保证正确，可以很慢） |
| `gen.cpp` | 数据生成器（随机生成测试数据） |
| 对拍脚本 | 循环：生成数据 → 跑两个程序 → 对比输出 |

### 4.3 示例：对拍求最大值

**问题**：给定 $n$ 个数，求最大值。（简单问题演示对拍流程）

**sol.cpp（正解）**：

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    int mx = INT_MIN;
    for (int i = 0; i < n; i++) {
        int x; cin >> x;
        mx = max(mx, x);
    }
    cout << mx << "\n";
    return 0;
}
```

**brute.cpp（暴力）**：

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    int n; cin >> n;
    vector<int> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    sort(a.begin(), a.end());   // 排序后取最后一个，与 max 逻辑不同
    cout << a.back() << "\n";
    return 0;
}
```

**gen.cpp（数据生成器）**：

```cpp
#include <bits/stdc++.h>
using namespace std;
int main(int argc, char* argv[]) {
    // 用 argv[1] 作为随机种子，保证每次生成不同数据
    int seed = atoi(argv[1]);
    srand(seed);
    
    int n = rand() % 20 + 1;      // n 在 1~20 之间
    cout << n << "\n";
    for (int i = 0; i < n; i++) {
        cout << (rand() % 200 - 100) << " \n"[i == n - 1];  // -100~99
    }
    return 0;
}
```

**对拍脚本（Linux/bash）**：

```bash
#!/bin/bash
g++ -o sol sol.cpp -O2
g++ -o brute brute.cpp -O2
g++ -o gen gen.cpp -O2

for i in $(seq 1 1000); do
    ./gen $i > input.txt
    ./sol < input.txt > out1.txt
    ./brute < input.txt > out2.txt
    if diff -q out1.txt out2.txt > /dev/null; then
        echo "Test $i: AC"
    else
        echo "Test $i: WA!"
        echo "--- Input ---"
        cat input.txt
        echo "--- Sol output ---"
        cat out1.txt
        echo "--- Brute output ---"
        cat out2.txt
        break   # 找到第一组错误就停
    fi
done
```

**对拍脚本（Windows/PowerShell）**：

```powershell
g++ -o sol.exe sol.cpp -O2
g++ -o brute.exe brute.cpp -O2
g++ -o gen.exe gen.cpp -O2

for ($i = 1; $i -le 1000; $i++) {
    ./gen.exe $i | Out-File -FilePath input.txt -Encoding ascii
    $out1 = ./sol.exe < input.txt
    $out2 = ./brute.exe < input.txt
    if ($out1 -ne $out2) {
        Write-Host "Test $i : WA!"
        Write-Host "Input:"; Get-Content input.txt
        Write-Host "Sol:    $out1"
        Write-Host "Brute:  $out2"
        break
    } else {
        Write-Host "Test $i : AC"
    }
}
```

### 4.4 数据生成器的关键技巧

!!! tip "生成器的质量决定对拍的效果"
    1. **用参数做种子**：`srand(atoi(argv[1]))`，每次运行生成不同数据
    2. **覆盖边界**：不要只生成随机数，要特意生成 $n=1$、$n=0$、全相同、全 0、最大值、最小值
    3. **数据范围匹配**：生成的数据要在题目范围内，但可以比样例大
    4. **生成树/图**：用并查集生成随机树，用随机边生成图

```cpp
// 生成随机树（n 个点）
int n = rand() % 10 + 2;   // 2~11 个点
cout << n << "\n";
for (int i = 2; i <= n; i++) {
    int parent = rand() % (i - 1) + 1;   // 1~i-1
    cout << parent << " " << i << "\n";
}
```

```cpp
// 生成随机图（n 点 m 边）
int n = rand() % 10 + 1;
int m = rand() % 20 + 1;
cout << n << " " << m << "\n";
set<pair<int,int>> edges;
while ((int)edges.size() < m) {
    int u = rand() % n + 1, v = rand() % n + 1;
    if (u != v) edges.insert({min(u,v), max(u,v)});
}
for (auto& e : edges) cout << e.first << " " << e.second << "\n";
```

### 4.5 暴力程序怎么写

暴力程序的核心要求：**绝对正确**，可以很慢（$O(n^3)$、$O(n!)$ 都行），但逻辑必须对。

| 算法类型 | 暴力方法 |
|---------|---------|
| 最短路 | Floyd $O(n^3)$ 或 BFS 逐点 |
| DP | 暴力枚举所有方案 |
| 图论 | 暴力 DFS/BFS 枚举 |
| 数学 | 暴力枚举验证 |
| 字符串 | 暴力匹配 $O(nm)$ |

!!! warning "暴力程序也要正确"
    暴力程序有 bug 比正解有 bug 更可怕——你会怀疑正确的正解。写完暴力程序后，先用手算样例验证一遍。

---

## 第五节 边界测试清单

### 5.1 必测的边界情况

| 编号 | 边界情况 | 说明 |
|:---:|---------|------|
| 1 | $n = 1$ | 最小规模，很多程序在此翻车 |
| 2 | $n = 0$ | 空输入（如果题目允许） |
| 3 | $n = $ 最大值 | 检查数组大小和复杂度 |
| 4 | 全相同元素 | 如排序、去重问题 |
| 5 | 升序/降序 | 已排序数据（如 BST 退化为链） |
| 6 | 全 0 / 全负数 | 检查初始化和特判 |
| 7 | 最大值 / 最小值 | 检查 `int` 溢出 |
| 8 | 只有一个不重复 | 去重、统计类问题 |
| 9 | 偶数/奇数长度 | 二分、区间类问题 |
| 10 | 答案为 0 / 答案为 1 | 检查输出格式 |

### 5.2 自测模板

每次写完代码，问自己这些问题：

- [ ] $n=1$ 时不越界吗？
- [ ] 数组大小够吗？（`a[N]` 中 $N$ 是否 $\geq$ 数据上限 + 1）
- [ ] `long long` 还是 `int`？中间运算会溢出吗？
- [ ] 初始化了吗？（全局变量默认 0，局部变量是随机值）
- [ ] 输出格式对吗？（换行、空格、多组数据的空行）
- [ ] 多组数据时数组清空了吗？
- [ ] 边界返回值对吗？（空数组返回什么？找不到返回 -1 还是 0？）

---

## 第六节 GDB 基础（选学）

GDB 是强大的命令行调试器，能设断点、单步执行、查看变量。CSP 复赛在 Linux 环境下可用。

### 6.1 基本操作

```bash
g++ -g -o test test.cpp    # -g 生成调试信息
gdb ./test                  # 启动 gdb
```

| 命令 | 简写 | 作用 |
|------|:---:|------|
| `break 15` | `b 15` | 在第 15 行设断点 |
| `break main` | `b main` | 在 main 函数设断点 |
| `run` | `r` | 运行程序 |
| `next` | `n` | 单步执行（不进入函数） |
| `step` | `s` | 单步执行（进入函数） |
| `continue` | `c` | 继续运行到下一个断点 |
| `print x` | `p x` | 查看变量 x 的值 |
| `print a[0]@5` | | 查看数组 a 的前 5 个元素 |
| `backtrace` | `bt` | 查看调用栈（RE 时定位崩溃位置） |
| `quit` | `q` | 退出 gdb |

### 6.2 典型调试流程

```
1. gdb ./test
2. b 20              # 在可疑行设断点
3. r < input.txt     # 用输入文件运行
4. p n               # 查看变量 n
5. p a[0]@5          # 查看数组前5个元素
6. n                 # 单步执行
7. c                 # 继续
```

!!! tip "RE 时用 gdb 定位"
    程序 RE 后，用 `gdb ./test` 然后 `r` 运行，gdb 会停在崩溃位置，用 `bt` 查看调用栈，立刻知道是哪一行出的错。

---

## 第七节 常见错误模式速查

### 7.1 WA 常见原因

| 编号 | 错误模式 | 检查方法 |
|:---:|---------|---------|
| 1 | `int` 溢出（该用 `long long`） | 中间运算最大值是否超 $2 \times 10^9$ |
| 2 | 数组越界（开小了） | 检查数组大小 vs 数据范围 |
| 3 | 未初始化变量 | 局部变量是否赋初值 |
| 4 | 浮点精度问题 | 该用 `double` 的用了 `float`，或比较用了 `==` |
| 5 | 输出格式错误 | 多/少空格、换行，多了调试输出 |
| 6 | 多组数据未清空 | 上一组的数据残留 |
| 7 | 边界 $n=1$ 未处理 | 特判 $n=1$ |
| 8 | 下标从 0 还是 1 | 混用导致差一错误 |

### 7.2 TLE 常见原因

| 编号 | 错误模式 | 修复方法 |
|:---:|---------|---------|
| 1 | 算法复杂度过高 | 换更优算法 |
| 2 | `cin/cout` 未解绑 | 加 `ios::sync_with_stdio(false)` |
| 3 | 用了 `endl` | 改为 `"\n"` |
| 4 | 死循环 | 检查循环条件和更新 |
| 5 | 多组数据预处理放循环内 | 预处理提到循环外 |
| 6 | `map` 常数过大 | 换 `unordered_map` 或手写哈希 |

### 7.3 RE 常见原因

| 编号 | 错误模式 | 修复方法 |
|:---:|---------|---------|
| 1 | 数组越界 | 检查下标范围 |
| 2 | 栈溢出（递归太深） | `ulimit -s` 或改迭代 |
| 3 | 除以零 | 检查除数 |
| 4 | 空指针 | 检查指针初始化 |
| 5 | `vector` 越界 | 检查 `v[i]` 中 `i < v.size()` |

---

## 第八节 考场调试策略

### 8.1 时间分配建议

| 阶段 | 时间 | 动作 |
|------|:---:|------|
| 写完代码 | 前 30% 时间 | 编译、过样例 |
| 手测边界 | 接下来 20% | 手造 $n=1$、$n=2$、全相同等小数据 |
| 对拍 | 接下来 30% | 如果有暴力程序，跑对拍 |
| 检查 | 最后 20% | 检查数组大小、数据类型、输出格式 |

### 8.2 考场上的优先级

1. **先过样例**：样例不过，一定有大 bug
2. **再测边界**：$n=1$、最大值、最小值
3. **然后对拍**：如果时间允许且能写暴力
4. **最后检查**：数组大小、`long long`、输出格式

### 8.3 考场禁忌

!!! danger "考场大忌"
    1. **不要只过样例就提交**：样例覆盖面极小
    2. **不要在 WA 后盲目修改**：先定位再修改，盲目改容易越改越错
    3. **不要忘记删调试输出**：`cout` 调试的输出要删干净（`cerr` 不用删）
    4. **不要死磕一题**：WA 了 5 次还找不到错，先去做别的题
    5. **不要忘记检查文件操作**：CSP 复赛要求 `freopen`，忘了直接 0 分

```cpp
// CSP 复赛标准文件操作（必加！）
#include <bits/stdc++.h>
using namespace std;
int main() {
    freopen("problem.in", "r", stdin);
    freopen("problem.out", "w", stdout);
    // ... 你的代码 ...
    return 0;
}
```

---

## 📖 本附录总结

- **调试流程**：读懂编译错误 → 过样例 → 手测边界 → 对拍 → 检查格式
- **四种评测状态**：WA（答案错）、RE（运行崩溃）、TLE（超时）、MLE（超内存）
- **调试工具**：`cerr`（不影响评测）、`assert`（检查假设）、中间状态打印、GDB
- **对拍四件套**：正解 + 暴力 + 数据生成器 + 对比脚本
- **边界测试**：$n=1$、$n=0$、最大值、最小值、全相同、升序/降序
- **考场策略**：先过样例 → 测边界 → 对拍 → 检查格式和文件操作

!!! tip "一句话总结"
    **"样例过了不代表对了，对拍过了才算真的过了。"** 养成对拍习惯，你的 WA 率会大幅下降。

---

### 知识衔接

本附录是**全书的工程实践补充**，与各章的联系如下：

- **前置知识**：第09章结构体与文件操作（`freopen` 文件重定向）、第10章算法效率与时间复杂度（复杂度分析判断 TLE）
- **本附录定位**：竞赛实战工具箱，不属于考纲知识点，但却是拿分的关键技能
- **配合使用**：第50章复赛真题精讲与考试策略（考场时间管理）、各章节的代码均可配合对拍验证

> 本附录与 [CSP-J/S 考纲速查手册](附录_CSP-JS考纲速查手册.md) 共同构成教材的附录部分。
