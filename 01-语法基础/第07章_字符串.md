# 第07章 字符串：string与字符数组

## 📌 学习目标

> ❓ 想一想：你有没有想过，为什么字符串处理在编程竞赛中如此重要？

> 💡 一句话概括：本章学习字符串的核心概念与常用操作，掌握 string 的使用方法，避开常见陷阱

读完本章，你应当能够：

1. **背出** ASCII 码表中 `'0'`、`'A'`、`'a'` 三个关键码值（48、65、97），并默写"大小写相差 32"的规律
2. **默写** `string` 的 5 大核心函数：`size()`/`length()`、`substr()`、`find()`、`replace()`、`[]` 下标访问
3. **区分** `cin >> s` 与 `getline(cin, s)` 的行为差异，并能在混用时正确使用 `cin.ignore()`
4. **独立 AC** 洛谷 P5733、P5734、P5015 三道字符串基础题
5. **掌握** 字符串遍历的两种写法（下标循环、范围 for），并理解 `size_t` 隐式转换警告的成因与修复

> 📌 要点
> - 字符串是 CSPJ 高频考点，本章覆盖了从入门到进阶的全部核心内容
> - 五个学习目标对应五类必考题型，逐一攻克即可从容应考

---

## 💡 生活类比

> ❓ 想一想：你有没有想过，字符串其实可以像一串珠子一样直观理解？

> 💡 一句话概括：用穿珠子的比喻理解字符串的结构与操作，让抽象概念变得直观可感

> **字符串 = 穿在一根绳上的珠子**

- 每颗**珠子**是一个 `char`（字符），整串就是 `string`
- 你可以**数珠子**（`length()`）、**找某颗珠子的位置**（`find()`）、**剪一段下来**（`substr()`）、**换掉某颗**（`s[i] = ...`）、**整段替换**（`replace()`）
- 珠子有编号，**从 0 开始**：第一颗是 `s[0]`，最后一颗是 `s[s.size() - 1]`
- 珠子的"材质"由 ASCII 码决定：`'A'` 是 65 号材质，`'a'` 是 97 号材质，相差 32

> 💡 提示：一句话记忆
> `char` 是**一颗珠子**，`string` 是**一串珠子**。

---

## 核心概念

> ❓ 想一想：你有没有想过，计算机是如何"认识"每一个字符的？

> 💡 一句话概括：ASCII 编码、char 与 string 的区别、string 常用函数速查——这是字符串处理的地基

### 一、ASCII 码表

📖 通俗解释：计算机只认识数字，所以每个字符都要有一个"身份证号"。这套编号规则就叫 ASCII 码。比如大写字母 A 的编号是 65，小写字母 a 的编号是 97。

计算机里所有字符都用一个整数表示，这套编码叫 **ASCII**（美国信息交换标准代码）。下表是必须记住的关键码值：

| 字符 | ASCII 码 | 说明 |
|------|----------|------|
| `'0'` ~ `'9'` | 48 ~ 57 | 数字字符 |
| `'A'` ~ `'Z'` | 65 ~ 90 | 大写字母 |
| `'a'` ~ `'z'` | 97 ~ 122 | 小写字母 |
| `' '`（空格） | 32 | 空格 |
| `'\0'` | 0 | 字符串结束符 |
| `'\n'` | 10 | 换行 |

> ⚠️ 警告：三个必须记住的码值
> - `'0'` = **48**
> - `'A'` = **65**
> - `'a'` = **97**
> 
> 由此推出两条关键规律：
> - **大小写相差 32**：`'a' - 'A' = 32`，所以 `'A' + 32 = 'a'`，`'a' - 32 = 'A'`
> - **数字字符与数值相差 48**：`'5' - '0' = 5`，`5 + '0' = '5'`

#### 补充：控制字符、可打印字符与扩展 ASCII

📖 通俗解释：ASCII 码表共 128 个编码（0~127），按是否可显示分为两大类：

| 类别 | 码值范围 | 说明 |
|------|----------|------|
| **控制字符** | 0 ~ 31 及 127 | 不可打印，用于控制设备（如 `\n`=10 换行、`\t`=9 制表符、`\0`=0 结束符、127=DEL） |
| **可打印字符** | 32 ~ 126 | 能显示的字符：空格(32)、数字(48~57)、大写字母(65~90)、小写字母(97~122)、标点符号 |

> 💡 提示：记忆要点
> - 0~31 和 127 是**控制字符**，不能打印；32~126 是**可打印字符**
> - 空格(32)是第一个可打印字符，数字、大写、小写依次往后排

**扩展 ASCII 码**：标准 ASCII 只有 128 个编码（7 位），无法表示中文和特殊符号。扩展 ASCII 用 8 位（0~255），后半段(128~255)用于表示框线、重音字母等，但不同编码页（如 Latin-1、GBK）对应内容不同。C++ 中 `char` 通常为 8 位，可存储 0~255，但中文需用多字节编码（如 UTF-8、GBK）或 `wchar_t`。

**代码示例：输出字符的 ASCII 码值**

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    // 输出几个常用字符的 ASCII 码值
    cout << (int)'0' << endl;     // 输出：48
    cout << (int)'A' << endl;     // 输出：65
    cout << (int)'a' << endl;     // 输出：97
    cout << (int)' ' << endl;     // 输出：32（空格）
    cout << (int)'\n' << endl;    // 输出：10（换行，控制字符）

    // 反过来：根据 ASCII 码值输出字符
    cout << (char)65 << endl;     // 输出：A
    cout << (char)97 << endl;     // 输出：a

    // 大小写转换（利用差值 32）
    char c = 'H';
    cout << (char)(c + 32) << endl;   // 输出：h（大写转小写）
    cout << (char)('a' - 32) << endl; // 输出：A（小写转大写）
    return 0;
}
```

> ⚠️ 警告：char 转 int 要显式强转
> 直接 `cout << 'A';` 输出的是字符 `A` 而非数字 65。要输出 ASCII 码值，必须写成 `(int)'A'` 或 `int('A')`。反过来 `(char)65` 才能输出字符。

### 二、char 与 string 的区别

| 对比项 | `char` | `string` |
|--------|--------|---------|
| 容量 | **1 个字符** | **任意长度字符串** |
| 声明 | `char c = 'A';` | `string s = "Hello";` |
| 引号 | **单引号** `'...'` | **双引号** `"..."` |
| 末尾 | 无 `\0` | 自动管理，无需关心 |
| 比较 | `c == 'A'` | `s == "Hello"` |
| 拼接 | 不能直接拼 | `s1 + s2` |
| 适用场景 | 单字符处理 | 字符串处理（推荐） |

> ⚠️ 警告：单引号 vs 双引号
> - `'A'` 是 **char**（一个字符），值为 65
> - `"A"` 是 **string**（一个字符串），含字符 `'A'` 和结尾 `\0`
> 
> 混用会编译报错！如 `char c = "A";` 是错的。

### 三、string 常用函数速查表

| 操作 | 函数 | 示例 | 结果 |
|------|------|------|------|
| 求长度 | `s.size()` 或 `s.length()` | `string("Hello").size()` | `5` |
| 下标访问 | `s[i]` | `string("Hello")[0]` | `'H'` |
| 拼接 | `s1 + s2` 或 `s.append()` | `string("He") + "llo"` | `"Hello"` |
| 查找子串 | `s.find("xx")` | `string("Hello").find("ll")` | `2` |
| 截取子串 | `s.substr(起点, 长度)` | `string("Hello").substr(1, 3)` | `"ell"` |
| 替换 | `s.replace(起点, 长度, 新串)` | `string("Hello").replace(1, 2, "XX")` | `"HXXlo"` |
| 插入 | `s.insert(位置, 字符串)` | `string("Hello").insert(2, "XX")` | `"HeXXllo"` |
| 删除 | `s.erase(起点, 长度)` | `string("Hello").erase(1, 2)` | `"Hlo"` |
| 比较 | `s1 < s2` | `string("apple") < string("banana")` | `true`（字典序） |

> 💡 提示：`size()` 与 `length()` 完全等价
> `string` 继承自 STL 容器，所以有两个名字一样的函数。用哪个都行，竞赛中 `size()` 更常见。

> 📌 要点
> - 三个关键 ASCII 码值（48、65、97）必须烂熟于心，大小写转换和数字字符转换全靠它们
> - C++ 竞赛中优先使用 `string` 而非 `char` 数组，`string` 更安全、功能更丰富
> - `size()`/`find()`/`substr()`/`replace()`/`[]` 五大函数是字符串操作的核心，必须能够默写

---

## 代码实现

> ❓ 想一想：你有没有想过，如何用 C++ 优雅地操作一大段文字？

> 💡 一句话概括：string 的核心操作与遍历技巧，是解决字符串题目的必备工具

### 一、char 数组基本操作

字符数组是 C 语言遗留的字符串表示方式，**末尾自动加 `\0`** 表示结束。

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    char s1[10] = "Hello";                    // 自动在末尾加 \0
    char s2[] = "World";                      // 自动算大小为 6（5 字母 + \0）
    
    cout << s1 << endl;                        // 输出：Hello
    cout << strlen(s1) << endl;                // 输出：5（不算 \0）
    
    // 字符数组大小至少要比字符数多 1
    // char s3[5] = "Hello";                   // 错！装不下 \0，会越界
    // char s3[6] = "Hello";                   // 对
    return 0;
}
```

> ⚠️ 警告：字符数组大小要 +1
> `"Hello"` 有 5 个字母，但字符数组需要 **6 个位置**（5 字母 + 1 个 `\0`）：
> 
> ```
> H  e  l  l  o  \0
> 0  1  2  3  4   5      ← 占了 6 个格子
> ```
> 
> 所以 `char s[5] = "Hello";` 是错的，必须 `char s[6]` 或更大。

### 二、string 各类操作示例

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s = "Hello World";
    
    // 1. 求长度
    cout << s.size() << endl;                   // 输出：11
    cout << s.length() << endl;                 // 输出：11（两种写法等价）
    
    // 2. 下标访问与修改
    cout << s[0] << endl;                       // 输出：H
    s[0] = 'h';                                // 修改第一个字符
    cout << s << endl;                          // 输出：hello World
    
    // 3. 查找子串
    size_t pos = s.find("World");
    if (pos != string::npos) {            // 找到了
        cout << "位置：" << pos << endl;         // 输出：位置：6
    }
    
    // 4. 截取子串
    string sub1 = s.substr(0, 5);              // 输出："hello"
    string sub2 = s.substr(6);                  // 输出："World"（截到末尾）
    
    // 5. 替换
    s.replace(0, 5, "HELLO");                   // 把前 5 个字符换成 HELLO
    cout << s << endl;                          // 输出：HELLO World
    
    // 6. 拼接
    string a = "Hello", b = "World";
    cout << a + " " + b << endl;                // 输出：Hello World
    
    // 7. 比较（按字典序）
    cout << ("apple" < "banana") << endl;       // 输出：1（true）
    
    return 0;
}
```

### 三、字符串遍历的两种方式

📖 通俗解释：遍历字符串就像依次检查绳子上的每颗珠子。可以用"第几颗"的方式（下标循环），也可以直接"把珠子一颗颗取出来"（范围 for）。

**方式 1：下标循环（推荐初学者）**

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s = "Hello";
    for (int i = 0; i < (int)s.size(); i++) {   // 注意 (int) 强转
        cout << s[i] << " ";
    }
    // 输出：H e l l o
    return 0;
}
```

**方式 2：范围 for 循环（C++11 起）**

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s = "Hello";
    for (char c : s) {                          // 依次取出每个字符
        cout << c << " ";
    }
    // 输出：H e l l o
    return 0;
}
```

> ⚠️ 警告：为什么下标循环要写 (int)s.size()
> `s.size()` 返回的是 `size_t`（无符号整数）。当 `i` 是 `int` 时，编译器会警告"有符号与无符号比较"。
> 
> 如果循环里出现 `i - 1` 之类的负数运算，更可能因无符号回绕导致死循环。
> 
> **修复方法**：显式转为 `int`：
> ```cpp
> for (int i = 0; i < (int)s.size(); i++)     // 正确
> for (int i = 0; i < s.size(); i++)          // 警告！
> ```

### 四、getline() vs cin >> 的区别

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    
    // cin >> 遇空格停止
    cin >> s;                                  // 输入 "Hello World"，s 只读到 "Hello"
    cout << s << endl;                          // 输出：Hello
    
    // getline 读整行（含空格）
    getline(cin, s);                            // 输入 "Hello World"，s 读到 "Hello World"
    cout << s << endl;                          // 输出：Hello World
    
    return 0;
}
```

> 💡 提示：何时用 getline
> - 题目说"一行字符串，可能包含空格"→ 用 `getline(cin, s)`
> - 题目说"一个单词"或"不含空格的字符串"→ 用 `cin >> s`

### 五、大小写转换

利用 ASCII 码差值（大小写相差 32）转换：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    getline(cin, s);
    
    // 全部转大写
    for (int i = 0; i < (int)s.length(); i++) {
        if (s[i] >= 'a' && s[i] <= 'z') {
            s[i] -= 32;                         // 小写转大写
        }
    }
    cout << "大写：" << s << endl;               // 输入：Hello → 输出：大写：HELLO
    
    // 全部转小写
    for (int i = 0; i < (int)s.length(); i++) {
        if (s[i] >= 'A' && s[i] <= 'Z') {
            s[i] += 32;                         // 大写转小写
        }
    }
    cout << "小写：" << s << endl;               // 输出：小写：hello
    return 0;
}
```

> 💡 提示：更简便的写法
> 也可以用 `<cctype>` 库的 `toupper` 和 `tolower` 函数（`bits/stdc++.h` 已包含）：
> ```cpp
> s[i] = toupper(s[i]);      // 转大写
> s[i] = tolower(s[i]);      // 转小写
> ```

> 📌 要点
> - 字符数组大小必须 +1 容纳 `\0`，建议竞赛中优先使用 `string`
> - 下标循环必须写 `(int)s.size()` 避免 size_t 警告；范围 for 更简洁但不适合需要修改下标的情况
> - `cin >>` 和 `getline` 的混用是最常见的坑，牢记 `cin.ignore()` 口诀

---

## ⚠️ 常见误区

> ❓ 想一想：你有没有想过，为什么很多同学明明逻辑正确，字符串题却总拿不到满分？

> 💡 一句话概括：cin/getline 混用、find 返回值、字符数组越界——五大高频错误及正确写法

### 误区 1：cin >> s 读不了带空格的字符串

❌ **错误写法**：
```cpp
string s;
cin >> s;                                      // 输入 "I love coding"
cout << s;                                     // 输出：I（只读到空格前）
```

**原因**：`cin >>` 默认以**空白字符**（空格、Tab、回车）为分隔符，遇到空格就停止读取。

✅ **正确写法**：用 `getline(cin, s)` 读整行。

### 误区 2：cin 和 getline 混用读到空字符串

❌ **错误写法**：
```cpp
int n;
cin >> n;                                       // 读入数字，回车 \n 留在缓冲区
string s;
getline(cin, s);                                // 立刻读到 \n，s 为空！
cout << s;                                      // 输出：空行
```

**原因**：`cin >> n` 读完数字后，**回车符 `\n` 还留在输入缓冲区**，`getline` 一上来就遇到回车，以为这行结束了。

✅ **正确写法**：在中间加 `cin.ignore()` 吃掉那个回车：

```cpp
int n;
cin >> n;
cin.ignore();                                   // 吃掉回车
getline(cin, s);                                // 现在能正常读了
```

### 误区 3：find() 找不到时直接和 -1 比较

❌ **错误写法**：
```cpp
string s = "Hello";
if (s.find("xyz") == -1) {                      // 不规范！可能出错
    cout << "没找到";
}
```

**原因**：`find` 返回 `size_t`（无符号整数），找不到时返回 `string::npos`（一个很大的数）。虽然 `-1` 转成无符号后通常等于 `string::npos`，但这是**不规范的写法**，不应依赖。

✅ **正确写法**：用 `string::npos` 判断：

```cpp
if (s.find("xyz") == string::npos) {            // 正确
    cout << "没找到";
}
```

### 误区 4：字符数组大小不够装 \0

❌ **错误写法**：
```cpp
char s[5] = "Hello";                            // 错！\0 装不下，会越界
```

✅ **正确写法**：`char s[6] = "Hello";` 或 `char s[] = "Hello";`（自动算大小）。

### 误区 5：i < s.length() 的 size_t 警告

❌ **错误写法**：
```cpp
for (int i = 0; i < s.length(); i++) {          // 编译器警告
    ...
}
```

**原因**：`s.length()` 返回 `size_t`（无符号），`i` 是 `int`（有符号），编译器警告"有符号与无符号比较"。

✅ **正确写法**：显式转 `int`：

```cpp
for (int i = 0; i < (int)s.length(); i++) {     // 正确
    ...
}
```

> 📌 要点
> - 五大误区是竞赛中失分的高发区，每个都必须能独立判断并写出正确写法
> - 核心口诀：读整行用 getline，混用加 ignore，find 用 npos，数组大小 +1，循环强转 int

---

## 典型例题

> ❓ 想一想：你有没有想过，学完理论后怎样在实战中检验自己的理解？

> 💡 一句话概括：通过一道经典洛谷真题，实战演练字符串的读入与遍历

### 【洛谷 P5015】标题统计

**题目描述**：凯凯刚写了一篇 maravilloso 的作文，请问这篇作文的标题中有多少个字符？注意：标题中可能包含大、小写英文字母、数字字符、空格和换行符（统计字符数时都按 1 个字符计算）。

**输入格式**：一行一个字符串 `s`，表示作文的标题。

**输出格式**：一个整数，表示标题中的字符数（不含行末回车）。

**思路分析**：
- 标题可能含空格 → 必须用 `getline` 读整行
- 统计字符数 → 遍历字符串，每个非空格字符计数 +1
- 注意行末回车不会被 `getline` 读入，无需特殊处理

**完整代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    getline(cin, s);                            // 读整行，含空格
    int cnt = 0;
    for (int i = 0; i < (int)s.size(); i++) {   // (int) 避免 size_t 警告
        if (s[i] != ' ') {                      // 不统计空格
            cnt++;
        }
    }
    cout << cnt << endl;                        // 输入：234 → 输出：3
    return 0;
}
```

**输入样例**：
```
234
```

**输出样例**：
```
3
```

**复杂度**：时间 O(n)，空间 O(n)。

> 💡 提示：本题关键点
> 1. 用 `getline` 而非 `cin >>`，否则遇到空格会截断
> 2. 循环条件写 `i < (int)s.size()` 避免 `size_t` 警告
> 3. 空格不计入字符数（按官方题意）。洛谷原题面表述有歧义，实际测试点不统计空格

> 📌 要点
> - 字符串题目的第一步永远是判断输入是否含空格，从而决定用 `cin >>` 还是 `getline`
> - 遍历统计类题目是 CSPJ 高频题型，掌握下标循环 + `(int)s.size()` 的写法即可应对

---

## 📚 本节练习

> ❓ 想一想：你有没有想过，光看不练是学不会字符串的？

> 💡 一句话概括：从基础到进阶，六道精选题目覆盖字符串全部考点

### 🟢 基础题（CSP-J 难度，巩固基础）

**练习 1**：[P5733] 自动修正 - https://www.luogu.com.cn/problem/P5733
> 输入一个字符串，将其中的小写字母全部转成大写字母并输出。
**提示**：用 `getline` 读入整行，遍历每个字符，若是小写字母则 `s[i] -= 32`，或直接用 `toupper(s[i])`。注意 `(int)s.size()` 写法。

**练习 2**：[P5015] 标题统计 - https://www.luogu.com.cn/problem/P5015
> 统计作文标题中的字符数（空格不计）。
**提示**：用 `getline` 读整行，遍历统计非空格字符个数。详见"典型例题"。

**练习 3**：[P1914] 小书童——凯撒密码 - https://www.luogu.com.cn/problem/P1914
> 给定整数 k 和小写字母字符串，将每个字母向后移 k 位（字母表循环）。
**提示**：对每个字符用 `(s[i] - 'a' + k) % 26 + 'a'` 计算移位后的字符，先 `k %= 26` 防溢出。

### 🟡 拓展题（CSP-S 入门，需思考）

**练习 4**：[P5734] 文字处理软件 - https://www.luogu.com.cn/problem/P5734
> 你有一款文字处理软件，初始文本为空，支持 4 种操作：在末尾插入字符串、查找子串位置、反转指定区间、替换指定区间。请实现这些操作。
**提示**：
- 操作 1（插入）：`s += t`
- 操作 2（查找）：`s.find(t)`，找不到输出 `-1`（用 `string::npos` 判断）
- 操作 3（反转）：`reverse(s.begin() + a, s.begin() + b + 1)`
- 操作 4（替换）：先 `s.erase(a, b - a + 1)`，再 `s.insert(a, t)`

**练习 5**：[P1308] 统计单词数 - https://www.luogu.com.cn/problem/P1308
> 给定一个单词和一篇文章，统计该单词在文章中出现的次数及第一次出现的位置（不区分大小写，单词以空格分隔）。
**提示**：把单词和文章全部转小写统一处理；逐个取出文章中的单词与目标单词比较。第一次出现位置指该单词首字符在文章全文中的下标（从 0 开始，含空格）。

### 🔴 CSP-S 真题变式（进阶）

**练习 6**：[P1042] 乒乓球 - https://www.luogu.com.cn/problem/P1042
> 华华通过录像记录了一场比赛的比分（`'W'` 表示华华得分，`'L'` 表示对手得分，`'E'` 表示结束），分别按 11 分制和 21 分制输出两人在每局结束时的比分。
**提示**：逐字符读取直到遇到 `'E'`，分别模拟 11 分制和 21 分制。关键：一局结束需领先 2 分，平分时（如 10:10）继续打到一方领先 2 分。建议先把比分流存起来，再分别处理两种赛制输出。

> 📌 要点
> - 基础题三道必须全部 AC，这是字符串章节的最低要求
> - 拓展题中的 P5734 是 string 四大函数的综合运用，做了它才算真正掌握 string

---

## 📖 本章总结

> ❓ 想一想：你有没有想过，如何把零散的知识点串联成完整的知识体系？

> 💡 一句话概括：回顾本章所有核心知识，形成完整的字符串知识体系

### 核心要点回顾

- ✅ ASCII 码：`'0'`=48，`'A'`=65，`'a'`=97，大小写相差 32
- ✅ `char` 是单字符（单引号），`string` 是字符串（双引号）
- ✅ `string` 5 大核心函数：`size()`/`length()`、`[]`、`find()`、`substr()`、`replace()`
- ✅ `cin >> s` 遇空格停止；读整行用 `getline(cin, s)`
- ✅ `cin` 与 `getline` 混用要加 `cin.ignore()` 吃掉回车
- ✅ 字符串遍历两种方式：下标循环（`i < (int)s.size()`）、范围 for（`for (char c : s)`）
- ✅ 下标循环必须写 `(int)s.size()` 避免 `size_t` 警告
- ✅ 字符数组末尾自动加 `\0`，大小至少比字符数多 1

### string 速查表

| 操作 | 代码 |
|------|------|
| 声明 | `string s;` |
| 赋值 | `s = "Hello";` |
| 读入（遇空格停） | `cin >> s;` |
| 读入整行 | `getline(cin, s);` |
| 求长度 | `s.size()` 或 `s.length()` |
| 访问第 i 个字符 | `s[i]` |
| 拼接 | `s1 + s2` 或 `s1 += s2` |
| 查找子串 | `s.find("xx")` |
| 截取子串 | `s.substr(起点, 长度)` |
| 替换 | `s.replace(起点, 长度, 新串)` |
| 比较 | `s1 == s2`、`s1 < s2` |

### 知识脉络

- **字符串**
  - **char 数组（C 风格）**
    - 声明：末尾自动加 `\0`，大小至少比字符数多 1
    - 输入：`cin` / `scanf` / `getline`
    - 操作：`strlen()` 求长度
    - 注意：`gets()` 已废弃，不要使用
  - **string（C++ 风格，推荐）**
    - 基本用法：声明、赋值、读入、输出
    - 常用函数
      - `size()` / `length()`：求长度
      - `find()`：查找子串
      - `substr()`：截取子串
      - `replace()`：替换
      - `[]`：下标访问
    - 实战应用
      - 大小写转换：`s[i] -= 32` / `s[i] += 32`
      - 反转：`reverse(s.begin(), s.end())`
      - 拼接：`s1 + s2` / `s1 += s2`
      - 统计字符：遍历 + 计数
      - 分割单词：结合 `find` 和 `substr`
- **ASCII 码**
  - 数字字符：`'0'`~`'9'` → 48~57
  - 大写字母：`'A'`~`'Z'` → 65~90
  - 小写字母：`'a'`~`'z'` → 97~122
  - 大小写相差 32：`'A' + 32 = 'a'`，`'a' - 32 = 'A'`
  - 数字字符转数值：`'5' - '0' = 5`，`5 + '0' = '5'`
  - 空格：32，`\0`：0，`\n`：10

### 常见陷阱

| # | ❌ 错误写法 | ✅ 正确写法 | 原因 |
|---|-----------|-----------|------|
| 1 | `cin >> s;` 读带空格的字符串 | `getline(cin, s);` | `cin >>` 遇空格停止 |
| 2 | `cin >> n;` 后直接 `getline(cin, s);` | 中间加 `cin.ignore();` | 回车符残留在缓冲区 |
| 3 | `s.find("x") == -1` | `s.find("x") == string::npos` | `find` 返回 `size_t`，`-1` 不规范 |
| 4 | `char s[5] = "Hello";` | `char s[6] = "Hello";` 或 `char s[]` | `\0` 需要额外 1 个位置 |
| 5 | `for (int i = 0; i < s.length(); i++)` | `for (int i = 0; i < (int)s.length(); i++)` | `size_t` 与 `int` 比较引发警告 |

### 关键代码模板

**模板 1：读入整行字符串**

```cpp
string s;
getline(cin, s);                                // 读入一整行，含空格
```

**模板 2：cin 与 getline 混用**

```cpp
int n;
cin >> n;
cin.ignore();                                   // 千万不要忘记这一行！
string s;
getline(cin, s);
```

**模板 3：遍历字符串（下标方式）**

```cpp
for (int i = 0; i < (int)s.size(); i++) {
    // 处理 s[i]
}
```

**模板 4：遍历字符串（范围 for）**

```cpp
for (char c : s) {
    // 处理 c
}
```

**模板 5：大小写转换**

```cpp
// 小写转大写
if (s[i] >= 'a' && s[i] <= 'z') s[i] -= 32;

// 大写转小写
if (s[i] >= 'A' && s[i] <= 'Z') s[i] += 32;

// 或用库函数
s[i] = toupper(s[i]);  // 转大写
s[i] = tolower(s[i]);  // 转小写
```

**模板 6：查找子串**

```cpp
int pos = s.find("目标");
if (pos != (int)string::npos) {
    // 找到了，pos 是位置
} else {
    // 没找到
}
```

**模板 7：字符数组声明**

```cpp
char s[100] = "Hello";                          // 大小必须 ≥ 字符数 + 1
char s[] = "Hello";                             // 让编译器自动算大小（推荐）
```

### 知识衔接

本章是CSP-J/S入门教材的基础语法章节，与前后章节的联系如下：

- **前置知识**：第06章「数组」——字符数组的基础操作、数组遍历；第02章「变量与数据类型」——char类型与ASCII编码
- **本章定位**：文本数据处理，掌握string类型与字符数组、ASCII编码与字符运算，是处理文字类竞赛题的基础
- **后续发展**：为第08章「函数与递归入门」铺路——下一章将学会把代码"打包"成可复用的工具，并启蒙递归思维

---

> 💡 提示：下一章预告
> 下一章我们将学习**函数与递归**，学会把代码"打包"成可复用的工具，还能让函数"自己调用自己"——这是编程中最神奇的概念之一！

---

> 📝 **修改摘要**
> 
> **修改日期**：2026-08-08
> 
> **修改内容**：
> 1. 为每个二级标题（`##` 开头）小节的开头增加了「想一想」引导性问题（共 8 处），放在「一句话概括」之前
> 2. 将所有 `!!! tip` 格式改为 `> 💡 提示` 格式（共 6 处）
> 3. 将所有 `!!! warning` 格式改为 `> ⚠️ 警告` 格式（共 4 处）
> 4. 所有代码块、练习链接、表格、总结内容保持完全不变