# 第19章 队列：先进先出的线性表

!!! info "本章内容"
    本章采用**双轨教学法**，从「数组模拟」与「STL 实现」两条轨道完整讲解队列。你将学习：

    - 队列的 FIFO 特性与核心操作
    - 数组模拟队列（非循环 + 循环）的完整模板
    - STL `queue` 的使用与成员函数速查
    - 双轨对比与赛场选择策略
    - 单调队列（CSP-S 核心优化利器）

!!! warning "本章特点"
    队列是**广度优先搜索（BFS）**的底层引擎，也是 CSP-J/S 高频考点。本章要求**双轨精通**：既能手写数组模拟（应对大常数、自定义场景），也能熟练使用 STL（提升编码速度）。单调队列是 CSP-S 拿分的关键优化技术。

---

## 📌 学习目标

!!! tip "学完本章你应当能够"
    1. **默写**数组模拟循环队列的完整模板（含判空、判满、入队、出队），5 分钟内无错写出。
    2. **熟练**使用 STL `queue` 的 6 个核心成员函数（`push/pop/front/back/empty/size`），说出每个操作的时间复杂度。
    3. **独立**完成洛谷 P1540（机器翻译）和 P1996（约瑟夫问题），1AC 通过。
    4. **理解**单调队列的原理，能套用模板解决 P1886（滑动窗口最值）。
    5. **对比**数组模拟与 STL 两种实现的优劣，在赛场上 30 秒内做出正确选型决策。

---

## 💡 生活类比

!!! tip "类比一：排队买票"
    电影院售票窗口前，**先排队的人先买到票、先离开**。新来的人只能排到队伍**末尾**。这就是队列的核心精神：**先来先服务（FCFS, First Come First Served）**。

```
   队首(出)                                    队尾(入)
     ↓                                          ↓
   ┌───┬───┬───┬───┬───┐
   │ A │ B │ C │ D │ E │
   └───┴───┴───┴───┴───┘
     ↑                                          ↑
   先买到票离开                            新来的人排这里
```

!!! tip "类比二：打印机任务队列"
    打印机收到多个打印任务时，会**按收到顺序**依次打印。你点"打印"的文档不会插队，先点的先打完。这就是队列在操作系统中的典型应用——**任务调度**。

---

## 核心概念

### 队列的定义

「队列 queue」是一种只允许在**一端插入、另一端删除**的线性表。允许删除的一端叫**队首（front/head）**，允许插入的一端叫**队尾（rear/tail）**。

队列遵循 **FIFO（First In First Out，先进先出）** 原则：**最先入队的元素最先出队**。

### 核心操作及复杂度

| 操作 | 含义 | 时间复杂度 | 备注 |
|------|------|-----------|------|
| `push(x)` / `enqueue` | 元素 x 入队（队尾） | $O(1)$ | 可能判满（数组版） |
| `pop()` / `dequeue` | 队首元素出队 | $O(1)$ | **必须先判空** |
| `front()` | 取队首元素（不出队） | $O(1)$ | 队空时未定义 |
| `back()` | 取队尾元素（不出队） | $O(1)$ | 队空时未定义 |
| `empty()` | 判断队列是否为空 | $O(1)$ | `head == tail` |
| `size()` | 返回队列元素个数 | $O(1)$ | `tail - head` |

### ASCII 示意图

```
        队首 head                          队尾 tail
          ↓                                  ↓
下标：  0     1     2     3     4     5
      ┌─────┬─────┬─────┬─────┬─────┬─────┐
      │  5  │  8  │  3  │  6  │     │     │
      └─────┴─────┴─────┴─────┴─────┴─────┘
        ↑                                   ↑
       出队方向                            入队方向

操作示例（初始空队列）：
push(5) → [5]              head=0, tail=1
push(8) → [5, 8]           head=0, tail=2
push(3) → [5, 8, 3]        head=0, tail=3
pop()   → [8, 3]           head=1, tail=3   （5 出队）
front() → 返回 8           （不出队）
push(6) → [8, 3, 6]        head=1, tail=4
```

!!! warning "非循环队列的隐患"
    上图中 `head` 不断右移，前面空间被"浪费"。若持续 push/pop，`tail` 会越界而队列实际元素很少——这就是**假溢出**。解决办法是**循环队列**（见第四节）。

### 循环队列示意图

把数组想象成**环形**，`head` 和 `tail` 沿环顺时针移动，到达末尾后绕回开头：

```
        数组下标：0  1  2  3  4  5  6  7
              ┌──┬──┬──┬──┬──┬──┬──┬──┐
              │  │ 8│ 3│ 6│ 7│  │  │  │
              └──┴──┴──┴──┴──┴──┴──┴──┘
                   ↑           ↑
                  head        tail
                  (队首)      (下一个入队位)

判空：head == tail        （两指针重合 → 无元素）
判满：(tail+1)%N == head  （tail 再走一格就追上 head → 满了，牺牲一格）

绕回示例：tail = 7 时 push，tail 变为 (7+1)%8 = 0，回到数组开头复用空间。
```

!!! tip "循环队列的本质"
    循环队列并非物理上的环形内存，而是**用取模运算模拟环形**。逻辑上数组首尾相接，物理上仍是一维数组。这样 `head` 移走后的空间可以被 `tail` 绕回来复用，彻底消除假溢出。

### 队列的典型应用场景

| 应用场景 | 说明 | 本章/后续章节 |
|---------|------|--------------|
| **BFS 广度优先搜索** | 队列存储"待扩展"的节点，一层层向外搜索 | 第 26 章/第 28 章 |
| **任务调度** | 操作系统按到达顺序分配 CPU/打印机资源 | 本章类比 |
| **约瑟夫问题** | 队列模拟"报数出圈"过程 | 本章例题 |
| **滑动窗口最值** | 单调队列 $O(n)$ 求窗口内极值 | 本章进阶 |
| **DP 优化** | 单调队列优化状态转移，降维复杂度 | P3957 |
| **拓扑排序** | 入度为 0 的节点入队，BFS 式输出顺序 | 第 29 章 |

---

## 数组模拟实现 ⭐

### 存储结构说明

用一维数组 `q[]` 存储元素，两个整型变量标记队首与队尾：

- `head`：队首元素的下标（下一个要出队的位置）
- `tail`：队尾的**下一个空位**（下一个入队的位置）

约定：`head == tail` 表示队列**为空**；队列有效元素为 `q[head]` 到 `q[tail-1]`。

### 版本一：非循环队列（基础模板）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100010;
int q[N], head = 0, tail = 0;       // 初始空队列

void push(int x) {                  // 入队：O(1)
    if (tail >= N) {                // 边界检查：防止越界
        cerr << "队列已满！\n";
        return;
    }
    q[tail++] = x;                  // 先存再 tail+1
}

void pop() {                        // 出队：O(1)
    if (head < tail) head++;        // 边界检查：非空才能 pop
    else cerr << "队列已空！\n";
}

int front() {                       // 取队首：O(1)
    if (head == tail) return -1;    // 空队列返回 -1（哨兵）
    return q[head];
}

int back() {                        // 取队尾：O(1)
    if (head == tail) return -1;
    return q[tail - 1];             // tail 指向空位，故 -1
}

bool empty() {                      // 判空：O(1)
    return head == tail;
}

int size() {                        // 元素个数：O(1)
    return tail - head;
}

int main() {
    push(5); push(8); push(3);
    cout << front() << endl;        // 输出 5
    pop();
    cout << front() << endl;        // 输出 8
    cout << size() << endl;         // 输出 2
    return 0;
}
```

!!! tip "实现要点"
    1. `tail` 指向**下一个空位**而非最后一个元素，这是竞赛通用约定，便于判空 `head==tail`。
    2. **pop/front 前必须判空**，否则读垃圾值或下标错乱。
    3. 非循环队列有"假溢出"问题：`head` 后面的空间无法复用。适合 push 总数有限、或一次性使用的场景。

### 版本二：循环队列（推荐模板）

通过**取模运算**让 `head`、`tail` 在数组内"绕圈"，复用前面空间。判满条件是 `(tail+1) % N == head`（**牺牲一个存储位**以区分"空"和"满"）。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100010;
int q[N], head = 0, tail = 0;       // 初始空：head == tail

bool empty() {                      // 判空：O(1)
    return head == tail;
}

bool full() {                       // 判满：O(1)，牺牲一格
    return (tail + 1) % N == head;
}

void push(int x) {                  // 入队：O(1)
    if (full()) {                   // 边界检查：判满
        cerr << "循环队列已满！\n";
        return;
    }
    q[tail] = x;
    tail = (tail + 1) % N;          // 关键：取模绕回
}

void pop() {                        // 出队：O(1)
    if (empty()) {                  // 边界检查：判空
        cerr << "循环队列已空！\n";
        return;
    }
    head = (head + 1) % N;          // 关键：取模绕回
}

int front() {                       // 取队首：O(1)
    if (empty()) return -1;
    return q[head];
}

int back() {                        // 取队尾：O(1)
    if (empty()) return -1;
    return q[(tail - 1 + N) % N];   // tail 可能是 0，要 +N 再 %N
}

int size() {                        // 元素个数：O(1)
    return (tail - head + N) % N;   // 处理 tail < head 的情况
}

int main() {
    for (int i = 1; i <= 5; i++) push(i);   // 1 2 3 4 5
    pop(); pop();                            // 剩 3 4 5
    push(6); push(7);                        // 3 4 5 6 7（复用前面空间）
    while (!empty()) {
        cout << front() << " ";
        pop();
    }
    // 输出：3 4 5 6 7
    return 0;
}
```

!!! warning "循环队列三大关键点"
    1. **判空**：`head == tail`（与非循环相同）。
    2. **判满**：`(tail + 1) % N == head`——牺牲一个位置，否则"空"和"满"无法区分（都是 `head==tail`）。
    3. **取模绕回**：`head`、`tail` 每次移动后都要 `% N`，`back()` 取尾元素要 `(tail-1+N) % N` 防负数。

---

## STL 实现 ⭐

### 头文件与声明

```cpp
#include <bits/stdc++.h>
using namespace std;
queue<int> q;                       // 声明一个存 int 的队列
```

### 完整代码示例

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    queue<int> q;
    q.push(5);                      // 入队：5
    q.push(8);                      // 入队：8
    q.push(3);                      // 入队：3，队列为 5 8 3

    cout << "队首: " << q.front() << endl;   // 输出 5
    cout << "队尾: " << q.back() << endl;    // 输出 3
    cout << "大小: " << q.size() << endl;    // 输出 3

    q.pop();                        // 出队：5 出
    cout << "出队后队首: " << q.front() << endl;  // 输出 8

    while (!q.empty()) {            // 清空队列
        cout << q.front() << " ";
        q.pop();
    }
    // 输出：8 3
    return 0;
}
```

### 成员函数速查表

| 成员函数 | 功能 | 时间复杂度 | 示例 | 备注 |
|---------|------|-----------|------|------|
| `q.push(x)` | 元素 x 入队尾 | $O(1)$ | `q.push(5);` | 自动扩容 |
| `q.pop()` | 队首出队（无返回值） | $O(1)$ | `q.pop();` | **空队调用未定义** |
| `q.front()` | 访问队首元素 | $O(1)$ | `int x = q.front();` | 不删除元素 |
| `q.back()` | 访问队尾元素 | $O(1)$ | `int y = q.back();` | 不删除元素 |
| `q.empty()` | 判断是否为空 | $O(1)$ | `if (q.empty())` | 返回 bool |
| `q.size()` | 返回元素个数 | $O(1)$ | `int n = q.size();` | 返回 unsigned |

!!! tip "STL queue 的底层"
    `std::queue` 是**容器适配器**，默认基于 `deque`（双端队列）实现，支持两端 $O(1)$ 操作。也可指定 `queue<int, list<int>>` 基于 list 实现。竞赛中直接用默认即可。

!!! warning "STL queue 没有 clear()"
    `std::queue` **没有 `clear()` 成员函数**。清空队列的常用写法：`while (!q.empty()) q.pop();`，或直接 `q = queue<int>();` 重新赋值。

---

## 双轨对比总结

### 数组模拟 vs STL 对比表

| 对比维度 | 数组模拟队列 | STL `queue` |
|---------|-------------|-------------|
| **编码速度** | 慢（需手写 6 个函数） | 快（一行声明） |
| **运行效率** | 极快（数组连续内存，无开销） | 略慢（deque 抽象层） |
| **内存控制** | 精确（开 `N` 大小数组） | 不可控（自动扩容） |
| **调试难度** | 中（需自检边界） | 易（封装完善） |
| **判满逻辑** | 需手写（循环队列牺牲一格） | 无需关心 |
| **可定制性** | 高（可加优先级、双端等） | 低（接口固定） |
| **适用场景** | 大数据、BFS、单调队列 | 普通模拟、快速实现 |

### 赛场选型建议

!!! tip "何时用数组模拟"
    1. **BFS 搜索**：队列规模大、常数敏感，数组模拟快 30%~50%。
    2. **单调队列**：需要同时访问队首队尾并灵活出队，数组模拟更直观。
    3. **内存受限**：可精确控制数组大小，避免 deque 的额外开销。
    4. **需要随机访问**：STL `queue` 不支持 `q[i]`，数组模拟可以直接 `q[head+i]`。

!!! tip "何时用 STL queue"
    1. **简单模拟题**：如 P1540，逻辑简单，用 STL 提升编码速度。
    2. **时间充裕**：常数不是瓶颈时优先 STL，减少手写出错。
    3. **新手练习**：先掌握 STL 理解队列思想，再过渡到手写。

---

## ⚠️ 常见误区

!!! danger "误区一：队空时直接 front/pop"
    **错误**：`int x = q[head];` 不判空，当 `head==tail` 时读到垃圾值。
    ```cpp
    // ❌ 错误写法
    int x = q[head]; head++;        // 空队列会越界读、head 越过 tail
    ```
    **正确**：所有 front/pop 操作**必须先判空**。
    ```cpp
    // ✅ 正确写法
    if (head < tail) { int x = q[head]; head++; }
    ```

!!! danger "误区二：循环队列判满条件混淆"
    **错误**：用 `tail == N` 判满（那是非循环队列的判满），或用 `head == tail` 判满（与判空冲突，无法区分空和满）。
    ```cpp
    // ❌ 错误写法
    if (head == tail) { /* 到底是空还是满？分不清 */ }
    ```
    **正确**：循环队列判满用 `(tail + 1) % N == head`，**故意牺牲一个存储位**让空满可区分。
    ```cpp
    // ✅ 正确写法
    bool full() { return (tail + 1) % N == head; }
    bool empty() { return head == tail; }
    ```

!!! danger "误区三：误解 head == tail 的含义"
    **错误**：以为 `head == tail` 表示"队列满了"或"队首等于队尾元素"。
    **正确**：`head == tail` **永远表示队列为空**（无元素）。在循环队列中，"满"的状态是 `(tail+1) % N == head`，此时 `head != tail`。务必记住：**空是 `head==tail`，满是差一格**。

!!! danger "误区四：循环队列 back() 取尾元素下标算错"
    **错误**：`return q[tail - 1];`——当 `tail == 0` 时变成 `q[-1]` 越界。
    **正确**：`return q[(tail - 1 + N) % N];`，加 `N` 再取模处理负数情况。

---

## 典型例题（双版本）

### 例题：约瑟夫问题（洛谷 P1996）

> n 个人围成一圈，从第 1 个人开始报数，数到 m 的人出圈。依次输出出圈人的编号。
> 数据范围：$1 \le m \le n \le 100$。

**思路**：用队列模拟。每次把队首取出，若是第 m 个则输出（出圈），否则重新入队（排到队尾）。

#### 版本一：数组模拟队列

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100010;
int q[N], head = 0, tail = 0;

void push(int x) { q[tail++] = x; }
void pop() { if (head < tail) head++; }
int front() { return head == tail ? -1 : q[head]; }
bool empty() { return head == tail; }
int size() { return tail - head; }

int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 1; i <= n; i++) push(i);   // 1~n 依次入队

    int cnt = 0;                            // 报数计数器
    bool first = true;
    while (!empty()) {
        int x = front(); pop();
        cnt++;
        if (cnt == m) {                     // 数到 m，出圈
            if (!first) cout << " ";
            cout << x;
            first = false;
            cnt = 0;                        // 重新报数
        } else {
            push(x);                        // 否则排到队尾
        }
    }
    cout << endl;
    return 0;
}
```

#### 版本二：STL queue

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    queue<int> q;
    for (int i = 1; i <= n; i++) q.push(i); // 1~n 依次入队

    int cnt = 0;
    bool first = true;
    while (!q.empty()) {
        int x = q.front(); q.pop();
        cnt++;
        if (cnt == m) {                     // 数到 m，出圈
            if (!first) cout << " ";
            cout << x;
            first = false;
            cnt = 0;
        } else {
            q.push(x);                      // 否则排到队尾
        }
    }
    cout << endl;
    return 0;
}
```

!!! tip "双版本对比"
    两份代码逻辑完全一致，仅队列实现不同。数组版略快（本题数据小无差异），STL 版更简洁。**建议先写 STL 版确保正确，再按需替换为数组版**。

!!! warning "复杂度提醒"
    本队列模拟法时间复杂度 $O(nm)$，对 $n \le 100$ 完全够用。若 $n$ 很大（如 $10^6$），需改用递推公式 $f(n) = (f(n-1)+m) \bmod n$，$O(n)$ 求最后幸存者（见本章例题）。

---

## 【进阶】单调队列（S 组核心）

!!! info "什么是单调队列"
    「单调队列 monotone queue」是一种特殊的双端队列，**维护队列内元素的单调性（递增或递减）**。它能在 $O(n)$ 时间内解决"滑动窗口最值"问题，是 CSP-S 的核心优化技术，也是动态规划优化的常用工具。

### 核心思想

维护一个**下标队列**（存原数组下标），使对应元素在队列中**单调**。新元素入队时，从队尾弹出所有"破坏单调性"的元素，再从队首弹出"滑出窗口"的元素。

- 求**最大值**：维护**单调递减**队列（队首总是最大）
- 求**最小值**：维护**单调递增**队列（队首总是最小）

### 单调队列工作过程图解

以求**最小值**为例（维护单调递增队列），窗口大小 $k=3$，数组 `[1, 3, -1, -3, 5, 3]`：

```
i=1: a[1]=1  队列下标: [1]            窗口未满
i=2: a[2]=3  3>1 不弹  队列: [1,2]     窗口未满
i=3: a[3]=-1 -1<3 弹2, -1<1 弹1, 入队: [3]  窗口[1,3,-1] 最小=-1
i=4: a[4]=-3 -3<-1 弹3, 入队: [4]      窗口[3,-1,-3] 最小=-3
i=5: a[5]=5  5>-3 不弹  队列: [4,5]    窗口[-1,-3,5] 最小=-3
i=6: a[6]=3  3<5 弹5, 3>-3 不弹  队列: [4,6]  窗口[-3,5,3] 最小=-3

队首始终是当前窗口最小值的下标。每个元素入队出队各一次，总计 O(n)。
```

### 轨道一：数组模拟单调队列（标准模板）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1000010;
int a[N];                             // 原数组
int q[N], head, tail;                 // 单调队列（存下标）

int main() {
    int n, k;
    scanf("%d%d", &n, &k);            // n 个元素，窗口大小 k
    for (int i = 1; i <= n; i++) scanf("%d", &a[i]);

    // 求滑动窗口最小值：维护单调递增队列
    head = 1; tail = 0;               // 初始空队列
    for (int i = 1; i <= n; i++) {
        // 1. 队尾弹出破坏单调性的元素（>= a[i] 的都弹掉）
        while (head <= tail && a[q[tail]] >= a[i]) tail--;
        // 2. 当前元素下标入队
        q[++tail] = i;
        // 3. 队首弹出滑出窗口的元素（下标 <= i-k）
        while (q[head] <= i - k) head++;
        // 4. 当 i >= k 时，队首即窗口最小值
        if (i >= k) printf("%d ", a[q[head]]);
    }
    printf("\n");

    // 求滑动窗口最大值：维护单调递减队列
    head = 1; tail = 0;
    for (int i = 1; i <= n; i++) {
        while (head <= tail && a[q[tail]] <= a[i]) tail--;  // <= 改方向
        q[++tail] = i;
        while (q[head] <= i - k) head++;
        if (i >= k) printf("%d ", a[q[head]]);
    }
    printf("\n");
    return 0;
}
```

!!! tip "数组下标从 1 开始的好处"
    单调队列存的是**下标**，从 1 开始便于判断"滑出窗口"：`q[head] <= i - k` 表示队首下标已不在窗口 `[i-k+1, i]` 内。若下标从 0 开始，边界要改成 `q[head] < i - k + 1`，容易写错。

### 轨道二：STL deque 实现单调队列

单调队列需要在**队尾出队**，普通 `queue` 不支持，必须用**双端队列 `deque`**。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 1000010;
int a[N];

int main() {
    int n, k;
    scanf("%d%d", &n, &k);
    for (int i = 1; i <= n; i++) scanf("%d", &a[i]);

    // 求滑动窗口最小值
    deque<int> dq;                     // 存下标
    for (int i = 1; i <= n; i++) {
        while (!dq.empty() && a[dq.back()] >= a[i]) dq.pop_back();
        dq.push_back(i);
        while (!dq.empty() && dq.front() <= i - k) dq.pop_front();
        if (i >= k) printf("%d ", a[dq.front()]);
    }
    printf("\n");

    // 求滑动窗口最大值
    dq.clear();
    for (int i = 1; i <= n; i++) {
        while (!dq.empty() && a[dq.back()] <= a[i]) dq.pop_back();
        dq.push_back(i);
        while (!dq.empty() && dq.front() <= i - k) dq.pop_front();
        if (i >= k) printf("%d ", a[dq.front()]);
    }
    printf("\n");
    return 0;
}
```

### 单调队列成员函数速查（STL deque）

| 成员函数 | 功能 | 时间复杂度 | 备注 |
|---------|------|-----------|------|
| `dq.push_back(x)` | 队尾入队 | $O(1)$ | 单调队列入队用 |
| `dq.push_front(x)` | 队首入队 | $O(1)$ | 较少用 |
| `dq.pop_back()` | 队尾出队 | $O(1)$ | **维护单调性**关键 |
| `dq.pop_front()` | 队首出队 | $O(1)$ | **滑出窗口**用 |
| `dq.front()` | 访问队首 | $O(1)$ | 取最值 |
| `dq.back()` | 访问队尾 | $O(1)$ | 比较单调性 |
| `dq.empty()` | 判空 | $O(1)$ | |
| `dq.size()` | 元素个数 | $O(1)$ | |
| `dq.clear()` | 清空 | $O(n)$ | queue 没有此函数 |

### 例题：滑动窗口（洛谷 P1886）

> 有 $n$ 个整数的数组，大小为 $k$ 的滑动窗口从左到右滑动。求每个窗口中的最大值和最小值。
> 数据范围：$1 \le k \le n \le 10^6$。

**分析**：$n$ 达 $10^6$，朴素 $O(nk)$ 必超时。单调队列 $O(n)$ 解决：每个元素最多入队出队各一次。

**复杂度**：时间 $O(n)$，空间 $O(n)$。上方代码即为完整 AC 代码。

!!! warning "单调队列三大易错点"
    1. **队尾弹出条件方向**：求最小值用 `>=`（弹掉比新元素大的），求最大值用 `<=`。
    2. **入队顺序**：先弹队尾、再入队、再弹队首、最后取答案。顺序不能乱！
    3. **输出时机**：只有 `i >= k`（窗口已满）才能输出，前 $k-1$ 个位置窗口未成形。

---

## 📚 本节练习

### 🟢 基础题（CSP-J 难度，巩固队列基本操作）

**练习 1**：机器翻译（洛谷 P1540）
> 内存中有 $M$ 个单元，每篇文章有 $N$ 个单词。查到不在内存的单词要存入内存（满则淘汰最先进去的），求查词典次数。
**提示**：典型队列应用。用一个 `bool inmem[]` 标记单词是否在内存，队列维护"进入内存的顺序"。内存满时队首出队、对应标记清零。注意单词编号 $\le 1000$，可用数组直接标记。

**练习 2**：约瑟夫问题（洛谷 P1996）
> $n$ 人围圈报数，数到 $m$ 出圈，依次输出出圈编号。
**提示**：见本章典型例题，可直接套用队列模拟。建议先用 STL 版写对，再改数组版练手。

### 🟡 拓展题（CSP-S 入门，需思考单调性）

**练习 3**：滑动窗口（洛谷 P1886）
> $n$ 个整数，窗口大小 $k$，求每个窗口最大值和最小值。$n \le 10^6$。
**提示**：单调队列模板题。务必用数组模拟版（常数小），`scanf/printf` 输入输出。注意求最大最小值时队尾弹出条件的方向相反。

### 🔴 CSP-S 进阶（单调队列优化 DP）

**练习 4**：跳房子（洛谷 P3957）
> 在数轴上从 0 跳到 $n+1$，每次跳距离在 $[s-d, s+d]$ 范围内，跳到位置 $i$ 得分 $a_i$（可为负）。求最低花费 $g$（即 $d$）使总得分 $\ge k$。
**提示**：二分 $d$ + 单调队列优化 DP。设 $f[i]$ 为跳到 $i$ 的最大得分，转移方程 $f[i] = \max\{f[j]\} + a[i]$（$j$ 满足距离约束）。窗口内的 $j$ 随 $i$ 单调右移，用单调队列维护最大值，将 $O(n^2)$ 优化到 $O(n \log n)$。

---

## 📖 本章总结

### 核心要点回顾

- ✅ 队列是 **FIFO** 线性表，只允许队尾入、队首出
- ✅ 核心操作 `push/pop/front/back/empty/size` 均为 $O(1)$
- ✅ **数组模拟**：非循环版有假溢出，循环队列用 `(tail+1)%N==head` 判满
- ✅ **STL queue**：编码快、封装好，但无 `clear()`、不支持随机访问
- ✅ **单调队列**：维护单调性，$O(n)$ 解决滑动窗口最值，CSP-S 必考

### 知识脉络思维导图

```
队列 Queue (FIFO)
│
├── 核心概念
│   ├── 定义：队尾入、队首出
│   ├── 原则：先进先出 (FIFO)
│   └── 操作：push pop front back empty size —— 均 O(1)
│
├── 轨道一：数组模拟
│   ├── 非循环队列
│   │   ├── head/tail 双指针
│   │   ├── 判空：head == tail
│   │   └── 缺点：假溢出
│   └── 循环队列（推荐）
│       ├── 取模绕回：(tail+1) % N
│       ├── 判满：(tail+1) % N == head（牺牲一格）
│       └── back()：(tail-1+N) % N 防负数
│
├── 轨道二：STL 实现
│   ├── #include <queue>
│   ├── queue<int> q
│   ├── 6 大成员函数
│   ├── 底层：deque 适配器
│   └── 注意：无 clear()、无随机访问
│
├── 双轨对比
│   ├── 数组模拟：快、可控、适合 BFS/单调队列
│   └── STL：简洁、安全、适合简单模拟
│
├── 常见误区
│   ├── 空队直接 front/pop
│   ├── 循环队列判满条件混淆
│   └── 误解 head == tail 含义
│
└── 进阶：单调队列（CSP-S 核心）
    ├── 维护单调性（递增求最小、递减求最大）
    ├── 三步：弹队尾 → 入队 → 弹队首
    ├── STL 用 deque（支持双端出队）
    └── 应用：滑动窗口最值、DP 优化
```

### 相关题目推荐

| 题目 | 来源 | 难度 | 考点 | 推荐实现 |
|------|------|------|------|---------|
| 机器翻译 | 洛谷 P1540 | ★ | 队列基本操作 | STL queue |
| 约瑟夫问题 | 洛谷 P1996 | ★ | 队列模拟 | 双版本均可 |
| 滑动窗口 | 洛谷 P1886 | ★★★ | 单调队列 | 数组模拟 |
| 跳房子 | 洛谷 P3957 | ★★★★ | 单调队列优化 DP | 数组模拟 |
| 邮票收集 | 洛谷 P2032 | ★★ | 滑动窗口最值 | 数组模拟 |

### 知识衔接

本章是CSP-J/S入门教材数据结构卷的第二章，与前后章节的联系如下：

- **前置知识**：第18章「栈」——栈与队列互为对称结构，理解LIFO后对比学习FIFO，且单调队列与单调栈思想一脉相承
- **本章定位**：先进先出(FIFO)结构，BFS（广度优先搜索）的基础数据结构，掌握队列的基本操作与单调队列（滑动窗口最值优化）
- **后续发展**：为第20章「链表」铺路——继续学习线性数据结构，从顺序存储（数组模拟）走向链式存储，为理解树、图等高级结构打基础

---

!!! tip "下一章预告"
    队列是 **BFS（广度优先搜索）** 的核心数据结构。下一章我们将学习**链表**——从链式存储的底层原理到静态链表（数组模拟）的竞赛写法，链表是理解树、图、哈希表等高级结构的基石。掌握队列，就打开了 BFS 的大门！
