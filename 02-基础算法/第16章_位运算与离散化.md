# 第16章 位运算与离散化

`[CSP-S 进阶]` 位运算用 $O(1)$ 操作替代循环，离散化把大值域压缩成小坐标——两者都是 CSP-S 高频考点

!!! info "本章内容"
    本章将带你掌握两种"压缩"技术：位运算把多个布尔状态压进一个整数，离散化把稀疏大值域压成稠密小编号。你将学习：

    - 进制转换系统讲解：二进制/八进制/十六进制/十进制互转的完整方法
    - 原码、反码、补码的定义、意义与 8 位补码表示范围
    - 六大位运算符的含义与运算规则
    - 位运算常用技巧：判断奇偶、不用临时变量交换、lowbit
    - 离散化三步法：排序 → 去重 → 二分查找映射
    - 经典例题 P1955 程序自动分析（离散化 + 并查集）

!!! warning "本章特点"
    位运算和离散化看似简单，但**陷阱极多**：`x & 1 == 0` 会编译成 `x & (1==0)`，离散化忘记去重会导致映射错位。建议学完排序、二分查找、并查集后再学本章，因为状态压缩 DP 和树状数组都会用到本章知识。

---

## 📌 学习目标

1. **熟练完成**二进制、八进制、十六进制、十进制四种进制之间的相互转换，掌握按权展开与除基取余两种核心方法
2. **理解**原码、反码、补码三种编码的定义与关系，能快速求出负数的 8 位补码，理解补码统一加减法的意义
3. 掌握 `&`、`|`、`^`、`~`、`<<`、`>>` 六种位运算符的含义与运算规则，能用 $O(1)$ 完成位级操作
4. 熟练运用 `x & 1` 判断奇偶、`a ^= b; b ^= a; a ^= b;` 交换两数、`lowbit(x) = x & -x` 取最低位的 1
5. 能用 `sort` + `unique` + `lower_bound` 三件套完成离散化，将 $10^9$ 值域压缩到 $10^5$ 规模
6. 理解位运算优先级低于比较运算符的陷阱，避免 `x & 1 == 0` 这类错误
7. 能综合运用离散化与并查集解决 P1955 程序自动分析等约束满足问题

---

## 💡 生活类比

!!! tip "生活类比：开关灯泡 + 重新编号小区"
    **位运算像"开关灯泡"**：把一个整数想成**一排灯泡**，每一位是一个开关：

    - **与 `&`**：两个开关**都按下**才亮（串联）
    - **或 `|`**：两个开关**任一按下**就亮（并联）
    - **异或 `^`**：两个开关**不同时按**才亮（"差异化"才是真亮）
    - **取反 `~`**：把所有灯泡状态**翻转**
    - **左移 `<<`**：所有灯泡**整体左挪**，右边补灭
    - **右移 `>>`**：所有灯泡**整体右挪**，左边补符号位

    **离散化像"把稀疏人口的城市重新编号成密集小区"**：一条街上稀稀拉拉立着几栋大楼，门牌号分别是 1、100、10000、9999999。门牌号跨度极大，但你只关心它们的**相对大小**。于是物业决定**重新编号**：按门牌号从小到大排，依次改成 1、2、3、4。新编号稠密紧凑，但"谁前谁后"的关系完全保留——这就是离散化。

---

## 核心概念

### 一、进制转换系统讲解

进制转换是位运算的基础。计算机内部一切信息都以二进制存储，而人类习惯十进制，程序员常用八进制和十六进制——四种进制之间的转换是 CSP 初赛必考、CSP-S 复赛也常涉及的基础技能。

#### 1. 常用进制一览

| 进制 | 基数 | 数字符号 | 前缀 | 典型用途 |
|------|------|----------|------|----------|
| 二进制 | 2 | 0, 1 | `0b` | 计算机内部存储 |
| 八进制 | 8 | 0–7 | `0` | 文件权限（如 `755`） |
| 十进制 | 10 | 0–9 | 无 | 日常计算 |
| 十六进制 | 16 | 0–9, A–F | `0x` | 内存地址、颜色值 |

#### 2. 二进制 → 十进制：按权展开

每一位的"权"是该位数字乘以 $2^{\text{位置}}$（从右往左，位置从 0 开始）：

$$N = d_n \times 2^n + d_{n-1} \times 2^{n-1} + \dots + d_1 \times 2^1 + d_0 \times 2^0$$

**例**：二进制 `1011` → 十进制：

$$1 \times 2^3 + 0 \times 2^2 + 1 \times 2^1 + 1 \times 2^0 = 8 + 0 + 2 + 1 = 11$$

**快速记忆**：从右到左的权值依次是 1, 2, 4, 8, 16, 32, 64, 128, 256, 512, 1024 ...

#### 3. 十进制 → 二进制：除 2 取余，逆序排列

不断除以 2，记录余数，直到商为 0，最后将余数**从下往上**读。

**例**：十进制 25 → 二进制：

```
25 ÷ 2 = 12 ... 1
12 ÷ 2 = 6  ... 0
 6 ÷ 2 = 3  ... 0
 3 ÷ 2 = 1  ... 1
 1 ÷ 2 = 0  ... 1
```

从下往上读：`11001`，即 $25 = 16 + 8 + 1 = 11001_2$。

!!! tip "验证技巧"
    转换后可用按权展开反向验证：$1 \times 16 + 1 \times 8 + 0 \times 4 + 0 \times 2 + 1 \times 1 = 25$ ✓

#### 4. 二进制 ↔ 八进制：每 3 位一组

因为 $2^3 = 8$，所以每 3 位二进制恰好对应 1 位八进制。**从右向左**分组，不足 3 位在左边补 0。

**例**：二进制 `101011` → 八进制：

```
101 011
 ↓   ↓
 5   3
```

结果：八进制 `53`。

**反向**：八进制 `53` → 二进制：`5 = 101`，`3 = 011`，拼接得 `101011`。

#### 5. 二进制 ↔ 十六进制：每 4 位一组

因为 $2^4 = 16$，所以每 4 位二进制恰好对应 1 位十六进制。**从右向左**分组，不足 4 位在左边补 0。

**例**：二进制 `101011` → 十六进制：

```
0010 1011
 ↓     ↓
 2     B
```

结果：十六进制 `2B`。

| 十进制 | 十六进制 | 二进制 |
|--------|----------|--------|
| 0–9 | 0–9 | 0000–1001 |
| 10 | A | 1010 |
| 11 | B | 1011 |
| 12 | C | 1100 |
| 13 | D | 1101 |
| 14 | E | 1110 |
| 15 | F | 1111 |

#### 6. 十进制 → 八进制 / 十六进制：除基取余

方法与"除 2 取余"完全相同，只需把除数换成 8 或 16。余数 ≥ 10 时用字母 A–F 表示。

**例**：十进制 200 → 十六进制：

```
200 ÷ 16 = 12 ... 8
 12 ÷ 16 = 0  ... 12(C)
```

从下往上读：`C8`。

#### 7. 进制转换代码实现

```cpp
#include <bits/stdc++.h>
using namespace std;

// 十进制转任意进制（支持 2~16 进制）
string decToBase(int n, int base) {
    if (n == 0) return "0";
    string s = "", digits = "0123456789ABCDEF";
    bool neg = n < 0;
    if (neg) n = -n;
    while (n > 0) {
        s = digits[n % base] + s;  // 余数对应字符前置（逆序）
        n /= base;
    }
    return neg ? "-" + s : s;
}

// 任意进制转十进制（按权展开，秦九韶算法）
int baseToDec(string s, int base) {
    int res = 0;
    for (char c : s) {
        int d = isdigit(c) ? c - '0' : toupper(c) - 'A' + 10;
        res = res * base + d;       // 按权累加
    }
    return res;
}

int main() {
    cout << decToBase(25, 2) << endl;       // 11001
    cout << decToBase(200, 16) << endl;     // C8
    cout << decToBase(255, 16) << endl;     // FF
    cout << baseToDec("1011", 2) << endl;   // 11
    cout << baseToDec("2B", 16) << endl;    // 43
    return 0;
}
```

!!! info "秦九韶算法（Horner 法）"
    `baseToDec` 中的 `res = res * base + d` 实际上是按权展开的高效写法。以二进制 `1011` 为例：
    ```
    res = 0
    res = 0*2 + 1 = 1
    res = 1*2 + 0 = 2
    res = 2*2 + 1 = 5
    res = 5*2 + 1 = 11
    ```
    避免了直接计算 $2^n$，只需 $O(n)$ 次乘加。

!!! warning "C++ 中的进制字面量"
    - 二进制字面量：`0b1011`（C++14 起支持）
    - 八进制字面量：`0` 前缀，如 `053`（注意：`053` 是八进制，等于十进制 43，**不是** 53）
    - 十六进制字面量：`0x` 前缀，如 `0x2B`
    - `cout` 默认输出十进制；可用 `hex`/`oct` 操纵器切换输出进制

---

### 二、原码、反码、补码

计算机内部用**补码**存储有符号整数。理解原码、反码、补码三种编码方式，是掌握位运算（尤其是 `~` 取反、`>>` 算术右移、`lowbit`）的前提。

#### 1. 三种编码的定义

以 **8 位有符号整数**为例，**最高位为符号位**（0 表示正数，1 表示负数），其余位为数值位。

| 编码 | 正数（以 +5 为例） | 负数（以 -5 为例） | 规则 |
|------|------------------|------------------|------|
| 原码 | `0000 0101` | `1000 0101` | 最高位为符号位，其余位为绝对值的二进制 |
| 反码 | `0000 0101` | `1111 1010` | 正数同原码；负数符号位不变，其余位取反 |
| 补码 | `0000 0101` | `1111 1011` | 正数同原码；负数 = 反码 + 1 |

**核心规律**：
- **正数**：原码 = 反码 = 补码（三者完全相同）
- **负数**：反码 = 原码符号位不变其余取反；补码 = 反码 + 1

#### 2. 补码的意义

!!! info "为什么计算机用补码？"
    1. **统一加减法**：用补码后，`a - b` 可转化为 `a + (-b)`，CPU 只需一个加法器就能完成加减运算，无需单独设计减法电路。
    2. **0 的唯一表示**：原码中 `+0 = 00000000` 和 `-0 = 10000000` 是两个不同编码，而补码中 0 只有一种表示 `00000000`，消除了歧义。
    3. **扩大表示范围**：8 位补码能表示 $[-128, +127]$ 共 256 个数，比原码多表示一个最小负数 $-128$。

#### 3. 8 位补码的表示范围

| 十进制 | 补码 | 说明 |
|--------|------|------|
| +127 | `0111 1111` | 最大正数 |
| +1 | `0000 0001` | |
| 0 | `0000 0000` | 唯一的零 |
| -1 | `1111 1111` | |
| -127 | `1000 0001` | |
| -128 | `1000 0000` | 最小负数（无对应原码/反码） |

**范围**：$[-128, +127]$，共 $2^8 = 256$ 个数。

!!! warning "易错：-128 的特殊性"
    `-128` 的补码是 `1000 0000`，它**没有对应的原码和反码**（8 位原码最多到 -127）。这是因为补码比原码"多出一个"最小负数。这个特殊的 -128 是初赛和复赛的高频考点。

#### 4. 快速求负数补码的技巧

**方法**：取绝对值的二进制 → 按位取反 → 加 1。

**例**：求 -5 的 8 位补码。
1. |−5| = 5 = `0000 0101`
2. 按位取反 = `1111 1010`
3. 加 1 = `1111 1011`

**反向**（补码 → 十进制）：最高位为 1 是负数，将补码"取反加 1"得到绝对值。

**例**：补码 `1111 1011` → 取反 `0000 0100` → 加 1 `0000 0101` = 5 → 原数为 −5。

#### 5. 观察补码的代码示例

```cpp
#include <bits/stdc++.h>
using namespace std;

// 输出一个整数的 8 位补码表示
void printComplement(int n) {
    for (int i = 7; i >= 0; i--)
        cout << ((n >> i) & 1);  // 从最高位到最低位逐位输出
    cout << endl;
}

int main() {
    cout << "+5  的补码: ";  printComplement(5);
    cout << "-5  的补码: ";  printComplement(-5);
    cout << "+0  的补码: ";  printComplement(0);
    cout << "-1  的补码: ";  printComplement(-1);
    cout << "-128的补码: ";  printComplement(-128);
    cout << "+127的补码: ";  printComplement(127);
    return 0;
}
/* 输出：
+5  的补码: 00000101
-5  的补码: 11111011
+0  的补码: 00000000
-1  的补码: 11111111
-128的补码: 10000000
+127的补码: 01111111
*/
```

!!! tip "补码与 lowbit 的联系"
    本章核心技巧 `lowbit(x) = x & (-x)` 正是利用了补码的性质：`-x` 在补码下等于 `~x + 1`。理解补码后，你就能真正明白为什么 `x & (-x)` 能取出最低位的 1——这是树状数组的灵魂操作。

---

### 三、六大位运算符

| 运算 | 符号 | 含义 | 例子（8 位） |
|------|------|------|--------------|
| 与 | `a & b` | 都是 1 才为 1 | `1100 & 1010 = 1000` |
| 或 | `a \| b` | 任一为 1 即为 1 | `1100 \| 1010 = 1110` |
| 异或 | `a ^ b` | 不同为 1 | `1100 ^ 1010 = 0110` |
| 取反 | `~a` | 0/1 翻转 | `~00001111 = 11110000` |
| 左移 | `a << k` | 各位左移 k 位，低位补 0 | `00000001 << 3 = 00001000` |
| 右移 | `a >> k` | 各位右移 k 位，高位补符号 | `11110000 >> 2 = 11111100` |

!!! info "异或 `^` 的三大性质"
    1. **自反性**：`a ^ a = 0`，`a ^ 0 = a`
    2. **交换律、结合律**：`a ^ b ^ c = c ^ a ^ b`，顺序无关
    3. **可还原性**：`a ^ b = c` 则 `c ^ b = a`——这是异或加密、找单独数字的核心

### 四、离散化的定义与步骤

「离散化 discretization」就是把一组**稀疏的大数值**，映射到一组**稠密的小整数** $\{1, 2, 3, \dots, k\}$ 上，并保持原有的相对大小关系。

| 性质 | 说明 |
|------|------|
| **保序性** | 若原值 $a < b$，则离散化后 $a' < b'$ |
| **紧凑性** | 映射后的编号从 1 开始连续，无空隙 |
| **去重性** | 相同的原值映射到同一编号 |

离散化三步法：

| 步骤 | 操作 | 函数 | 复杂度 | 说明 |
|------|------|------|--------|------|
| ① 排序 | 将原数据从小到大排好 | `sort` | $O(n \log n)$ | 为去重做准备 |
| ② 去重 | 删除相邻重复元素 | `unique` | $O(n)$ | 只去相邻重复，必须先排序 |
| ③ 映射 | 二分查找原值对应的新编号 | `lower_bound` | $O(\log n)$ | 找第一个 ≥ x 的位置 |

```
原坐标：   1,        100,        10000,        9999999
            ↓          ↓            ↓              ↓
离散化后：   1          2            3              4
```

!!! tip "什么时候需要离散化"
    - 值域很大（如 $10^9$），但实际数据量小（如 $10^5$）
    - 需要用数组下标来记录/统计某些值
    - 只关心值的**相对大小**，不关心绝对大小（如"比它小的有几个"）

---

## 代码实现

### 一、位运算常用技巧

```cpp
#include <bits/stdc++.h>
using namespace std;

// lowbit：取出 x 最低位的 1
int lowbit(int x) {
    return x & (-x);                       // 利用补码：-x = ~x + 1
}

// 不用临时变量交换两数（异或交换）
void swapXor(int &a, int &b) {
    a ^= b;                                // a 变成 a^b
    b ^= a;                                // b = (a^b)^b = a
    a ^= b;                                // a = (a^b)^a = b
}

int main() {
    int x = 13;                             // 二进制 1101

    // ① 判断奇偶：与 1 做"与"，结果为 1 是奇数
    cout << (x & 1 ? "奇" : "偶") << "\n";  // 奇

    // ② 判断第 k 位是 0 还是 1（k 从 0 开始）
    int k = 2;
    cout << ((x >> k) & 1) << "\n";         // 取第 k 位，结果 1

    // ③ 把第 k 位置 1
    x |= (1 << k);

    // ④ 把第 k 位置 0
    x &= ~(1 << k);

    // ⑤ 翻转第 k 位
    x ^= (1 << k);

    // ⑥ lowbit：取最低位的 1
    int y = 12;                             // 二进制 1100
    cout << lowbit(y) << "\n";              // 4 = (100)_2

    // ⑦ 不用临时变量交换
    int a = 5, b = 9;
    swapXor(a, b);
    cout << a << " " << b << "\n";          // 9 5

    return 0;
}
```

!!! tip "为什么 x & (-x) 能取到最低位？"
    设 $x = \dots 1\underbrace{00\dots0}_{k \text{ 个 0}}$，最低位的 1 后面有 $k$ 个 0。

    - `~x` 把所有位翻转，那个 1 变 0，后面的 0 变 1，更高位也翻转
    - `-x = ~x + 1`（补码定义），加 1 后那个 0 又变回 1，进位终止
    - 于是 `x` 和 `-x` **只有最低位那个 1 是公共的**，`x & (-x)` 就只剩最低位的 1

    lowbit 是**树状数组（BIT）**的核心操作，务必熟记。

### 二、离散化三步法代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 100005;

int n;                          // 数据个数
int raw[MAXN];                  // 原始数据
int disc[MAXN];                 // 离散化数组（排序去重后）
int cnt;                        // 离散化后的不同值个数

// 建立离散化映射：把 raw[1..n] 离散化到 disc[1..cnt]
void build() {
    for (int i = 1; i <= n; i++) disc[i] = raw[i];
    sort(disc + 1, disc + n + 1);                       // 第一步：排序
    cnt = unique(disc + 1, disc + n + 1) - (disc + 1);  // 第二步：去重
}

// 第三步：查询原值 x 对应的离散化编号（1-based）
int id(int x) {
    return lower_bound(disc + 1, disc + cnt + 1, x) - disc;
}

int main() {
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> raw[i];

    build();                    // 建立离散化映射

    // 示例：统计每个离散化编号出现次数
    static int freq[MAXN] = {0};
    for (int i = 1; i <= n; i++) {
        freq[id(raw[i])]++;
    }

    // 输出每个不同值及其出现次数
    for (int i = 1; i <= cnt; i++) {
        cout << disc[i] << " : " << freq[i] << "\n";
    }
    return 0;
}
```

!!! info "unique 与 lower_bound 详解"
    `std::unique` 把相邻的重复元素"挤"到末尾，返回去重后末尾的指针。它要求输入数组**已排序**。

    ```cpp
    int a[7] = {1, 1, 2, 3, 3, 3, 5};
    int m = unique(a, a + 7) - a;   // m = 4
    // a 的前 m 个元素变为：1, 2, 3, 5
    ```

    `std::lower_bound` 在有序数组中找**第一个 ≥ x** 的位置。去重后每个值唯一，找到的就是 x 本身的位置。

---

## ⚠️ 常见误区

!!! warning "误区一：位运算优先级低于比较运算符，需加括号"
    C++ 中位运算优先级**比比较运算低**！`x & 1 == 0` 会被解析成 `x & (1 == 0)` 即 `x & 0`，永远是 0。

    **正确写法**：`((x & 1) == 0)` 或 `(x & 1) == 0`。

    | 优先级 | 运算符 | 说明 |
    |--------|--------|------|
    | 3 | `~` | 按位取反（最高） |
    | 5 | `<<` `>>` | 移位 |
    | 8 | `&` | 按位与 |
    | 9 | `^` | 按位异或 |
    | 10 | `\|` | 按位或 |
    | — | `==` `<` 等 | 比较运算（**优先级高于 `&`**） |

    **记忆口诀**：取反最高、移位其次，然后是"与、异或、或"（与异或谐音"蚁翼或"）。**所有位运算都建议多用括号**，永远不要省。

    常见错误：
    - `x & 1 == 0` ❌ → `((x & 1) == 0)` ✅
    - `a ^ b == c` ❌ → `((a ^ b) == c)` ✅
    - `a | b & c` ❌（其实是 `a | (b & c)`）→ 显式写 `(a | b) & c` ✅

!!! warning "误区二：离散化忘记去重"
    `unique` 只能去掉**相邻**的重复元素。如果数组未排序，相同元素不相邻，去重会失败。

    ```cpp
    int a[5] = {1, 3, 1, 3, 1};
    unique(a, a+5);  // 结果：1, 3, 1, 3, 1 —— 没去掉任何重复！
    ```

    所以标准顺序永远是：**先 sort，再 unique**。如果忘记去重，`lower_bound` 仍能查到值，但 `cnt` 偏大，后续树状数组/线段树会开错大小或访问越界，导致 WA 或 RE。

!!! warning "误区三：离散化后下标从 0 还是 1 容易混淆"
    竞赛习惯：树状数组、线段树通常用 1-based 下标（因为 `lowbit(0) = 0` 会导致死循环）。如果 `disc` 数组用 1-based 存储，`lower_bound(disc+1, disc+cnt+1, x) - disc` 直接得到 1-based 编号；若用 0-based 存储，则需 `+1` 转换。**关键是与后续数据结构保持一致**，否则会越界或漏算。

    ```cpp
    // 1-based 写法（推荐，配合树状数组/线段树）
    int id1(int x) {
        return lower_bound(disc + 1, disc + cnt + 1, x) - disc;
    }
    // 0-based 写法（需手动 +1 转 1-based）
    int id0(int x) {
        return lower_bound(disc, disc + cnt, x) - disc + 1;
    }
    ```

!!! warning "误区四：异或交换的同地址陷阱"
    若 `a` 和 `b` 是**同一变量**（如 `swapXor(v[i], v[i])`），异或交换会把该变量清零：`a = a ^ a = 0`。实际工程中**用 `swap(a, b)` 即可**，异或交换仅用于"炫技"或单片机等极端环境。

---

## 典型例题

### 例题：[P1955] 程序自动分析（离散化 + 并查集）

> 💡 提示：本例题需要并查集知识（第23章），建议学完第23章后再做此题。

**题目链接**：https://www.luogu.com.cn/problem/P1955

**题目描述**：在程序自动分析过程中，需要判断一些约束条件是否能被同时满足。共有 $T$ 组数据，每组数据给出 $n$ 个约束条件 $x_i = y_i$ 或 $x_i \neq y_i$。请判断是否存在一种赋值方案同时满足所有约束。$x_i, y_i \le 10^9$，$n \le 10^5$。

**思路分析**：
1. 坐标值达 $10^9$，直接开数组不可行，先对所有 $x_i, y_i$ **离散化**压缩到 $[1, 2n]$
2. **先处理所有相等约束**，用并查集把相等的变量合并（相等关系有传递性）
3. **再处理所有不等约束**，检查每个不等约束的两端是否在同一集合——若在则矛盾，输出 `NO`

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 200005;                    // 注意是 2n，因为每条约束有 2 个数

int T, n;
int fa[MAXN];

int find(int x) {
    return fa[x] == x ? x : fa[x] = find(fa[x]);
}

void unite(int x, int y) {
    int rx = find(x), ry = find(y);
    if (rx != ry) fa[rx] = ry;
}

struct Constraint {
    int x, y, op;                            // op=1 相等，op=0 不等
} c[MAXN];

int disc[MAXN], cnt;

int id(int x) {
    return lower_bound(disc + 1, disc + cnt + 1, x) - disc;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    cin >> T;
    while (T--) {
        cin >> n;
        cnt = 0;
        for (int i = 1; i <= n; i++) {
            cin >> c[i].x >> c[i].y >> c[i].op;
            disc[++cnt] = c[i].x;
            disc[++cnt] = c[i].y;
        }

        // 第一步：离散化（排序 + 去重）
        sort(disc + 1, disc + cnt + 1);
        cnt = unique(disc + 1, disc + cnt + 1) - (disc + 1);

        // 初始化并查集
        for (int i = 1; i <= cnt; i++) fa[i] = i;

        // 第二步：先处理所有相等约束
        for (int i = 1; i <= n; i++) {
            if (c[i].op == 1) {
                unite(id(c[i].x), id(c[i].y));
            }
        }

        // 第三步：再处理所有不等约束，检查是否矛盾
        bool ok = true;
        for (int i = 1; i <= n; i++) {
            if (c[i].op == 0) {
                if (find(id(c[i].x)) == find(id(c[i].y))) {
                    ok = false;
                    break;
                }
            }
        }

        cout << (ok ? "YES" : "NO") << "\n";
    }
    return 0;
}
```

!!! tip "为什么先处理相等约束"
    相等约束具有**传递性**（$a=b, b=c \Rightarrow a=c$），用并查集能高效维护。而不等约束没有传递性。若先处理不等约束，无法判断后续相等约束是否会破坏它。所以必须**先合并所有相等关系，再逐一检查不等关系**。这是 CSP-S 中"约束满足"类问题的经典套路。

    **复杂度**：离散化 $O(n \log n)$，并查集近似 $O(n \alpha(n))$，整体 $O(n \log n)$，可过 $n \le 10^5$。

---

## 📚 本节练习

### 🟢 基础题（模板，巩固操作）

**练习 1**：[P1226] 快速幂（位运算基础）
- 题目链接：https://www.luogu.com.cn/problem/P1226
- 题意：计算 $a^b \bmod p$，$0 \le a, b, p \le 10^9$。
- 提示：用位运算优化快速幂。`b & 1` 判断当前位是否为 1，`b >>= 1` 处理下一位。注意 `res` 初始为 `1 % p`（防 $p=1$ 时答案应为 0），中间乘法用 `long long` 防溢出。

**练习 2**：[B3647] 离散化基础练习
- 题目链接：https://www.luogu.com.cn/problem/B3647
- 题意：给定 $n$ 个整数（值域 $10^9$，$n \le 10^5$），输出每个数离散化后的编号（1-based，保序）。
- 提示：直接套用本章"离散化三步法"模板。`sort` + `unique` + `lower_bound` 三件套，注意 `unique` 前必须先排序。

### 🟡 拓展题（CSP-S 经典，需技巧）

**练习 3**：[P1908] 逆序对（值域 $10^9$ 版）
- 题目链接：https://www.luogu.com.cn/problem/P1908
- 题意：给定 $n$ 个数（$n \le 5 \times 10^5$，值域 $10^9$），求逆序对个数。
- 提示：先离散化把值域压到 $[1, n]$，再用树状数组从左到右扫描，每个数贡献的逆序对数 = 已插入总数 − 当前值的前缀和。注意答案用 `long long`，树状数组的 `lowbit` 用 `x & (-x)`。

**练习 4**：[P1637] 三元上升子序列
- 题目链接：https://www.luogu.com.cn/problem/P1637
- 题意：给定 $n$ 个数，求满足 $a_i < a_j < a_k$（$i < j < k$）的三元组个数，值域 $10^9$。
- 提示：离散化后枚举中间元素 $a_j$，用树状数组维护左右两侧"比 $a_j$ 小的个数"。左侧用前缀和，右侧用总数减前缀和，两者相乘即为以 $a_j$ 为中间元素的三元组数。

**练习 5**：[P1496] 火烧赤壁
- 题目链接：https://www.luogu.com.cn/problem/P1496
- 题意：给定 $n$ 个区间 $[a_i, b_i)$（坐标达 $10^9$），求这些区间覆盖的总长度。
- 提示：对所有端点离散化，然后扫描离散化后的坐标，统计被覆盖的"原始长度"之和。注意相邻离散化点之间的"原始区间长度"要用原始坐标差值，而不是简单的 1。

### 🔴 进阶题（综合应用）

**练习 6**：[P1903] 数颜色（带修改莫队 / 离散化）
- 题目链接：https://www.luogu.com.cn/problem/P1903
- 题意：维护一个序列，支持单点修改和查询区间内不同颜色数。
- 提示：颜色值和修改操作涉及大值域，需要离散化。可用带修改莫队或离线 + 树状数组（按颜色出现次数维护）。这是一道综合离散化与高级数据结构的经典题，难度较大，建议先掌握树状数组与莫队基础再做。

---

## 📖 本章总结

### 核心要点回顾

- ✅ **进制转换**：任意→十进制按权展开；十进制→任意除基取余；二↔八每 3 位一组；二↔十六每 4 位一组
- ✅ **原码/反码/补码**：正数三码相同；负数补码 = 反码 + 1 = 绝对值取反加 1；8 位补码范围 $[-128, 127]$
- ✅ **六大位运算**：与 `&`、或 `|`、异或 `^`、取反 `~`、左移 `<<`、右移 `>>`
- ✅ **判断奇偶**：`x & 1`，结果为 1 是奇数，0 是偶数
- ✅ **不用临时变量交换**：`a ^= b; b ^= a; a ^= b;`（注意同地址陷阱）
- ✅ **lowbit**：`x & (-x)` 取最低位的 1，是树状数组核心
- ✅ **离散化三步法**：`sort`（排序）→ `unique`（去重）→ `lower_bound`（映射）
- ✅ **unique 只去相邻重复**，必须先排序
- ✅ **位运算优先级低于比较运算符**，永远多加括号
- ✅ **离散化 + 并查集**：P1955 经典套路，先合并相等约束，再检查不等约束

### 速查表

| 技巧 | 代码 | 含义 |
|------|------|------|
| 二→十进制 | 按权展开 $d_i \times 2^i$ | $1011_2 = 11$ |
| 十→二进制 | 除 2 取余逆序排 | $25 = 11001_2$ |
| 二↔八进制 | 每 3 位一组 | `101 011` → `53` |
| 二↔十六进制 | 每 4 位一组 | `0010 1011` → `2B` |
| 负数补码 | 绝对值取反加 1 | $-5$ → `11111011` |
| 判奇偶 | `x & 1` | 1 为奇，0 为偶 |
| 取第 k 位 | `(x >> k) & 1` | 0 或 1 |
| 置第 k 位 1 | `x \|= (1 << k)` | 该位变 1 |
| 置第 k 位 0 | `x &= ~(1 << k)` | 该位变 0 |
| 翻转第 k 位 | `x ^= (1 << k)` | 0↔1 翻转 |
| lowbit | `x & (-x)` | 最低位的 1 |
| 异或交换 | `a^=b; b^=a; a^=b;` | 不用临时变量 |
| 离散化排序 | `sort(disc+1, disc+n+1)` | $O(n \log n)$ |
| 离散化去重 | `unique(disc+1, disc+n+1) - (disc+1)` | $O(n)$ |
| 离散化映射 | `lower_bound(disc+1, disc+cnt+1, x) - disc` | $O(\log n)$ |

### 知识脉络

- **第16章 位运算与离散化**
  - **进制转换**
    - 二→十：按权展开；十→二：除 2 取余逆序排
    - 二↔八：每 3 位一组；二↔十六：每 4 位一组
    - 代码实现：`decToBase`（除基取余）+ `baseToDec`（秦九韶算法）
  - **原码、反码、补码**
    - 正数三码相同；负数补码 = 反码 + 1 = 绝对值取反加 1
    - 补码意义：统一加减法、0 唯一表示、范围 $[-128, 127]$
    - lowbit 利用补码：`x & (-x)` = `x & (~x + 1)`
  - **位运算**
    - **六大运算符**：`&`（与）、`|`（或）、`^`（异或）、`~`（取反）、`<<`（左移）、`>>`（右移）
    - **常用技巧**：
      - 奇偶判断：`x & 1`
      - 异或交换：`a ^= b; b ^= a; a ^= b;`
      - lowbit：`x & (-x)`（树状数组核心）
    - **优先级陷阱**：取反 > 移位 > 与 > 异或 > 或，比较运算优先级高于位运算 → 永远多加括号
  - **离散化**
    - **三步法**：排序 → 去重 → 映射（`sort` + `unique` + `lower_bound`）
    - **应用套路**：
      - 逆序对（P1908）：离散化 + 树状数组
      - 程序自动分析（P1955）：离散化 + 并查集
    - **常见误区**：忘记去重（`unique` 前必须先排序）、下标 0/1 混淆、忘记位运算优先级加括号

### 相关题目推荐

| 题目 | 来源 | 难度 | 考点 | 组别 |
|------|------|------|------|------|
| 快速幂 | 洛谷 P1226 | ★★ | 位运算 + 快速幂 | J-S |
| 离散化基础 | 洛谷 B3647 | ★ | 三件套模板 | J-S |
| 逆序对 | 洛谷 P1908 | ★★ | 离散化 + 树状数组 | S |
| 程序自动分析 | 洛谷 P1955 | ★★★ | 离散化 + 并查集 | S |
| 三元上升子序列 | 洛谷 P1637 | ★★★ | 离散化 + 双侧树状数组 | S |
| 火烧赤壁 | 洛谷 P1496 | ★★ | 离散化 + 区间合并 | S |
| 数颜色 | 洛谷 P1903 | ★★★★ | 离散化 + 莫队 | S+ |

### 知识衔接

本章是CSP-J/S入门教材模块二（基础算法）的收尾章节，与前后章节的联系如下：

- **前置知识**：第12章「排序算法」——排序是离散化的前提，离散化本质是将大坐标映射到小坐标后排序去重
- **本章定位**：底层位操作+坐标映射，掌握位运算（与/或/异或/移位）、进制转换与补码表示、离散化（坐标压缩），是高级数据结构与算法的预处理标配
- **后续发展**：为第17章「区间合并与贪心初步」铺路——模块二算法篇结束，下一章将进入贪心思想入门，随后开启数据结构卷

---

!!! tip "下一章预告"
    第17章我们将学习区间合并与贪心初步——从合并重叠区间出发，引出贪心思想！
