# 第55章 单调栈与单调队列
`[CSP-S 高频]` 难度6

!!! info "本章内容"
    本章讲解单调栈与单调队列这两种高频数据结构。单调栈用于在 $O(n)$ 时间内求出每个元素左边/右边第一个比它大/小的元素；单调队列用于在 $O(n)$ 时间内求出滑动窗口的最值。两者都是 CSP-S 的高频考点，也是优化动态规划的重要工具。

!!! note "考纲说明"
    本章讲解单调栈与单调队列。KMP字符串匹配算法已在第24章Trie树与KMP中讲解。

---

## 📌 学习目标

1. 能够手写实现单调栈，求出每个元素左边/右边第一个比它大/小的元素的位置
2. 能够手写实现单调队列，求出滑动窗口的最大值/最小值
3. 能够说出单调栈与单调队列的适用场景，并区分二者
4. 能够分析单调栈/单调队列的时间复杂度为 $O(n)$（每个元素最多入栈/出栈一次）
5. 能够用单调栈解决"柱状图最大矩形"等经典问题
6. 能够用单调队列优化形如 $f_i = \min_{j \in [i-k, i-1]} \{f_j + cost\}$ 的 DP 转移

## 💡 生活类比

!!! tip "生活类比：排队买票与传送带质检"
    **单调栈**像"排队买票"——后面来的人如果比前面矮，前面高的人就挡住了视线，矮的人看不到前面更远的人。当我们想知道"每个人右边第一个比自己高的人是谁"时，可以利用这种"视线遮挡"特性高效求解。

    **单调队列**像"传送带质检"——一个固定长度的窗口在传送带上滑过，质检员只关心窗口内的"最优候选"。当新零件进入窗口时，比它差且更老的候选就可以淘汰掉，因为它们永远不可能再成为最优。这样每个零件只进出窗口一次，效率极高。

## 核心内容

### 55.1 单调栈的概念

**单调栈**是一种特殊的栈，要求栈内元素（或对应下标对应的值）保持**单调递增**或**单调递减**的顺序。

- **单调递增栈**：从栈底到栈顶元素值严格（或非严格）递增
- **单调递减栈**：从栈底到栈顶元素值严格（或非严格）递减

核心操作：当新元素入栈时，若破坏了单调性，就不断弹出栈顶元素，直到恢复单调性为止。被弹出的元素在弹出时就可以得到答案。

### 55.2 单调栈的适用场景

单调栈最经典的应用是：**对每个元素，求它左边/右边第一个比它大/小的元素**。

| 问题 | 使用的栈类型 | 答案来源 |
|------|------------|---------|
| 每个元素右边第一个比它大的元素 | 单调递减栈（从左到右遍历） | 弹出时当前元素就是答案 |
| 每个元素右边第一个比它小的元素 | 单调递增栈（从左到右遍历） | 弹出时当前元素就是答案 |
| 每个元素左边第一个比它大的元素 | 单调递减栈（从右到左遍历） | 弹出时当前元素就是答案 |
| 每个元素左边第一个比它小的元素 | 单调递增栈（从右到左遍历） | 弹出时当前元素就是答案 |

也可以从左到右遍历时，用"当前元素入栈前的栈顶"来求左边第一个满足条件的元素。

### 55.3 单调栈的实现

以求"每个元素左边第一个比它小的元素"为例（从左到右遍历，维护单调递增栈，入栈前看栈顶）：

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 100005;
int a[N], ans[N];
int stk[N], top;
int main() {
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> a[i];
    for (int i = 1; i <= n; i++) {
        // 维护单调递增栈：弹出所有 >= a[i] 的元素
        while (top > 0 && a[stk[top]] >= a[i]) top--;
        if (top > 0) ans[i] = stk[top];       // 左边第一个比 a[i] 小的下标
        else ans[i] = 0;                        // 不存在
        stk[++top] = i;                          // 当前元素入栈
    }
    for (int i = 1; i <= n; i++) cout << a[ans[i]] << " \n"[i == n];
    return 0;
}
// 输入：7
//       2 1 4 3 5 6 2
// 输出：0 0 1 1 3 5 1
// 说明：下标 1 的 a[1]=2 左边没有更小元素，输出 a[0]=0；下标 3 的 a[3]=4 左边第一个更小是 a[2]=1
```

### 55.4 单调队列的概念

**单调队列**是一种双端队列（deque），既可以在队首删除，也可以在队尾删除/插入，且队列内元素保持单调性。它常用于维护**滑动窗口的最值**。

与单调栈的区别：

| 特性 | 单调栈 | 单调队列 |
|------|--------|---------|
| 操作端 | 仅栈顶操作 | 队首 + 队尾双端操作 |
| 元素出队原因 | 单调性被破坏 | 单调性被破坏 **或** 超出窗口范围 |
| 典型应用 | 找第一个大/小元素 | 滑动窗口最值 |
| 与普通栈/队列区别 | 普通栈只保证 LIFO，单调栈额外保证元素单调 | 普通队列只保证 FIFO，单调队列额外保证元素单调且可双端操作 |

### 55.5 单调队列的实现（滑动窗口最值）

以求"长度为 k 的滑动窗口最小值"为例：维护一个**单调递增**的双端队列，队首始终是当前窗口最小值的下标。

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 1000005;
int a[N];
int q[N], head, tail; // 队列存下标，head 闭区间，tail 开区间
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, k;
    cin >> n >> k;
    for (int i = 1; i <= n; i++) cin >> a[i];
    head = 1; tail = 0;
    for (int i = 1; i <= n; i++) {
        // 队尾维护单调递增：弹出所有 >= a[i] 的元素
        while (head <= tail && a[q[tail]] >= a[i]) tail--;
        q[++tail] = i;                       // 当前元素入队
        // 队首弹出超出窗口的元素
        while (head <= tail && q[head] <= i - k) head++;
        // 当窗口形成后输出
        if (i >= k) cout << a[q[head]] << " \n"[i == n];
    }
    return 0;
}
// 输入：8 3
//       1 3 -1 -3 5 3 6 7
// 输出：-1 -3 -3 -3 3 3
// 说明：窗口 [1,3,-1] 最小 -1；[3,-1,-3] 最小 -3；依此类推
```

求最大值只需把队尾的比较符号改为 `<=`（维护单调递减队列）即可。

### 55.6 单调队列优化DP简介

当 DP 转移形如：

$$f_i = \min_{j \in [i-k,\ i-1]} \{ f_j + g(i,j) \}$$

其中 $g(i,j)$ 可以拆分为只与 $j$ 有关的部分时，可以用单调队列在 $O(1)$ 内求出区间最值，将整体复杂度从 $O(nk)$ 降到 $O(n)$。

经典例子：**P1725 琪露诺**、**P3957 跳房子**、**P3800 Power收集** 等。

### 55.7 两种数据结构与普通栈/队列的区别

- **普通栈/队列**：只保证"后进先出/先进先出"的访问顺序，不关心元素大小关系。
- **单调栈/队列**：在普通栈/队列基础上，额外维护元素值的单调性，使得可以在均摊 $O(1)$ 时间内回答"区间最值"或"第一个满足条件的元素"等问题。

核心思想：**用一个额外的不变量（单调性），换取查询的高效性**。这是算法设计中"维护不变量"思想的典型体现。

## 常见误区

1. **误区**：以为单调栈的时间复杂度是 $O(n^2)$，因为有嵌套循环。
   **纠正**：每个元素最多入栈、出栈各一次，均摊 $O(n)$。

2. **误区**：混淆"严格单调"与"非严格单调"。当题目要求"第一个严格大于/小于"时，弹出条件要用 `>=` 或 `<=`，否则会误弹。
   **纠正**：求"严格大于"用非严格（`>=`）弹栈，求"大于等于"用严格（`>`）弹栈。

3. **误区**：单调队列忘记判断"队首是否超出窗口"。
   **纠正**：每次输出答案前必须检查队首下标是否还在窗口 `[i-k+1, i]` 内。

4. **误区**：单调队列中存值而非存下标，导致无法判断是否过期。
   **纠正**：队列里**存下标**，通过下标判断是否在窗口内，通过 `a[下标]` 取值比较。

5. **误区**：把单调栈和单调队列混用。单调栈没有"窗口"概念，元素一旦入栈只因为单调性被破坏才出栈。
   **纠正**：单调队列多了一个"过期出队"的机制。

## 典型例题

### 例题1：P5788 【模板】单调栈

**题意**：给出项数为 $n$ 的数列 $a$，定义 $f(i)$ 为第 $i$ 项之后第一个大于 $a_i$ 的元素的下标，若不存在则 $f(i)=0$。求 $f(1), f(2), \dots, f(n)$。

**思路**：从右到左遍历，维护单调递减栈（栈顶最小），弹出所有 $\le a_i$ 的元素后，栈顶就是右边第一个比 $a_i$ 大的下标。也可以从左到右遍历，当元素被弹出时，当前下标就是它的答案。

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 3000005;
int a[N], f[N];
int stk[N], top;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> a[i];
    // 从左到右遍历，维护单调递减栈
    // 当 a[i] 入栈时，把所有 <= a[i] 的栈顶弹出，它们右边第一个更大的就是 i
    for (int i = 1; i <= n; i++) {
        while (top > 0 && a[stk[top]] < a[i]) {
            f[stk[top]] = i;
            top--;
        }
        stk[++top] = i;
    }
    // 栈中剩余元素没有右边更大的，f 值为 0（已初始化）
    for (int i = 1; i <= n; i++) cout << f[i] << " \n"[i == n];
    return 0;
}
// 输入：5
//       1 4 2 3 5
// 输出：2 5 4 5 0
```

### 例题2：P1886 滑动窗口（单调队列模板）

**题意**：有一个长为 $n$ 的序列，大小为 $k$ 的窗口从左向右滑动，求每个窗口内的最大值和最小值。

**思路**：用两个单调队列分别维护窗口最大值和最小值。求最小值维护单调递增队列（队首最小），求最大值维护单调递减队列（队首最大）。

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 1000005;
int a[N];
int q[N], head, tail; // 单调队列，存下标
int ansMin[N], ansMax[N];
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, k;
    cin >> n >> k;
    for (int i = 1; i <= n; i++) cin >> a[i];
    // 求最小值：单调递增队列
    head = 1; tail = 0;
    for (int i = 1; i <= n; i++) {
        while (head <= tail && a[q[tail]] >= a[i]) tail--;
        q[++tail] = i;
        while (head <= tail && q[head] <= i - k) head++;
        if (i >= k) ansMin[i] = a[q[head]];
    }
    // 求最大值：单调递减队列
    head = 1; tail = 0;
    for (int i = 1; i <= n; i++) {
        while (head <= tail && a[q[tail]] <= a[i]) tail--;
        q[++tail] = i;
        while (head <= tail && q[head] <= i - k) head++;
        if (i >= k) ansMax[i] = a[q[head]];
    }
    for (int i = k; i <= n; i++) cout << ansMin[i] << " \n"[i == n];
    for (int i = k; i <= n; i++) cout << ansMax[i] << " \n"[i == n];
    return 0;
}
// 输入：8 3
//       1 3 -1 -3 5 3 6 7
// 输出：-1 -3 -3 -3 3 3
//       3 3 5 5 6 7
```

## 📖 本章总结

- 单调栈与单调队列
  - 单调栈
    - 概念：栈内元素保持单调性（递增/递减）
    - 场景：找左边/右边第一个比当前大/小的元素
    - 复杂度：$O(n)$，每个元素最多入栈出栈一次
    - 变体：严格/非严格单调
  - 单调队列
    - 概念：双端队列 + 单调性 + 窗口过期机制
    - 场景：滑动窗口最值
    - 复杂度：$O(n)$
    - 要点：队列存下标，判断是否过期
  - 与普通栈/队列的区别
    - 普通：只保证访问顺序（LIFO/FIFO）
    - 单调：额外维护值的不变量
  - DP优化
    - 单调队列优化形如 $f_i = \min_{j \in [i-k,i-1]} \{f_j + cost(j)\}$ 的转移
    - 复杂度从 $O(nk)$ 降到 $O(n)$

### 知识衔接

本章是考纲补强卷的第五章，与前后章节的联系如下：

- **前置知识**：第18章栈（单调栈的底层结构）、第19章队列（单调队列的底层结构）、第43章线性DP（单调队列优化DP转移的基础）
- **本章定位**：讲解维护单调性的两类利器，单调栈求"两侧第一个更大/更小元素"，单调队列优化滑动窗口最值与DP转移，是CSP-S高频模板
- **后续发展**：本章为第56章STL进阶、bitset与类基础铺路，从算法优化转向工具与语言特性的进阶

!!! info "下一章预告"
    下一章我们将学习 **STL进阶、bitset与类基础**，包括 map/set/unordered_map 等关联容器的原理与使用、2025年新增的 bitset 位运算优化技巧（如优化01背包、求传递闭包），以及面向对象中 class 的基本语法。这是 CSP-S 2025 大纲新增的重要内容。
