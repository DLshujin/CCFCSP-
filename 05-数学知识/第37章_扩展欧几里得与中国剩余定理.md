# 第37章 扩展欧几里得与中国剩余定理

> 辗转相除不仅能求最大公约数，还能"反推"出整数系数——这就是扩展欧几里得。配上裴蜀定理，它能解线性不定方程、求逆元；再叠加中国剩余定理，就能解一元同余方程组。从韩信点兵到 NOIP 同余方程，这条数论主线贯穿 CSP-S 始终。

!!! info "本章内容"
    - 欧几里得算法（辗转相除法）求 gcd
    - 扩展欧几里得算法：求 $ax+by=\gcd(a,b)$ 的一组整数解
    - 裴蜀定理（Bézout）：$ax+by=c$ 有解的充要条件
    - 解线性不定方程与求乘法逆元
    - 中国剩余定理（CRT）：模数两两互质时的通解公式
    - 扩展中国剩余定理（EXCRT）：模数不互质时两两合并
    - 常见误区：中间结果爆 `long long`、合并时整除性判断、解的调整

---

## 📌 学习目标

学完本章后，你应当能够：

1. **默写**欧几里得算法与扩展欧几里得算法的递归实现，并能推导回代公式
2. **说清楚**裴蜀定理的内容，并用它判断 $ax+by=c$ 是否有解
3. **熟练求解**线性不定方程的通解，并会求 $a$ 在模 $b$ 下的乘法逆元
4. **默写**中国剩余定理（CRT）的通解公式，并解释 $M_i$ 与 $M_i^{-1}$ 的含义
5. **讲清楚**EXCRT 的合并流程——"差值整除性判断 + 扩欧求逆元 + 更新模数"

---

## 💡 生活类比

打个比方：韩信带兵打仗，让士兵 3 人一排多 2 人、5 人一排多 3 人、7 人一排多 2 人，问至少多少人？这就是著名的"韩信点兵"（物不知数），本质是一个**同余方程组**：

$$
x \equiv 2 \pmod{3},\quad x \equiv 3 \pmod{5},\quad x \equiv 2 \pmod{7}
$$

中国剩余定理告诉我们：只要这堆"排队人数"（模数）**两两互质**，就一定有解，而且解在模所有模数乘积下唯一。如果模数不互质（比如 4 人一排和 6 人一排），就要用更通用的 EXCRT 两两合并——像把两个排队规则一点点融合成一个更严格的规则。

而"扩欧"则是更底层的工具：它回答"能否把两个数凑成指定的和"——就像问"用面值 $a$ 和 $b$ 的硬币，能否恰好凑出金额 $c$"，裴蜀定理说：当且仅当 $c$ 是 $\gcd(a,b)$ 的倍数时才行。

---

## 第一节 欧几里得算法

### 1.1 辗转相除法

**最大公约数** $\gcd(a,b)$ 是同时整除 $a$ 和 $b$ 的最大正整数。欧几里得算法基于一个核心性质：

$$
\gcd(a,b) = \gcd(b,\; a \bmod b)
$$

**证明**：设 $d = \gcd(a,b)$，则 $d \mid a$ 且 $d \mid b$。又 $a \bmod b = a - \lfloor a/b \rfloor \cdot b$，所以 $d \mid (a \bmod b)$，即 $d$ 是 $b$ 和 $a \bmod b$ 的公约数。反过来 $b$ 和 $a \bmod b$ 的任一公约数也能整除 $a$，所以两边公约数集合相同，最大者自然相等。

反复套用该式，第二个参数递减，直到 $b=0$ 时 $\gcd(a,0)=a$，算法终止。

### 1.2 递归实现

```cpp
#include <bits/stdc++.h>
using namespace std;

// 求 a 和 b 的最大公约数（递归版）
int gcd(int a, int b) {
    return b ? gcd(b, a % b) : a;
}

int main() {
    int a, b; cin >> a >> b;
    cout << gcd(a, b) << endl;
    return 0;
}
```

### 1.3 复杂度与注意事项

| 维度 | 内容 |
|------|------|
| 时间复杂度 | $O(\log(\min(a,b)))$（每两步至少减半） |
| 空间复杂度 | $O(\log(\min(a,b)))$（递归深度） |
| 迭代版 | `while (b) { int t = a % b; a = b; b = t; } return a;` |

!!! warning "负数处理"
    C++ 中 `a % b` 的符号与 `a` 相同（向零取整）。如果输入可能为负，应**先取绝对值**：`a = abs(a); b = abs(b);`。一般竞赛题保证非负，但写模板时养成习惯更稳妥。另外 `gcd(0, 0)` 在数学上未定义，代码会返回 0，遇到时需按题意特判。

---

## 第二节 扩展欧几里得算法

### 2.1 问题提出

欧几里得算法只求出 $\gcd(a,b)$，但很多问题需要找出一组整数 $(x, y)$ 满足：

$$
ax + by = \gcd(a,b)
$$

扩展欧几里得算法（exgcd）在求 gcd 的同时"顺带"求出这样一组解。

### 2.2 递归推导

**边界**：当 $b=0$ 时，$\gcd(a,0)=a$，方程 $ax = a$，取 $x=1, y=0$ 即可。

**递归**：假设已求出 $b \cdot x' + (a \bmod b) \cdot y' = \gcd(b, a \bmod b) = \gcd(a,b)$。

把 $a \bmod b = a - \lfloor a/b \rfloor \cdot b$ 代入：

$$
b \cdot x' + \bigl(a - \lfloor a/b \rfloor \cdot b\bigr) \cdot y' = \gcd(a,b)
$$

整理成 $a \cdot (\;) + b \cdot (\;) = \gcd(a,b)$ 的形式：

$$
a \cdot y' + b \cdot \bigl(x' - \lfloor a/b \rfloor \cdot y'\bigr) = \gcd(a,b)
$$

对比 $ax + by = \gcd(a,b)$，得到回代公式：

$$
\boxed{\;x = y',\qquad y = x' - \lfloor a/b \rfloor \cdot y'\;}
$$

### 2.3 代码实现

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// 扩展欧几里得：求 ax + by = gcd(a,b) 的一组解 (x,y)，返回 gcd(a,b)
ll exgcd(ll a, ll b, ll &x, ll &y) {
    if (!b) { x = 1; y = 0; return a; }       // 边界：gcd(a,0)=a
    ll d = exgcd(b, a % b, y, x);              // 注意参数顺序：y 接收 x'，x 接收 y'
    y -= a / b * x;                            // 回代：y = x' - (a/b)*y'
    return d;
}

int main() {
    ll a, b, x, y;
    cin >> a >> b;
    ll d = exgcd(a, b, x, y);
    cout << d << " = " << a << "*" << x << " + " << b << "*" << y << endl;
    return 0;
}
```

!!! tip "参数顺序的小技巧"
    `exgcd(b, a%b, y, x)` 这一行把 `y` 放在第三个参数位、`x` 放在第四个参数位，递归返回时 `y` 自动拿到 $x'$、`x` 自动拿到 $y'$，省去了一个临时变量交换。接着 `y -= a/b * x` 就完成了回代。这一行是模板的精髓，必须理解透彻。

### 2.4 通解形式

设 exgcd 求出一组特解 $(x_0, y_0)$，则 $ax+by=\gcd(a,b)$ 的**全部整数解**为：

$$
x = x_0 + k \cdot \frac{b}{\gcd(a,b)},\qquad y = y_0 - k \cdot \frac{a}{\gcd(a,b)},\quad k \in \mathbb{Z}
$$

验证：代入 $a(x_0 + k \cdot b/d) + b(y_0 - k \cdot a/d) = ax_0 + by_0 = d$，成立。

---

## 第三节 裴蜀定理（Bézout）

### 3.1 定理内容

!!! info "裴蜀定理"
    对任意整数 $a, b$，存在整数 $x, y$ 使 $ax + by = c$ **当且仅当** $\gcd(a,b) \mid c$。

**必要性**：$ax+by=c$ 成立时，$\gcd(a,b)$ 整除左边每一项，故整除 $c$。

**充分性**：由 exgcd 可求出 $ax_0 + by_0 = \gcd(a,b)$，两边同乘 $c/\gcd(a,b)$ 即得一组解。

### 3.2 推论与意义

| 结论 | 说明 |
|------|------|
| $\gcd(a,b)=1$ 时 | $ax+by=1$ 必有解，即 $a$ 在模 $b$ 下存在逆元 |
| $a,b$ 不互质时 | $ax \equiv c \pmod b$ 有解当且仅当 $\gcd(a,b) \mid c$ |
| 解的步长 | 通解中 $x$ 每次变化 $b/\gcd(a,b)$ |

### 3.3 解线性不定方程的一般步骤

给定 $ax + by = c$：

1. 求 $d = \gcd(a,b)$，若 $d \nmid c$ 则**无解**
2. 用 exgcd 求 $ax_0 + by_0 = d$ 的一组特解
3. 令 $x_0 \gets x_0 \cdot (c/d)$，$y_0 \gets y_0 \cdot (c/d)$，得到原方程特解
4. 通解：$x = x_0 + k \cdot (b/d)$，$y = y_0 - k \cdot (a/d)$

```cpp
// 求 ax + by = c 的一组解，返回是否有解
bool solve(ll a, ll b, ll c, ll &x, ll &y) {
    ll d = exgcd(a, b, x, y);          // ax + by = d
    if (c % d != 0) return false;       // 裴蜀定理：d 不整除 c 则无解
    ll k = c / d;
    x *= k; y *= k;                      // 放缩得到原方程特解
    // 通解：x += b/d * t,  y -= a/d * t
    return true;
}
```

---

## 第四节 乘法逆元

### 4.1 定义

若 $\gcd(a, b) = 1$，则存在整数 $x$ 使 $a \cdot x \equiv 1 \pmod b$，称 $x$ 为 $a$ 在模 $b$ 下的**乘法逆元**，记作 $a^{-1} \bmod b$。

### 4.2 用扩欧求逆元

$a \cdot x \equiv 1 \pmod b$ 等价于 $ax + by = 1$。由于 $\gcd(a,b)=1$，由裴蜀定理知必有解，直接调用 exgcd：

```cpp
// 求 a 在模 b 下的逆元（要求 gcd(a,b)=1），返回最小非负逆元
ll inv(ll a, ll b) {
    ll x, y;
    exgcd(a, b, x, y);                  // ax + by = 1
    return (x % b + b) % b;             // 调整到 [0, b) 范围
}
```

!!! warning "解必须调整到最小非负"
    exgcd 返回的 $x$ 可能为负数或超出 $[0, b)$ 范围。**务必**用 `(x % b + b) % b` 调整。很多 WA 都源于忘了这一步。

### 4.3 三种求逆元方法对比

| 方法 | 适用条件 | 时间复杂度 | 备注 |
|------|----------|:----------:|------|
| 扩展欧几里得 | $\gcd(a,b)=1$，$b$ 不一定是质数 | $O(\log b)$ | **最通用**，本章重点 |
| 费马小定理 | $b$ 为**质数**且 $\gcd(a,b)=1$ | $O(\log b)$ | $a^{-1} \equiv a^{b-2} \pmod b$ |
| 线性递推 | 求 $1 \sim n$ 所有逆元，$b$ 为质数 | $O(n)$ | `inv[i] = (b - b/i) * inv[b%i] % b` |

!!! tip "何时用扩欧求逆元"
    当模数 $b$ **不一定是质数**时（如 CRT 中的 $M_i$），费马小定理失效，必须用扩欧。这是本章后续 CRT/EXCRT 的核心工具。

---

## 第五节 中国剩余定理（CRT）

### 5.1 问题模型

给定 $n$ 个同余方程：

$$
x \equiv a_i \pmod{m_i}, \quad i = 1, 2, \dots, n
$$

若模数 $m_1, m_2, \dots, m_n$ **两两互质**，则该方程组在模 $M = \prod m_i$ 下有唯一解。

### 5.2 通解公式

令 $M = \prod_{i=1}^{n} m_i$，$M_i = M / m_i$，$M_i^{-1}$ 为 $M_i$ 在模 $m_i$ 下的逆元（存在，因为 $\gcd(M_i, m_i)=1$）。则：

$$
\boxed{\;x = \sum_{i=1}^{n} a_i \cdot M_i \cdot M_i^{-1} \pmod M\;}
$$

### 5.3 正确性直觉

- **满足第 $i$ 个方程**：除 $i$ 外的项都含因子 $m_i$，故 $\equiv 0 \pmod{m_i}$；而 $a_i \cdot M_i \cdot M_i^{-1} \equiv a_i \cdot 1 = a_i \pmod{m_i}$。
- **唯一性**：若 $x_1, x_2$ 都是解，则 $m_i \mid (x_1 - x_2)$ 对所有 $i$ 成立；模数两两互质，故 $M \mid (x_1 - x_2)$，即 $x_1 \equiv x_2 \pmod M$。

### 5.4 手算示例

解 $x \equiv 2 \pmod 3$，$x \equiv 3 \pmod 5$，$x \equiv 2 \pmod 7$：

- $M = 3 \times 5 \times 7 = 105$
- $M_1 = 35$，$35^{-1} \bmod 3 = 2$（因 $35 \cdot 2 = 70 \equiv 1$）
- $M_2 = 21$，$21^{-1} \bmod 5 = 1$（因 $21 \cdot 1 = 21 \equiv 1$）
- $M_3 = 15$，$15^{-1} \bmod 7 = 1$（因 $15 \cdot 1 = 15 \equiv 1$）
- $x = (2 \times 35 \times 2 + 3 \times 21 \times 1 + 2 \times 15 \times 1) \bmod 105 = 233 \bmod 105 = 23$

验证：$23 = 7 \times 3 + 2$ ✓，$23 = 4 \times 5 + 3$ ✓，$23 = 3 \times 7 + 2$ ✓。

### 5.5 代码实现

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll exgcd(ll a, ll b, ll &x, ll &y) {
    if (!b) { x = 1; y = 0; return a; }
    ll d = exgcd(b, a % b, y, x);
    y -= a / b * x;
    return d;
}

int main() {
    int n; cin >> n;
    vector<ll> a(n), m(n);
    for (int i = 0; i < n; i++) cin >> m[i] >> a[i];
    ll M = 1;
    for (int i = 0; i < n; i++) M *= m[i];          // 模数乘积
    ll ans = 0;
    for (int i = 0; i < n; i++) {
        ll Mi = M / m[i], x, y;
        exgcd(Mi, m[i], x, y);                       // 求 Mi 在模 m[i] 下的逆元
        x = (x % m[i] + m[i]) % m[i];                // 调整到非负
        // 用 __int128 防止 a[i]*Mi*x 爆 long long
        ans = (ans + (__int128)a[i] * Mi % M * x % M) % M;
    }
    cout << (ans % M + M) % M << endl;
    return 0;
}
```

!!! warning "中间结果爆 long long"
    $a_i \cdot M_i \cdot M_i^{-1}$ 三个因子都可能接近 $10^{18}$，三者直接相乘**必然溢出**。必须每乘一步取一次模，或用 `__int128`。这是 CRT 模板最常见的 WA 原因。

---

## 第六节 扩展中国剩余定理（EXCRT）

### 6.1 为什么需要 EXCRT

CRT 要求模数**两两互质**。但很多题目模数不互质（如 $m_1=4, m_2=6$），此时 CRT 公式失效——$M_i$ 与 $m_i$ 不互质，逆元不存在。EXCRT 的思路是：**不依赖互质假设，两两合并同余方程**。

### 6.2 合并两个方程

假设已合并前 $i-1$ 个方程得到 $x \equiv r \pmod M$，现要合并 $x \equiv a_i \pmod{m_i}$。

设 $x = r + M \cdot t$（$t$ 为未知整数），代入第二个方程：

$$
r + M \cdot t \equiv a_i \pmod{m_i} \;\Longrightarrow\; M \cdot t \equiv a_i - r \pmod{m_i}
$$

令 $c = (a_i - r) \bmod m_i$，$g = \gcd(M, m_i)$。

**整除性判断**：由裴蜀定理，$M \cdot t + m_i \cdot k = c$ 有解当且仅当 $g \mid c$。若 $g \nmid c$，**整个方程组无解**。

**求逆元+解 t**：用 exgcd 求 $M \cdot t_0 + m_i \cdot y_0 = g$，则 $t = t_0 \cdot (c/g)$。取 $t \bmod (m_i/g)$ 得最小非负解。

**更新**：

$$
r_{\text{new}} = r + M \cdot t,\qquad M_{\text{new}} = \mathrm{lcm}(M, m_i) = \frac{M}{g} \cdot m_i
$$

最后 $r_{\text{new}} \bmod M_{\text{new}}$ 即合并后的解。

### 6.3 三步合并流程图

```
已有：x ≡ r (mod M)        待合并：x ≡ a (mod m)
            │
            ▼
   ① 求 g = gcd(M, m)，c = (a - r) mod m
            │
            ▼
   ② 判断 g | c ?  —— 否 → 整个方程组无解
            │ 是
            ▼
   ③ exgcd 求 M*t0 + m*y0 = g，得 t0
      令 t = t0 * (c/g) mod (m/g)
            │
            ▼
   ④ 更新：r = (r + M*t) mod (M/g * m)
           M = M/g * m        ← 即 lcm(M, m)
```

### 6.4 代码实现

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll exgcd(ll a, ll b, ll &x, ll &y) {
    if (!b) { x = 1; y = 0; return a; }
    ll d = exgcd(b, a % b, y, x);
    y -= a / b * x;
    return d;
}

// 快速乘：防止 a*b 爆 long long（用 __int128）
inline ll mul(ll a, ll b, ll mod) {
    return (__int128)a * b % mod;
}

int main() {
    int n; scanf("%d", &n);
    vector<ll> a(n), m(n);
    for (int i = 0; i < n; i++) scanf("%lld%lld", &m[i], &a[i]);
    // 维护已合并的同余式 x ≡ r (mod M)
    ll r = a[0] % m[0], M = m[0];
    for (int i = 1; i < n; i++) {
        ll x, y;
        ll g = exgcd(M, m[i], x, y);                 // 求 M*x + m[i]*y = g
        ll c = ((a[i] - r) % m[i] + m[i]) % m[i];    // 差值
        ll newM = M / g * m[i];                      // 新模数 = lcm(M, m[i])
        // 整除性判断（题目保证有解时可省，但建议保留）
        if (c % g != 0) { printf("-1\n"); return 0; }
        // 求 t = t0 * (c/g) mod (m[i]/g)
        ll mod = m[i] / g;
        x = (x % mod + mod) % mod;
        x = mul(x, c / g % mod, mod);
        // 更新 r 和 M
        r = (r + mul(M, x, newM)) % newM;
        M = newM;
        r = (r % M + M) % M;                         // 取最小非负
    }
    printf("%lld\n", r);
    return 0;
}
```

!!! tip "EXCRT 与 CRT 的选择"
    EXCRT 是 CRT 的推广：模数互质时两者等价，模数不互质时只能用 EXCRT。所以**直接背 EXCRT 模板即可通吃所有情况**。但 CRT 公式更简洁、常数更小，模数互质且 $n$ 很大时优先 CRT。

---

## 第七节 常见误区

!!! warning "误区一：中间结果爆 long long"
    CRT 中 $a_i \cdot M_i \cdot M_i^{-1}$ 三者都可能接近 $10^{18}$，直接相乘必然溢出。EXCRT 中 $M \cdot t$ 同理。**解决**：每乘一步取模，或全程用 `__int128`。这是本章**最高频的 WA 原因**。

!!! warning "误区二：EXCRT 合并时忘记判断整除性"
    合并 $x \equiv r \pmod M$ 与 $x \equiv a_i \pmod{m_i}$ 时，若 $\gcd(M, m_i) \nmid (a_i - r)$，则**无解**。直接用 exgcd 求解会得到错误答案（因为 $c/g$ 不是整数）。务必在除之前判断 `c % g == 0`。题目若保证有解可省略，但保留判断更稳健。

!!! warning "误区三：扩欧解未调整到最小非负"
    exgcd 返回的 $x$ 可能是负数或超出 $[0, b)$。求逆元、求 $t$ 时都要用 `(x % mod + mod) % mod` 调整。漏掉这一步会输出负数或超范围的解，导致 WA。

!!! warning "误区四：把 EXCRT 和 CRT 公式混用"
    CRT 的 $M_i = M/m_i$ 与逆元公式**依赖模数互质**。模数不互质时若仍套 CRT 公式，$M_i$ 与 $m_i$ 不互质导致逆元不存在，exgcd 求出的"解"是错的。判断不清时统一用 EXCRT。

!!! warning "误区五：负数取模写错"
    C++ 中 `(-7) % 3 = -1`，不是 2。涉及差值 `a[i] - r` 时务必用 `((x % mod) + mod) % mod` 规范化为非负。EXCRT 合并中 `c = ((a[i] - r) % m[i] + m[i]) % m[i]` 这一步不能省。

---

## 第八节 配套习题精讲

### 8.1 [P1082 NOIP2012] 同余方程

**题意**：求关于 $x$ 的同余方程 $ax \equiv 1 \pmod b$ 的最小正整数解。$2 \le a, b \le 2 \times 10^9$。

**思路**：$ax \equiv 1 \pmod b$ 等价于 $ax + by = 1$。题目保证有解（即 $\gcd(a,b)=1$），直接用 exgcd 求出 $x$，再调整到 $[1, b)$ 范围内的最小正整数。

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll exgcd(ll a, ll b, ll &x, ll &y) {
    if (!b) { x = 1; y = 0; return a; }
    ll d = exgcd(b, a % b, y, x);
    y -= a / b * x;
    return d;
}

int main() {
    ll a, b, x, y;
    cin >> a >> b;
    exgcd(a, b, x, y);                  // ax + by = 1
    x = (x % b + b) % b;                // 调整到 [0, b)
    if (x == 0) x += b;                 // 题目要求正整数，0 不算
    cout << x << endl;
    return 0;
}
```

!!! tip "这道题是扩欧求逆元的模板"
    NOIP2012 把"求逆元"包装成"同余方程"，是 CSP-S 入门数论的经典题。$a, b$ 达到 $2 \times 10^9$，必须用 `long long`，`int` 会溢出。

### 8.2 [P1495] 中国剩余定理(CRT)

**题意**：给定 $n$ 个同余方程 $x \equiv a_i \pmod{m_i}$，模数两两互质，求最小非负整数解。$n \le 100$，$m_i \le 10^9$，保证乘积 $M \le 10^{18}$。

**思路**：直接套 CRT 公式。注意 $a_i \cdot M_i \cdot M_i^{-1}$ 三者相乘会溢出 `long long`，用 `__int128` 或分步取模。

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll exgcd(ll a, ll b, ll &x, ll &y) {
    if (!b) { x = 1; y = 0; return a; }
    ll d = exgcd(b, a % b, y, x);
    y -= a / b * x;
    return d;
}

int main() {
    int n; cin >> n;
    vector<ll> a(n), m(n);
    for (int i = 0; i < n; i++) cin >> m[i] >> a[i];
    ll M = 1;
    for (int i = 0; i < n; i++) M *= m[i];
    ll ans = 0;
    for (int i = 0; i < n; i++) {
        ll Mi = M / m[i], x, y;
        exgcd(Mi, m[i], x, y);                       // 求 Mi 模 m[i] 的逆元
        x = (x % m[i] + m[i]) % m[i];
        ans = (ans + (__int128)a[i] * Mi % M * x % M) % M;   // __int128 防溢出
    }
    cout << (ans % M + M) % M << endl;
    return 0;
}
```

!!! tip "为什么用 __int128"
    $M$ 接近 $10^{18}$，$a_i \cdot M_i$ 可能达到 $10^{27}$，远超 `long long` 上限（约 $9.2 \times 10^{18}$）。`__int128` 范围约 $1.7 \times 10^{38}$，足够。洛谷评测机支持 `__int128`。

### 8.3 [P4777] 扩展中国剩余定理(EXCRT)

**题意**：给定 $n$ 个同余方程 $x \equiv a_i \pmod{m_i}$，**模数不保证互质**，求最小非负整数解。$n \le 10^5$，$m_i \le 10^{18}$，保证有解。

**思路**：模数不互质，必须用 EXCRT 两两合并。注意 $m_i$ 达到 $10^{18}$，所有乘法都要用 `__int128` 防溢出，用 `scanf/printf` 避免 cin/cout 慢。

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll exgcd(ll a, ll b, ll &x, ll &y) {
    if (!b) { x = 1; y = 0; return a; }
    ll d = exgcd(b, a % b, y, x);
    y -= a / b * x;
    return d;
}

// 快速乘：用 __int128 防止 a*b 爆 long long
inline ll mul(ll a, ll b, ll mod) {
    return (__int128)a * b % mod;
}

int main() {
    int n; scanf("%d", &n);
    vector<ll> a(n), m(n);
    for (int i = 0; i < n; i++) scanf("%lld%lld", &m[i], &a[i]);
    ll r = a[0] % m[0], M = m[0];            // 已合并：x ≡ r (mod M)
    for (int i = 1; i < n; i++) {
        ll x, y;
        ll g = exgcd(M, m[i], x, y);         // M*x + m[i]*y = g
        ll c = ((a[i] - r) % m[i] + m[i]) % m[i];
        if (c % g != 0) { printf("-1\n"); return 0; }  // 整除性判断（无解）
        ll newM = M / g * m[i];              // lcm(M, m[i])
        ll mod = m[i] / g;
        x = (x % mod + mod) % mod;
        x = mul(x, c / g % mod, mod);        // t = t0 * (c/g) mod (m/g)
        r = (r + mul(M, x, newM)) % newM;    // 新解
        M = newM;                            // 新模数
        r = (r % M + M) % M;                 // 规范化
    }
    printf("%lld\n", r);
    return 0;
}
```

!!! tip "EXCRT 模板的核心三步"
    每次合并牢记三步：**①判断 $g \mid c$（整除性，不整除则无解输出 -1）→ ②exgcd 求逆元并算 $t$ → ③更新 $r$ 和 $M = \mathrm{lcm}(M, m_i)$**。虽然本题保证有解，模板里仍建议保留无解判断更稳健。$n = 10^5$ 时注意用快速 IO。

---

## 本节练习

### 基础题

| 题号 | 题目 | 考点 |
|------|------|------|
| [P1029 NOIP2001] 最大公约数和最小公倍数问题 | 给定 $P, Q$，求满足 $\gcd(x,y)=P$ 且 $\mathrm{lcm}(x,y)=Q$ 的 $(x,y)$ 对数 | gcd/lcm 性质 |
| [P2613] 有理数取模 | 求 $\dfrac{a}{b} \bmod p$，转化为 $a \cdot b^{-1} \bmod p$ | 扩欧求逆元 |

### 提高题

| 题号 | 题目 | 考点 |
|------|------|------|
| [P1516] 青蛙的约会 | 两只青蛙在同向环上跳，求相遇步数，转化为同余方程 | 扩欧解不定方程 |
| [P3868 猜数字] | 给定 $n$ 个 $B_i$ 和 $P_i$，求最小的 $X$ 使 $X \bmod P_i = B_i$ | EXCRT 模板 |

### 典型例题

| 题号 | 题目 | 考点 |
|------|------|------|
| [P3986] 数列（NOI 变式） | 斐波那契形式线性组合，转化为 $ax+by=S$ 求解 | 裴蜀定理 + exgcd |
| [P4774 BZOJ2911] 双色球 | 同余方程组求最小解，模数不互质 | EXCRT + 二分答案 |

!!! info "练习建议"
    基础题先巩固 gcd 性质与逆元概念；提高题训练把应用题转化为同余方程的**建模能力**（如青蛙的约会）；CSP-S 变式则综合 exgcd 与其他算法。建议按顺序刷，每题先独立思考 30 分钟再看题解。

---

## 📖 本章总结

- **欧几里得算法**：gcd(a,b)=gcd(b, a mod b)，O(log min(a,b))
- **扩展欧几里得**：求 ax+by=gcd(a,b) 的解 (x,y)
  - 回代公式：x=y', y=x'−(a/b)·y'
- **裴蜀定理**：ax+by=c 有解 ⟺ gcd(a,b)|c
- **乘法逆元**：ax ≡ 1 (mod b) ⟺ ax+by=1，用 exgcd 求
  - 三种方法：扩欧(通用) / 费马小定理(b为质数) / 线性递推(批量)
- **中国剩余定理 CRT**：模数两两互质
  - x = Σ aᵢ·Mᵢ·Mᵢ⁻¹ mod M，M=Πmᵢ
- **扩展中国剩余定理 EXCRT**：模数不互质，两两合并
  - ① 判断 g|c ② exgcd 求 t ③ 更新 r 和 M=lcm(M,m)
- **常见误区**
  - 中间结果爆 long long → __int128 / 分步取模
  - EXCRT 忘记判断整除性
  - 扩欧解未调整到最小非负 → (x%mod+mod)%mod
  - 负数取模写错
- **配套习题**：P1082 逆元 / P1495 CRT / P4777 EXCRT

### 自测清单（合上书自检）

!!! tip "能做到以下几点，才算真正掌握本章"
    - [ ] 默写 gcd 递归与迭代两种写法
    - [ ] 默写 exgcd 并能推导回代公式 $x=y',\; y=x'-(a/b)y'$
    - [ ] 说出裴蜀定理的内容并解释充要性
    - [ ] 用 exgcd 求逆元并调整到最小非负
    - [ ] 默写 CRT 通解公式，解释 $M_i$ 和 $M_i^{-1}$ 的含义
    - [ ] 默写 EXCRT 三步合并流程：整除判断 → 求 $t$ → 更新 $r,M$
    - [ ] 解释为什么 CRT 中间结果会爆 `long long`，如何避免
    - [ ] 独立 AC P1082、P1495、P4777 三道模板题

### 知识衔接

本章是**数论进阶**的关键章节，与前后章节的联系如下：

- **前置知识**：第35章质数与约数（GCD算法基础）、第36章欧拉函数与快速幂（模运算与逆元基础）
- **本章定位**：用扩展欧几里得算法求解线性同余方程与中国剩余定理，是模方程系统的核心解法
- **后续发展**：为第38章组合数与容斥原理提供CRT与逆元工具，扩展GCD在求解线性方程组中也是基础

!!! info "下一章预告"
    下一章将进入**第38章 组合数与容斥原理**：用逆元实现组合数取模（$C_n^m \bmod p$）、用 Lucas 定理处理大组合数、用容斥原理解决错排与多重集组合问题。本章的 exgcd 与逆元是组合数取模的核心工具，掌握它们后你已具备攻破 CSP-S 数论题的核心能力。
