# 第12章 排序算法

!!! info "本章导读"
    排序是 CSP-S 的基础工具，几乎所有综合题都会用到。本章从三种 $O(n^2)$ 基础排序入手，深入讲解竞赛首选的 STL `sort()`、分治思想的快速排序与归并排序，并通过「逆序对」经典例题展示归并排序的实战价值。

---

## 📌 学习目标

完成本章学习后，你应当能够：

1. **手写实现** 选择、冒泡、插入三种 $O(n^2)$ 排序，并能说出各自适用场景与稳定性
2. **熟练使用** C++ STL `sort()` 函数，掌握自定义比较函数 `cmp` 的写法，能处理结构体多关键字排序
3. **理解分治思想**，手写快速排序与归并排序，知道二者 $O(n\log n)$ 复杂度的来源
4. **运用归并排序** 求解「逆序对」问题（洛谷 P1908），掌握分治统计的转化技巧
5. **避开常见陷阱**：`sort` 的 `cmp` 返回值、快排基准选取、冒泡的提前退出优化、`cnt` 溢出

---

## 💡 生活类比

> **排序，本质上就是「给扑克牌排顺序」。**

不同排序算法，对应不同的「理牌」策略：

- 🃏 **选择排序** = 把所有牌摊开，每次扫一遍挑出最小的放最前面，再从剩下的里挑最小的……
- 🃏 **冒泡排序** = 相邻两张牌比一比，大的往右挪，一轮下来最大的牌就「冒」到了最右边
- 🃏 **插入排序** = 左手握着已排好的牌，右手每次摸一张新牌，从右往左找到合适位置插进去（这是人最自然的理牌方式）
- 🃏 **快速排序** = 随手抽一张牌当「基准」，比它小的丢左边、大的丢右边，再对左右两堆分别再来一次
- 🃏 **归并排序** = 把牌堆对半分、再对半分……分到每堆只剩一张，然后两两合并成有序堆，层层合并回去

!!! tip "为什么学这么多种？"
    竞赛里 99% 的时候直接用 `sort()`。但理解每种排序的原理，是 CSP-S 初赛选择题的考点；而归并排序的「分治」思想更是求解逆序对、统计类问题的钥匙。

---

## 核心概念

### 五种排序算法对比

| 排序算法 | 平均时间 | 最坏时间 | 空间复杂度 | 稳定性 | 竞赛用途 |
|---------|---------|---------|-----------|--------|---------|
| **选择排序** | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ❌ 不稳定 | 初赛选择题 |
| **冒泡排序** | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ 稳定 | 初赛选择题 |
| **插入排序** | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ 稳定 | 小数据/近乎有序 |
| **快速排序** | $O(n\log n)$ | $O(n^2)$ | $O(\log n)$ | ❌ 不稳定 | sort 底层实现 |
| **归并排序** | $O(n\log n)$ | $O(n\log n)$ | $O(n)$ | ✅ 稳定 | **逆序对/外排序** |

!!! info "稳定性是什么？"
    「稳定性」指：值相等的元素，排序后**相对顺序不变**。例如学生按成绩排序，同分的人若要保持原学号顺序，就要用稳定排序。选择排序和快排是不稳定的，冒泡、插入、归并是稳定的。

!!! warning "CSP-S 重点"
    - `sort()` 的底层是**快速排序的优化版**（Introsort），平均 $O(n\log n)$
    - **归并排序**是 CSP-S 的真考点——不是用来排序，而是用来**求逆序对**！必须掌握

### 何时选用哪种排序

| 场景 | 推荐选择 | 理由 |
|------|---------|------|
| 竞赛常规排序 | `sort()` | $O(n\log n)$，写法最简，95% 题目首选 |
| 需保持相等元素原序 | `stable_sort()` | 稳定排序，$O(n\log n)$ |
| 数据近乎有序 | 插入排序 | 接近 $O(n)$，小常数 |
| 求逆序对/统计类 | 归并排序 | 合并时天然可统计 |
| 初赛选择题 | 五种都要会 | 考原理、复杂度、稳定性 |

!!! tip "竞赛黄金法则"
    能用 `sort()` 就用 `sort()`，除非题目本身要求你「手写排序」或「利用排序过程统计某些量」（如逆序对）。不要为了炫技手写快排，既费时又易错。

---

## 代码实现

### 12.1 选择排序

「选择排序」：每轮从剩余元素中选出最小的，放到已排序段末尾。

```
初始：    [5  2  8  1  9  3]
第1轮：找最小1，与第1位交换 → [1] 2  8  5  9  3
第2轮：找最小2，已在位置    → [1  2] 8  5  9  3
第3轮：找最小3，与第3位交换 → [1  2  3] 5  9  8
...
完成：    [1  2  3  5  8  9]
```

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int a[10005];
    for (int i = 0; i < n; i++) cin >> a[i];

    // 选择排序
    for (int i = 0; i < n - 1; i++) {
        int minIdx = i;                    // 假设当前位最小
        for (int j = i + 1; j < n; j++) {
            if (a[j] < a[minIdx]) {
                minIdx = j;                // 更新最小值下标
            }
        }
        swap(a[i], a[minIdx]);             // 交换到正确位置
    }

    for (int i = 0; i < n; i++) cout << a[i] << " ";
    return 0;
}
```

**复杂度**：两层循环，比较次数固定 $O(n^2)$；交换次数最多 $n-1$ 次（比冒泡少）。

---

### 12.2 冒泡排序

「冒泡排序」：相邻元素两两比较，大的往后冒，每轮把最大值「冒」到末尾。

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int a[10005];
    for (int i = 0; i < n; i++) cin >> a[i];

    // 冒泡排序（含提前退出优化）
    for (int i = 0; i < n - 1; i++) {
        bool swapped = false;              // 本轮是否发生过交换
        for (int j = 0; j < n - 1 - i; j++) {
            if (a[j] > a[j + 1]) {
                swap(a[j], a[j + 1]);
                swapped = true;
            }
        }
        if (!swapped) break;               // 本轮无交换，说明已有序
    }

    for (int i = 0; i < n; i++) cout << a[i] << " ";
    return 0;
}
```

!!! tip "n - 1 - i 的含义"
    每完成一轮，最大的就「冒」到了最后，下一轮不用再比末尾 i 个已排好的元素。

!!! tip "提前退出优化"
    加一个 `swapped` 标志：若某一轮全程没有交换，说明数组已经有序，直接结束。**对近乎有序的数据，最优可到 $O(n)$**。

---

### 12.3 插入排序

「插入排序」：把每个元素插入到已排序部分的正确位置。

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int a[10005];
    for (int i = 0; i < n; i++) cin >> a[i];

    // 插入排序
    for (int i = 1; i < n; i++) {
        int key = a[i];                    // 待插入的"牌"
        int j = i - 1;
        while (j >= 0 && a[j] > key) {     // 比它大的往右挪
            a[j + 1] = a[j];
            j--;
        }
        a[j + 1] = key;                    // 插入空位
    }

    for (int i = 0; i < n; i++) cout << a[i] << " ";
    return 0;
}
```

**复杂度**：最坏 $O(n^2)$；但对**近乎有序**的数据接近 $O(n)$，是小数据量下的优选。

---

### 12.4 快速排序

「快速排序」基于**分治思想**：选一个基准（pivot），把比它小的放左边、大的放右边，再递归处理左右两段。

```cpp
#include <bits/stdc++.h>
using namespace std;

int a[100005];

// 对区间 [left, right] 进行快速排序
void quickSort(int left, int right) {
    if (left >= right) return;
    int pivot = a[left + (right - left) / 2];   // 取中间元素为基准（避免取端点退化）
    int i = left, j = right;
    while (i <= j) {
        while (a[i] < pivot) i++;               // 左边找 >= pivot 的
        while (a[j] > pivot) j--;               // 右边找 <= pivot 的
        if (i <= j) {
            swap(a[i], a[j]);
            i++;
            j--;
        }
    }
    quickSort(left, j);                         // 递归左半
    quickSort(i, right);                        // 递归右半
}

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) cin >> a[i];

    quickSort(0, n - 1);

    for (int i = 0; i < n; i++) cout << a[i] << " ";
    return 0;
}
```

!!! warning "基准选取陷阱"
    若每次都取**端点**（第一个或最后一个）为基准，面对有序或逆序数组，每次只能分出一个元素，**退化为 $O(n^2)$**，洛谷 P1177 会 TLE。取中间元素或随机选取可规避此问题。

**复杂度**：平均 $O(n\log n)$，最坏 $O(n^2)$；空间 $O(\log n)$（递归栈）。不稳定。

---

### 12.5 归并排序

「归并排序」同样是分治：先把数组对半分成两段，分别排好，再**合并**两个有序段。

```cpp
#include <bits/stdc++.h>
using namespace std;

int a[100005], tmp[100005];

// 合并两个有序段 [left, mid] 和 [mid+1, right]
void merge(int left, int mid, int right) {
    int i = left, j = mid + 1, k = left;
    while (i <= mid && j <= right) {
        if (a[i] <= a[j]) tmp[k++] = a[i++];
        else              tmp[k++] = a[j++];
    }
    while (i <= mid)   tmp[k++] = a[i++];        // 左段剩余
    while (j <= right) tmp[k++] = a[j++];        // 右段剩余
    for (int p = left; p <= right; p++) a[p] = tmp[p];
}

// 对区间 [left, right] 归并排序
void mergeSort(int left, int right) {
    if (left >= right) return;
    int mid = left + (right - left) / 2;         // 防溢出写法
    mergeSort(left, mid);                        // 排左半
    mergeSort(mid + 1, right);                   // 排右半
    merge(left, mid, right);                     // 合并
}

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) cin >> a[i];

    mergeSort(0, n - 1);

    for (int i = 0; i < n; i++) cout << a[i] << " ";
    return 0;
}
```

!!! info "分治三步走"
    1. **分**：把数组对半分成两段
    2. **治**：递归排序两段
    3. **合**：把两个有序段合并成一个有序段

**复杂度**：时间 $O(n\log n)$（最坏也是），空间 $O(n)$（辅助数组）。稳定。

!!! tip "归并排序的价值"
    单论排序，`sort()` 已经够用。归并排序的真正价值在于：合并时「左段某元素 > 右段某元素」这一现象，正好对应**逆序对**——这正是 CSP-S 的考点！

---

### 12.6 分治算法思想（CSP-S 考点）

📖 通俗解释：分治（Divide and Conquer）就是"**分而治之**"——把一个大问题拆成几个相同的小问题，分别解决，再把结果合并。前面学的快速排序和归并排序，都是分治思想的典型应用。

!!! info "分治三步走"
    1. **分解（Divide）**：把问题拆成若干规模更小的相同子问题
    2. **解决（Conquer）**：递归地求解子问题（足够小时直接求解）
    3. **合并（Combine）**：把子问题的解组合成原问题的解

#### 归并排序中的分治

归并排序是分治的教科书例子：
- **分**：把数组从中间切成左右两半
- **治**：递归地对左半、右半分别排序
- **合**：把两个有序子数组合并成一个有序数组

#### 快速排序中的分治

快速排序的分治体现在"划分"：
- **分**：选一个基准 pivot，把数组分成「比 pivot 小」和「比 pivot 大」两段
- **治**：递归排序左右两段
- **合**：无需合并（划分时已就位）

!!! tip "归并 vs 快排的分治区别"
    - 归并：**分**很简单（对半切），**合**很关键（合并两个有序段）
    - 快排：**分**很关键（按 pivot 划分），**合**很简单（无需合并）

#### 分治与递归的关系

分治是一种**算法思想**，递归是一种**实现手段**。分治通常用递归实现：函数调用自己处理更小的子问题，直到达到"足够小"的边界条件直接求解。可以说：**分治是"做什么"，递归是"怎么做"**。

#### 分治的时间复杂度分析

分治把规模为 $n$ 的问题分成 2 个规模 $n/2$ 的子问题，合并代价 $O(n)$，得到递推式：

$$T(n) = 2T(n/2) + O(n)$$

由主定理（Master Theorem）可得 $T(n) = O(n\log n)$。这正是归并排序和快排（平均）的复杂度来源。

| 递推式 | 复杂度 | 典型算法 |
|--------|--------|---------|
| $T(n) = 2T(n/2) + O(n)$ | $O(n\log n)$ | 归并排序、快速排序（平均） |
| $T(n) = 2T(n/2) + O(1)$ | $O(n)$ | 二叉树遍历 |
| $T(n) = T(n/2) + O(1)$ | $O(\log n)$ | 二分查找 |

#### 代码示例：归并排序求逆序对

分治的实战价值在于：在"合并"阶段可以顺便统计某些量。经典应用就是**求逆序对**（详见典型例题 P1908），核心代码如下：

```cpp
#include <bits/stdc++.h>
using namespace std;

int a[500005], tmp[500005];
long long cnt = 0;                       // 逆序对数，必须 long long

void mergeSort(int left, int right) {
    if (left >= right) return;           // 边界：单个元素直接返回
    int mid = left + (right - left) / 2; // 分：对半切
    mergeSort(left, mid);                // 治：递归左半
    mergeSort(mid + 1, right);           // 治：递归右半
    // 合：合并两段，同时统计逆序对
    int i = left, j = mid + 1, k = left;
    while (i <= mid && j <= right) {
        if (a[i] <= a[j]) {
            tmp[k++] = a[i++];
        } else {
            // a[i] > a[j]：左段 [i, mid] 全部 > a[j]
            cnt += (mid - i + 1);
            tmp[k++] = a[j++];
        }
    }
    while (i <= mid)   tmp[k++] = a[i++];
    while (j <= right) tmp[k++] = a[j++];
    for (int p = left; p <= right; p++) a[p] = tmp[p];
}

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) cin >> a[i];
    mergeSort(0, n - 1);
    cout << cnt << endl;
    return 0;
}
```

!!! tip "分治的适用场景"
    当一个问题能拆成**结构相同**的子问题，且子问题**相互独立**、可以**合并**结果时，就适合用分治。除了排序，后续的"最近点对"、"大整数乘法"等也都是分治应用。

!!! tip "分治要点"
    - 分治三步：分解 → 解决 → 合并；归并排序是"分易合难"，快排是"分难合易"
    - 分治通常用递归实现，递推式 $T(n)=2T(n/2)+O(n)$ 对应 $O(n\log n)$
    - 分治的实战价值：在合并阶段顺便统计（如逆序对）

---

### 12.7 STL sort 详解（竞赛首选）

实际竞赛中**几乎不用手写排序**，直接用 `<algorithm>` 的 `sort()`，底层是优化的 Introsort（快排+堆排），平均 $O(n\log n)$。

#### 基本用法

```cpp
#include <bits/stdc++.h>                 // 已包含 <algorithm>
using namespace std;

int main() {
    int n;
    cin >> n;
    int a[100005];
    for (int i = 0; i < n; i++) cin >> a[i];

    sort(a, a + n);                      // 默认升序

    for (int i = 0; i < n; i++) cout << a[i] << " ";
    return 0;
}
```

#### 降序排列：自定义比较函数

```cpp
#include <bits/stdc++.h>
using namespace std;

bool cmp(int a, int b) {
    return a > b;                        // 降序：a 在 b 前面当且仅当 a > b
}

int main() {
    int n;
    cin >> n;
    int a[100005];
    for (int i = 0; i < n; i++) cin >> a[i];

    sort(a, a + n, cmp);                 // 降序

    for (int i = 0; i < n; i++) cout << a[i] << " ";
    return 0;
}
```

#### 多关键字排序：结构体 + cmp

洛谷 P1093「奖学金」典型场景：先按总分降序，同分按语文降序，再同分按学号升序。

```cpp
#include <bits/stdc++.h>
using namespace std;

struct Student {
    int id, chinese, math, english, total;
};

bool cmp(const Student &x, const Student &y) {
    if (x.total != y.total) return x.total > y.total;        // 总分降序
    if (x.chinese != y.chinese) return x.chinese > y.chinese;// 语文降序
    return x.id < y.id;                                      // 学号升序
}

int main() {
    int n;
    cin >> n;
    Student s[305];
    for (int i = 0; i < n; i++) {
        s[i].id = i + 1;
        cin >> s[i].chinese >> s[i].math >> s[i].english;
        s[i].total = s[i].chinese + s[i].math + s[i].english;
    }
    sort(s, s + n, cmp);
    for (int i = 0; i < 5; i++)
        cout << s[i].id << " " << s[i].total << endl;
    return 0;
}
```

!!! warning "cmp 的致命陷阱"
    `cmp` 必须满足**严格弱序**：`cmp(a, a)` 必须返回 `false`！
    - ❌ 错误：`return a >= b;`（相等时返回 true，会导致 sort 越界/崩溃）
    - ✅ 正确：`return a > b;`（降序）或 `return a < b;`（升序）

!!! tip "sort 的参数"
    `sort(起始地址, 结束地址, 比较函数)`
    - 第一个参数：排序区间起点
    - 第二个参数：排序区间终点（**不包含**，左闭右开 `[ , )`）
    - 比较函数可省略，默认升序
    - 对 `vector<int> v`：`sort(v.begin(), v.end());`

---

## ⚠️ 常见误区

### 误区 1：sort 的 cmp 写成 `>=` 或 `<=`

```cpp
// ❌ 错误：相等时返回 true，违反严格弱序，可能 RE
bool cmp(int a, int b) { return a >= b; }

// ✅ 正确：用严格大于
bool cmp(int a, int b) { return a > b; }
```

`cmp` 的语义是「a 是否应排在 b **前面**」，相等元素不应排在前面，所以必须用 `>` 或 `<`，绝不能用 `>=` 或 `<=`。

### 误区 2：冒泡排序未优化

```cpp
// ❌ 无优化：即使数组已有序，仍要跑满 n-1 轮
for (int i = 0; i < n - 1; i++)
    for (int j = 0; j < n - 1 - i; j++)
        if (a[j] > a[j+1]) swap(a[j], a[j+1]);

// ✅ 优化：加 swapped 标志，有序时提前退出
for (int i = 0; i < n - 1; i++) {
    bool swapped = false;
    for (int j = 0; j < n - 1 - i; j++)
        if (a[j] > a[j+1]) { swap(a[j], a[j+1]); swapped = true; }
    if (!swapped) break;
}
```

### 误区 3：快排选端点为基准导致 O(n²) 退化

面对有序或逆序数据，若每次取**第一个元素**为基准，划分极度不均，递归深度退化为 $n$，时间 $O(n^2)$，洛谷 P1177 直接 TLE。

```cpp
// ❌ 危险：取左端点为基准，有序数据退化
int pivot = a[left];

// ✅ 安全：取中间元素（或随机）为基准
int pivot = a[left + (right - left) / 2];
```

### 误区 4：误以为 sort 是稳定排序

`sort()` **不保证稳定性**！若需要稳定排序（同分保序），要么用 `stable_sort`，要么在 `cmp` 里追加「学号/原下标」作为最后一关键字。

```cpp
// 需要稳定排序时，用 stable_sort
stable_sort(a, a + n, cmp);              // 保持相等元素原顺序
```

### 误区 5：归并排序忘记把 tmp 拷回原数组

```cpp
// ❌ 忘记回写，a 数组根本没变
void merge(int l, int mid, int r) {
    // ... 合并到 tmp ...
    // 缺少：for(...) a[p] = tmp[p];
}

// ✅ 合并后必须把 tmp 拷回 a
for (int p = left; p <= right; p++) a[p] = tmp[p];
```

---

## 典型例题

### 例题：逆序对（洛谷 P1908）

> **题目描述**：给定一个长度为 $n$ 的序列 $a$，求序列中逆序对的数量。
> 逆序对定义：若 $i < j$ 且 $a_i > a_j$，则 $(i, j)$ 是一个逆序对。
> $n \leq 5 \times 10^5$，每个元素 $a_i \leq 10^9$。

**数据范围 $5\times10^5$**：暴力 $O(n^2)$ 双重循环必 TLE，必须用归并排序在 $O(n\log n)$ 内统计。

#### 思路分析

归并排序合并两段时，左段 `[left, mid]` 与右段 `[mid+1, right]` 都已各自有序。当出现 `a[i] > a[j]`（取右段的 `a[j]`）时，**左段从 `i` 到 `mid` 的所有元素都比 `a[j]` 大**，正好贡献 `mid - i + 1` 个逆序对！在合并过程中累加即可。

```
左段：[3, 5, 8]   右段：[1, 4, 7]
合并：取 1（来自右段），此时左段 3,5,8 都 > 1 → 贡献 3 个逆序对
      取 3（左段），不贡献
      取 4（右段），左段 5,8 都 > 4 → 贡献 2 个逆序对
      取 5（左段），不贡献
      取 7（右段），左段 8 > 7 → 贡献 1 个逆序对
      取 8（左段），不贡献
总计：3 + 2 + 1 = 6 个逆序对
```

#### 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

int a[500005], tmp[500005];
long long cnt = 0;                       // 逆序对数，最多 n(n-1)/2，需 long long

void merge(int left, int mid, int right) {
    int i = left, j = mid + 1, k = left;
    while (i <= mid && j <= right) {
        if (a[i] <= a[j]) {
            tmp[k++] = a[i++];
        } else {
            // a[i] > a[j]：左段 [i, mid] 全部 > a[j]，贡献逆序对
            cnt += (mid - i + 1);
            tmp[k++] = a[j++];
        }
    }
    while (i <= mid)   tmp[k++] = a[i++];
    while (j <= right) tmp[k++] = a[j++];
    for (int p = left; p <= right; p++) a[p] = tmp[p];
}

void mergeSort(int left, int right) {
    if (left >= right) return;
    int mid = left + (right - left) / 2;         // 防溢出写法
    mergeSort(left, mid);
    mergeSort(mid + 1, right);
    merge(left, mid, right);
}

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; i++) cin >> a[i];

    mergeSort(0, n - 1);

    cout << cnt << endl;
    return 0;
}
```

!!! tip "为什么 cnt 要用 long long？"
    $n = 5\times10^5$ 时，最坏情况（完全逆序）逆序对数为 $\frac{n(n-1)}{2} \approx 1.25\times10^{11}$，远超 `int` 上限 $2.1\times10^9$，必须用 `long long`。

!!! warning "常见错误"
    - 统计条件写成 `a[i] < a[j]`（方向反了），应**取右段元素时**统计左段剩余
    - 忘记 `a[i] <= a[j]` 中的等号：相等元素**不算**逆序对，必须用 `<=` 让左段先走

---

## 📚 本节练习

### 🟢 基础题（巩固排序模板）

**练习 1**：【模板】排序（洛谷 P1177）
- 链接：https://www.luogu.com.cn/problem/P1177
- 题意：给定 $n$ 个整数（$n\leq 10^5$），按升序输出。
- 提示：直接 `sort(a, a+n)` 即可，$O(n\log n)$。注意数值可达 $10^9$，数组用 `long long` 防溢出。也可手写快排/归并练习。

**练习 2**：明明的随机数（洛谷 P1059）
- 链接：https://www.luogu.com.cn/problem/P1059
- 题意：去重并升序输出。
- 提示：先 `sort` 再去重，或用 `unique`：`sort(a,a+n); int m=unique(a,a+n)-a;`。

### 🟡 提高题（多关键字与逆序对）

**练习 3**：逆序对（洛谷 P1908）
- 链接：https://www.luogu.com.cn/problem/P1908
- 题意：求序列逆序对数，$n\leq 5\times10^5$。
- 提示：**参考本章典型例题**的归并排序解法，注意 `cnt` 用 `long long`。

**练习 4**：奖学金（洛谷 P1093）
- 链接：https://www.luogu.com.cn/problem/P1093
- 题意：按总分、语文、学号多关键字排序，取前 5 名。
- 提示：参考 4.6 节结构体 + cmp 多关键字排序写法。

### 🔴 挑战题（自定义比较与进阶应用）

**练习 5**：拼数（洛谷 P1012）
- 链接：https://www.luogu.com.cn/problem/P1012
- 题意：给 $n$ 个正整数，拼成最大的数。
- 提示：`cmp` 不能简单比大小，要比**拼接后**的大小：`cmp(x,y)` 返回 `x+y > y+x`（字符串拼接）。注意数字可能很大，用 `string` 处理。

**练习 6**：车厢重组（洛谷 P1116）
- 链接：https://www.luogu.com.cn/problem/P1116
- 题意：求把序列排好序所需的最少相邻交换次数。
- 提示：相邻交换次数 = 逆序对数！用归并排序或冒泡（数据小）统计。本质就是 P1908 的变形。

---

## 📖 本章总结

### 核心要点回顾

- ✅ **选择排序**：每轮选最小放前面，$O(n^2)$，不稳定
- ✅ **冒泡排序**：相邻比较交换，$O(n^2)$，稳定，可加 `swapped` 提前退出
- ✅ **插入排序**：逐个插入正确位置，$O(n^2)$，稳定，近乎有序时高效
- ✅ **快速排序**：分治+基准划分，平均 $O(n\log n)$，最坏 $O(n^2)$，不稳定
- ✅ **归并排序**：分治+合并，$O(n\log n)$，稳定，**求逆序对的核心工具**
- ✅ **STL sort**：竞赛首选，$O(n\log n)$，`cmp` 必须满足严格弱序（用 `<`/`>`，禁用 `<=`/`>=`）
- ✅ **逆序对**：归并合并时，取右段元素即累加 `mid - i + 1`

### 知识脉络

- **排序算法**
  - **O(n²) 基础排序**
    - 选择排序：每轮选最小放前面，$O(n^2)$，不稳定
    - 冒泡排序：相邻比较交换，$O(n^2)$，稳定，可加 `swapped` 提前退出优化
    - 插入排序：逐个插入正确位置，$O(n^2)$，稳定，近乎有序时接近 $O(n)$
  - **O(n log n) 高级排序**
    - 快速排序：分治 + 基准划分，平均 $O(n\log n)$，最坏 $O(n^2)$（取端点基准退化），不稳定
    - 归并排序：分治 + 合并，$O(n\log n)$，稳定，**求逆序对的核心工具**
  - **STL 工具**
    - `sort()`：竞赛首选，$O(n\log n)$，`cmp` 必须严格弱序（`<`/`>`，禁用 `<=`/`>=`）
    - `stable_sort()`：稳定排序，$O(n\log n)$
  - **逆序对（P1908）**
    - 归并合并时统计：取右段元素时累加 `cnt += mid - i + 1`
    - 注意 `cnt` 用 `long long`（$n=5\times10^5$ 时最多约 $1.25\times10^{11}$）
  - **陷阱清单**
    - `cmp` 用 `>=` / `<=` → RE/崩溃
    - 冒泡未优化 → 有序数据浪费时间
    - 快排取端点基准 → 有序数据退化 $O(n^2)$
    - `sort` 不稳定 → 需保序用 `stable_sort` 或加下标
    - 逆序对 `cnt` 用 `int` → 溢出，必须 `long long`

### 题目速查表

| 题目 | 洛谷编号 | 难度 | 考点 |
|------|---------|------|------|
| 【模板】排序 | P1177 | 🟢 | sort / 手写快排 |
| 明明的随机数 | P1059 | 🟢 | sort + 去重 |
| 逆序对 | P1908 | 🟡 | 归并排序统计 |
| 奖学金 | P1093 | 🟡 | 多关键字排序 |
| 拼数 | P1012 | 🔴 | 自定义 cmp（拼接比较） |
| 车厢重组 | P1116 | 🔴 | 逆序对变形 |

### 知识衔接

本章是CSP-J/S入门教材模块二（基础算法）的核心章节，与前后章节的联系如下：

- **前置知识**：第11章「枚举与模拟算法」——模拟思想与暴力枚举；第10章「算法效率与时间复杂度」——复杂度分析，用于比较各排序算法效率
- **本章定位**：竞赛高频基础算法，掌握sort函数与常见排序算法（冒泡、选择、插入、归并、快排），含分治思想，是CSP-J/S必考内容
- **后续发展**：为第13章「二分查找」铺路——二分的前提是有序序列，排序是二分查找的直接前置条件

---

!!! tip "下一章预告"
    排序打通后，下一章我们进入**二分查找与二分答案**。在有序数组上「每次砍一半」的 $O(\log n)$ 思想，以及「二分答案」这一万能套路，是 CSP-S 高频得分点，敬请期待！
