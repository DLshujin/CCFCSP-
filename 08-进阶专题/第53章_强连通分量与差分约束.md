# 第53章 强连通分量与差分约束系统

[CSP-S 进阶] 难度7

!!! info "本章内容"
    - 强连通分量（SCC）的定义与意义
    - Tarjan 算法：dfn 时间戳、low 最小追溯值、栈的使用
    - Kosaraju 算法：正反两遍 DFS
    - 缩点技术：将 SCC 缩成一个点，把有向图变为 DAG
    - 缩点后的拓扑排序与 DP
    - 差分约束系统：将不等式组转化为最短路/最长路
    - 差分约束建图：$x_j - x_i \le c$ 转化为 $i \to j$ 权值 $c$ 的边
    - 用 SPFA 判负环与差分约束的可行性

---

## 📌 学习目标

学完本章后，你应当能够：

1. **准确说出**强连通分量的定义，并解释"任意两点互相可达"的含义
2. **默写** Tarjan 算法的完整实现，包括 `dfn`、`low`、栈的操作与 SCC 判定条件
3. **默写** Kosaraju 算法（正反两遍 DFS），并能说出两种算法的复杂度均为 $O(n+m)$
4. **独立完成**缩点建图，并在缩点后的 DAG 上做拓扑排序 + DP
5. **解释**差分约束系统的原理：将 $x_j - x_i \le c$ 转化为边 $i \to j$ 权值 $c$，跑最短路
6. **运用** SPFA 判负环解决差分约束的"无解"判定，并能处理最长路形式的约束

---

## 💡 生活类比

!!! tip "生活类比：朋友圈与时间表约束"
    **强连通分量像"朋友圈"**——如果 A 能联系到 B，B 也能联系到 A，他们就在同一个朋友圈（SCC）里。把整个朋友圈当成一个整体对外联系，就完成了"缩点"——复杂的关系网被简化成一张没有环的"层级图"（DAG）。

    **差分约束像"时间表约束"**——"B 比 A 晚至少 30 分钟"就是一条约束边 $A \to B$ 权值 30。把所有约束连起来，每个变量的合法取值就是从起点到它的最短路径长度。如果约束之间出现矛盾（如"A 比 B 晚，B 比 A 晚"），就会形成负环，系统无解。

---

## 第一节 强连通分量的定义

### 1.1 什么是强连通分量

在有向图 $G=(V,E)$ 中：

- **强连通 Strongly Connected**：若顶点 $u$ 到 $v$ 有路径，且 $v$ 到 $u$ 也有路径，则称 $u$ 与 $v$ 强连通。
- **强连通分量 Strongly Connected Component (SCC)**：极大的强连通子图，即不能再扩充的、任意两点互相可达的子图。

```
有向图示例：
    1 → 2 → 3 → 4
    ↑   ↓   ↑   ↓
    5 ← 6   7 ← 8

SCC 划分：
    {1, 2, 5, 6}    互相可达（1→2→6→5→1 形成环）
    {3}             单点
    {4, 8}          4→8→...→4？需 8→4 路径
    {7}             单点

缩点后的 DAG：
    {1,2,5,6} → {3} → {4,8}
                    {7} ← {3}
```

### 1.2 SCC 的性质

| 性质 | 说明 |
|------|------|
| 划分唯一 | 任一有向图的 SCC 划分是唯一的 |
| 互不相交 | 不同 SCC 之间没有公共顶点 |
| 缩点成 DAG | 把每个 SCC 缩成一个点后，图变为有向无环图（DAG） |
| 单点也是 SCC | 一个没有自环的孤立点也是一个 SCC |

!!! info "为什么 SCC 如此重要"
    有向图上的许多问题（如最长路径、传递闭包）在一般图上是 NP 难的，但在 DAG 上是多项式可解的。SCC + 缩点是把一般有向图"化简为 DAG"的标准手段，是 CSP-S 进阶的必备技能。

---

## 第二节 Tarjan 算法

### 2.1 核心思想

Tarjan 算法基于一次 DFS，维护两个关键数组：

- **`dfn[u]`**：顶点 $u$ 的 DFS 时间戳（被访问的次序）。
- **`low[u]`**：$u$ 通过"树边 + 至多一条返祖边/横叉边"能追溯到的、最小 `dfn` 值。

利用栈维护当前未确定 SCC 的节点。当一个节点 $u$ 满足 `dfn[u] == low[u]` 时，栈中从 $u$ 到栈顶的所有节点构成一个 SCC。

### 2.2 low 值的更新规则

在 DFS 过程中，对 $u$ 的每条出边 $(u, v)$：

| 情况 | 更新规则 | 说明 |
|------|----------|------|
| $v$ 未访问 | `dfs(v)` 后 `low[u] = min(low[u], low[v])` | 树边，用子节点的 low 更新 |
| $v$ 已访问且在栈中 | `low[u] = min(low[u], dfn[v])` | 返祖边/横叉边，用 v 的 dfn 更新 |
| $v$ 已访问且不在栈中 | 不更新 | $v$ 已属于其他 SCC |

### 2.3 SCC 判定

当 `dfn[u] == low[u]` 时，说明 $u$ 无法追溯到更早的节点，$u$ 是某个 SCC 的"根"。此时不断弹栈直到 $u$ 自身被弹出，弹出的所有节点构成一个 SCC。

### 2.4 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 10005;
vector<int> g[MAXN];
int dfn[MAXN], low[MAXN], stk[MAXN], top = 0, ts = 0;
bool inStack[MAXN];
int scc[MAXN], sccCnt = 0;
int n, m;

void tarjan(int u) {
    dfn[u] = low[u] = ++ts;     // 记录时间戳
    stk[++top] = u;             // 入栈
    inStack[u] = true;
    for (int v : g[u]) {
        if (!dfn[v]) {          // v 未访问 → 树边
            tarjan(v);
            low[u] = min(low[u], low[v]);
        } else if (inStack[v]) { // v 在栈中 → 返祖边/横叉边
            low[u] = min(low[u], dfn[v]);
        }
    }
    if (dfn[u] == low[u]) {     // u 是 SCC 的根
        sccCnt++;
        while (true) {
            int v = stk[top--];
            inStack[v] = false;
            scc[v] = sccCnt;
            if (v == u) break;
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        g[u].push_back(v);
    }
    for (int i = 1; i <= n; i++) {
        if (!dfn[i]) tarjan(i);
    }
    cout << "SCC 数量: " << sccCnt << "\n";
    for (int i = 1; i <= n; i++) {
        cout << "点 " << i << " 属于 SCC " << scc[i] << "\n";
    }
    return 0;
}
// 输入：
// 6 7
// 1 2
// 2 3
// 3 1
// 3 4
// 4 5
// 5 6
// 6 4
// 输出：
// SCC 数量: 2
// 点 1 属于 SCC 2
// 点 2 属于 SCC 2
// 点 3 属于 SCC 2
// 点 4 属于 SCC 1
// 点 5 属于 SCC 1
// 点 6 属于 SCC 1
```

!!! tip "Tarjan 的关键记忆点"
    - `dfn[u] == low[u]` 时弹栈 → 形成一个 SCC
    - 树边用 `low[v]` 更新，返祖边用 `dfn[v]` 更新
    - `inStack` 标记决定是否更新 low（横叉边只在同 SCC 内才有意义）

### 2.5 复杂度

| 维度 | 复杂度 | 说明 |
|------|:------:|------|
| 时间 | $O(n + m)$ | 每条边、每个点访问常数次 |
| 空间 | $O(n + m)$ | 邻接表 + 栈 + 数组 |

---

## 第三节 Kosaraju 算法

### 3.1 核心思想

Kosaraju 算法用两遍 DFS：

1. **第一遍 DFS**：在原图上按 DFS 顺序记录节点的"完成时间"（后序）。
2. **第二遍 DFS**：在**反图**（所有边反向）上，按第一遍记录的"完成时间"从大到小依次对未访问节点 DFS，每次 DFS 能到达的所有节点构成一个 SCC。

### 3.2 正确性直觉

- 在原图中，SCC 之间的边形成 DAG。SCC $A$ 到 SCC $B$ 有边，则 $A$ 的"最晚完成时间"通常大于 $B$ 的。
- 在反图中，原图的 $A \to B$ 变为 $B \to A$，所以从 $B$ 出发不会进入 $A$。按"完成时间从大到小"枚举起点，恰好保证每次 DFS 只遍历一个 SCC。

### 3.3 完整代码

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 10005;
vector<int> g[MAXN], rg[MAXN];   // 原图与反图
bool vis[MAXN];
int order[MAXN], orderCnt = 0;
int scc[MAXN], sccCnt = 0;
int n, m;

void dfs1(int u) {
    vis[u] = true;
    for (int v : g[u]) {
        if (!vis[v]) dfs1(v);
    }
    order[++orderCnt] = u;       // 后序记录
}

void dfs2(int u, int id) {
    scc[u] = id;
    for (int v : rg[u]) {
        if (!scc[v]) dfs2(v, id);
    }
}

void kosaraju() {
    for (int i = 1; i <= n; i++) {
        if (!vis[i]) dfs1(i);
    }
    for (int i = orderCnt; i >= 1; i--) {   // 按完成时间从大到小
        int u = order[i];
        if (!scc[u]) {
            sccCnt++;
            dfs2(u, sccCnt);
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        g[u].push_back(v);      // 原图
        rg[v].push_back(u);     // 反图
    }
    kosaraju();
    cout << "SCC 数量: " << sccCnt << "\n";
    for (int i = 1; i <= n; i++) {
        cout << "点 " << i << " 属于 SCC " << scc[i] << "\n";
    }
    return 0;
}
// 输入：
// 6 7
// 1 2
// 2 3
// 3 1
// 3 4
// 4 5
// 5 6
// 6 4
// 输出：
// SCC 数量: 2
// 点 1 属于 SCC 2
// 点 2 属于 SCC 2
// 点 3 属于 SCC 2
// 点 4 属于 SCC 1
// 点 5 属于 SCC 1
// 点 6 属于 SCC 1
```

### 3.4 Tarjan vs Kosaraju 对比

| 对比项 | Tarjan | Kosaraju |
|--------|--------|----------|
| DFS 次数 | 1 次 | 2 次（原图 + 反图） |
| 是否需要反图 | 否 | 是 |
| 空间 | 较小 | 多一份反图 |
| 编码复杂度 | 略高（low 更新易错） | 较低（两次朴素 DFS） |
| 时间复杂度 | $O(n+m)$ | $O(n+m)$ |
| 适用场景 | 一般优先 | 不想写栈和 low 时 |

CSP-S 中两者都可，**Tarjan 更主流**（少建一张图，常数小）。

---

## 第四节 缩点与拓扑排序 DP

### 4.1 缩点的意义

把每个 SCC 缩成一个"超级点"后，原图变为 DAG。在 DAG 上可以做：

- **拓扑排序**：天然的顺序，无后效性。
- **DP**：如最长路、最大点权和路径等。

### 4.2 缩点建图流程

```
1. 用 Tarjan/Kosaraju 求出每个点所属的 SCC 编号 scc[u]
2. 枚举每条原图边 (u, v)：
   若 scc[u] != scc[v]，则在新图中连边 scc[u] -> scc[v]
3. 注意去重（视题目要求，通常用 set 或直接允许重边不影响 DP）
4. 统计每个 SCC 的点权和（如原图点权之和）
```

### 4.3 缩点后的拓扑 DP

缩点后得到 DAG，可以用**拓扑序 DP** 求：

- 每个超级点能到达的最大点权和（路径权值和最大）
- 状态：`dp[i]` 表示从某个入度为 0 的 SCC 走到 SCC $i$ 的最大点权和
- 转移：`dp[v] = max(dp[v], dp[u] + val[v])`，沿拓扑序推进

### 4.4 完整代码示例

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 10005;
vector<int> g[MAXN], g2[MAXN];   // g 原图, g2 缩点后新图
int dfn[MAXN], low[MAXN], stk[MAXN], top = 0, ts = 0;
bool inStack[MAXN];
int scc[MAXN], sccCnt = 0;
int val[MAXN], sccVal[MAXN];     // 原点权与 SCC 点权和
int inDeg[MAXN], dp[MAXN];
int n, m;

void tarjan(int u) {
    dfn[u] = low[u] = ++ts;
    stk[++top] = u;
    inStack[u] = true;
    for (int v : g[u]) {
        if (!dfn[v]) {
            tarjan(v);
            low[u] = min(low[u], low[v]);
        } else if (inStack[v]) {
            low[u] = min(low[u], dfn[v]);
        }
    }
    if (dfn[u] == low[u]) {
        sccCnt++;
        while (true) {
            int v = stk[top--];
            inStack[v] = false;
            scc[v] = sccCnt;
            sccVal[sccCnt] += val[v];
            if (v == u) break;
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> m;
    for (int i = 1; i <= n; i++) cin >> val[i];
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        g[u].push_back(v);
    }
    for (int i = 1; i <= n; i++) {
        if (!dfn[i]) tarjan(i);
    }
    // 缩点建图
    set<pair<int,int>> edges;
    for (int u = 1; u <= n; u++) {
        for (int v : g[u]) {
            if (scc[u] != scc[v]) {
                edges.insert({scc[u], scc[v]});
            }
        }
    }
    for (auto &e : edges) {
        g2[e.first].push_back(e.second);
        inDeg[e.second]++;
    }
    // 拓扑排序 + DP
    queue<int> q;
    for (int i = 1; i <= sccCnt; i++) {
        if (inDeg[i] == 0) {
            q.push(i);
            dp[i] = sccVal[i];
        }
    }
    while (!q.empty()) {
        int u = q.front(); q.pop();
        for (int v : g2[u]) {
            dp[v] = max(dp[v], dp[u] + sccVal[v]);
            if (--inDeg[v] == 0) q.push(v);
        }
    }
    int ans = 0;
    for (int i = 1; i <= sccCnt; i++) ans = max(ans, dp[i]);
    cout << ans << "\n";
    return 0;
}
// 输入：
// 2 2
// 1 2
// 1 2
// 2 1
// 输出：
// 3
```

!!! tip "缩点 DP 的核心"
    缩点把"有环图上求最长路径"转化为"DAG 上求最长路径"。DAG 上的最长路径可以用拓扑排序 + DP 在 $O(n+m)$ 解决，这是缩点的最大价值。

---

## 第五节 差分约束系统

### 5.1 问题定义

给定一组形如：

$$x_j - x_i \le c$$

的不等式（其中 $c$ 为常数），求一组满足所有约束的整数解 $\{x_1, x_2, \dots, x_n\}$，或判断无解。

### 5.2 核心思想：转化为最短路

观察单源最短路的松弛条件：若存在边 $i \to j$ 权值为 $w$，则约束

$$dist[j] \le dist[i] + w$$

这与 $x_j - x_i \le c$ 形式完全一致！于是：

!!! info "差分约束建图规则"
    - 对每条约束 $x_j - x_i \le c$，连一条边 $i \xrightarrow{c} j$。
    - 引入虚拟源点 $0$，对每个变量 $x_i$ 连边 $0 \xrightarrow{0} i$（即 $x_i \le x_0$，保证 $x_0 = 0$ 时所有 $x_i \le 0$）。
    - 跑以 $0$ 为源点的最短路（SPFA 或 Bellman-Ford），`dist[i]` 即为一组可行解。
    - 若出现负环，则约束系统无解。

### 5.3 建图规则速查

| 不等式形式 | 转化 | 边 |
|------------|------|-----|
| $x_j - x_i \le c$ | $dist[j] \le dist[i] + c$ | $i \xrightarrow{c} j$（最短路） |
| $x_j - x_i \ge c$ | 即 $x_i - x_j \le -c$ | $j \xrightarrow{-c} i$（最短路） |
| $x_j - x_i = c$ | 拆为 $\le$ 和 $\ge$ 两条 | $i \to j$ 权 $c$，$j \to i$ 权 $-c$ |
| $x_j - x_i \le c$ 求最大解 | 跑最短路 | `dist[j]` 为 $x_j$ 的最大值 |
| $x_j - x_i \ge c$ 求最小解 | 跑最长路 | `dist[j]` 为 $x_j$ 的最小值 |

### 5.4 用 SPFA 判负环

差分约束的"无解"对应图中存在负环。SPFA 判负环的方法：

- 每个节点入队时记录其入队次数 `cnt[v]`，若 `cnt[v] > n`，则存在负环。
- 或记录每个节点的最短路经过的边数，超过 $n$ 即负环。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 10005, INF = 0x3f3f3f3f;
struct Edge { int v, w; };
vector<Edge> g[MAXN];
int dist[MAXN], cnt[MAXN];      // cnt: 入队次数
bool inQueue[MAXN];
int n, m;

bool spfa() {
    memset(dist, 0x3f, sizeof(dist));
    memset(cnt, 0, sizeof(cnt));
    memset(inQueue, false, sizeof(inQueue));
    // 虚拟源点 0 到所有点连边权 0
    dist[0] = 0;
    queue<int> q;
    q.push(0);
    inQueue[0] = true;
    while (!q.empty()) {
        int u = q.front(); q.pop();
        inQueue[u] = false;
        for (auto &e : g[u]) {
            if (dist[e.v] > dist[u] + e.w) {
                dist[e.v] = dist[u] + e.w;
                cnt[e.v] = cnt[u] + 1;
                if (cnt[e.v] > n) return false;  // 负环 → 无解
                if (!inQueue[e.v]) {
                    q.push(e.v);
                    inQueue[e.v] = true;
                }
            }
        }
    }
    return true;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int op, a, b, c;
        cin >> op >> a >> b >> c;
        // op=1: x_a - x_b <= c   → 边 b -> a 权 c
        // op=2: x_a - x_b >= c   → 边 a -> b 权 -c
        // op=3: x_a - x_b == c    → 两条边
        if (op == 1) {
            g[b].push_back({a, c});
        } else if (op == 2) {
            g[a].push_back({b, -c});
        } else {
            g[b].push_back({a, c});
            g[a].push_back({b, -c});
        }
    }
    // 虚拟源点 0 连边到所有点
    for (int i = 1; i <= n; i++) g[0].push_back({i, 0});
    if (spfa()) {
        cout << "有解\n";
        for (int i = 1; i <= n; i++) cout << "x" << i << " = " << dist[i] << "\n";
    } else {
        cout << "无解\n";
    }
    return 0;
}
// 输入：
// 3 3
// 1 1 2 -1
// 1 2 3 -1
// 1 3 1 -1
// 输出：
// 无解
// 说明：三条约束分别为 x1-x2≤-1、x2-x3≤-1、x3-x1≤-1，
//       相加得 0≤-3，矛盾，存在负环 1→3→2→1（权 -3），故无解。
```

!!! warning "SPFA 判负环的细节"
    - 虚拟源点 $0$ 必须连到所有点，否则有些点无法被松弛，负环检测失效。
    - `cnt[v] = cnt[u] + 1` 而非 `cnt[v]++`，记录的是"经过的边数"，超过 $n$ 才能确定负环。
    - 多组数据务必清空 `g[]`。

---

## 常见误区

### 误区一：Tarjan 中 low 用 low[v] 还是 dfn[v] 更新

```cpp
// ✗ 错误：返祖边也用 low[v] 更新
if (inStack[v]) {
    low[u] = min(low[u], low[v]);   // 可能导致 SCC 划分错误
}

// ✓ 正确：返祖边用 dfn[v] 更新
if (inStack[v]) {
    low[u] = min(low[u], dfn[v]);   // 严格按定义
}
```

!!! danger "low 更新规则"
    Tarjan 原版定义中，返祖边/横叉边用 `dfn[v]` 更新而非 `low[v]`。虽然部分题目用 `low[v]` 也能过，但理论上不严谨，可能导致 SCC 划分偏大。务必用 `dfn[v]`。

### 误区二：忘记 inStack 检查

```cpp
// ✗ 错误：v 已访问但不在栈中也更新 low
if (dfn[v]) {
    low[u] = min(low[u], dfn[v]);   // v 可能属于已弹出的其他 SCC
}

// ✓ 正确：只在 v 在栈中时才更新
if (dfn[v] && inStack[v]) {
    low[u] = min(low[u], dfn[v]);
}
```

不在栈中的点已经属于其他 SCC，更新 low 会导致错误合并不同 SCC。

### 误区三：缩点时未去重导致重复边

虽然 DP 中重边一般不影响正确性，但在某些计数问题中重边会出错。建议用 `set<pair<int,int>>` 去重：

```cpp
set<pair<int,int>> edges;
for (int u = 1; u <= n; u++) {
    for (int v : g[u]) {
        if (scc[u] != scc[v]) edges.insert({scc[u], scc[v]});
    }
}
```

### 误区四：差分约束建图方向搞反

最常见错误：把 $x_j - x_i \le c$ 建成 $j \to i$。

```
约束：x_j - x_i <= c
等价：x_j <= x_i + c
对应：dist[j] <= dist[i] + c
所以：边 i -> j，权值 c
```

记忆口诀："**减号后面的点指向减号前面的点**"。

### 误区五：差分约束忘记虚拟源点

不连虚拟源点，部分孤立点不会被松弛，负环检测会漏判。务必：

```cpp
for (int i = 1; i <= n; i++) g[0].push_back({i, 0});
```

### 误区六：SPFA 判负环用 cnt[v]++ 而非 cnt[u]+1

```cpp
// ✗ 错误：入队次数加 1，不能反映路径长度
cnt[v]++;

// ✓ 正确：路径边数 = 父节点边数 + 1
cnt[v] = cnt[u] + 1;
if (cnt[v] > n) return false;  // 负环
```

---

## 典型例题

### 例题一：【P3387】缩点

**题目链接**：https://www.luogu.com.cn/problem/P3387

**题意**：给定 $n$ 个点 $m$ 条边的有向图，每个点有一个点权。求一条路径，使路径经过的点权之和最大（点只算一次）。

**思路**：

1. 同一个 SCC 内的点可以互相到达，所以经过一个 SCC 就能拿到其所有点权。
2. 用 Tarjan 求 SCC，缩点后变为 DAG。
3. 在 DAG 上做拓扑排序 + DP，求最大点权和路径。

**完整 AC 代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 10005;
vector<int> g[MAXN], g2[MAXN];
int dfn[MAXN], low[MAXN], stk[MAXN], top = 0, ts = 0;
bool inStack[MAXN];
int scc[MAXN], sccCnt = 0;
int val[MAXN], sccVal[MAXN];
int inDeg[MAXN], dp[MAXN];
int n, m;

void tarjan(int u) {
    dfn[u] = low[u] = ++ts;
    stk[++top] = u;
    inStack[u] = true;
    for (int v : g[u]) {
        if (!dfn[v]) {
            tarjan(v);
            low[u] = min(low[u], low[v]);
        } else if (inStack[v]) {
            low[u] = min(low[u], dfn[v]);
        }
    }
    if (dfn[u] == low[u]) {
        sccCnt++;
        while (true) {
            int v = stk[top--];
            inStack[v] = false;
            scc[v] = sccCnt;
            sccVal[sccCnt] += val[v];
            if (v == u) break;
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> m;
    for (int i = 1; i <= n; i++) cin >> val[i];
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        g[u].push_back(v);
    }
    for (int i = 1; i <= n; i++) {
        if (!dfn[i]) tarjan(i);
    }
    set<pair<int,int>> edges;
    for (int u = 1; u <= n; u++) {
        for (int v : g[u]) {
            if (scc[u] != scc[v]) {
                edges.insert({scc[u], scc[v]});
            }
        }
    }
    for (auto &e : edges) {
        g2[e.first].push_back(e.second);
        inDeg[e.second]++;
    }
    queue<int> q;
    for (int i = 1; i <= sccCnt; i++) {
        if (inDeg[i] == 0) {
            q.push(i);
            dp[i] = sccVal[i];
        }
    }
    while (!q.empty()) {
        int u = q.front(); q.pop();
        for (int v : g2[u]) {
            dp[v] = max(dp[v], dp[u] + sccVal[v]);
            if (--inDeg[v] == 0) q.push(v);
        }
    }
    int ans = 0;
    for (int i = 1; i <= sccCnt; i++) ans = max(ans, dp[i]);
    cout << ans << "\n";
    return 0;
}
// 输入：
// 2 2
// 1 2
// 1 2
// 2 1
// 输出：
// 3
```

**关键点**：

- 缩点后用 `set` 去重，避免重边影响入度计数。
- 拓扑序 DP 初始值是 `dp[i] = sccVal[i]`（入度为 0 的 SCC）。

---

### 例题二：【P1993】小 K 的农场

**题目链接**：https://www.luogu.com.cn/problem/P1993

**题意**：有 $n$ 个农场，给定 $m$ 条描述：
- 操作 1：农场 $a$ 比农场 $b$ 至少多种 $c$ 单位作物 → $x_a - x_b \ge c$
- 操作 2：农场 $a$ 比农场 $b$ 至多多种 $c$ 单位作物 → $x_a - x_b \le c$
- 操作 3：农场 $a$ 与农场 $b$ 作物数量相等 → $x_a = x_b$

问是否存在一组合法的作物数量分配。

**思路**：

1. 转化为差分约束系统。
2. $x_a - x_b \ge c$ 即 $x_b - x_a \le -c$，建边 $a \xrightarrow{-c} b$（最短路形式）。
3. $x_a - x_b \le c$，建边 $b \xrightarrow{c} a$。
4. $x_a = x_b$，建两条边 $a \xrightarrow{0} b$ 和 $b \xrightarrow{0} a$。
5. 加虚拟源点 0 连所有点，跑 SPFA 判负环。

**完整 AC 代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 10005;
struct Edge { int v, w; };
vector<Edge> g[MAXN];
int dist[MAXN], cnt[MAXN];
bool inQueue[MAXN];
int n, m;

bool spfa() {
    memset(dist, 0, sizeof(dist));      // 虚拟源点为 0，dist 初始全 0
    memset(cnt, 0, sizeof(cnt));
    memset(inQueue, false, sizeof(inQueue));
    queue<int> q;
    for (int i = 0; i <= n; i++) {
        q.push(i);
        inQueue[i] = true;
    }
    while (!q.empty()) {
        int u = q.front(); q.pop();
        inQueue[u] = false;
        for (auto &e : g[u]) {
            if (dist[e.v] > dist[u] + e.w) {
                dist[e.v] = dist[u] + e.w;
                cnt[e.v] = cnt[u] + 1;
                if (cnt[e.v] > n) return false;   // 负环 → 无解
                if (!inQueue[e.v]) {
                    q.push(e.v);
                    inQueue[e.v] = true;
                }
            }
        }
    }
    return true;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int op;
        cin >> op;
        if (op == 1) {
            int a, b, c;
            cin >> a >> b >> c;
            // x_a - x_b >= c  =>  x_b - x_a <= -c  =>  边 a -> b 权 -c
            g[a].push_back({b, -c});
        } else if (op == 2) {
            int a, b, c;
            cin >> a >> b >> c;
            // x_a - x_b <= c  =>  边 b -> a 权 c
            g[b].push_back({a, c});
        } else {
            int a, b;
            cin >> a >> b;
            // x_a = x_b  =>  两条边
            g[a].push_back({b, 0});
            g[b].push_back({a, 0});
        }
    }
    // 虚拟源点 0 连所有点（权 0）
    for (int i = 1; i <= n; i++) g[0].push_back({i, 0});
    if (spfa()) {
        cout << "Yes\n";
    } else {
        cout << "No\n";
    }
    return 0;
}
// 输入：
// 3 3
// 3 1 2
// 1 1 2 1
// 2 2 3 2
// 输出：
// No
```

**关键点**：

- $x_a - x_b \ge c$ 转化为 $x_b - x_a \le -c$，方向是从 $a$ 到 $b$。
- 判负环用 `cnt[v] > n`，注意 $n$ 是变量数（不含虚拟源点）。
- 也可以把所有点初始入队，省去虚拟源点。

!!! tip "差分约束的两种建图方向"
    - 求**最大解**（每个 $x_i$ 尽量大）：用 $\le$ 约束，跑**最短路**。
    - 求**最小解**（每个 $x_i$ 尽量小）：用 $\ge$ 约束，跑**最长路**。
    - 本题只问有无解，两种方式等价。

---

## 📖 本章总结

- **强连通分量（SCC）**
  - 定义：有向图中任意两点互相可达的极大子图
  - 性质：划分唯一、互不相交、缩点成 DAG
- **Tarjan 算法**
  - 核心：`dfn` 时间戳、`low` 最小追溯值、栈
  - 更新：树边用 `low[v]`，返祖边用 `dfn[v]`（且 `inStack[v]` 为真）
  - 判定：`dfn[u] == low[u]` 时弹栈形成一个 SCC
  - 复杂度：$O(n+m)$
- **Kosaraju 算法**
  - 两遍 DFS：原图后序 + 反图按完成时间倒序
  - 需建反图，复杂度 $O(n+m)$
- **缩点与拓扑 DP**
  - 每个 SCC 缩成超级点，原图变 DAG
  - 建图：枚举原图边，`scc[u] != scc[v]` 则连边
  - DAG 上拓扑排序 + DP 求最长路径等
- **差分约束系统**
  - 原理：$x_j - x_i \le c$ 对应最短路松弛 `dist[j] <= dist[i] + c`
  - 建图：连边 $i \xrightarrow{c} j$
  - 求解：虚拟源点 + SPFA，`dist[i]` 为可行解
  - 无解判定：存在负环
- **SPFA 判负环**
  - `cnt[v] = cnt[u] + 1`，超过 $n$ 即负环
  - 务必加虚拟源点连所有点
- **常见误区**
  - Tarjan 中 low 用 `dfn[v]` 而非 `low[v]` 更新返祖边
  - 忘记 `inStack` 检查
  - 缩点未去重
  - 差分约束建图方向搞反（减号后面 → 减号前面）
  - 忘记虚拟源点
  - SPFA 判负环用 `cnt[v]++` 而非 `cnt[u]+1`
- **典型例题**
  - P3387 缩点：Tarjan + 缩点 + 拓扑 DP
  - P1993 小 K 的农场：差分约束 + SPFA 判负环

### 自测清单（合上书自检）

!!! tip "能做到以下几点，才算真正掌握本章"
    - [ ] 说出强连通分量的定义，解释"极大强连通子图"
    - [ ] 默写 Tarjan 算法，说明 `dfn`、`low`、栈的作用
    - [ ] 说明 `dfn[u] == low[u]` 时为何弹栈能得到一个 SCC
    - [ ] 默写 Kosaraju 算法，解释"两遍 DFS"的顺序
    - [ ] 独立完成缩点建图与拓扑 DP
    - [ ] 说出差分约束的建图规则：$x_j - x_i \le c$ → 边 $i \to j$ 权 $c$
    - [ ] 默写 SPFA 判负环的代码，说明 `cnt[v] > n` 的含义
    - [ ] 用 Tarjan + 缩点 + DP 解决 P3387
    - [ ] 用差分约束 + SPFA 解决 P1993

### 核心速查表

| 操作 | 代码/公式 | 说明 |
|------|----------|------|
| Tarjan 树边更新 | `low[u]=min(low[u],low[v])` | v 未访问时 |
| Tarjan 返祖边更新 | `low[u]=min(low[u],dfn[v])` | v 在栈中时 |
| SCC 判定 | `dfn[u]==low[u]` 时弹栈 | u 是 SCC 根 |
| 缩点建图 | `if(scc[u]!=scc[v]) 连边` | 去重用 set |
| 拓扑 DP | `dp[v]=max(dp[v],dp[u]+val[v])` | DAG 上 |
| 差分约束 $\le$ | 边 $i \xrightarrow{c} j$ | 跑最短路 |
| 差分约束 $\ge$ | 边 $j \xrightarrow{-c} i$ | 等价于 $\le$ |
| 虚拟源点 | `g[0].push_back({i,0})` | 连所有点 |
| SPFA 判负环 | `cnt[v]=cnt[u]+1; if(cnt[v]>n) 无解` | 边数超 n |

### 知识衔接

本章是考纲补强卷的第三章，与前后章节的联系如下：

- **前置知识**：第27章DFS进阶（Tarjan算法的DFS底层）、第29章拓扑排序（缩点后的DAG拓扑结构）、第31章最短路II_SPFA（差分约束转化为最短路的核心工具）
- **本章定位**：讲解有向图缩点（Tarjan求SCC）与不等式系统（差分约束转最短路），是图论进阶的核心考点
- **后续发展**：本章图论技巧告一段落，为第54章Manacher算法与扫描线铺路，转向字符串与计算几何领域

!!! info "下一章预告"
    本章的图论技巧告一段落。下一章我们将转向字符串与计算几何领域，学习 **Manacher 算法**（线性时间求最长回文子串）与**扫描线算法**（用一条线扫过平面解决矩形面积并等问题）。这两个算法都体现了"利用已有信息加速计算"的优雅思想，是 CSP-S 2025 新增考纲中的重点内容。
