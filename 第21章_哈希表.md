# 第21章 哈希表：O(1)查找的魔法（CSP-S）

!!! info "本章内容（双轨教学法）"
    本章严格采用**双轨教学法**：每个数据结构都从两个维度完整讲解。

    - **轨道一**：数组模拟实现（手写，含完整模板 + 逐行注释 + 边界检查 + 复杂度标注）
    - **轨道二**：STL 实现（头文件 + 核心成员函数 + 速查表）

    哈希表是 CSP-S 高频考点，也是并查集、字符串匹配、状态压缩 DP 的常用工具。掌握"数组模拟哈希表"是复赛拿分的关键。

!!! warning "前置要求"
    需要掌握：链表（第 20 章，理解拉链法）、数组（第 6 章）、时间复杂度（第 10 章）、string 类型（第 7 章）。

---

## 📌 学习目标

学完本章后，你应当能够：

1. **说出**哈希表的核心思想（键→值映射），并能解释为何期望复杂度为 $O(1)$
2. **设计**简单的哈希函数（模质数法），并指出模数为合数的危害
3. **默写**拉链法的 5 个核心操作（`find`/`insert`/`init`），使用 `h[N], e[N], ne[N], idx` 四件套
4. **默写**开放寻址法的 `find` 函数（线性探测），并指出表大小需开 2~3 倍
5. **使用** STL `unordered_set` / `unordered_map` 完成哈希题，熟记至少 6 个常用成员函数
6. **实现**字符串哈希（P 进制法），用 $O(1)$ 判断两子串是否相等

---

## 💡 生活类比

### 类比一：图书馆的"书号→书架"映射

想象一个有 1000 万本书的图书馆。如果每次找书都从第一排书架扫到最后一排，要花几天。聪明的图书管理员想到：**给每本书编号，编号除以一个质数 997，余数是几就放第几号书架**。比如编号 12345 的书，$12345 \bmod 997 = 70$，放第 70 号书架。下次找它，直接去 70 号书架——**一步到位**。

```
书号 12345 ──mod 997──→ 书架 70
书号   301 ──mod 997──→ 书架 301
书号  1024 ──mod 997──→ 书架 27
```

这就是哈希表的精髓：**用一个函数把"键"压缩成"下标"**，从而避免逐个比较。

### 类比二：教室座位分配

新学期排座位：每个学生按"学号 mod 50"入座。一般情况一个座位一人；偶尔两个学生撞到同一座位（**冲突**），就在那张桌子上"摞一摞"（**拉链法**），或者让他去下一个空位（**开放寻址法**）。

!!! tip "一句话记忆"
    哈希 = 用函数把键压成下标，期望 $O(1)$；冲突是常态，处理方法是核心。

---

## 第三节 核心概念

### 3.1 哈希表是什么

「哈希表 hash table」是一种通过**哈希函数**把键（key）映射到数组下标，从而实现快速查找的数据结构。

| 维度 | 哈希表 | 数组 | 链表 |
|------|--------|------|------|
| 按值查找 | $O(1)$（期望） | $O(n)$ | $O(n)$ |
| 插入 | $O(1)$（期望） | $O(n)$（保持有序） | $O(1)$ |
| 删除 | $O(1)$（期望） | $O(n)$ | $O(1)$（已知节点） |
| 键类型 | 任意（可哈希） | 整数下标 | 任意 |
| 是否有序 | 否 | 是 | 是 |

### 3.2 哈希函数设计原则

哈希函数 `h(key)` 把键映射到 $[0, N-1]$。好函数满足三条：

1. **计算快**：$O(1)$ 算出来
2. **分布均匀**：不同键尽量映射到不同下标，减少冲突
3. **确定性**：同一键每次映射结果相同

竞赛中最常用的是**模质数法**：

$$h(x) = (x \bmod N + N) \bmod N$$

其中 $N$ 取大于数据量 2 倍的**质数**（如 200003）。

!!! warning "为什么模数要取质数"
    若模数 $N$ 是合数（如 $N=100000=2^5\times 5^5$），而键 $x$ 多含因子 2 或 5，则 $x \bmod N$ 会集中落在某些"倍数下标"上，冲突率飙升。模质数能尽量避免这种"公因子聚集"。这是笔试常考点。

!!! tip "负数取模处理"
    C++ 中 `-5 % 3` 的结果是 `-2`（不是 1），负下标会越界。统一写成 `(x % N + N) % N`，先把负余数变正再加，保证落在 $[0, N-1]$。这是必须养成的肌肉记忆。

### 3.3 冲突处理两大流派

无论哈希函数多好，键多于桶时必有冲突。两大处理方法：

| 方法 | 思想 | 实现 | 优点 | 缺点 |
|------|------|------|------|------|
| **拉链法** | 每个桶挂一条链表，冲突元素挂同一条链 | `h[]` + `e[]/ne[]` | 删除方便，装填因子可 >1 | 多个数组，指针跳转 |
| **开放寻址法** | 冲突时找下一个空桶 | 单数组 `h[]` | 只用一个数组，缓存友好 | 装填因子需 <0.7，删除麻烦 |

```
拉链法示意：                    开放寻址法示意（线性探测）：
桶 0 → [3] → [10] → NULL       桶 0 : 3
桶 1 → NULL                    桶 1 : 10（原本要去桶0，被3占了，挤到1）
桶 2 → [17] → NULL             桶 2 : 17
桶 3 → NULL                    桶 3 : 空
```

### 3.4 核心操作复杂度表

| 操作 | 拉链法 | 开放寻址法 | 备注 |
|------|--------|-----------|------|
| 插入 `insert` | $O(1)$ 期望 | $O(1)$ 期望 | 最坏 $O(n)$（全冲突） |
| 查找 `find` | $O(1)$ 期望 | $O(1)$ 期望 | 最坏 $O(n)$ |
| 删除 `remove` | $O(1)$ 期望 | 需打标记 | 开放寻址法删除复杂 |
| 遍历所有键 | $O(n + N)$ | $O(N)$ | N 为桶数 |

!!! info "期望 $O(1)$ 的前提"
    哈希函数足够好 + 装填因子 $\alpha = n/N$ 足够小（拉链法 $\alpha$ 可较大，开放寻址法 $\alpha < 0.7$）。最坏情况（所有键冲突到同一桶）退化为 $O(n)$，但好哈希函数下概率极低。

---

## 第四节 拉链法（数组模拟）⭐（轨道一·竞赛首选）

### 4.1 完整模板（AcWing 风格，必须熟记）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100003;          // 桶数：取大于 1e5 的质数
int h[N], e[N], ne[N], idx;    // h:桶头  e:数据  ne:下一节点  idx:分配器

// 初始化                              O(N)
void init() {
    memset(h, -1, sizeof h);   // -1 表示空链表（NULL）
    idx = 0;                   // idx 是分配计数器
}

// 哈希函数                            O(1)
int hashFunc(int x) {
    return (x % N + N) % N;    // 负数也安全
}

// 查找 x 是否存在                     O(1) 期望
bool find(int x) {
    int t = hashFunc(x);
    for (int i = h[t]; i != -1; i = ne[i])  // 遍历桶 t 的链表
        if (e[i] == x) return true;
    return false;
}

// 插入 x                              O(1) 期望
void insert(int x) {
    int t = hashFunc(x);
    e[idx] = x;                // 存数据
    ne[idx] = h[t];            // 新节点指向原链头
    h[t] = idx++;              // 链头更新为新节点
}

int main() {
    init();                    // 务必先初始化！
    insert(3);
    insert(10);
    insert(17);
    insert(100003);            // 100003 mod 100003 = 0，与 3 冲突
    cout << find(10) << "\n";  // 1（存在）
    cout << find(5) << "\n";   // 0（不存在）
    return 0;
}
```

### 4.2 实现要点

1. **四件套分工**：`h[t]` 存桶 t 的链头下标（-1 表空）；`e[i]` 存第 i 个节点的值；`ne[i]` 存下一节点下标；`idx` 是分配器。
2. **`memset(h, -1, sizeof h)`**：每个 `int` 字节全设 0xFF，正好是 -1（补码）。比 for 循环快。
3. **头插法**：新节点插到链头，$O(1)$。无需找尾。
4. **`idx` 单调递增不复用**：被删除节点下标不回收，竞赛题通常够用。

!!! tip "拉链法与单链表的同构"
    拉链法本质是"一个桶头数组 + 每个桶挂一条单链表"。`insert` 就是单链表的 `addHead`，`find` 就是单链表的遍历查找。务必先掌握第 20 章静态链表！

### 4.3 扩展操作：删除

```cpp
// 删除 x（在链表节点上操作）         O(链长) 期望
//   真正摘除节点：找前驱、改指针、打删除标记
bool st[N];  // st[i]=true 表示第 i 个节点已删除
void remove(int x) {
    int t = hashFunc(x);
    for (int i = h[t], pre = -1; i != -1; pre = i, i = ne[i]) {
        if (e[i] == x) {
            if (pre == -1) h[t] = ne[i];   // 删除链头
            else ne[pre] = ne[i];          // 删除中间/尾节点
            st[i] = true;                  // 标记该节点已删除
            return;
        }
    }
}
```

!!! warning "拉链法删除的坑"
    真正"摘除"节点需找到前驱（单链表不能 $O(1)$ 删已知节点）。竞赛题如需删除，常用**打标记法**：另开数组标记某值已删除，查找时跳过。本题 CSP-S 几乎不考，了解即可。

---

## 第五节 开放寻址法（数组模拟）⭐（轨道一·简洁版）

### 5.1 完整模板（线性探测）

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 200003, null = 0x3f3f3f3f;  // null: 表示空桶的哨兵
int h[N];

// 初始化                              O(N)
void init() {
    memset(h, 0x3f, sizeof h);  // 每个桶设为 null（0x3f3f3f3f）
}

// 查找 x：返回 x 应在的位置            O(1) 期望
//   若 h[k]==x，已存在；若 h[k]==null，空位可插入
int find(int x) {
    int t = (x % N + N) % N;
    while (h[t] != null && h[t] != x) {
        t++;                     // 线性探测：往后找
        if (t == N) t = 0;       // 到末尾回卷到 0
    }
    return t;                    // 返回 x 的位置或可插入的空位
}

// 插入 x                              O(1) 期望
void insert(int x) {
    int k = find(x);
    h[k] = x;                    // 若已存在则覆盖（无影响）；若空位则放入
}

int main() {
    init();
    insert(3);
    insert(10);
    insert(100003);              // 与 3 冲突，挤到下一空位
    int k = find(10);
    cout << (h[k] == 10) << "\n"; // 1（存在）
    k = find(5);
    cout << (h[k] == null) << "\n"; // 1（不存在，k 是空位）
    return 0;
}
```

### 5.2 实现要点

1. **表大小开 2~3 倍**：开放寻址法要求装填因子 $\alpha < 0.7$，故 N 取数据量的 2~3 倍的质数。
2. **哨兵 `null`**：用一个"业务中不可能出现的值"表示空桶。`0x3f3f3f3f`（约 $10^9$）常用于 int。
3. **`memset(h, 0x3f, sizeof h)`**：每个字节设 0x3f，4 字节拼成 `0x3f3f3f3f`。
4. **线性探测 + 回卷**：`t++` 后若 `t == N` 回到 0，形成环形。

!!! tip "拉链法 vs 开放寻址法的选择"
    - 拉链法：数组多但思路清晰，删除相对容易，**CSP-S 推荐**。
    - 开放寻址法：只用一个数组，代码更短，但删除麻烦、$\alpha$ 限制严。**适合只需插入+查找的题**。

!!! warning "开放寻址法的删除难题"
    线性探测删除元素不能直接置空（会断裂探测链）。需用"墓碑标记"（tombstone），查找时跳过、插入时可复用。CSP-S 几乎不考开放寻址法的删除，了解即可。

---

## 第六节 STL 实现 ⭐（轨道二）

### 6.1 unordered_set 完整示例

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    unordered_set<int> s;
    
    // 插入
    s.insert(3);
    s.insert(10);
    s.insert(17);
    s.insert(3);                  // 重复插入无效（set 互异）
    
    // 查找
    cout << s.count(10) << "\n";  // 1（存在）
    cout << s.count(5) << "\n";   // 0（不存在）
    
    // 大小与判空
    cout << s.size() << "\n";     // 3（互异）
    cout << s.empty() << "\n";    // 0
    
    // 遍历（顺序不保证！）
    for (int x : s) cout << x << " ";
    cout << "\n";
    
    // 删除
    s.erase(10);
    cout << s.count(10) << "\n";  // 0
    
    s.clear();                    // 清空
    return 0;
}
```

### 6.2 unordered_map 完整示例

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    unordered_map<string, int> mp;
    
    // 插入/修改
    mp["apple"] = 3;
    mp["banana"] = 5;
    mp.insert({"cherry", 2});
    
    // 查找
    cout << mp["apple"] << "\n";          // 3
    cout << mp.count("banana") << "\n";   // 1
    
    // 注意：mp["xxx"] 会在键不存在时自动插入默认值 0
    cout << mp.count("grape") << "\n";    // 0
    cout << mp["grape"] << "\n";          // 0（且 grape 被插入！）
    cout << mp.count("grape") << "\n";    // 1（副作用！）
    
    // 安全查找：用 find，返回迭代器
    auto it = mp.find("pear");
    if (it == mp.end()) cout << "not found\n";
    
    // 遍历
    for (auto &p : mp) cout << p.first << ":" << p.second << " ";
    cout << "\n";
    
    // 删除
    mp.erase("apple");
    return 0;
}
```

### 6.3 STL 哈希容器速查表

| 函数 | `unordered_set` | `unordered_map` | 复杂度 |
|------|----------------|-----------------|--------|
| `insert(x)` | 插入元素 x | 插入键值对 x | $O(1)$ 期望 |
| `erase(x)` | 删除元素 x | 删除键 x | $O(1)$ 期望 |
| `count(x)` | x 的个数（0 或 1） | 键 x 的个数（0 或 1） | $O(1)$ 期望 |
| `find(x)` | 返回 x 的迭代器 | 返回键 x 的迭代器 | $O(1)$ 期望 |
| `size()` | 元素个数 | 键值对数 | $O(1)$ |
| `empty()` | 是否为空 | 是否为空 | $O(1)$ |
| `clear()` | 清空 | 清空 | $O(n)$ |
| `[]` | ❌ 不支持 | 取/插值（**有副作用**） | $O(1)$ 期望 |
| `begin()/end()` | 迭代器 | 迭代器 | — |

!!! warning "`mp[x]` 的副作用陷阱"
    `unordered_map` 用 `mp[x]` 访问不存在的键时，会**自动插入** `{x, 0}`。这会导致：
    - `count` 结果被污染（先 `mp[x]` 再 `count` 永远是 1）
    - 统计题答案偏多
    
    **安全做法**：判断存在性用 `count(x)` 或 `find(x)`；只有确定要插入时才用 `mp[x] =`。

!!! warning "迭代器遍历顺序不保证"
    `unordered_*` 的遍历顺序与插入顺序**无关**，与哈希函数有关。若需按插入顺序或有序遍历，请用 `map`/`set`（红黑树，$O(\log n)$）或 `vector` 配合。

---

## 第七节 双轨对比总结

### 7.1 三种实现横向对比

| 维度 | 拉链法（数组模拟） | 开放寻址法 | STL `unordered_*` |
|------|-------------------|-----------|-------------------|
| 内存 | 数组预分配 | 单数组预分配 | 动态分配 |
| 速度 | 最快 | 快 | 较快（常数大） |
| 出错风险 | 低 | 低 | 最低 |
| 代码量 | 中等 | 短 | 最短 |
| 哈希函数可控 | ✓（自定） | ✓ | ✗（库内部） |
| 删除 | 较易（打标记） | 难（墓碑） | 易（erase） |
| 竞赛适用 | **CSP-S 首选** | 仅插查题 | 简单题/快速实现 |

### 7.2 赛场建议

- **CSP-S 复赛**：数据量大、卡常数时用**拉链法**；时间紧用 STL。
- **CSP-S 初赛**：能读懂两种数组模拟实现，会默写拉链法 `find`/`insert`。
- **快速验证思路**：先用 STL 写一版对拍，再改数组模拟交正式版。
- **需要自定义哈希**（如存 pair、状态压缩）：必须用数组模拟或手写仿函数。

!!! tip "选择口诀"
    "理解用拉链，比赛用拉链，验证用 STL。卡常遇开放，自定义必手写。"

---

## ⚠️ 常见误区

### 误区 1：哈希函数模数取合数

```cpp
// ❌ 错误：N=100000 是合数，键多含因子 2/5 时冲突率高
const int N = 100000;
int hashFunc(int x) { return (x % N + N) % N; }

// ✅ 正确：N 取质数 100003
const int N = 100003;
```

**症状**：数据构造特殊时大量冲突，查找退化为 $O(n)$，TLE。
**预防**：永远模质数。常用质数：100003、200003、9999971。

### 误区 2：负数取模直接用 `x % N`

```cpp
// ❌ 错误：-5 % 3 = -2，负下标越界
int hashFunc(int x) { return x % N; }

// ✅ 正确：先模再加，保证非负
int hashFunc(int x) { return (x % N + N) % N; }
```

**症状**：数组越界，RE 或 WA。
**预防**：哈希函数永远写 `(x % N + N) % N`，养成肌肉记忆。

### 误区 3：拉链法忘记初始化 `h` 为 -1

```cpp
// ❌ 错误：h 全局默认 0，find 时访问 h[0] 的链表，逻辑错乱
int h[N], e[N], ne[N], idx;
int main() {
    // 忘记 init()
    insert(3);          // ne[0] = h[3] = 0（指向自己！）→ 死循环
}

// ✅ 正确：先 init
int main() {
    init();             // memset(h, -1, sizeof h)
    insert(3);
}
```

**症状**：`find` 死循环 TLE，或访问非法下标 RE。
**预防**：`main` 第一行必写 `init()`。

### 误区 4：开放寻址法表开得不够大

```cpp
// ❌ 错误：N 等于数据量，装填因子接近 1，探测极长
const int N = 100003;   // 数据量也是 1e5

// ✅ 正确：N 取数据量 2~3 倍的质数
const int N = 200003;
```

**症状**：插入接近满时探测链很长，$O(n)$ 退化 TLE。
**预防**：开放寻址法 N ≥ 2 × 数据量，且为质数。

### 误区 5：用 `mp[x]` 判断键是否存在

```cpp
// ❌ 错误：mp[x] 会自动插入 {x, 0}
unordered_map<int,int> mp;
if (mp[5] == 0) { /* 误判：5 原本不存在，现在被插入了 */ }

// ✅ 正确：用 count 或 find
if (mp.count(5) == 0) { /* 5 不存在 */ }
auto it = mp.find(5);
if (it == mp.end()) { /* 5 不存在 */ }
```

---

## 【进阶】字符串哈希（P 进制法）

### 8.1 核心思想

把字符串看作一个 **P 进制数**，计算其数值并对 $Q$ 取模，得到哈希值。预处理前缀哈希后，可以 $O(1)$ 比较任意两个子串是否相等。

| 符号 | 含义 | 常用取值 |
|------|------|---------|
| $P$ | 进制 | 131 或 13331（经验值，冲突率低） |
| $Q$ | 模数 | $2^{64}$（用 `unsigned long long` 自然溢出） |
| $h[i]$ | 前 $i$ 个字符的哈希值 | $h[i] = h[i-1] \cdot P + s[i]$ |
| $p[i]$ | $P^i \bmod Q$ | 预处理 |

### 8.2 子串哈希公式

子串 $s[l..r]$（1-based）的哈希值为：

$$\text{hash}(l, r) = h[r] - h[l-1] \cdot P^{r-l+1}$$

直观理解：$h[r]$ 是前 $r$ 位的"P 进制数"，$h[l-1] \cdot P^{r-l+1}$ 把前 $l-1$ 位"左移"对齐到高位，相减就剩中间这段。

```
h[r]     = [s1 s2 ... s(l-1) s(l) ... s(r)]   ← 整段
h[l-1]*P = [s1 s2 ... s(l-1) 0    ... 0   ]   ← 前缀左移
相减     = [                  s(l) ... s(r)]   ← 子串哈希
```

### 8.3 完整模板

```cpp
#include <bits/stdc++.h>
using namespace std;

typedef unsigned long long ULL;
const int N = 100010, P = 131;

int n, m;
char str[N];
ULL h[N], p[N];               // h:前缀哈希  p:P的幂

// 预处理                            O(n)
void init() {
    p[0] = 1;
    for (int i = 1; i <= n; i++) {
        h[i] = h[i-1] * P + str[i];   // 前缀哈希
        p[i] = p[i-1] * P;            // P^i
    }
}

// 查询子串 [l, r] 的哈希值          O(1)
ULL getHash(int l, int r) {
    return h[r] - h[l-1] * p[r - l + 1];
}

int main() {
    scanf("%d%d%s", &n, &m, str + 1);  // 1-based 读入
    init();
    while (m--) {
        int l1, r1, l2, r2;
        scanf("%d%d%d%d", &l1, &r1, &l2, &r2);
        if (getHash(l1, r1) == getHash(l2, r2))
            printf("Yes\n");           // 哈希相同视为字符串相等
        else
            printf("No\n");
    }
    return 0;
}
```

### 8.4 实现要点

1. **用 `unsigned long long`**：自然溢出相当于模 $2^{64}$，省去手动取模，速度快。
2. **1-based 存储**：公式 `h[r] - h[l-1]*p[r-l+1]` 依赖 `h[0]=0`，1-based 更自然。
3. **`P=131` 或 `13331`**：经验值，冲突率极低。配合 $2^{64}$ 模数，几乎不会冲突。
4. **哈希相等 ≠ 字符串相等**：理论上有极小概率冲突。CSP-S 中通常视为相等即可；若担心，可用**双哈希**（两个不同的 P 和 Q）。

!!! warning "哈希冲突与双哈希"
    单哈希有被"卡"的风险（出题人故意构造冲突数据）。稳妥做法是**双哈希**：用两组不同的 $(P, Q)$ 计算两个哈希值，只有两组都相等才视为相等。CSP-S 一般单哈希够用，但若题目刻意卡，需双哈希。

!!! tip "字符串哈希的威力"
    有了 $O(1)$ 子串判等，许多问题迎刃而解：判断回文（正反两套哈希）、找最长重复子串（二分+哈希）、字符串匹配（比 KMP 短得多）。是 CSP-S 字符串题的瑞士军刀。

---

## 典型例题：[P3370] 字符串哈希

**题目链接**：https://www.luogu.com.cn/problem/P3370

**题意**：给定 $N$ 个字符串（总长度 $\le 10^5$），求其中不同字符串的个数。$N \le 10^4$。

**思路**：对每个字符串计算哈希值，所有哈希值排序去重，剩下的就是不同字符串个数。

### 版本一：手写字符串哈希

```cpp
#include <bits/stdc++.h>
using namespace std;

typedef unsigned long long ULL;
const int P = 131;

ULL hashStr(const string &s) {
    ULL h = 0;
    for (char c : s) h = h * P + c;   // P 进制
    return h;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n; cin >> n;
    vector<ULL> v(n);
    for (int i = 0; i < n; i++) {
        string s; cin >> s;
        v[i] = hashStr(s);
    }
    sort(v.begin(), v.end());
    v.erase(unique(v.begin(), v.end()), v.end());
    cout << v.size() << "\n";
    return 0;
}
```

### 版本二：STL unordered_set

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n; cin >> n;
    unordered_set<string> s;
    for (int i = 0; i < n; i++) {
        string t; cin >> t;
        s.insert(t);
    }
    cout << s.size() << "\n";
    return 0;
}
```

!!! tip "双版本对比"
    手写哈希速度快、可控；STL 代码短、不易错。本题 $n \le 10^4$ 两版都能过。数据大时手写更稳。

---

## 📚 本节练习

### 🟢 基础题（巩固模板）

**练习 1**：模拟散列表（AcWing 840）★
> 维护一个集合，支持插入 x、查询 x 是否存在。
- **考点**：拉链法 / 开放寻址法模板默写。
- **提示**：直接套用本章拉链法 `init/find/insert` 三件套，注意模质数 100003。

### 🟡 进阶题（CSP-S 难度）

**练习 2**：[P3370] 字符串哈希 ★★
> 求 $N$ 个字符串中不同字符串的个数。
- **考点**：字符串哈希 + 排序去重，或 STL `unordered_set`。
- **提示**：见本章典型例题。注意用 `unsigned long long` 自然溢出。

**练习 3**：[P2580] 于是他错误的点名开始了 ★★
> 给定 $M$ 个名字的字典，$N$ 次查询。每次查一个名字：不在字典输出 `WRONG`；首次查到输出 `OK`；重复查输出 `REPEAT`。
- **考点**：哈希表（或 Trie）+ 状态标记。
- **提示**：用 `unordered_map<string, int>` 记录每个名字的查询状态（0 未查、1 已查）。`count` 判断是否在字典，`mp[name]` 判断是否重复。也可手写字符串哈希 + 拉链法。

### 🔴 挑战题

**练习 4**：[P1045] 麦森数 ★★★
> 输出 $2^P - 1$ 的位数和最后 500 位（$P \le 3100000$）。
- **考点**：高精度 + 快速幂（**与哈希表关联较弱，作为综合练习**）。
- **提示**：位数用公式 $\lfloor P \log_{10} 2 \rfloor + 1$ 直接算。最后 500 位用高精度快速幂计算 $2^P$，再减 1。注意 $2^P - 1$ 的位数与 $2^P$ 相同（$2^P$ 末位不为 0，减 1 不退位到首位）。此题主要训练高精度与数论，与哈希表配合的是"大整数存储用数组"的思路。

---

## 📖 本章总结

### 思维导图

- **哈希表**
  - **核心概念**
    - 键→值映射，期望 $O(1)$
    - 哈希函数：模质数法 $(x \bmod N + N) \bmod N$
    - 冲突是常态，处理方法是核心
  - **轨道一：数组模拟**
    - 拉链法 ⭐竞赛首选
      - `h[N]` + `e[N]` + `ne[N]` + `idx`
      - `init / find / insert`
      - 桶挂链表，头插法
    - 开放寻址法（线性探测）
      - 单数组 `h[N]` + 哨兵 `null`
      - `find` 探测：`t++; if(t==N) t=0;`
      - 表开 2~3 倍，$\alpha < 0.7$
  - **轨道二：STL**
    - `#include <unordered_set>` / `<unordered_map>`
    - `insert / erase / count / find / size`
    - `mp[x]` 有副作用，判存在用 `count`
  - **进阶：字符串哈希**
    - P 进制法，$P=131$，模 $2^{64}$
    - `h[i] = h[i-1]*P + s[i]`
    - 子串哈希：`h[r] - h[l-1]*p[r-l+1]`
    - $O(1)$ 子串判等
  - **典型应用**
    - P3370 字符串哈希
    - P2580 名字点名
  - **常见陷阱**
    - 模数取合数 → 冲突率上升
    - 负数取模 → 越界
    - 忘记 init → 死循环
    - `mp[x]` 判存在 → 副作用

### 速记口诀

```
哈希四件套：h、e、ne、idx。
模数取质数，负数要补正。
拉链头插法，开放线性探。
STL unordered，count 比 [] 安。
字符串哈希 P 进制，子串判等 O(1) 间。
```

### 复杂度总览

| 操作 | 拉链法 | 开放寻址法 | STL unordered |
|------|--------|-----------|---------------|
| 插入 | $O(1)$ 期望 | $O(1)$ 期望 | $O(1)$ 期望 |
| 查找 | $O(1)$ 期望 | $O(1)$ 期望 | $O(1)$ 期望 |
| 删除 | $O(1)$ 期望（打标记） | 难 | $O(1)$ 期望 |
| 最坏 | $O(n)$ | $O(n)$ | $O(n)$ |
| 子串判等（字符串哈希） | $O(1)$ | — | — |

### 速查表

| 操作 | 拉链法代码 | 开放寻址法代码 | STL 代码 |
|------|-----------|---------------|---------|
| 哈希函数 | `(x%N+N)%N` | `(x%N+N)%N` | 库内部 |
| 初始化 | `memset(h,-1,sz)` | `memset(h,0x3f,sz)` | 构造函数 |
| 插入 | 头插到桶 `h[t]` | `h[find(x)]=x` | `s.insert(x)` |
| 查找 | 遍历桶链 | `h[find(x)]==x` | `s.count(x)` |
| 子串哈希 | `h[r]-h[l-1]*p[r-l+1]` | — | — |

### 知识衔接

本章是**高效查找数据结构**的核心章节，与前后章节的联系如下：

- **前置知识**：第20章链表（拉链法的实现基础）、第16章位运算与离散化（映射思想）、第13章二分查找（对比查找效率）
- **本章定位**：以 $O(1)$ 平均查找复杂度成为查找领域的利器，是离散化思想的通用化与工程化
- **后续发展**：为第22章堆提供查找工具，哈希思想在第23章并查集的路径压缩中也有体现，子串哈希是字符串算法的重要工具

!!! tip "下一章预告"
    下一章我们将学习**堆与优先队列**——CSP-S 高频考查的树形数据结构。堆是贪心、图论（Dijkstra）、动态规划优化的核心工具。哈希表与后续的 **Trie 字典树**（第24章）也是对照结构：Trie 用树形结构实现字符串集合，能 $O(|s|)$ 查找并支持前缀查询，而哈希表 $O(|s|)$ 计算哈希值后 $O(1)$ 查找但不支持前缀。两者各有千秋，务必都掌握！
