# 第57章 概率与期望DP
`[CSP-S 进阶]` 难度7

!!! info "本章内容"
    本章讲解概率论基础（样本空间、事件、概率公理、条件概率、独立性），期望及其线性性，以及概率DP（从起点正推）和期望DP（从终点倒推）两种核心模型。涉及掷骰子、走迷宫、物品掉落等经典场景，是 CSP-S 进阶的重要内容。

---

## 📌 学习目标

1. 能够说出概率公理三条（非负性、规范性、可列可加性），并能判断一个函数是否为概率
2. 能够计算条件概率 $P(A|B) = P(AB)/P(B)$，并能判断两个事件是否独立
3. 能够运用期望的线性性 $E[X+Y]=E[X]+E[Y]$（即使 $X,Y$ 不独立也成立）简化计算
4. 能够写出期望DP的倒推转移方程：$E[u] = \sum p(u \to v) \cdot (w(u,v) + E[v])$
5. 能够写出概率DP的正推转移方程，并区分期望DP与概率DP的方向
6. 能够解决"掷骰子""走迷宫""物品掉落"三类经典概率/期望模型问题

## 💡 生活类比

!!! tip "生活类比：规划回家路线与买彩票"
    **期望DP** 像"规划回家路线"——从终点倒着算，每个路口的期望回家时间 = 当前路段时间 + 下一个路口的期望时间。因为终点是确定的（期望为0），所以从终点一步步倒推到起点，就能算出起点的期望时间。

    **期望的线性性** 像"买彩票"——总奖金 = 一等奖奖金 + 二等奖奖金 + ... + 末等奖奖金，不管这些奖项之间是否有关联。期望也可以这样"拆开"求和，即使两个随机变量不独立，和的期望仍然等于期望之和。

## 核心内容

### 57.1 概率基础

**样本空间** $\Omega$：所有可能结果的集合。
**事件** $A$：样本空间的子集，即一些结果的集合。
**概率** $P$：从事件到 $[0,1]$ 的函数，满足三条**概率公理**：

1. **非负性**：$P(A) \geq 0$
2. **规范性**：$P(\Omega) = 1$
3. **可列可加性**：若 $A_1, A_2, \dots$ 两两互斥，则 $P(\bigcup A_i) = \sum P(A_i)$

### 57.2 条件概率与独立性

**条件概率**：在事件 $B$ 已发生的条件下，事件 $A$ 发生的概率：

$$P(A | B) = \frac{P(AB)}{P(B)}, \quad P(B) > 0$$

**独立性**：若 $P(AB) = P(A) \cdot P(B)$，则称 $A, B$ 独立。等价于 $P(A|B) = P(A)$。

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    // 掷两枚骰子，求已知点数和>7的条件下，第一枚为4的概率
    // 样本空间 36 种，和>7 的有 (2,6)(3,5)(3,6)(4,4)(4,5)(4,6)(5,3)(5,4)(5,5)(5,6)(6,2)(6,3)(6,4)(6,5)(6,6) 共15种
    // 其中第一枚为4的有 (4,4)(4,5)(4,6) 共3种
    // P = 3/15 = 1/5
    int favorable = 0, total = 0;
    for (int i = 1; i <= 6; i++)
        for (int j = 1; j <= 6; j++)
            if (i + j > 7) {
                total++;
                if (i == 4) favorable++;
            }
    cout << favorable << "/" << total << " = " << (double)favorable / total << "\n";
    return 0;
}
// 输入：（无输入）
// 输出：3/15 = 0.2
```

### 57.3 期望及其线性性

**期望**：离散随机变量 $X$ 的期望 $E[X] = \sum_x x \cdot P(X = x)$。

**期望的线性性**（最重要性质）：

$$E[X + Y] = E[X] + E[Y]$$

无论 $X, Y$ 是否独立都成立。这是期望DP的理论基础。

若 $X, Y$ 独立，还有 $E[XY] = E[X] \cdot E[Y]$。

**全期望公式**：$E[X] = \sum_i P(B_i) \cdot E[X | B_i]$，其中 $\{B_i\}$ 是对样本空间的一个划分。

### 57.4 期望DP：从终点倒推

期望DP通常求"从某状态到终点的期望步数/代价"。由于终点的期望为0，我们从终点倒推到起点。

**通用转移**：设 $E[u]$ 为从状态 $u$ 到终点的期望代价，则

$$E[u] = \sum_{v} P(u \to v) \cdot \big(w(u, v) + E[v]\big)$$

其中 $P(u \to v)$ 是从 $u$ 转移到 $v$ 的概率，$w(u,v)$ 是这次转移的代价。

**关键**：终点 $E[\text{终点}] = 0$，按拓扑逆序（或从后往前）计算。

### 57.5 概率DP：从起点正推

概率DP通常求"从起点出发到达各状态的概率"。从起点正推。

**通用转移**：设 $P[u]$ 为到达状态 $u$ 的概率，则

$$P[v] \mathrel{+}= P[u] \cdot P(u \to v)$$

**关键**：起点 $P[\text{起点}] = 1$，按拓扑序（或从前向后）计算。

### 57.6 概率与期望的区别和联系

| 方面 | 概率 | 期望 |
|------|------|------|
| 含义 | 某事件发生的可能性 | 随机变量的"平均值" |
| DP方向 | 从起点正推 | 从终点倒推 |
| 初始值 | $P[\text{起点}]=1$ | $E[\text{终点}]=0$ |
| 转移 | $P[v] += P[u] \cdot p$ | $E[u] = \sum p \cdot (w + E[v])$ |
| 联系 | 期望 $= \sum x \cdot P(X=x)$ | 概率是期望的特殊形式 |

注意：期望 $E[X] = \sum x \cdot P(X=x)$，所以"求期望"本质上是在"求概率"的基础上加权求和。当所有可能值都是 0 或 1 时，期望就等于概率。

### 57.7 常见模型

#### 模型1：掷骰子

期望掷几次骰子才能得到连续 $n$ 个 6。

设 $E[i]$ 为已连续掷出 $i$ 个 6，到达到 $n$ 个的期望次数：

$$E[i] = \frac{1}{6}(1 + E[i+1]) + \frac{5}{6}(1 + E[0])$$

边界 $E[n] = 0$，倒推求解。

#### 模型2：走迷宫

DAG 上从起点到终点的期望路径长度，每条出边等概率。$E[u] = \frac{1}{deg(u)} \sum_{v}(w + E[v])$。

#### 模型3：物品掉落

每次操作以概率 $p$ 掉落物品，求期望操作次数使物品数 $\geq k$。$E[k] = 1 + p \cdot E[k-1] + (1-p) \cdot E[k]$（注意自环项需移项处理）。

```cpp
#include <bits/stdc++.h>
using namespace std;
// 模型3简化版：每次以概率 p 成功，求期望次数使成功1次（几何分布）
// E = 1/p
int main() {
    double p = 0.25;   // 每次成功概率 1/4
    // E[成功1次] = 1 + (1-p)*E[成功1次] => E = 1/p
    cout << fixed << setprecision(4) << 1.0 / p << "\n";
    return 0;
}
// 输入：（无输入）
// 输出：4.0000
```

### 57.8 期望DP中的自环处理

当转移中出现 $E[u] = a \cdot E[u] + b$（自环）时，移项得 $E[u] = \frac{b}{1-a}$。这是处理带"失败重来"的期望问题的标准技巧。

```cpp
#include <bits/stdc++.h>
using namespace std;
// 期望掷骰子直到出现6，期望次数 = 1/(1/6) = 6
int main() {
    double p = 1.0 / 6;   // 成功概率
    // E = 1 + (1-p)*E  =>  E = 1/p
    double E = 1.0 / p;
    cout << fixed << setprecision(4) << E << "\n";
    return 0;
}
// 输入：（无输入）
// 输出：6.0000
```

## 常见误区

1. **误区**：期望DP从起点正推。
   **纠正**：期望DP一般从**终点倒推**，因为终点的期望确定（为0），而起点的期望未知。概率DP才是从起点正推。

2. **误区**：认为 $E[X+Y] = E[X]+E[Y]$ 只在 $X,Y$ 独立时成立。
   **纠正**：期望的线性性**无需独立性**，这是期望最强大的性质。只有 $E[XY]=E[X]E[Y]$ 才需要独立性。

3. **误区**：遇到自环 $E[u] = aE[u]+b$ 不知道怎么处理。
   **纠正**：移项得 $E[u] = b/(1-a)$，其中 $a$ 是回到自身的概率。

4. **误区**：概率DP转移忘记累加，写成赋值。
   **纠正**：$P[v] += P[u] \cdot p$（累加），因为可能从多个 $u$ 到达 $v$。

5. **误区**：在有环的图上直接倒推期望。
   **纠正**：有环时需用高斯消元解方程组，或利用自环移项技巧。简单倒推仅适用于 DAG。

## 典型例题

### 例题1：P1365 WJMZBMR打osu（期望DP）

**题意**：有 $n$ 次点击，每次以概率 $p_i$ 成功（得1分），$1-p_i$ 失败（得0分并断开连续段）。连续 $x$ 次成功的得分为 $x^2$（即每次成功累加 $2x-1$）。求总期望得分。

**思路**：设 $E1[i]$ 为前 $i$ 次操作结束时，以第 $i$ 次成功结尾的连续成功长度的期望；$E[i]$ 为前 $i$ 次操作的总期望得分。

- 若第 $i$ 次成功：连续长度从 $E1[i-1]$ 变为 $E1[i-1]+1$，得分增加 $(E1[i-1]+1)^2 - E1[i-1]^2 = 2E1[i-1]+1$。
- $E1[i] = p_i \cdot (E1[i-1] + 1)$
- $E[i] = E[i-1] + p_i \cdot (2 \cdot E1[i-1] + 1)$

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n;
    cin >> n;
    double E = 0, E1 = 0;   // E: 总期望得分; E1: 以当前位置结尾的连续成功长度期望
    for (int i = 1; i <= n; i++) {
        double p;
        cin >> p;
        E += p * (2.0 * E1 + 1.0);   // 期望增加的得分
        E1 = p * (E1 + 1.0);          // 更新连续长度期望
    }
    cout << fixed << setprecision(4) << E << "\n";
    return 0;
}
// 输入：3
//       1.0 0.5 0.5
// 输出：4.0000
// 说明：第1次必成功(+1)；第2次50%成功则+3(2·1+1=3)，50%失败(+0)；
//       第3次50%成功则+3(2·1+1=3)，50%失败(+0)。E=1+1.5+1.5=4.0
```

### 例题2：P4316 绿豆蛙的归宿（期望DP倒推）

**题意**：给定一个 $n$ 个点的 DAG，起点为1，终点为 $n$，每条边有长度。从每个点出发等概率随机选一条出边走。求从起点到终点的期望路径长度。

**思路**：期望DP倒推。$E[u] = \frac{1}{deg(u)} \sum_{(u,v,w)} (w + E[v])$，边界 $E[n]=0$。按拓扑逆序计算。

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 100005, M = 200005;
int head[N], nxt[M], to[M], w[M], deg[N], etot;
double E[N];
void addEdge(int u, int v, int c) {
    to[++etot] = v; w[etot] = c; nxt[etot] = head[u]; head[u] = etot;
    deg[u]++;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, m;
    cin >> n >> m;
    for (int i = 1; i <= m; i++) {
        int u, v, c;
        cin >> u >> v >> c;
        addEdge(u, v, c);
    }
    // 拓扑逆序：从 n 倒推到 1（DAG，n 是终点无出边）
    // 用拓扑排序获取逆序
    vector<int> topo;
    vector<int> inDeg(n + 1, 0);
    // 重新计算入度用于拓扑
    for (int u = 1; u <= n; u++)
        for (int e = head[u]; e; e = nxt[e]) inDeg[to[e]]++;
    queue<int> que;
    for (int i = 1; i <= n; i++) if (inDeg[i] == 0) que.push(i);
    while (!que.empty()) {
        int u = que.front(); que.pop();
        topo.push_back(u);
        for (int e = head[u]; e; e = nxt[e]) {
            if (--inDeg[to[e]] == 0) que.push(to[e]);
        }
    }
    // 逆序处理
    for (int i = n - 1; i >= 0; i--) {
        int u = topo[i];
        if (deg[u] == 0) { E[u] = 0; continue; }
        double sum = 0;
        for (int e = head[u]; e; e = nxt[e])
            sum += w[e] + E[to[e]];
        E[u] = sum / deg[u];
    }
    cout << fixed << setprecision(2) << E[1] << "\n";
    return 0;
}
// 输入：4 4
//       1 2 1
//       1 3 2
//       2 3 3
//       3 4 1
// 输出：4.00
// 说明：1->2->3->4: 1+3+1=5; 1->3->4: 2+1=3; 从1等概率选2或3
//       E[1] = 0.5*(1+E[2]) + 0.5*(2+E[3])
//       E[2] = 3+E[3]; E[3]=1; E[2]=4; E[1]=0.5*5+0.5*3=4
```

## 📖 本章总结

- 概率与期望DP
  - 概率基础
    - 样本空间、事件、概率公理（非负/规范/可加）
    - 条件概率 $P(A|B)=P(AB)/P(B)$
    - 独立性 $P(AB)=P(A)P(B)$
  - 期望
    - 定义 $E[X]=\sum x \cdot P(X=x)$
    - 线性性 $E[X+Y]=E[X]+E[Y]$（无需独立）
    - 全期望公式
  - 期望DP（倒推）
    - $E[u] = \sum p(u \to v)(w + E[v])$
    - 终点 $E=0$，拓扑逆序计算
    - 自环处理：$E=b/(1-a)$
  - 概率DP（正推）
    - $P[v] += P[u] \cdot p$
    - 起点 $P=1$，拓扑序计算
  - 概率与期望的联系
    - 期望 = 概率的加权求和
    - 概率是期望的特例（0/1变量）
  - 经典模型
    - 掷骰子、走迷宫、物品掉落

### 知识衔接

本章是考纲补强卷的第七章，与前后章节的联系如下：

- **前置知识**：第41章DP入门（DP状态设计与转移框架）、第38章组合数与容斥原理（概率与计数基础）、第43章线性DP（DP转移方程的推导方法）
- **本章定位**：讲解概率与期望的DP计算，涵盖从终点倒推的期望DP和从起点正推的概率DP，是数学与DP的交叉考点
- **后续发展**：本章为第58章平衡树基础概念铺路，全书正文接近尾声

!!! info "下一章预告"
    下一章我们将学习 **平衡树基础概念**，包括二叉搜索树（BST）的定义与退化问题、AVL树、Treap、Splay、替罪羊树等平衡树的原理，以及在 CSP-S 中如何用 STL 的 set/map 替代手写平衡树。全书正文到此接近尾声。
