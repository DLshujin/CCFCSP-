# 第48章 贪心算法进阶

> 当贪心从"凭直觉选"升级为"用证明说话"，你就真正跨进了 CSP-S 的核心战场。本章将系统梳理区间贪心三大模型、Huffman 编码、排序不等式、绝对值不等式与推公式类贪心，每一种策略都配套完整的正确性证明思路，让你在考场上"敢用、会用、用得对"。

!!! info "本章内容"
    - 贪心算法的核心：局部最优 → 全局最优，正确性证明是灵魂
    - 区间贪心三大模型：区间选点、区间覆盖、区间分组的排序策略对比
    - Huffman 树与 Huffman 编码：贪心 + 优先队列的经典联姻
    - 排序不等式与推公式类问题：交换相邻元素分析法
    - 绝对值不等式：中位数最优的直观解释
    - 国王游戏排序规则的完整推导（按 a×b 排序）

---

## 📌 学习目标

学完本章后，你应当能够：

1. **准确区分**区间选点、区间覆盖、区间分组三类问题的排序准则（右端点 / 左端点 / 双关键字），不再混淆。
2. **独立编写**三类区间贪心的标准模板代码，并在 20 分钟内 AC 一道模板题。
3. **用交换论证法**推导国王游戏中"按 a×b 升序排序"的贪心策略，并解释为何不能按 a 或 b 单独排序。
4. **解释**Huffman 编码贪心策略的正确性思路（每次合并最小两个），并能用优先队列实现 O(n log n) 算法。
5. **运用**绝对值不等式和中位数性质解决"仓库选址"类问题，并说出中位数最优的直观原因。

---

## 💡 生活类比

打个比方，贪心算法进阶就像是一位**老练的物流调度员**：

- **区间选点**：你要在一条公路上设最少的加油站，让所有过往车辆（区间）都能加上油。聪明的做法是"把站建在最早结束的那趟车的终点附近"——这样这一站能照顾到尽可能多的车。
- **区间覆盖**：你要用最少的清扫车把一条长街从头到尾扫干净，每辆车负责一段。聪明的做法是"按出发位置排序，每次贪心地让当前这辆车扫得尽量远"。
- **Huffman 编码**：你要把一堆频率不同的字符压缩成 01 串。出现频率高的字符应该用短编码，频率低的用长编码——就像快递分拣，把"最轻的两个包裹先合并打包"，重复合并直到只剩一个大包，自然就形成了最优编码树。
- **国王游戏**：国王要给排成一队的大臣发奖，每个人的奖金取决于前面所有人左手数字的乘积除以自己右手数字。排队顺序不同，结果天差地别——这就需要"交换相邻两人看哪个更优"来推出最优排序。

贪心的核心是**策略 + 证明**：策略告诉你怎么做，证明告诉你为什么对。本章每一种贪心都配套证明思路，杜绝"凭感觉"。

---

## 核心概念

### 48.1 贪心算法的核心思想回顾

**贪心算法**：在对问题求解时，总是做出在当前看来最好的选择，期望通过局部最优决策累积成全局最优解。

贪心算法的两个关键问题：

1. **策略是什么**：每一步按什么规则做选择？
2. **为什么是对的**：凭什么局部最优能保证全局最优？

!!! warning "贪心的命门：正确性证明"
    贪心算法最大的陷阱不是"想不出策略"，而是"想出的策略根本不对"。许多看起来合理的贪心策略实际上是错的。因此，**每一个贪心策略都必须经过证明**才能放心使用。

    常用的证明方法：
    - **交换论证**：假设最优解与贪心解不同，通过交换相邻元素证明可调整为贪心解而不变差。
    - **反证法**：假设贪心解不是最优解，推出矛盾。
    - **数学归纳法**：证明前 k 步贪心选择构成最优子结构。

### 48.2 区间贪心三大模型

区间问题是贪心算法最经典的战场。CSP-S 中常见的区间问题有三类，它们的排序准则各不相同，**绝不能混淆**。

#### 48.2.1 区间选点问题

**问题**：给定 n 个闭区间 `[a_i, b_i]`，在数轴上选尽量少的点，使得每个区间内至少有一个点（含端点）。

**贪心策略**：按**右端点**升序排序，每次选择当前区间的右端点作为选点。

**正确性证明思路（交换论证）**：
- 设最优解选了点集 S，贪心解选了点集 G。
- 考虑第一个区间，贪心选其右端点 `r_1`。若最优解在此区间内选的点不是 `r_1`，设为 `p < r_1`。
- 将 `p` 替换为 `r_1`：由于后续区间的右端点 ≥ `r_1`，且按右端点排序后后续区间与 `[a_1, r_1]` 相交意味着 `a_i ≤ r_1`，所以 `r_1` 仍能覆盖这些区间。
- 替换后点数不变且仍为合法解，反复调整可得贪心解不劣于最优解。

**算法流程**：

```
1. 将所有区间按右端点升序排序
2. 初始化 ans = 0, last = -∞
3. 对每个区间 [a_i, b_i]：
   若 a_i > last（当前区间未被覆盖）：
       ans++, last = b_i（选右端点）
4. 输出 ans
```

#### 48.2.2 区间覆盖问题

**问题**：给定 n 个闭区间 `[a_i, b_i]` 和一个目标区间 `[s, t]`，选尽量少的区间完全覆盖 `[s, t]`。

**贪心策略**：按**左端点**升序排序，每次在能接上当前覆盖末尾的区间中，选右端点最远的那个。

**正确性证明思路**：
- 按左端点排序后，能覆盖当前位置 `pos` 的区间构成一个连续段。
- 在这些区间中选右端点最大的，能让覆盖范围延伸最远，从而减少所需区间数。
- 这是典型的"贪心延伸"——每步延伸最远不会比延伸更近差。

**算法流程**：

```
1. 将所有区间按左端点升序排序
2. 初始化 ans = 0, pos = s, i = 0
3. 当 pos < t：
   在所有 a_i ≤ pos 的区间中，找 b_i 最大的，记为 maxR
   若 maxR ≤ pos：无解，输出 -1 并结束
   否则：ans++, pos = maxR
4. 输出 ans
```

#### 48.2.3 区间分组问题

**问题**：给定 n 个闭区间，将它们分成尽量少的组，使得每组内的区间互不相交。

**贪心策略**：按**左端点**升序排序，用小根堆维护每组的结束时间。对每个区间，若能加入结束最早的组（`堆顶 ≤ a_i`），则更新该组结束时间；否则开新组。

**正确性证明思路**：
- 若当前区间不能加入结束最早的组，则它必定与所有现有组都相交，必须开新组。
- 若能加入结束最早的组而不加入，反而开新组，只会增加组数，不优。

#### 48.2.4 三种模型排序策略对比

| 模型 | 目标 | 排序准则 | 贪心动作 | 典型例题 |
|------|------|----------|----------|----------|
| 区间选点 | 最少点覆盖所有区间 | 右端点升序 | 选当前区间右端点 | 射击气球 |
| 区间覆盖 | 最少区间覆盖目标区间 | 左端点升序 | 选能接上且右端最远 | 种树 |
| 区间分组 | 最少组使组内不相交 | 左端点升序 + 小根堆 | 加入结束最早的组 | 会议分配 |
| 选最多不相交区间 | 最多不相交区间数 | 右端点升序 | 选当前区间后跳到其右端 | P1803 线段覆盖 |

!!! tip "记忆口诀"
    - **选点**：右端点排序，选右端点（让点尽量靠右，覆盖更多区间）
    - **覆盖**：左端点排序，延伸右端点（让每段尽量长）
    - **分组**：左端点排序，小根堆维护组结束时间
    - **选最多**：右端点排序，选完跳右端

### 48.3 Huffman 树与 Huffman 编码

#### 48.3.1 问题引入

给定 n 个字符及其出现频率 `f_i`，为每个字符设计一个二进制编码，使得总编码长度 `Σ f_i × len_i` 最小，且任意字符的编码不是另一个字符的前缀（前缀码）。

#### 48.3.2 贪心策略

**Huffman 算法**：每次从所有待合并节点中选出**频率最小的两个**合并，合并后的新节点频率为两者之和，放回集合中。重复直到只剩一个节点。

**正确性证明思路（交换论证 + 最优子结构）**：
1. **引理**：最优前缀码对应的二叉树中，频率最小的两个字符一定是深度最大的兄弟节点。
   - 若不然，将深度较大的高频字符与深度较浅的低频字符交换，总编码长度减少，矛盾。
2. **最优子结构**：合并最小两个节点后，子问题（n-1 个节点）的最优解 + 这次合并的代价 = 原问题最优解。
3. 由引理，贪心地合并最小两个是安全的（不会丢失最优解）。

#### 48.3.3 算法实现

用**小根堆（优先队列）**维护所有待合并节点，每次取出最小的两个合并，将新节点入堆。

- 时间复杂度：O(n log n)
- 空间复杂度：O(n)

**合并果子问题**（P1090）是 Huffman 算法的直接应用：合并 n 堆果子，每次合并两堆的代价为两堆之和，求最小总代价。

### 48.4 排序不等式

**排序不等式**：对于两个有序序列 `a_1 ≤ a_2 ≤ ... ≤ a_n` 和 `b_1 ≤ b_2 ≤ ... ≤ b_n`，有：

```
逆序和 ≤ 乱序和 ≤ 顺序和
即：Σ a_i × b_{n+1-i} ≤ Σ a_i × b_{σ(i)} ≤ Σ a_i × b_i
```

**直观理解**：大的配大的、小的配小的，乘积之和最大；大的配小的，乘积之和最小。

**应用**：排队打水问题（让打水时间短的人先打，总等待时间最小）、国王游戏等。

### 48.5 绝对值不等式与中位数

#### 48.5.1 问题

在数轴上选一个点 x，使得 `Σ |x - a_i|` 最小，求最小值。

#### 48.5.2 结论与证明

**结论**：x 取所有 `a_i` 的**中位数**时，`Σ |x - a_i|` 最小。

**证明思路**：
- 将 `a_i` 排序后，`|x - a_1| + |x - a_n|` 的最小值在 `x ∈ [a_1, a_n]` 时取得，为 `a_n - a_1`。
- 同理，`|x - a_2| + |x - a_{n-1}|` 在 `x ∈ [a_2, a_{n-1}]` 时取得最小值 `a_{n-1} - a_2`。
- 不断向中间收缩，所有区间的交集即为中位数所在的点。
- 因此 x 取中位数时，总和最小。

**直观解释**：中位数是"居中"的位置，向左移一点，左边少走的距离 < 右边多走的距离（若右边点数 ≥ 左边），所以不会更优。

#### 48.5.3 扩展

若求 `Σ |x - a_i|` 的最小值，排序后取中位数 `a_{(n+1)/2}`（n 为奇数）或 `a_{n/2}` 到 `a_{n/2+1}` 之间任意值（n 为偶数）。

### 48.6 推公式类贪心：国王游戏

#### 48.6.1 问题

国王左手 a_0，右手 b_0。n 个大臣排成一队，第 i 个大臣左手 a_i，右手 b_i。每位大臣获得的金币数为：**前面所有人（含国王）左手数字之积 ÷ 自己右手数字**向下取整。求重新排列大臣顺序，使获得金币最多的大臣获得金币数最少。

#### 48.6.2 排序规则推导（交换相邻元素分析法）

设前 i-1 个人的左手乘积为 S。考察相邻两个大臣 i 和 i+1：

**原顺序（i 在前）**：
- 大臣 i 获得金币：`S / b_i`
- 大臣 i+1 获得金币：`S × a_i / b_{i+1}`
- 两人最大值：`max(S / b_i, S × a_i / b_{i+1})`

**交换后（i+1 在前）**：
- 大臣 i+1 获得金币：`S / b_{i+1}`
- 大臣 i 获得金币：`S × a_{i+1} / b_i`
- 两人最大值：`max(S / b_{i+1}, S × a_{i+1} / b_i)`

**比较**：我们希望原顺序不劣于交换后。由于 `S × a_i / b_{i+1} ≥ S / b_{i+1}`（因 `a_i ≥ 1`），且 `S × a_{i+1} / b_i ≥ S / b_i`，所以：

- 原顺序最大值 = `S × a_i / b_{i+1}`（较大项）
- 交换后最大值 = `S × a_{i+1} / b_i`（较大项）

希望原顺序更优，即 `S × a_i / b_{i+1} ≤ S × a_{i+1} / b_i`，化简得：

```
a_i × b_i ≤ a_{i+1} × b_{i+1}
```

即**按 `a × b` 升序排序**。

#### 48.6.3 高精度处理

由于乘积可能极大（n ≤ 1000，每个 a_i, b_i ≤ 10000），需要用**高精度**计算。这是本题的另一难点。

!!! warning "为什么不能按 a 或 b 单独排序"
    - 按 a 升序：考虑 a_i 小但 b_i 极大的大臣排前面，他获得 `S/b_i` 很少，但后面的人会因为他的小 a 而积变小——看似有利，但若后面有 `a_{i+1}` 大而 `b_{i+1}` 极小的人，他会拿走巨额金币。
    - 按 b 升序：同理可构造反例。
    - 只有 `a × b` 同时约束"自己拿得少"和"不让后面的人拿得多"，才能保证全局最优。

### 48.7 推公式类贪心：耍杂技的牛

#### 48.7.1 问题

n 头牛叠罗汉，第 i 头牛重量 `W_i`，力量 `S_i`（能承受的最大压力）。一头牛的危险值 = 其上方所有牛的总重量 - 自身力量。求一种排列使最大危险值最小。

#### 48.7.2 排序规则推导

设上方牛总重为 `G`。考察相邻两头牛 i（上）和 j（下）：

**原顺序（i 在上）**：
- 牛 i 危险值：`G - S_i`
- 牛 j 危险值：`G + W_i - S_j`
- 最大值：`max(G - S_i, G + W_i - S_j)`

**交换后（j 在上）**：
- 牛 j 危险值：`G - S_j`
- 牛 i 危险值：`G + W_j - S_i`
- 最大值：`max(G - S_j, G + W_j - S_i)`

由于 `G + W_i - S_j > G - S_j` 且 `G + W_j - S_i > G - S_i`，较大项分别是 `G + W_i - S_j` 和 `G + W_j - S_i`。

希望原顺序更优：`G + W_i - S_j ≤ G + W_j - S_i`，化简得：

```
W_i + S_i ≤ W_j + S_j
```

即**按 `W + S` 升序排序**。

!!! tip "推公式类贪心的通用方法"
    1. 假设相邻两个元素，写出交换前后的目标函数值。
    2. 令"原顺序不劣于交换后"，化简不等式。
    3. 得到的排序规则即为贪心策略。
    4. 用交换论证说明：任何不满足此顺序的排列都可通过相邻交换变优，故排序后最优。

---

## 代码实现

### 48.8 区间选点模板

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int n;
struct Range {
    int a, b;  // 区间 [a, b]
    bool operator<(const Range &o) const {
        return b < o.b;  // 按右端点升序
    }
} r[N];

int main() {
    cin >> n;
    for (int i = 0; i < n; i++) cin >> r[i].a >> r[i].b;
    sort(r, r + n);
    int ans = 0, last = -2e9;  // last 记录上一个选点位置
    for (int i = 0; i < n; i++) {
        if (r[i].a > last) {   // 当前区间未被覆盖
            ans++;             // 选一个点
            last = r[i].b;     // 选在右端点
        }
    }
    cout << ans << endl;
    return 0;
}
```

### 48.9 区间覆盖模板

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int n, L, R;  // 目标区间 [L, R]
struct Range {
    int a, b;
    bool operator<(const Range &o) const {
        return a < o.a;  // 按左端点升序
    }
} r[N];

int main() {
    cin >> n >> L >> R;
    for (int i = 0; i < n; i++) cin >> r[i].a >> r[i].b;
    sort(r, r + n);
    int ans = 0, pos = L, i = 0;
    while (pos < R) {
        int maxR = pos;  // 本轮能延伸到的最远右端
        // 在所有左端 ≤ pos 的区间中，选右端最大的
        while (i < n && r[i].a <= pos) {
            maxR = max(maxR, r[i].b);
            i++;
        }
        if (maxR == pos) {  // 无法延伸，无解
            cout << -1 << endl;
            return 0;
        }
        ans++;
        pos = maxR;  // 贪心地延伸到最远
    }
    cout << ans << endl;
    return 0;
}
```

### 48.10 区间分组模板

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 1e5 + 10;
int n;
struct Range {
    int a, b;
    bool operator<(const Range &o) const {
        return a < o.a;  // 按左端点升序
    }
} r[N];

int main() {
    cin >> n;
    for (int i = 0; i < n; i++) cin >> r[i].a >> r[i].b;
    sort(r, r + n);
    priority_queue<int, vector<int>, greater<int>> pq;  // 小根堆维护各组结束时间
    for (int i = 0; i < n; i++) {
        if (!pq.empty() && pq.top() < r[i].a) {
            pq.pop();  // 能加入结束最早的组，更新该组结束时间
        }
        pq.push(r[i].b);  // 否则开新组（或更新后重新入堆）
    }
    cout << pq.size() << endl;  // 堆大小即组数
    return 0;
}
```

### 48.11 Huffman / 合并果子模板

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    // 小根堆：每次取最小的两个合并
    priority_queue<long long, vector<long long>, greater<long long>> pq;
    for (int i = 0; i < n; i++) {
        long long x;
        cin >> x;
        pq.push(x);
    }
    long long ans = 0;
    while (pq.size() >= 2) {
        long long x = pq.top(); pq.pop();  // 最小
        long long y = pq.top(); pq.pop();  // 次小
        ans += x + y;                       // 合并代价
        pq.push(x + y);                     // 新堆入堆
    }
    cout << ans << endl;
    return 0;
}
```

### 48.12 国王游戏（高精度版）

```cpp
#include <bits/stdc++.h>
using namespace std;

// 高精度乘法：大数 a × 小整数 b
vector<int> mul(vector<int> &a, int b) {
    vector<int> c;
    int t = 0;
    for (int i = 0; i < (int)a.size() || t; i++) {
        if (i < (int)a.size()) t += a[i] * b;
        c.push_back(t % 10);
        t /= 10;
    }
    while (c.size() > 1 && c.back() == 0) c.pop_back();
    return c;
}

// 高精度除法：大数 a ÷ 小整数 b，返回商
vector<int> div(vector<int> &a, int b) {
    vector<int> c;
    int r = 0;
    for (int i = a.size() - 1; i >= 0; i--) {
        r = r * 10 + a[i];
        c.push_back(r / b);
        r %= b;
    }
    reverse(c.begin(), c.end());
    while (c.size() > 1 && c.back() == 0) c.pop_back();
    return c;
}

// 高精度比较：a < b ?
bool less_than(const vector<int> &a, const vector<int> &b) {
    if (a.size() != b.size()) return a.size() < b.size();
    for (int i = a.size() - 1; i >= 0; i--)
        if (a[i] != b[i]) return a[i] < b[i];
    return false;
}

void print(const vector<int> &a) {
    for (int i = a.size() - 1; i >= 0; i--) putchar('0' + a[i]);
    putchar('\n');
}

const int N = 1005;
int n;
struct Person {
    int a, b;
    bool operator<(const Person &o) const {
        return (long long)a * b < (long long)o.a * o.b;  // 按 a*b 升序
    }
} p[N];

int main() {
    cin >> n;
    for (int i = 0; i <= n; i++) cin >> p[i].a >> p[i].b;  // 第0个是国王
    sort(p + 1, p + n + 1);  // 大臣排序，国王不动
    vector<int> product = {p[0].a};  // 国王左手数字（高精度）
    vector<int> ans = {0};           // 答案初始化为0
    for (int i = 1; i <= n; i++) {
        vector<int> coins = div(product, p[i].b);  // 前面积积 / b_i
        if (less_than(ans, coins)) ans = coins;     // 取最大值
        product = mul(product, p[i].a);             // 更新乘积
    }
    print(ans);
    return 0;
}
```

### 48.13 仓库选址（绝对值不等式）

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<long long> a(n);
    for (int i = 0; i < n; i++) cin >> a[i];
    sort(a.begin(), a.end());
    long long median = a[n / 2];  // 中位数（偶数取 a[n/2] 或 a[n/2-1] 均可）
    long long ans = 0;
    for (int i = 0; i < n; i++)
        ans += abs(a[i] - median);  // 每个点到中位数距离之和
    cout << ans << endl;
    return 0;
}
```

---

## ⚠️ 常见误区

### 误区1：贪心策略未经证明直接使用

**错误**：看到题目感觉"按某个关键字排序就行"，不验证就写代码。

**后果**：贪心策略可能是错的，样例过了但正式数据全 WA。

**修复**：每一个贪心策略都要尝试用交换论证或反证法证明。若证不出来，考虑是否该用 DP 或二分。

### 误区2：区间问题排序规则用错

**错误**：区间选点用了左端点排序，或区间覆盖用了右端点排序。

**后果**：策略完全失效，得到错误答案。

**修复**：牢记口诀——**选点按右端点，覆盖按左端点，分组按左端点 + 堆**。理解每种排序的必要性：选点要让点尽量靠右覆盖更多区间；覆盖要让每段从左到右贪心延伸。

### 误区3：国王游戏只按 a 或 b 排序

**错误**：直觉认为"左手大的排后面"或"右手大的排前面"。

**后果**：可构造反例证伪。例如两大臣 `(a=1,b=100)` 和 `(a=100,b=1)`：
- 按 a 排序：第一个人 `(1,100)` 在前，第二人得 `1×1/1=1`，第一人得 `1/100=0`，最大 1。
- 按 a×b 排序：两人 `a×b` 都=100，顺序不变，结果相同。
- 但若 `(a=2,b=100)` 和 `(a=100,b=1)`：按 a 排序第一人在前，第二人得 `2×1/1=2`；按 a×b 排序（200 < 100 不成立，故第二人 `a×b=100` 在前），第一人得 `1/100=0`，第二人得 `1×100/1=100`，最大 100——更差！

**修复**：必须用交换相邻元素分析法严格推导，得到 `a×b` 升序的结论。

### 误区4：Huffman 编码用普通队列或排序后顺序合并

**错误**：把所有元素排序后，按顺序两两合并（而非每次重新选最小两个）。

**后果**：合并后的新节点可能比剩余元素大，应该让它和较小的元素合并，而非和下一个原始元素合并。

**修复**：必须用**优先队列（小根堆）**，每次动态取出当前最小的两个。例如 `{1, 2, 3, 4}`：错误做法合并 1+2=3，再 3+3=6，再 6+4=10，总代价 3+6+10=19；正确做法合并 1+2=3，再 3+3=6，再 6+4=10，总代价 19（此例恰好相同）。但 `{1, 1, 1, 1}`：错误顺序合并 1+1=2, 2+1=3, 3+1=4，总代价 9；正确合并 1+1=2, 1+1=2, 2+2=4，总代价 8——明显更优。

### 误区5：绝对值不等式问题用平均值

**错误**：认为 `Σ|x-a_i|` 在 x 取平均值时最小。

**后果**：平均值不一定是中位数，结果可能非最优。例如 `{0, 0, 100}`：平均值 33.3，距离和 33.3+33.3+66.7=133.3；中位数 0，距离和 0+0+100=100。

**修复**：`Σ|x-a_i|` 最小值在 x 取**中位数**时取得。注意区分：`Σ(x-a_i)²` 才在平均值时最小。

---

## 典型例题

### 例题1：[P1803] 凌乱的yyy / 线段覆盖

**题意**：给定 n 个区间 `[a_i, b_i)`，选尽可能多的互不相交的区间。n ≤ 10^6。

**分析**：这是经典的"选最多不相交区间"问题。要让选的区间数最多，应该优先选**结束早**的区间——它占用时间少，留给后续区间的空间大。

**算法选择**：按右端点升序排序，贪心地选当前区间，然后跳过所有与之相交的区间。

**代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;
const int N = 1e6 + 10;
int n;
struct Range {
    int a, b;
    bool operator<(const Range &o) const {
        return b < o.b;  // 按右端点升序
    }
} r[N];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n;
    for (int i = 0; i < n; i++) cin >> r[i].a >> r[i].b;
    sort(r, r + n);
    int ans = 0, last = -2e9;
    for (int i = 0; i < n; i++) {
        if (r[i].a >= last) {  // 不与上一个选中区间相交
            ans++;
            last = r[i].b;     // 更新结束时间
        }
    }
    cout << ans << endl;
    return 0;
}
```

**复杂度**：排序 O(n log n)，扫描 O(n)，总 O(n log n)。

**正确性**：按右端点排序后，第一个选中的区间是结束最早的。若最优解不选它，而选另一个结束更晚的区间，则可替换为它（不影响后续选择，因为它的结束更早），故贪心解不劣于最优解。

---

### 例题2：[P1090] 合并果子

**题意**：n 堆果子，每次合并两堆代价为两堆之和，求把所有果子合并成一堆的最小总代价。n ≤ 10^4。

**分析**：这是 Huffman 算法的直接应用。每次选最小的两堆合并，合并后的新堆放回，重复直到只剩一堆。

**算法选择**：优先队列（小根堆）模拟。

**代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    priority_queue<long long, vector<long long>, greater<long long>> pq;
    for (int i = 0; i < n; i++) {
        long long x;
        cin >> x;
        pq.push(x);
    }
    long long ans = 0;
    while (pq.size() >= 2) {
        long long x = pq.top(); pq.pop();
        long long y = pq.top(); pq.pop();
        ans += x + y;        // 合并代价
        pq.push(x + y);      // 新堆入队
    }
    cout << ans << endl;
    return 0;
}
```

**复杂度**：每次合并 O(log n)，共 n-1 次合并，总 O(n log n)。

**正确性**：由 Huffman 编码的引理——频率最小的两堆一定在最优解中被最先合并（否则交换后总代价不增）。因此每次贪心合并最小两堆是安全的。

---

### 例题3：[P1080] 国王游戏

**题意**：见 48.6 节。n ≤ 1000，a_i, b_i ≤ 10^4。求最大大臣金币数的最小值。

**分析**：由交换相邻元素分析法推导出按 `a×b` 升序排序。由于乘积可能极大（最高 10^{4000+}），需要高精度计算。

**算法选择**：贪心排序 + 高精度乘除法。

**代码**：见 48.12 节。

**复杂度**：排序 O(n log n)，高精度计算 O(n² × 位数)，整体可接受。

**正确性**：交换论证证明——任何不满足 `a_i × b_i ≤ a_{i+1} × b_{i+1}` 的相邻对，交换后最大值不增，故排序后最优。

**三道例题对比**：

| 维度 | P1803 线段覆盖 | P1090 合并果子 | P1080 国王游戏 |
|------|----------------|----------------|----------------|
| 类型 | 区间贪心 | Huffman 贪心 | 推公式贪心 |
| 排序 | 右端点升序 | 无（用堆） | a×b 升序 |
| 数据结构 | 数组 + 排序 | 优先队列 | 高精度 |
| 难点 | 排序准则 | Huffman 思想 | 推导 + 高精度 |
| 复杂度 | O(n log n) | O(n log n) | O(n²) |

---

## 📚 本节练习

### 🟢 基础

**练习1**：[P1803] 凌乱的yyy / 线段覆盖 - https://www.luogu.com.cn/problem/P1803

> 提示：按右端点升序排序，贪心选不相交区间。注意数据范围 n ≤ 10^6，需快读。

**练习2**：[P1090] 合并果子 - https://www.luogu.com.cn/problem/P1090

> 提示：Huffman 算法模板，优先队列模拟。注意用 `long long` 防止溢出。

**练习3**：[P1208] 混合牛奶 - https://www.luogu.com.cn/problem/P1208

> 提示：按单价升序排序，贪心地从便宜的开始买。简单的排序贪心。

### 🟡 进阶

**练习4**：[P1080] 国王游戏 - https://www.luogu.com.cn/problem/P1080

> 提示：交换相邻元素推导出按 a×b 升序排序。注意高精度乘除法，乘积可能高达 4000+ 位。

**练习5**：[P1045] 麦森数 - https://www.luogu.com.cn/problem/P1045

> 提示：快速幂 + 高精度。虽非贪心，但高精度训练是国王游戏的前置技能。

**练习6**：[P1223] 排队接水 - https://www.luogu.com.cn/problem/P1223

> 提示：排序不等式。按接水时间升序排序，总等待时间最小。

**练习7**：[P1047] 校门外的树 - https://www.luogu.com.cn/problem/P1047

> 提示：区间标记或差分，复习区间处理基础。

### 🔴 综合

**练习8**：[P3619] 魔法 - https://www.luogu.com.cn/problem/P3619

> 提示：区间贪心变形，需要分类讨论（t < 0 和 t ≥ 0 分开处理）。考察贪心策略的灵活运用。

**练习9**：[P2123] 皇后游戏 - https://www.luogu.com.cn/problem/P2123

> 提示：国王游戏的进阶版，排序规则更复杂，需分情况讨论 a_i 与 b_i 的关系。极具挑战性。

**练习10**：[P2887] 防晒霜 - https://www.luogu.com.cn/problem/P2887

> 提示：区间选点变形。每头牛有 SPF 范围 `[minSPF, maxSPF]`，每瓶防晒霜有固定 SPF 值和数量。按牛的 maxSPF 升序排序，贪心分配。

---

## 📖 本章总结

### 思维导图

- 贪心算法进阶（局部最优 → 全局最优）
  - 区间贪心三大模型
    - 选点：右端点排序，选右端点
    - 覆盖：左端点排序，延伸右端点
    - 分组：左端点排序 + 小根堆维护组结束时间
  - Huffman 编码：优先队列，每次取最小两个合并，O(n log n)
  - 排序不等式：顺序和 ≥ 乱序和 ≥ 逆序和
  - 绝对值不等式：中位数最优，`Σ|x-a_i|` 最小 = 中位数
  - 推公式贪心
    - 交换相邻元素分析法：推导排序规则
    - 国王游戏：按 `a×b` 升序排序
    - 耍杂技的牛：按 `W+S` 升序排序

### 核心口诀

```
贪心策略要证明，交换论证是利器。
区间选点按右端，区间覆盖按左端。
区间分组左端堆，选最多也右端排。
Huffman 取最小二，合并果子同类题。
排序不等式顺序和，逆序搭配最小得。
绝对值和中位数，仓库选址不迷路。
推公式题怎么办？交换相邻比大小。
国王游戏 a 乘 b，耍杂技牛 W 加 S。
```

### 速查表

| 问题类型 | 排序准则 | 关键数据结构 | 典型题目 |
|----------|----------|--------------|----------|
| 区间选点 | 右端点升序 | 数组 | 射击气球 |
| 区间覆盖 | 左端点升序 | 数组 + 双指针 | 种树 |
| 区间分组 | 左端点升序 | 数组 + 小根堆 | 会议分配 |
| 选最多不相交区间 | 右端点升序 | 数组 | P1803 |
| Huffman / 合并果子 | 无（动态取最小） | 优先队列 | P1090 |
| 排队打水 | 时间升序 | 数组 | P1223 |
| 国王游戏 | a×b 升序 | 数组 + 高精度 | P1080 |
| 耍杂技的牛 | W+S 升序 | 数组 | P2859 |
| 仓库选址 | 取中位数 | 数组 + 排序 | P1046 |
| 绝对值和最小 | 中位数 | 数组 + 排序 | — |

### 贪心正确性证明方法速查

| 方法 | 适用场景 | 思路 |
|------|----------|------|
| 交换论证 | 排序类贪心 | 假设最优解与贪心解不同，交换相邻元素证明可调整为贪心解不变差 |
| 反证法 | 一般贪心 | 假设贪心解非最优，推出矛盾 |
| 最优子结构 | Huffman 类 | 证明合并最小两个后，子问题最优解 + 当前代价 = 原问题最优解 |
| 贪心选择性质 | 区间类 | 证明每步贪心选择都包含在某个最优解中 |

### 备考建议

1. **区间贪心三大模型务必背熟**：排序准则不能混，CSP-S 几乎每年必考其一。
2. **Huffman 和合并果子是同一类题**：看到"每次合并两堆代价为和"立刻想到优先队列。
3. **推公式类贪心掌握"交换相邻元素分析法"**：这是通用武器，国王游戏、耍杂技的牛、排队接水都用它。
4. **高精度是国王游戏的硬门槛**：考前务必手写一遍高精度乘除法模板。
5. **遇到贪心题先问"为什么对"**：想不出证明就别写代码，可能该用 DP 或二分。

---

### 知识衔接

本章是实战冲刺卷的开篇之章，与前后章节的联系如下：

- **前置知识**：第17章区间合并与贪心初步（贪心基本思想）、第12章排序算法（贪心的核心预处理手段）、第22章堆（Huffman编码与优先队列贪心的基础）
- **本章定位**：将贪心思想系统化进阶，涵盖区间贪心三大模型、Huffman编码、排序不等式与推公式贪心，是CSP-S中"思维量高但代码量低"的性价比考点
- **后续发展**：本章标志实战冲刺卷开始，为第49章初赛题型专项训练铺路，初赛中贪心也是阅读程序与完善程序题的高频考点

!!! info "下一章预告"
    贪心算法进阶涵盖了区间贪心、Huffman 编码、排序不等式与推公式贪心等核心模型，是 CSP-S 中"性价比"极高的考点。下一章将进入**初赛题型专项训练**——系统梳理计算机基础知识、进制转换、网络协议、操作系统等初赛必考内容，并提供程序阅读与完善题的解题方法论，为初赛通关保驾护航。
