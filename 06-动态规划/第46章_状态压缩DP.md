# 第46章 状态压缩DP

> 当问题的"状态"是一个集合时，普通数组下标无法直接表示它。状态压缩 DP 用二进制位充当集合的"压缩编码"，让一个整数就能描述一组选取情况，再借助位运算完成状态转移。这是 CSP-S 高频且区分度极大的题型，掌握它意味着拿到了通往更高组别的一张入场券。

!!! info "本章内容"
    - 状态压缩的核心思想与适用场景
    - 位运算六大操作：`& | ^ ~ << >>` 在集合操作中的角色
    - 枚举子集的高效写法 `for (int s = mask; s; s = (s-1) & mask)`
    - 经典模型一：旅行商问题（TSP）—— `dp[mask][i]` 形态
    - 经典模型二：棋盘覆盖 / 国王放置（逐行转移）
    - 状态数 2^n 的指数增长与 n 的安全上界

## 📌 学习目标

学完本章后，你应当能够：

1. **判断**一道 DP 题是否适合用状态压缩（识别"状态本质是一个集合"的特征，元素数 ≤ 20）。
2. **熟练使用** `& | ^ ~ << >>` 六种位运算，独立完成取位、设位、清位、翻转、判断存在等集合操作。
3. **写出** `dp[mask][i]` 形式的 TSP 状态转移方程，并正确完成初始化与答案提取。
4. **掌握**逐行转移型状压 DP 的合法性检查框架（同位冲突、相邻位冲突、跨行冲突），独立 AC 题 P1896。
5. **预估** n 的安全上界（TSP 类 n ≤ 20，棋盘类 n,m ≤ 9~11，可用滚动数组把 n 推到 25 左右），避免 MLE/TLE。

## 💡 生活类比

打个比方，你是一家旅行社的调度员，手里有 10 个待选景点，每个景点"去"或"不去"是两种选择。要把所有可能的行程列出来，共 2^10 = 1024 种组合。如果用数组 `bool visited[10]` 描述一种行程，既占空间又不好比较；但若把 10 个布尔值"打包"成一个 10 位二进制数，例如 `1010010010`，这一个整数就唯一对应一种行程方案——第 i 位是 1 表示去过第 i 个景点，是 0 表示没去。

再打个比方，状态压缩就像把一排 10 盏灯的开关状态，记在一张纸条上，纸条本身只是一个数，却"压缩"了 10 盏灯的全部信息。哪盏灯亮着？看对应位是不是 1。想关掉第 3 盏？把第 3 位清成 0。这一切都用整数的位运算瞬间完成，无需拆开"纸条"。

状态压缩 DP 的精髓就在于此：**用一个整数的二进制位表示一个集合，再用位运算进行集合的增删查改**。

## 核心概念

### 46.1 什么是状态压缩

普通 DP 的状态往往是一个或几个整数（如 `dp[i]`、`dp[i][j]`），可以直接当数组下标。但有一类问题，它的"阶段"附带一个**集合**信息：已经访问过哪些点、已经选了哪些物品、本行哪些格子被占用……这个集合本身才是状态的核心。

如果集合大小为 n，朴素地用数组/哈希表存集合，常数大、难转移。而一个 n 位的二进制数恰好能编码 n 个元素的"选/不选"信息：

- 第 i 位 = 1：元素 i 在集合中
- 第 i 位 = 0：元素 i 不在集合中

于是集合 `{0, 2, 5}` 可以写成二进制 `100101`（最低位是第 0 位），也就是十进制 37。一个 `int`（32 位）就能编码 n ≤ 32 的集合，`long long`（64 位）可到 n ≤ 64。

!!! info "状态压缩 DP 的判别信号"
    - 状态里含一个"子集"信息，且元素总数 n 很小（一般 n ≤ 20，棋盘类 n,m ≤ 11）。
    - 转移时需要频繁做"加入/删除某元素""判断某元素是否在集合中"。
    - 总状态数 = 2^n，再乘以其它维度。务必先估算 2^n 是否在可承受范围。

### 46.2 位运算六大操作详解

状态压缩的全部"魔法"来自六个位运算符。下面逐个说明它们在集合操作中扮演的角色。

| 操作 | 运算符 | 集合含义 | 典型用法 | 示例（mask = `10110`，即 22） |
|------|------|----------|----------|-------------------------------|
| 按位与 | `&` | 交集 / 取位 | 判断第 i 位是否为 1 | `mask & (1<<3)` = `01000` ≠ 0 → 第 3 位是 1 |
| 按位或 | `\|` | 并集 / 设位 | 把第 i 位置 1 | `mask \| (1<<1)` = `10110`（本已是 1，不变） |
| 按位异或 | `^` | 对称差 / 翻转 | 翻转第 i 位 | `mask ^ (1<<2)` = `10010`（第 2 位被翻转） |
| 按位取反 | `~` | 补集 | 全部取反 | `~mask`（高位全变 1，配合 `&` 用） |
| 左移 | `<<` | 生成掩码 | `1 << i` 表示仅第 i 位为 1 | `1 << 4` = `10000` = 16 |
| 右移 | `>>` | 整体右移 | 检查相邻位 | `mask >> 1` 与原 mask 做 `&` 判断有无相邻 1 |

下面是组合出最常见的几个集合操作：

```cpp
// 设 mask 是一个 n 位集合，元素下标 0..n-1
bool inSet   = (mask >> i) & 1;          // 判断元素 i 是否在集合中
int  add     = mask | (1 << i);          // 将元素 i 加入集合
int  remove  = mask & ~(1 << i);         // 将元素 i 从集合中移除
int  toggle  = mask ^ (1 << i);          // 翻转元素 i 的存在性
int  size    = __builtin_popcount(mask); // 集合大小（1 的个数）
int  fullset = (1 << n) - 1;             // 全集 {0,1,...,n-1}
```

!!! tip "__builtin_popcount 速记"
    - `__builtin_popcount(x)`：x 中 1 的个数（集合大小）。
    - `__builtin_ctz(x)`：x 末尾 0 的个数（最低位 1 的位置，x=0 时未定义）。
    - `__builtin_clz(x)`：x 前导 0 的个数，配合 `31 - __builtin_clz(x)` 可求最高位 1 的位置。

#### 逐个操作的实战演示

设 `n = 5`，当前集合 `mask = 0b10110 = 22`，表示集合 `{1, 2, 4}`（第 1、2、4 位为 1）。下面逐一演示六大运算的真实计算过程：

**① 取位 `&`——判断元素 2 是否在集合中**

```
mask       = 10110
1 << 2     = 00100
mask & (1<<2) = 00100  ≠ 0  → 元素 2 在集合中 ✓
```

**② 设位 `|`——把元素 0 加入集合**

```
mask       = 10110
1 << 0     = 00001
mask | (1<<0) = 10111 = 23  → 集合变为 {0,1,2,4}
```

**③ 清位 `& ~`——把元素 2 从集合中移除**

```
mask       = 10110
1 << 2     = 00100
~(1<<2)    = ...11011   (高位全 1，低位第 2 位变 0)
mask & ~(1<<2) = 10010 = 18  → 集合变为 {1,4}
```

**④ 翻转 `^`——切换元素 1 的存在性**

```
mask       = 10110
1 << 1     = 00010
mask ^ (1<<1) = 10100 = 20  → 元素 1 被移除（原在则去，原无则加）
```

**⑤ 取反 `~`——求补集**

```
mask       = 10110
~mask      = ...01001  (高位全变 1)
~mask & fullset = 01001 = 9  → 补集 {0,3}（需 & 全集截断高位）
```

**⑥ 移位 `<<` / `>>`——检查相邻位冲突**

```
mask       = 10110
mask >> 1  = 01011
mask & (mask>>1) = 00010 ≠ 0  → 存在相邻的 1（第 1、2 位相邻）
```

这正是棋盘类问题判断"单行内有无相邻国王"的核心写法。

!!! info "集合大小怎么算"
    `__builtin_popcount(mask)` 返回 1 的个数。若编译器不支持，可手写：

    ```cpp
    int popcount(int x) {
        int c = 0;
        while (x) { c += x & 1; x >>= 1; }
        return c;
    }
    // 更快的位运算技巧
    int popcount_fast(int x) {
        x = (x & 0x55555555) + ((x >> 1) & 0x55555555);
        x = (x & 0x33333333) + ((x >> 2) & 0x33333333);
        x = (x & 0x0F0F0F0F) + ((x >> 4) & 0x0F0F0F0F);
        return (x * 0x01010101) >> 24;
    }
    ```

### 46.3 枚举子集的高效写法

很多状压 DP 的转移需要枚举 mask 的**所有子集**。朴素做法是 `for (int s = 0; s <= mask; s++) if ((s & mask) == s)`，复杂度 O(2^n)，但绝大多数 s 不是 mask 的子集，白白浪费。

正确写法是位运算技巧：

```cpp
for (int s = mask; s; s = (s - 1) & mask) {
    // s 依次遍历 mask 的所有非空子集（按递减顺序）
}
// 单独处理空子集 s = 0（如需要）
```

**为什么正确？** `s - 1` 把 s 最低位的 1 变成 0、其后所有 0 变成 1；再 `& mask` 把不属于 mask 的位重新清零，于是得到"比 s 小"的下一个 mask 的子集。该写法遍历恰好 2^|mask| 个子集（含空集需单独加），无任何浪费。

#### 手动追踪：枚举 mask = `10110`（22）的子集

下表演示 `for (int s = 22; s; s = (s-1) & 22)` 的完整执行过程：

| 步骤 | s（二进制） | s-1 | (s-1) & 22 | 集合表示 |
|------|-----------|-----|------------|---------|
| 1 | 10110 = 22 | 10101 = 21 | 10100 = 20 | {2,4} |
| 2 | 10100 = 20 | 10011 = 19 | 10010 = 18 | {1,4} |
| 3 | 10010 = 18 | 10001 = 17 | 10000 = 16 | {4} |
| 4 | 10000 = 16 | 01111 = 15 | 00110 = 6  | {1,2} |
| 5 | 00110 = 6  | 00101 = 5  | 00100 = 4  | {2} |
| 6 | 00100 = 4  | 00011 = 3  | 00010 = 2  | {1} |
| 7 | 00010 = 2  | 00001 = 1  | 00000 = 0  | 循环结束 |
| (补) | — | — | — | 空集 {} |

可以看到，恰好遍历了 2^3 = 8 个子集（mask 有 3 个 1），无重复无遗漏，且严格递减。

!!! warning "两个常见坑"
    1. 漏掉空子集：循环条件 `s` 为真时跳过了 `s = 0`，需要单独处理（如初始化 `dp[0]`）。
    2. 写成 `s = s - 1`（漏掉 `& mask`）：会遍历大量非子集，逻辑错误。

!!! tip "复杂度证明：枚举全部子集总代价 O(3^n)"
    对每个 mask 枚举其子集，总次数为 $\sum_{mask} 2^{popcount(mask)} = \sum_{k=0}^{n} \binom{n}{k} 2^k = 3^n$。因此涉及"枚举子集转移"的状压 DP 通常以 O(3^n) 为复杂度上限，n ≤ 16~18 时可行。

### 46.4 状态压缩 DP 的通用框架

状压 DP 的代码骨架可以概括为：

1. **确定状态维度**：通常是 `dp[mask]` 或 `dp[mask][i]`，有时再加阶段（如行号）。
2. **确定状态总数**：`2^n`（× 其它维度），据此估算时间与空间。
3. **初始化**：起点状态（如 `dp[1<<start][start] = 0`，或 `dp[0][...]`）。
4. **枚举状态**：按 mask 从小到大（或按阶段）枚举，对每个合法状态枚举决策。
5. **转移**：用位运算构造新 mask，更新 `dp[newmask]`。
6. **提取答案**：在终态（如 `mask == full`）中取最优值。

```cpp
// 模板骨架（伪代码）
int full = (1 << n) - 1;
for (int mask = 0; mask <= full; mask++) {
    for (int i = 0; i < n; i++) {
        if (!(mask & (1 << i))) continue;        // i 不在集合中，跳过
        // 由 dp[mask][i] 转移到 dp[mask | (1<<j)][j]
        for (int j = 0; j < n; j++) {
            if (mask & (1 << j)) continue;       // j 已在集合中
            int nmask = mask | (1 << j);
            dp[nmask][j] = min(dp[nmask][j], dp[mask][i] + cost[i][j]);
        }
    }
}
```

!!! info "刷表法 vs 拉表法"
    上面模板用的是**刷表法**（push）：由已知状态 `dp[mask][i]` 主动更新后继状态 `dp[nmask][j]`。它的好处是"已知合法才转移"，自然过滤不可达状态。

    也可以用**拉表法**（pull）：求 `dp[mask][i]` 时枚举前驱 `dp[mask ^ (1<<i)][j]`。两种写法等价，但刷表法在状压 DP 中更常用，因为"i 在 mask 中"的检查更直观。

### 46.5 如何识别状压 DP 题目

在考场上，最快的判断依据是**数据范围的特征**。下表汇总了常见的状压 DP 信号：

| 信号特征 | 典型范围 | 推断 |
|----------|----------|------|
| 点数 / 物品数 n 很小 | n ≤ 20 | 大概率状压 DP（TSP 类） |
| 棋盘行数列数很小 | n, m ≤ 9~11 | 逐行状压 DP（棋盘覆盖类） |
| 状态涉及"已选/未选"集合 | — | 状压 DP 的核心信号 |
| 状态涉及"本行占用情况" | m ≤ 10 | 逐行转移状压 |
| 求方案数 / 最优解 + 集合 | — | 状压 DP 或记忆化搜索 |

**常见题型分类**：

1. **TSP 类**：访问所有点恰一次，求最短/最长路径。`dp[mask][i]`，转移枚举下一个未访问点。
2. **棋盘覆盖类**：在棋盘上放置棋子，棋子间有攻击约束（相邻、同列、斜对角）。逐行转移，状态为每行占用情况。代表题：P1896（国王）、P2704（炮兵）。
3. **配对/匹配类**：将元素两两配对，求最优匹配。`dp[mask]` 表示 mask 中元素已配对完成的最优值，转移时取出最低位未配对元素与另一元素配对。
4. **集合划分类**：将全集划分成若干满足条件的子集。`dp[mask]` 表示已处理 mask 的最优值，枚举 mask 的子集作为一组转移。复杂度 O(3^n)。
5. **图着色 / 独立集类**：在图上选独立集、求色数等。状态压缩 + 子集枚举。

!!! tip "看到这些数就条件反射"
    - n ≤ 20 且涉及"选取/访问" → TSP 状压。
    - n, m ≤ 10 且棋盘 + 摆放约束 → 逐行状压。
    - n ≤ 16 且"分组/划分" → O(3^n) 子集枚举状压。

## 代码实现

下面用 TSP（旅行商问题）作为状态压缩 DP 的"入门样板"。给定 n 个城市及两两距离，求从 0 号城市出发，每个城市恰好访问一次，最终回到起点的最小总距离。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 20;
int n;
double dist[MAXN][MAXN];
double dp[1 << MAXN][MAXN];   // dp[mask][i]: 已访问集合 mask，当前在城市 i 的最小代价

double solve() {
    int full = (1 << n) - 1;
    // 初始化为无穷大
    for (int mask = 0; mask <= full; mask++)
        for (int i = 0; i < n; i++) dp[mask][i] = 1e18;
    dp[1][0] = 0;              // 起点：只访问了 0 号城市，代价 0

    // 按 mask 升序枚举（保证转移方向正确）
    for (int mask = 1; mask <= full; mask++) {
        for (int i = 0; i < n; i++) {
            if (!(mask & (1 << i))) continue;     // i 不在 mask 中，不合法
            if (dp[mask][i] >= 1e18) continue;    // 该状态不可达
            for (int j = 0; j < n; j++) {
                if (mask & (1 << j)) continue;    // j 已访问过
                int nmask = mask | (1 << j);       // 把 j 加入集合
                double nd = dp[mask][i] + dist[i][j];
                if (nd < dp[nmask][j]) dp[nmask][j] = nd;
            }
        }
    }
    // 答案：所有点都访问过，停在任意点，再加上回到起点的距离
    double ans = 1e18;
    for (int i = 0; i < n; i++)
        ans = min(ans, dp[full][i] + dist[i][0]);
    return ans;
}

int main() {
    cin >> n;
    for (int i = 0; i < n; i++)
        for (int j = 0; j < n; j++) cin >> dist[i][j];
    printf("%.2f\n", solve());
    return 0;
}
```

!!! tip "如何改造为 P1433 吃奶酪"
    P1433 不要求回到起点，只要访问完所有奶酪即可。只需把答案提取改为 `min(dp[full][i])`（去掉 `+ dist[i][0]`），并且起点可任意时把所有 `dp[1<<i][i] = 0` 初始化即可。距离用欧几里得距离 `sqrt((xi-xj)^2 + (yi-yj)^2)`。

### TSP 状态转移追踪（n = 3 小样例）

设 3 个城市，距离矩阵如下（`dist[i][j]` 表示 i 到 j）：

```
       0   1   2
   0 [ 0, 10, 15 ]
   1 [10,  0, 20 ]
   2 [15, 20,  0 ]
```

`full = 0b111 = 7`。初始化 `dp[1][0] = 0`（起点只访问 0 号）。转移过程：

| mask | 当前点 i | j（下一个） | 新 mask | dp 更新 |
|------|---------|-----------|---------|---------|
| 001=1 | 0 | 1 | 011=3 | dp[3][1] = dp[1][0] + dist[0][1] = 0 + 10 = 10 |
| 001=1 | 0 | 2 | 101=5 | dp[5][2] = dp[1][0] + dist[0][2] = 0 + 15 = 15 |
| 011=3 | 1 | 2 | 111=7 | dp[7][2] = dp[3][1] + dist[1][2] = 10 + 20 = 30 |
| 101=5 | 2 | 1 | 111=7 | dp[7][1] = dp[5][2] + dist[2][1] = 15 + 20 = 35 |

最终答案：`min(dp[7][1] + dist[1][0], dp[7][2] + dist[2][0])` = `min(35+10, 30+15)` = `min(45, 45)` = **45**。

最优路径为 0 → 1 → 2 → 0 或 0 → 2 → 1 → 0，总距离均为 45。

!!! info "为什么按 mask 升序枚举就够了"
    转移总是从 mask 转到 `mask | (1<<j)`，新 mask 严格大于旧 mask。因此按 mask 从小到大枚举，保证处理 `mask | (1<<j)` 时，所有前驱 `mask` 已计算完毕。无需担心拓扑顺序——位运算的"加入元素"天然满足 DAG 性质。

!!! warning "TSP 空间/时间估算"
    - 空间：`dp[1<<n][n]`，n=20 时为 `2^20 × 20 × 8B`(double) ≈ 160MB，可能超 256MB 限制（含其他开销）。改用 `int` 或滚动可减半。
    - 时间：`O(n² · 2^n)`，n=20 时约 4×10⁷ 次，1 秒内可过；n=22 约 1.8×10⁸，需开 O2 优化。
    - n=25 完全不可行（2^25 ≈ 3.3×10⁷ × 25² ≈ 2×10¹⁰）。

## ⚠️ 常见误区

1. **状态数 2^n 过大导致 MLE / TLE**
   - 症状：n = 25 还硬开 `dp[1<<25][25]`，内存爆炸。
   - 修复：先估算 `2^n × 数组元素大小`。`dp[1<<20][20]` 的 `double` 约 80MB，已接近上限。若 n 更大，改用滚动数组（按阶段滚动行）或 `map` 存稀疏状态。
   - 经验：TSP 类安全上界 n ≤ 20；棋盘逐行类 n,m ≤ 9~11（因为还要做合法性筛选，合法状态数远小于 2^n）。

2. **枚举子集写法错误**
   - 错误：`for (int s = mask; s; s--)`（会遍历大量非子集）。
   - 正确：`for (int s = mask; s; s = (s - 1) & mask)`。

3. **位运算优先级陷阱**
   - `mask & 1 << i` 会被解析成 `mask & (1 << i)`（因为 `<<` 优先级高于 `&`），虽结果正确但不安全。建议永远加括号：`(mask >> i) & 1` 或 `mask & (1 << i)`。

4. **初始化遗漏或方向错误**
   - 错误：忘记把 `dp` 初始化为无穷大，转移时被残留的 0 污染。
   - 错误：转移时 `dp[mask][i]` 还没算就被用来更新别人（应保证按 mask 升序枚举，或用刷表法）。

5. **合法状态不预处理，每次现算**
   - 棋盘类问题，若每个状态都现做合法性检查，常数巨大。应预先筛出所有"合法状态"存入 `vector<int>`，转移时只枚举合法状态，常数可降一个数量级。

## 典型例题

### [P1896] 互不侵犯

**题意**：在 n×n（n ≤ 9）的棋盘上放 k 个国王，国王攻击周围 8 格，求使国王互不攻击的放置方案数。

**题意分析**：国王的攻击范围是上下左右四格 + 四个对角共 8 格。约束既有"同行内不相邻"，又有"跨行同列、斜对角不相邻"。这是典型的逐行状压 DP——一行一行地决定摆放，行的"占用情况"用 n 位二进制压缩。

**算法选择**：设 `dp[row][mask][c]` 表示处理到第 `row` 行、本行占用状态为 `mask`、前 `row` 行共放了 `c` 个国王的方案数。转移时枚举上一行状态 `pre`，检查 `cur` 与 `pre` 是否冲突。

**状态合法性检查**（核心）：
- **单行合法**：本行不能有相邻国王 → `(mask & (mask >> 1)) == 0`。
- **跨行合法**：本行与上一行不能同列 → `(cur & pre) == 0`；不能左上斜冲突 → `(cur & (pre << 1)) == 0`；不能右上斜冲突 → `(cur & (pre >> 1)) == 0`。

**完整代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int n, k;
long long dp[12][1 << 10][90];   // dp[行][本行状态][已放国王数]
int cnt[1 << 10];                 // 每个状态中 1 的个数（国王数）

// 单行合法性：无相邻 1
bool valid(int s) {
    return (s & (s >> 1)) == 0;
}

int main() {
    cin >> n >> k;
    int full = (1 << n) - 1;
    // 预处理每个状态的国王数
    for (int s = 0; s <= full; s++)
        cnt[s] = __builtin_popcount(s);
    // 预先筛选单行合法状态，存入 vector，转移时只枚举它们
    vector<int> states;
    for (int s = 0; s <= full; s++)
        if (valid(s)) states.push_back(s);

    // 初始化：第 0 行（虚拟行）状态为 0、放 0 个国王，方案数 1
    dp[0][0][0] = 1;
    for (int row = 1; row <= n; row++) {
        for (int cur : states) {
            for (int pre : states) {
                if (cur & pre) continue;           // 同列冲突
                if (cur & (pre << 1)) continue;     // 左上斜冲突
                if (cur & (pre >> 1)) continue;     // 右上斜冲突
                // 枚举上一行已放国王数 c，转移
                for (int c = 0; c + cnt[cur] <= k; c++) {
                    dp[row][cur][c + cnt[cur]] += dp[row - 1][pre][c];
                }
            }
        }
    }
    // 答案：第 n 行、放满 k 个国王、状态任意的方案数之和
    long long ans = 0;
    for (int s : states) ans += dp[n][s][k];
    cout << ans << endl;
    return 0;
}
```

**复杂度分析**：
- 单行合法状态数约为 C(n) ≈ O(φ^n)（φ≈1.618），n=9 时仅 89 个，远小于 2^9=512。
- 时间复杂度：O(n · |S|² · k)，其中 |S| 为合法状态数。n=9 时 |S|² ≈ 8000，n·k ≈ 90，总计约 7×10⁵ 次运算，秒杀。
- 空间复杂度：O(n · 2^n · k) ≈ 9 × 512 × 82 × 8B ≈ 3MB，安全。

!!! tip "为什么状态数远小于 2^n"
    合法状态必须满足"无相邻 1"，这是斐波那契数列：长度为 n 的无相邻 1 的二进制串个数 F(n+2)。n=9 时 F(11)=89。**预先筛选合法状态**是状压 DP 的关键优化，常把 2^n 降到 φ^n。

### P1896 小样例追踪（n = 3, k = 2）

3×3 棋盘放 2 个互不攻击的国王。先列出 n=3 的单行合法状态（无相邻 1）：

```
状态  二进制  国王数
 0    000      0
 1    001      1
 2    010      1
 4    100      1
 5    101      2
```

共 5 个合法状态（3 位无相邻 1 的串数 = F(5) = 5）。

**逐行转移**（仅展示关键路径，`dp[row][cur][c]` 记方案数）：

- 初始：`dp[0][0][0] = 1`（虚拟第 0 行不放）。
- 第 1 行：可与 `pre=0` 兼容的 `cur` 有全部 5 个状态。
  - `dp[1][0][0] = 1`（不放）
  - `dp[1][1][1] = 1`、`dp[1][2][1] = 1`、`dp[1][4][1] = 1`（放 1 个）
  - `dp[1][5][2] = 1`（放 2 个）
- 第 2 行：枚举 `(cur, pre)` 对，检查跨行冲突后累加。例如 `cur=4(100)` 与 `pre=1(001)`：
  - `cur & pre = 0` ✓；`cur & (pre<<1) = 0` ✓；`cur & (pre>>1) = 0` ✓ → 合法
  - `dp[2][4][1] += dp[1][1][1] = 1`
- 第 3 行：同理累加。
- 最终答案：`dp[3][*][2]` 之和，对 n=3, k=2 标准答案为 **16**。

**为什么是 16？** 3×3 棋盘放 2 个不互相攻击的国王，手算可验证共 16 种放法，与程序输出一致。

!!! info "合法性检查的位运算直觉"
    - `cur & pre == 0`：本行国王的列与上一行国王的列不重合（不上下相邻）。
    - `cur & (pre << 1) == 0`：本行国王不在上一行国王的"右上"对角。
    - `cur & (pre >> 1) == 0`：本行国王不在上一行国王的"左上"对角。

    三个条件合起来正好覆盖国王的上方 3 格攻击范围（上、左上、右上）。下方 3 格由"下一行检查本行"覆盖，故无需重复。

## 📚 本节练习

### 🟢 基础

**练习1**：[P1433] 吃奶酪 - https://www.luogu.com.cn/problem/P1433
> 提示：经典 TSP 变体，不要求回到起点。把每个奶酪坐标两两算欧几里得距离，状态 `dp[mask][i]` 表示已吃掉 mask 这批奶酪、当前在第 i 块的最短距离。起点是原点 (0,0)，初始化所有 `dp[1<<i][i] = dist(origin, i)`。注意坐标用 `double`，距离用 `sqrt` 且开 `ios::sync_with_stdio(false)` 提速。n ≤ 15，2^15·15 ≈ 5×10⁵，毫无压力。

### 🟡 进阶

**练习2**：[P1171] 售货员的难题 - https://www.luogu.com.cn/problem/P1171
> 提示：同样是 TSP，但 n ≤ 20 且边权可能不对称。注意 n=20 时 `dp[1<<20][20]` 的 `int` 数组约 80MB，可能压线；改用 `short`/滚动数组或邻接表存稀疏图。状态转移与 P1433 同构，但需小心常数与空间。建议先在 n ≤ 15 时验证正确性再优化。

**练习3**：[P1896] 互不侵犯 - https://www.luogu.com.cn/problem/P1896
> 提示：本章已作为典型例题详解。请先合上书独立写一遍，重点检查三处合法性：单行 `(s & (s>>1))==0`、同列 `cur & pre == 0`、斜对角 `cur & (pre<<1)==0` 且 `cur & (pre>>1)==0`。用 `long long` 存方案数。

### 🔴 综合

**练习4**：[P2704] [NOI2001] 炮兵阵地 - https://www.luogu.com.cn/problem/P2704
> 提示：状压 DP 综合题。n,m ≤ 100，但每行只需考虑 m（≤10）位的占用情况，且攻击范围达 2 格，故状态要同时与上两行兼容。设 `dp[row][cur][pre]`，由于行数大需滚动数组（只保留前两行）。先筛合法状态（无相邻 1 且无间隔为 2 的 1：`(s & (s>>1))==0 && (s & (s>>2))==0`），再枚举三行状态做合法性检查（H 高地不能放）。合法状态数仅约 60，整体复杂度可承受。

## 📖 本章总结

### 思维导图

- 状态压缩 DP
  - 核心思想：用二进制位编码集合，一个整数装 n 个元素
  - 位运算工具
    - 取位/设位/清位/翻转：`& | ^ ~ << >>`
    - 枚举子集技巧：`for (s = mask; s; s = (s-1) & mask)`
    - 常数优化：`2^k` 状态数，合法状态筛选可降至 `φ^n`
  - 两大模型
    - TSP：`dp[mask][i]`，起点 `dp[1][0]=0`，答案 `dp[full]`，复杂度 O(n²·2^n)，n ≤ 20
    - 棋盘逐行：`dp[row][mask][...]`，合法性检查（单行无相邻、跨行无冲突），n,m ≤ 9~11
  - 安全上界：TSP 类 n ≤ 20，棋盘类 n,m ≤ 9~11，子集枚举类 n ≤ 16

### 核心口诀

- **"集合压缩成二进制，一个整数装 n 元素"** —— 状态压缩的本质。
- **"取位用 & 配移位，设位用 | 加掩码"** —— 位运算的两大主操作。
- **"枚举子集 (s-1)&mask，一遍走完不浪费"** —— 子集枚举的口诀。
- **"TSP 形如 dp[mask][i]，棋盘逐行查相邻"** —— 两大模型的形态。
- **"先筛合法状态存 vector，常数降阶省时间"** —— 棋盘类的关键优化。
- **"2^n 增长猛如虎，先估上界再开数"** —— 防止 MLE/TLE 的底线。

### 速查表

| 问题类型 | 状态定义 | 转移核心 | 时间复杂度 | n 的上界 |
|----------|----------|----------|-----------|----------|
| TSP / 最短访问路径 | `dp[mask][i]` | 枚举下一个未访问点 j | O(n²·2^n) | ≤ 20 |
| 棋盘覆盖（无相邻） | `dp[row][mask][...]` | 检查单行 + 跨行冲突 | O(n·\|S\|²·k) | n,m ≤ 9~11 |
| 子集 DP（如划分） | `dp[mask]` | 枚举 mask 的子集 s | O(3^n) | ≤ 16 |
| 恰好选 k 个 | `dp[mask][c]` | 转移时累加 popcount | O(2^n·n·k) | ≤ 18 |

!!! warning "指数复杂度的安全线"
    - `2^20 ≈ 10^6`，配 `n=20` 内层循环约 2×10⁷，1 秒可过。
    - `2^25 ≈ 3.3×10^7`，需滚动数组压缩空间，时间也偏紧。
    - `2^30 ≈ 10^9`，状态压缩 DP 已无法承受，必须换思路。
    - 棋盘类因合法状态筛选，实际状态数 ~φ^n，n=9~11 仍可行。

!!! info "考场策略"
    1. 看到 n ≤ 20 + "集合/选取"特征，立刻想到状压 DP。
    2. 先估算 `2^n` 是否在 10^6~10^7 量级，再决定是否筛合法状态。
    3. TSP 模板（`dp[mask][i]`）要烂熟于心，能 5 分钟默写。
    4. 棋盘类务必预处理合法状态表，转移只枚举它，避免 TLE。
    5. 数据范围贴边时优先用滚动数组压空间、用 `int` 替代 `long long` 省一半内存。

### 自测清单

!!! tip "能做到以下几点，才算真正掌握本章"
    - [ ] 默写位运算六大操作（取位、设位、清位、翻转、枚举子集、popcount）
    - [ ] 默写枚举子集的标准写法 `for (s = mask; s; s = (s-1) & mask)` 并解释原理
    - [ ] 默写 TSP 状压 DP 模板（`dp[mask][i]`），5 分钟内写出 AC 代码
    - [ ] 说清棋盘类状压 DP 的两个合法性检查（单行无相邻、跨行无冲突）
    - [ ] 解释为什么棋盘类预处理合法状态后实际状态数约 $\varphi^n$
    - [ ] 看到 $n \leq 20$ + "集合/选取"特征能立刻想到状压 DP
    - [ ] 独立 AC P1896（互不侵犯）和 TSP 模板题

---

### 知识衔接

本章是动态规划卷的第六章，与前后章节的联系如下：

- **前置知识**：第45章树形DP（DP在非线性结构上的应用）、第16章位运算与离散化（二进制位操作是状态编码的基础）
- **本章定位**：用二进制位编码集合状态，解决TSP、棋盘覆盖等NP难问题在小规模下的精确求解，是n≤20场景下的核心利器
- **后续发展**：本章为第47章计数类DP与数位统计DP铺路，从"求最优值"转向"求方案数"，DP的应用维度进一步拓宽

!!! info "下一章预告"
    状压 DP 用二进制位编码集合状态，解决了 TSP、棋盘覆盖等 NP 难问题在小规模下的精确求解。下一章将进入**计数类 DP 与数位统计 DP**——当不再追求"最大/最小"而是追问"有多少种方案"时，DP 的另一扇大门随之打开。数位 DP 更是 CSP-S 中极具辨识度的高频考点。
