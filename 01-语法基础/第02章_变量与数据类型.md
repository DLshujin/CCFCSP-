# 第02章 变量与数据类型

> 💡 一句话概括：本章是 C++ 编程的"地基"。程序的本质就是**处理数据**，而变量和数据类型决定了"数据怎么存、存什么、能算什么"。

---

## 📌 学习目标

> 💡 一句话概括：掌握 C++ 的六大数据类型，学会根据数据范围选对类型，避免因类型错误导致的竞赛丢分。

读完本章，你应当能够：

1. **准确说出** 6 种基本数据类型（`int` / `long long` / `double` / `char` / `bool` / `string`）的内存占用与取值范围
2. **独立编写** 包含声明、初始化、赋值、输出的完整 C++ 程序，且 0 警告通过编译
3. **判断** 何时该用 `long long`（数据超过 10⁹ 时）、何时该用 `double`（涉及小数时）
4. **预测** `int a = 3.14;`、`7 / 2`、`(double)7 / 2` 等表达式的实际结果，并能解释精度丢失与整数除法的原理
5. **完成** 至少 4 道洛谷基础题，无因类型选择错误导致的 Wrong Answer

---

## 一、变量的本质

> 想一想：你在程序中写了 `int age = 12;`，那么"12"这个数字到底存在电脑的哪里？为什么你能随时读出它、修改它？

> 💡 一句话概括：变量 = 内存里一个**有名字、有类型、有值**的小柜子。

### 1.1 什么是变量

「变量 variable」是程序中用于**存储数据的命名内存单元**。每个变量有三个属性：

| 属性 | 说明 | 示例 |
|------|------|------|
| **名字** | 变量的标识符，用于在代码中引用 | `age` |
| **类型** | 决定占多少内存、能存什么数据 | `int` |
| **值** | 当前存储的具体数据 | `12` |

> 💡 生活类比
> 把计算机的**内存**想象成一排**带编号的储物柜**：
> - **变量名**：贴在柜子上的标签，方便你找东西（如 `age`、`score`）
> - **数据类型**：柜子的大小和形状——装苹果的格子（`int`）和装水滴的格子（`double`）尺寸不同
> - **变量的值**：柜子里实际存放的东西
> - **赋值**：把东西放进柜子的动作，新东西会**挤走**旧东西

### 1.2 变量命名规则

下表列出了变量命名的四条规则：

| 规则 | 正确示例 | 错误示例 |
|------|---------|---------|
| 只能由字母、数字、下划线组成 | `age`、`score_1`、`_name` | `a-b`、`a+b` |
| 不能以数字开头 | `age1` | `1age` |
| 不能是 C++ 关键字 | `count` | `int`、`return`、`for` |
| 区分大小写 | `Age` 与 `age` 是两个独立变量 | —— |

因此，命名时建议用**有意义的英文单词**，多词用下划线分隔：`student_name`、`total_score`。见名知意是工程师的基本素养。

### 1.3 内存模型

变量在内存中以**二进制**存储，不同类型占用不同字节数。以 `int a = 12;` 为例（12 的二进制是 `...00001100`）：

| 属性 | 说明 |
|------|------|
| 类型 | `int`，占 4 字节（32 位） |
| 存储内容 | 12 的二进制补码：`00000000 00000000 00000000 00001100` |
| 存储方式 | 小端序（低位字节在低地址），即 `0x0C 0x00 0x00 0x00` |

不同类型占用的内存大小对比：

| 类型 | 字节数 | 可视化 |
|------|--------|--------|
| `char` | 1 字节 | ■ |
| `int` / `bool` | 4 字节 | ■■■■ |
| `double` / `long long` | 8 字节 | ■■■■■■■■ |

> 提示：小端存储
> 多数 PC 采用「小端序 little-endian」：低位字节存放在低地址。这个细节初学不必深究，但要知道**内存里存的是二进制，不是"12"这两个字符**。

> 📌 本节要点：
> - 变量有三要素：名字、类型、值
> - 命名不可用数字开头，不可用关键字，严格区分大小写
> - 不同类型占用不同内存空间，决定了能存的数据范围

---

## 二、六大基本数据类型

> 想一想：C++ 有 6 种基本类型，为什么不能只用 `int` 一种？什么时候必须用 `long long`、什么时候必须用 `double`？

> 💡 一句话概括：C++ 有六种基本数据类型，分别对应整数、大整数、小数、字符、真假和文本。

### 2.1 数据类型总览

下表汇总了六种基本数据类型的内存占用、取值范围与用途：

| 类型 | 关键字 | 内存占用 | 取值范围 | 用途 |
|------|--------|---------|---------|------|
| 整数 | `int` | 4 字节 | 约 ±2.1 × 10⁹（±21 亿） | 一般整数运算 |
| 大整数 | `long long` | 8 字节 | 约 ±9.2 × 10¹⁸ | 防溢出、大数运算 |
| 双精度浮点 | `double` | 8 字节 | ±1.7 × 10³⁰⁸，15~16 位有效数字 | 小数运算 |
| 字符 | `char` | 1 字节 | -128 ~ 127（ASCII 0~127） | 单个字符 |
| 布尔 | `bool` | 1 字节 | `true`(1) / `false`(0) | 真假判断 |
| 字符串 | `string` | 32 字节 | 任意长度文本 | 存放一串字符 |

因此，选择类型时只需记住：普通整数用 `int`，超过 21 亿用 `long long`，涉及小数用 `double`。

> 💡 提示：关于 string
> `string` 是 C++ 标准库提供的类型，用于存放一串字符（如 `"Hello"`）。本章只做初步介绍，**完整用法（拼接、查找、遍历）将在第 07 章详解**。现阶段只需知道它能存一段文字即可。

### 2.2 类型选择指南

| 场景 | 推荐类型 | 理由 |
|------|---------|------|
| 计数、循环、一般整数 | `int` | 4 字节，范围够用，速度最快 |
| 数据可能超过 10⁹ | `long long` | 防止溢出 |
| 涉及小数计算 | `double` | 精度高，15 位有效数字 |
| 单个字母/符号 | `char` | 1 字节，省空间 |
| 条件判断的真假值 | `bool` | 语义清晰 |
| 一段文字 | `string` | 可变长度，操作方便 |

> 📌 本节要点：
> - 六大类型各有适用场景，选错类型是竞赛中常见的丢分原因
> - 经验法则：普通整数用 `int`，大数用 `long long`，小数用 `double`
> - `string` 用于存放文本，详细用法见第 07 章

---

## 三、变量声明与赋值

> 想一想：`int a = 5;` 和 `a = 8;` 有什么区别？为什么 `x = x + 1` 在数学上不成立，但在编程中完全合法？

> 💡 一句话概括：变量声明是"告诉编译器要一个柜子"，赋值是"把东西放进柜子"。

### 3.1 声明与初始化

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    // 1. 先声明后赋值
    int age;
    age = 12;

    // 2. 声明并初始化（推荐）
    int score = 100;
    long long population = 7800000000LL;   // 全球人口，加 LL 后缀
    double pi = 3.1415926535;
    char grade = 'A';                       // 字符用单引号
    bool isPass = true;
    string name = "XiaoMing";               // 字符串用双引号

    cout << age << " " << score << " " << population << endl;
    cout << pi << " " << grade << " " << isPass << " " << name << endl;
    return 0;
}
// 输出：
// 12 100 7800000000
// 3.14159 A 1 XiaoMing
```

### 3.2 赋值与覆盖

`=` 是「赋值运算符」，把**右边**的值放进**左边**的变量，原值被覆盖：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int x = 5;        // x = 5
    x = 8;            // x 被覆盖为 8
    x = x + 1;        // x = 8 + 1 = 9
    cout << x << endl;   // 输出 9
    return 0;
}
// 输出：9
```

变量值的变化追踪：

| 执行语句 | `x` 的值 |
|---------|---------|
| `int x = 5;` | 5 |
| `x = 8;` | 8 |
| `x = x + 1;` | 9 |

> ⚠️ 警告：易错提醒
> `=` 是**赋值**，`==` 才是**判断相等**。新手极易混淆，下一章会详讲。

> 📌 本节要点：
> - 推荐在声明变量时同时初始化（`int score = 100;`）
> - 赋值会覆盖变量原有的值，`x = x + 1` 是先读右边再赋给左边
> - `=` 是赋值不是相等，`==` 才是相等判断

---

## 四、类型转换

> 想一想：`int a = 10; int b = 3;` 那么 `a / b` 等于多少？为什么不是 3.333...？

> 💡 一句话概括：C++ 中不同类型混合运算时，会自动或手动转换类型，理解转换规则是避免精度丢失的关键。

### 4.1 隐式类型转换（自动）

C++ 运算时会**自动**把低精度转为高精度，规则：`int` → `long long` → `double`。

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a = 3;
    double b = 2.5;
    double c = a + b;        // a 自动提升为 3.0，结果 5.5
    cout << c << endl;       // 5.5

    int x = 1000000000;      // 10 亿
    long long y = x * 10;    // ⚠️ 危险！x*10 先按 int 算，溢出！
    long long z = (long long)x * 10;   // 正确：先转换再乘
    cout << y << " " << z << endl;
    return 0;
}
// 输出：
// 5.5
// 1410065408 10000000000
```

📖 通俗解释：`y = x * 10` 中，`x` 和 `10` 都是 `int`，它们的乘积 `10000000000` 已经超过了 `int` 的 21 亿上限，计算时就已经溢出。等号右边的"车祸"发生在赋值给 `long long` 的 `y` 之前——急救已经来不及了。

### 4.2 显式类型转换（强制）

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a = 10, b = 3;
    double r1 = a / b;            // 整数除法得 3，再转 3.0
    double r2 = (double)a / b;    // a 先转 10.0，再除得 3.33333
    cout << r1 << endl;           // 3
    cout << r2 << endl;           // 3.33333

    // 字符与 ASCII 码互转
    char ch = 'A';
    cout << (int)ch << endl;      // 65
    int n = 97;
    cout << (char)n << endl;      // a

    // 浮点转整数（直接截断，非四舍五入）
    cout << (int)3.9 << endl;     // 3
    return 0;
}
// 输出：
// 3
// 3.33333
// 65
// a
// 3
```

> ⚠️ 警告：整数除法陷阱
> **两个 `int` 相除，结果还是 `int`**（直接舍去小数）。`7 / 2` 得 `3` 而非 `3.5`。想得小数，必须至少把一个数转成 `double`。

> 📌 本节要点：
> - 隐式转换自动将低精度→高精度，但要注意运算的时机
> - 整数除法结果仍是整数，小数部分直接舍去
> - 浮点转整数是截断而非四舍五入

---

## 五、常见误区与避坑指南

> 想一想：你在洛谷上交代码，明明逻辑对了却 WA（Wrong Answer），有没有可能是类型选错了？比如该用 `long long` 却用了 `int`？

> 💡 一句话概括：竞赛中 80% 的类型错误来自精度丢失、整数溢出、混算时机和浮点比较这四个坑。

### 5.1 误区一：`int` 存小数导致精度丢失

❌ **错误写法**：
```cpp
int a = 3.14;
cout << a;    // 输出 3，不是 3.14！
```

✅ **正确写法**：
```cpp
double area = 3.14159 * 10 * 10;   // 314.159
```

**后果说明**：`a` 是 `int` 类型，只能存整数。把 `3.14` 赋给 `int` 变量时，编译器会**直接截断小数部分**（不是四舍五入），只保留 `3`。这种"静默截断"不会报错，但会让计算结果悄悄出错。

📖 通俗解释：就像把 3.14 升水倒进一个只标了整数的量杯，多出来的 0.14 升就洒掉了，你只能看到"3 升"。

### 5.2 误区二：`int` 溢出导致答案变负数

❌ **错误写法**：
```cpp
int big = 3000000000;     // 30 亿，超过 int 上限
cout << big;              // 输出一个奇怪的负数！
```

✅ **正确写法**：
```cpp
long long big = 3000000000LL;   // 用 long long + LL 后缀
cout << big;                    // 3000000000
```

**后果说明**：`int` 最大约 21 亿，30 亿超范围会发生「溢出 overflow」，结果回绕成负数。对策：数据可能超过 10⁹ 时，一律用 `long long`，字面量加 `LL` 后缀。

### 5.3 误区三：`long long` 与 `int` 混算时溢出

❌ **错误写法**：
```cpp
long long x = 1000000 * 1000000;    // 看似 10^12
cout << x;                          // 可能输出负数！
```

✅ **正确写法**：
```cpp
long long x = 1000000LL * 1000000;  // 加 LL 后缀，让乘法在 long long 下进行
cout << x;                          // 1000000000000
```

**后果说明**：等号右边两个 `1000000` 都是 `int`，相乘时按 `int` 计算，**先溢出再赋值**给 `long long` 已经晚了。必须让其中一个操作数变成 `long long`，整个乘法才会在 `long long` 精度下进行。

### 5.4 误区四：浮点数用 `==` 比较

❌ **错误写法**：
```cpp
double x = 0.1 + 0.2;
if (x == 0.3) { ... }    // 可能不成立！
```

✅ **正确写法**：
```cpp
double x = 0.1 + 0.2;
if (fabs(x - 0.3) < 1e-9) { ... }   // 差值小于 10^-9 视为相等
```

**后果说明**：计算机用二进制存小数，`0.1 + 0.2` 实际为 `0.30000000000000004`，与 `0.3` 不完全相等。浮点数比较永远用"差值小于极小值"的方式。

> 💡 提示：竞赛铁律
> CSP-J 中**小数一律用 `double`**（不用 `float`），**大数一律用 `long long`**（数据超 10⁹ 时）。宁可多占几个字节，也别因溢出或精度丢分。

> 📌 本节要点：
> - 涉及小数一律用 `double`，不要用 `int` 存小数
> - 数据超过 10⁹ 时用 `long long`，字面量加 `LL` 后缀
> - 两个 `int` 相乘先确保结果不溢出，否则先转 `long long`
> - 浮点数比较用 `fabs(a - b) < 1e-9`，不要用 `==`

---

## 六、典型例题

### 例题：计算圆面积（演示 `double` 与类型转换）

**题目**：输入圆的半径 `r`（整数），输出圆的面积，保留 4 位小数。π 取 `3.14159`。

**分析**：面积公式为 S = π × r²。半径是整数，但结果是小数，必须用 `double` 存储。输出保留 4 位小数用 `printf("%.4f", ...)`。

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int r;
    cin >> r;
    double pi = 3.14159;
    double area = pi * r * r;       // int 与 double 运算，自动提升为 double
    printf("%.4f\n", area);         // 保留 4 位小数输出
    return 0;
}
```

**输入**：`5`
**输出**：`78.5397`

> 💡 提示：教练点拨
> 这道题看似简单，却考察了三个要点：① 整数与小数混算的隐式转换；② 用 `double` 存浮点结果；③ 用 `printf` 控制小数位数。竞赛中"读题→选类型→写公式→控格式"是基本功。

---

## 📚 本节练习

> 💡 提示：刷题建议
> 按难度顺序完成，每题先独立思考 15 分钟再查提示。AC（通过）后对照题解复盘"为什么这么选类型"。

### 🟢 基础巩固（★☆☆）

- [P1001] A+B Problem — https://www.luogu.com.cn/problem/P1001
  > 输入两个整数 A 和 B，输出 A+B。考察 `int` 声明、`cin` 读入、`cout` 输出。

- [B2002] 输出第二个整数 — https://www.luogu.com.cn/problem/B2002
  > 输入三个整数，输出第二个。练习链式读入 `cin >> a >> b >> c` 与变量选择。

### 🟡 进阶提升（★★☆）

- [B2005] 字符三角形 — https://www.luogu.com.cn/problem/B2005
  > 给定一个字符，构造底边长 5、高 3 的等腰三角形。考察 `char` 类型与 `cout` 格式控制。

- [B2029] 大象喝水 — https://www.luogu.com.cn/problem/B2029
  > 已知圆柱形水桶底面半径和高（整数，厘米），计算大象至少喝多少桶水才能喝够 20 升。考察 `double` 计算、`π` 取值、向上取整（`ceil` 或 `(int)x + 1`）。

### 🔴 挑战真题（★★★）

- [P1421] 小玉买文具 — https://www.luogu.com.cn/problem/P1421
  > 班费买文具，已知钱数（元+角，1 元 = 10 角），每支笔 x 元 y 角，问最多买几支。考察**整数除法与取余**的灵活运用，注意单位统一。

- [P1046] 陶陶摘苹果 — https://www.luogu.com.cn/problem/P1046
  > 已知 10 个苹果的高度和陶陶的身高（含 30cm 板凳），求能摘到几个。考察 `int` 比较运算，需用 10 个变量（数组尚未学），考验**变量管理**能力。

> 💡 提示：教练提醒
> 做完每题后，问自己三个问题：① 我选的类型对吗？会不会溢出？② 有没有用到整数除法/类型转换？③ 输出格式（小数位数、换行）对吗？养成这个习惯，能减少 80% 的低级错误。

---

## 📖 本章总结

### 核心概念
- 变量三要素
  - 名字：标识符，见名知意，用下划线分隔多词
  - 类型：决定占用内存大小和能存的数据种类
  - 值：变量当前存储的具体数据
- 六大数据类型
  - `int`：4 字节，±21 亿，用于一般整数
  - `long long`：8 字节，±9.2×10¹⁸，用于大数防溢出
  - `double`：8 字节，15~16 位有效数字，用于小数
  - `char`：1 字节，ASCII 0~127，用于单个字符，用单引号 `'A'`
  - `bool`：1 字节，`true`/`false`，用于条件判断
  - `string`：32 字节，任意文本，用双引号 `"ABC"`（详见第 07 章）
- 类型转换
  - 隐式转换：低精度自动提升为高精度（`int` → `double`）
  - 显式转换：`(double)a` 强制转换，浮点转整数为截断（非四舍五入）
  - 整数除法：两个 `int` 相除结果为整数，直接舍去小数

### 常见陷阱
- ❌ `int a = 3.14;` → ✅ 用 `double` 存小数，`int` 会截断小数部分
- ❌ 数据 > 10⁹ 仍用 `int` → ✅ 大数用 `long long`，字面量加 `LL` 后缀
- ❌ `long long x = 1000000 * 1000000;` 先溢出 → ✅ 先让一个操作数变为 `long long`
- ❌ `if (x == 0.3)` 浮点直接比较 → ✅ 用 `fabs(x - 0.3) < 1e-9` 判差值
- ❌ `7 / 2` 期望得到 3.5 → ✅ 整数除法得整数，想得小数用 `(double)7 / 2`

### 关键代码模板

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a = 10;                    // 整数
    long long b = 10000000000LL;   // 大整数，加 LL 后缀
    double c = 3.14159;            // 小数
    char d = 'A';                  // 字符，单引号
    bool e = true;                 // 布尔值
    string f = "Hello";            // 字符串，双引号

    // 类型转换
    double result = (double)a / 3; // 显式转换，确保小数结果

    cout << a << " " << b << " " << c << endl;
    return 0;
}
```

### 核心口诀

- ✅ 变量**先声明再使用**，命名见名知意
- ✅ **整数**用 `int`，**大数**（>10⁹）用 `long long`，字面量加 `LL`
- ✅ **小数**一律用 `double`，比较相等用 `fabs` 判差值
- ✅ **字符**用单引号 `'A'`，**字符串**用双引号 `"ABC"`
- ✅ **整数除法得整数**，想得小数先转 `double`
- ✅ `int a = 3.14;` → 得 `3`（截断，非四舍五入）
- ✅ `=` 是赋值，`==` 是相等

### 类型速查表

| 类型 | 关键字 | 字节 | 范围 | 典型用途 |
|------|--------|------|------|---------|
| 整数 | `int` | 4 | ±2.1×10⁹ | 计数、一般运算 |
| 大整数 | `long long` | 8 | ±9.2×10¹⁸ | 防溢出、大数 |
| 浮点 | `double` | 8 | 15~16 位有效数字 | 小数运算 |
| 字符 | `char` | 1 | ASCII 0~127 | 单个字符 |
| 布尔 | `bool` | 1 | true/false | 条件判断 |
| 字符串 | `string` | 32 | 任意文本 | 一串字符（详见第07章） |

### 知识衔接

本章是CSP-J/S入门教材的基础语法章节，与前后章节的联系如下：

- **前置知识**：第01章「初识编程与HelloWorld」——编程基础概念、编辑编译运行流程、C++程序骨架与cout输出
- **本章定位**：让程序能"记住"数据，掌握int、double、char、bool等基本数据类型与变量声明，是所有后续章节的数据基础
- **后续发展**：为第03章「输入输出与基本运算」铺路——下一章将让程序与用户交互，用cin读取输入并进行加减乘除等运算

---

> 💡 提示：下一章预告
> 下一章我们将学习**输入输出与基本运算**，让程序真正"动起来"——从键盘读取数据，完成加减乘除、取余等运算，并向屏幕输出结果。掌握了变量与运算，你就能解决一大批基础竞赛题了！

---

## 📝 本章修改摘要
- 新增引导性问题 4 处（四个主小节开头各增加"想一想"引导）
- 统一提示框格式：`!!! info` → `> 💡 提示`，`!!! warning` → `> ⚠️ 警告`，`!!! tip` → `> 💡 提示`
- 保留所有代码块、练习、链接和总结内容不变