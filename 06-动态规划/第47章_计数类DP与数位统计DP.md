# 第47章 计数类DP与数位统计DP

> 当我们不再追求"最大/最小"，而是追问"有多少种方案"时，DP 的另一扇大门便随之打开。计数类DP用于统计方案总数，数位统计DP则把"区间计数"这件事做到 O(位数) 级别，是 CSP-S 中极具辨识度的高频考点。

!!! info "本章内容"
    - 计数类DP的通用思路：状态设计、转移方程、初始化
    - 整数划分问题：完全背包解法 与 搜索+剪枝解法
    - 数位统计DP的本质：前缀和思想 + 逐位决策
    - 数位DP记忆化搜索模板：`dfs(pos, limit, lead, state)`
    - `limit` 与 `lead` 参数的含义及"何时不记忆化"

---

## 📌 学习目标

学完本章后，你应当能够：

1. **区分**计数类DP与最优化DP，能正确写出"方案数"转移方程并完成初始化与取模。
2. **掌握**整数划分的两种解法：完全背包 O(n²) 解法 与 DFS+剪枝解法，并能解释其等价性。
3. **独立编写**数位DP记忆化搜索模板，准确说出 `pos`、`limit`、`lead`、`state` 四个参数各自的作用。
4. **解释**"当 `limit=true` 或 `lead=true` 时不记忆化"的原理，并能判断哪些题目需要 `lead`。
5. **独立完成** P1025、P2602、P2657 三道经典题，并通过前缀和思想解决区间 `[L, R]` 的计数问题。

---

## 💡 生活类比

打个比方，计数类DP就像是**统计菜谱方案**：你手上有若干种食材（物品），每种食材可以用若干次，问"凑出一桌满汉全席（目标和）"共有多少种搭配方式。你关心的不是哪桌最便宜，而是"到底有多少种凑法"。

而数位统计DP更像是一位**严谨的户籍警**：他要统计"1 到 R 之间有多少个门牌号满足某个条件"。一个一个数太慢了，于是他逐位审查——先看最高位，再依次看次高位……只要某一位没有顶到上界，后面的位就可以"放飞自我"地自由组合，于是大量方案可以被批量计算出来。这就是数位DP的精髓：**按位枚举 + 限制传递 + 记忆化批量计数**。

---

## 核心概念

### 47.1 计数类DP概述

最优化DP求的是"最优值"（最大、最小），而**计数类DP求的是"方案总数"**。两者的状态设计往往相似，但转移方程和初始化方式有本质区别：

| 对比项 | 最优化DP | 计数类DP |
|--------|----------|----------|
| 转移符 | `max` / `min` | `+=` |
| 初始化 | 常用 `-INF` / `INF` | `dp[0]=1`（空方案计1） |
| 结果 | 某个最优值 | 方案总数 |
| 常见坑 | 漏状态 | 漏方案 / 重复计数 |

!!! tip "计数DP的灵魂：空方案"
    计数类DP几乎都有一个"从无到有"的起点：`dp[0] = 1`。它表示"凑出 0 的方案有 1 种——什么都不选"。没有这个 1，所有转移都会从 0 开始累加，最终结果全是 0。这是初学者最常犯的错误。

### 47.2 整数划分问题

#### 47.2.1 问题定义

**整数划分**：给定正整数 n，求将其分解为若干个正整数之和的方案数。例如 n=4：

```
4 = 4
4 = 3 + 1
4 = 2 + 2
4 = 2 + 1 + 1
4 = 1 + 1 + 1 + 1
```

共 5 种方案（不考虑顺序，即 `2+1+1` 与 `1+2+1` 视为同一种）。

#### 47.2.2 解法一：完全背包

把数字 1, 2, ..., n 看作 n 种"物品"，数字 i 可以选无限次（完全背包），"背包容量"为 n。求装满背包的方案数。

- 状态：`dp[j]` = 凑出和为 j 的方案数
- 转移：`dp[j] += dp[j - i]`（选或不选数字 i）
- 初始化：`dp[0] = 1`
- 外层循环枚举物品 i，内层循环枚举容量 j（正向，完全背包）

```cpp
#include <bits/stdc++.h>
using namespace std;
const int MOD = 1e9 + 7;
int n;
long long dp[1005];

int main() {
    cin >> n;
    dp[0] = 1;                       // 空方案：凑出0有1种
    for (int i = 1; i <= n; i++)     // 枚举"物品"（数字i）
        for (int j = i; j <= n; j++) // 正向：完全背包
            dp[j] = (dp[j] + dp[j - i]) % MOD;
    cout << dp[n] << endl;
    return 0;
}
```

!!! warning "循环顺序不能颠倒"
    外层必须枚举**物品** i，内层枚举**容量** j。若反过来，`2+1` 与 `1+2` 会被当成两种方案，变成"排列数"而非"组合数"。这是整数划分要求"不考虑顺序"的关键。

**DP表演化演示**（n=4）：

| 步骤 | 操作 | dp[0] | dp[1] | dp[2] | dp[3] | dp[4] |
|------|------|-------|-------|-------|-------|-------|
| 初始 | dp[0]=1 | 1 | 0 | 0 | 0 | 0 |
| i=1 | 用数字1更新 | 1 | 1 | 1 | 1 | 1 |
| i=2 | 用数字2更新 | 1 | 1 | 2 | 2 | 3 |
| i=3 | 用数字3更新 | 1 | 1 | 2 | 3 | 4 |
| i=4 | 用数字4更新 | 1 | 1 | 2 | 3 | 5 |

最终 `dp[4]=5`，与手工枚举的 5 种方案一致。每一步 `dp[j] += dp[j-i]` 的含义是：凑出 j 的方案数 = 不用数字 i 的方案数 + 至少用一个数字 i 的方案数。

#### 47.2.3 解法二：搜索 + 剪枝

当题目对"份数"有额外限制（如 P1025 要求恰好分成 k 份）时，DFS + 剪枝更直观。

核心思想：为保证不重复，约定每份不小于上一份（非降序）。用 `dfs(rest, count, last)` 表示：剩余 rest 要分、还要分 count 份、上一份至少为 last。

剪枝：当前份 i 至少为 `last`，且 `i * count <= rest`（否则后面分不够）。

```cpp
#include <bits/stdc++.h>
using namespace std;
int n, k;

// rest: 剩余待分; count: 还要分几份; last: 上一份的值（保证非降序）
int dfs(int rest, int count, int last) {
    if (count == 0) return rest == 0 ? 1 : 0;  // 分完了
    int ans = 0;
    for (int i = last; i * count <= rest; i++) // 剪枝：i至少last，且i*count<=rest
        ans += dfs(rest - i, count - 1, i);
    return ans;
}

int main() {
    cin >> n >> k;
    cout << dfs(n, k, 1) << endl;
    return 0;
}
```

两种解法是等价的：完全背包本质也是在"非降序"地选数字，只是把递归过程展开成了迭代。

### 47.3 数位统计DP概述

#### 47.3.1 什么是数位DP

**数位统计DP**（Digit DP）：统计区间 `[L, R]` 内满足某种条件的数字个数。条件通常与数字的"各位"有关，例如：

- 数字 d 出现了多少次
- 相邻两位之差 ≥ 2
- 各位数之和等于 S
- 不含某个数字

#### 47.3.2 前缀和思想

数位DP几乎都用**前缀和思想**把区间问题转化为前缀问题：

$$
\text{ans}(L, R) = \text{solve}(R) - \text{solve}(L - 1)
$$

其中 `solve(x)` 统计 `[1, x]`（或 `[0, x]`）内满足条件的数字个数。这样只需实现"统计 ≤ x 的合法数个数"这一个函数。

#### 47.3.3 逐位决策与记忆化搜索

把 x 的各位数字从高到低存入数组 `a[]`，然后从最高位向最低位逐位决策：

- 当前位填什么数字？（0 到上界之间枚举）
- 是否受上界限制？（`limit`）
- 是否有前导零？（`lead`）
- 还需要记录什么状态？（`state`，如上一位数字、数字和等）

由于"从某一位开始、状态相同、且不受上界限制"时后续方案数完全相同，可以用记忆化加速。

#### 47.3.4 逐位决策过程演示

以 x = 234 为例，统计 `[1, 234]` 中满足某条件的数。先把 234 拆位：

```
百位 a[2]=2, 十位 a[1]=3, 个位 a[0]=4
```

从百位开始决策（pos=2, limit=true, lead=true）：

```
百位(pos=2, limit=true, lead=true):
├── 取0: limit→false, lead→true  (前导零，实际是两位数)
│   └── 十位(pos=1, limit=false, lead=true):
│       ├── 取0: →一位数 ...
│       └── 取1~9: →正常的两位数，后续自由枚举 ★可记忆化
├── 取1: limit→false, lead→false (百位=1，不顶上界)
│   └── 十位、个位均可取0~9，完全自由 ★可记忆化
└── 取2: limit→true,  lead→false (百位=2，顶到上界)
    └── 十位(pos=1, limit=true, lead=false):
        ├── 取0~2: limit→false ★可记忆化
        └── 取3: limit→true → 个位只能取0~4
```

关键观察：

- **标★的分支**：一旦 `limit=false`，后续所有位的取值范围都是 0~9，与 x 无关。这些子问题可以被记忆化复用。
- **limit=true 的路径只有一条**：即沿着"2→3→4"走的这条主路径。这条路径上的状态不能记忆化，因为换一个 x 结果就不同。
- 这就是为什么数位DP的时间复杂度是 O(位数 × 状态数)：大部分子问题被记忆化后只计算一次，只有"贴上界"的那一条链需要逐位处理。

!!! info "形象理解"
    想象你在走一棵决策树。`limit=true` 的路径就像树干（只有一条），`limit=false` 的分支就像树叶（可以批量统计）。记忆化的本质就是：相同形状的子树只展开一次。

### 47.4 数位DP核心模板

完整的记忆化搜索模板如下：

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int a[20];                 // 存储x的各位数字（a[1]为最低位，a[pos]为最高位）
ll dp[20][STATE];          // 记忆化数组，STATE视题目而定

// pos: 当前处理第几位（从高到低，pos递减到0终止）
// state: 题目相关状态
// limit: 当前位是否受上界限制
// lead: 之前是否全为前导零
ll dfs(int pos, int state, bool limit, bool lead) {
    if (pos == 0) return 1;  // 走完所有位，找到一个合法数
    // 记忆化：仅在不受限制且无前导零时查表
    if (!limit && !lead && dp[pos][state] != -1)
        return dp[pos][state];
    int up = limit ? a[pos] : 9;  // 当前位上界
    ll res = 0;
    for (int i = 0; i <= up; i++) {
        // 更新state、lead、limit
        res += dfs(pos - 1,
                   newState /* TODO */,
                   limit && (i == up),       // 之前都顶到上界且当前也顶到上界
                   lead && (i == 0));        // 之前都是前导零且当前也是0
    }
    if (!limit && !lead) dp[pos][state] = res;  // 仅此时记忆化
    return res;
}

ll solve(ll x) {
    int pos = 0;
    while (x) { a[++pos] = x % 10; x /= 10; }  // a[1]~a[pos]，pos为位数
    return dfs(pos, 0, true, true);              // 从最高位开始
}
```

### 47.5 四大参数详解

#### 47.5.1 `pos`：当前位

从最高位向最低位递归，`pos == 0` 表示所有位已处理完，找到一个合法数，返回 1。

#### 47.5.2 `limit`：上界限制

- `limit = true`：当前位的取值上界是 `a[pos]`（不能超过 x 的对应位）
- `limit = false`：当前位可以取 0~9

传递规则：`limit` 为 true 且当前位恰好取到 `up`（即 `a[pos]`）时，下一位仍受限；否则下一位自由。

#### 47.5.3 `lead`：前导零标记

- `lead = true`：之前所有位都是 0（即这个数还没有"真正开始"）
- `lead = false`：已经填过非零位

传递规则：`lead` 为 true 且当前位取 0 时，下一位仍为前导零；否则下一位不再是前导零。

!!! warning "前导零为什么重要？"
    数字 `005` 本质就是 `5`，它的位数是 1 而不是 3。如果不处理前导零，"统计数字 0 出现次数"会把前导零也算进去，导致严重错误。是否需要 `lead` 取决于题目：若条件与"位数"或"数字0"有关，则必须处理；若条件只涉及相邻位关系且 0 本身合法（如 windy 数），则也需要处理。

#### 47.5.4 `state`：题目相关状态

这是数位DP的灵魂，因题而异：

- 数字计数：`sum`（已统计的某数字个数）
- windy 数：`last`（上一位数字）
- 数位之和：`sum`（各位数字之和）
- 不含某数字：无需额外状态，直接跳过

**何时需要 `lead` 参数？** 可按以下规则判断：

| 题目条件 | 是否需要 lead | 原因 |
|----------|---------------|------|
| 统计数字 0 出现次数 | ✅ 需要 | 前导零的 0 不能算 |
| 相邻位差值条件（如 windy 数） | ✅ 需要 | 首个非零位无"前一位" |
| 与位数相关（如"恰好 3 位数"） | ✅ 需要 | 前导零影响实际位数 |
| 各位数字之和 | ❌ 不需要 | 前导零的 0 对和不影响 |
| 不含某非零数字（如不含 4） | ❌ 不需要 | 前导零的 0 ≠ 4，不影响 |

若不确定，**默认加上 `lead`** 最安全——多一个参数不会错，只是常数稍大。

#### 47.5.5 为什么 `limit` 和 `lead` 为 true 时不记忆化？

这是数位DP最容易踩坑的地方。原因如下：

- 当 `limit = true` 时，后续能填的数字范围是**受 x 约束的**，与具体 x 有关。如果记忆化，下次查询另一个 x' 时会得到错误结果。
- 当 `lead = true` 时，"前导零状态"下的方案数与"非前导零状态"下的方案数含义不同，混记会导致统计混乱。
- 而 `limit = false` 且 `lead = false` 时，后续的方案数**只依赖 pos 和 state**，与 x 无关，可以安全记忆化。

!!! tip "一句话口诀"
    **"受限制的状态是临时状态，不能记忆；自由的状态是通用状态，可以记忆。"**

    由于 `limit` 和 `lead` 为 true 的情况是少数（最多各一条路径），不记忆化也不会影响整体时间复杂度，仍是 O(位数 × 状态数)。

#### 47.5.6 反例：错误记忆化导致的结果

假设统计 `[1, x]` 中不含数字 4 的数的个数。若在 `limit=true` 时也记忆化：

- 第一次查询 `solve(999)`：在 `limit=true` 路径上记忆了 `dp[2][...] = 某值`（这个值是在"百位顶到 9"的约束下算出来的）。
- 第二次查询 `solve(123)`：百位枚举到 1 时 `limit=false`（正确），但枚举到"顶上界"路径时，可能误用第一次记忆的 `dp[2][...]`，而这个值是按"上界为 9"算的，与上界为 2 完全不同。

结果：`solve(123)` 返回了 `solve(999)` 的一部分答案，严重偏大。

**正确做法**：`limit=true` 时跳过记忆化，每次都老老实实递归。由于 `limit=true` 的路径只有一条，额外开销可忽略。

---

## 代码实现

### 47.6 P2602 数字计数（完整实现）

统计 `[1, n]` 中每个数字 0~9 各出现了多少次。

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll dp[15][15];   // dp[pos][cnt]: 从pos位开始，已统计cnt个数字d时的总贡献
int a[15], d;

// pos: 当前位, cnt: 数字d已出现次数, limit: 上界, lead: 前导零
ll dfs(int pos, int cnt, bool limit, bool lead) {
    if (pos == 0) return cnt;           // 走完所有位，返回该数字出现次数
    if (!limit && !lead && dp[pos][cnt] != -1)
        return dp[pos][cnt];
    int up = limit ? a[pos] : 9;
    ll res = 0;
    for (int i = 0; i <= up; i++) {
        // 前导零的0不计入数字0的统计
        int add = (i == d && (!lead || i != 0)) ? 1 : 0;
        res += dfs(pos - 1, cnt + add,
                   limit && i == up, lead && i == 0);
    }
    if (!limit && !lead) dp[pos][cnt] = res;
    return res;
}

ll solve(ll x, int digit) {
    d = digit;
    memset(dp, -1, sizeof(dp));
    int pos = 0;
    while (x) { a[++pos] = x % 10; x /= 10; }  // a[1]~a[pos]，pos为位数
    return dfs(pos, 0, true, true);
}

int main() {
    ll L, R;
    cin >> L >> R;
    for (int i = 0; i <= 9; i++)
        cout << solve(R, i) - solve(L - 1, i) << " ";
    cout << endl;
    return 0;
}
```

### 47.7 P2657 windy 数（完整实现）

windy 数：相邻两位数字之差至少为 2。统计 `[a, b]` 中 windy 数的个数。

```cpp
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

ll dp[15][15];   // dp[pos][last]: pos位开始、上一位为last时的方案数
int a[15];

// pos: 当前位, last: 上一位数字(0~9), limit: 上界, lead: 前导零
ll dfs(int pos, int last, bool limit, bool lead) {
    if (pos == 0) return 1;              // 合法数+1
    if (!limit && !lead && dp[pos][last] != -1)
        return dp[pos][last];
    int up = limit ? a[pos] : 9;
    ll res = 0;
    for (int i = 0; i <= up; i++) {
        if (lead && i == 0) {
            // 仍是前导零，不更新last（用last=10标记"无前位"）
            res += dfs(pos - 1, 10, limit && i == up, true);
        } else if (lead) {
            // 第一个非零位，无需检查差值
            res += dfs(pos - 1, i, limit && i == up, false);
        } else {
            // 正常位，检查相邻差值
            if (abs(i - last) >= 2)
                res += dfs(pos - 1, i, limit && i == up, false);
        }
    }
    if (!limit && !lead) dp[pos][last] = res;
    return res;
}

ll solve(ll x) {
    memset(dp, -1, sizeof(dp));
    int pos = 0;
    while (x) { a[++pos] = x % 10; x /= 10; }
    return dfs(pos, 10, true, true);  // last=10表示初始无前位
}

int main() {
    ll L, R;
    cin >> L >> R;
    cout << solve(R) - solve(L - 1) << endl;
    return 0;
}
```

!!! info "关于 last=10 的设计"
    当 `lead=true` 且当前位为 0 时，我们用 `last=10` 表示"还没有真正的首位"。由于 `lead=true` 时不记忆化，`dp[pos][10]` 永远不会被写入或读取，所以数组第二维只需开到 11 即可安全。

---

## ⚠️ 常见误区

### 误区1：记忆化时把 `limit` 状态也记录了

**错误**：`if (dp[pos][state] != -1) return dp[pos][state];`（无条件记忆化）

**后果**：第一次查询 x=999 时记忆了"自由状态"的值，第二次查询 x=123 时直接复用，导致统计了 `[1, 999]` 而非 `[1, 123]` 的方案数。

**修复**：加上 `!limit && !lead` 条件，只在"完全自由"时记忆。

### 误区2：不处理前导零

**错误**：统计数字 0 出现次数时，把 `005` 中的两个前导 0 也算进去。

**后果**：数字 0 的统计结果严重偏大。

**修复**：用 `lead` 标记前导零，前导零的 0 不计入统计。判断条件：`(!lead || i != 0) && i == d`。

### 误区3：循环顺序写反（整数划分）

**错误**：完全背包解法中，外层枚举容量、内层枚举物品。

**后果**：`1+2` 和 `2+1` 被当成两种方案，结果变成排列数。

**修复**：外层枚举物品、内层枚举容量，保证每种组合只算一次。

### 误区4：忘记 `dp[0] = 1`

**错误**：计数DP中不初始化空方案。

**后果**：所有转移从 0 开始累加，最终结果恒为 0。

**修复**：`dp[0] = 1`，表示"凑出 0 的方案有 1 种"。

### 误区5：`solve(L-1)` 时 L=1 导致越界

**错误**：`solve(0)` 时 `while (x)` 不进入循环，`pos=0`，递归从 `dfs(-1, ...)` 开始。

**后果**：若模板中 `pos < 0` 返回值设置不当，可能返回错误结果。

**修复**：模板中 `pos < 0` 或 `pos == 0` 的边界要仔细确认；当 x=0 时 `solve` 应返回 0（或不统计 0）。建议统一用 `pos == 0` 作为终止条件，并在 `solve` 中对 x=0 特判。

---

## 典型例题

### 例题1：[P1025] 数的划分

**题意**：将整数 n 分成 k 份，且每份不能为空，任意两个方案不相同（不考虑顺序）。求方案数。n≤200，k≤6。

**分析**：经典的"将 n 分成 k 个正整数之和"。由于不考虑顺序，约定每份不小于上一份（非降序），即可避免重复计数。

**算法选择**：DFS + 剪枝。也可用 DP：`dp[i][j] = dp[i-1][j-1] + dp[i][j-i]`。

**DP推导**：设 `dp[i][j]` 表示把 j 分成 i 份的方案数。考虑最小的那一份：

- 若最小的一份**恰好为 1**：拿掉这个 1，剩下 `j-1` 分成 `i-1` 份 → `dp[i-1][j-1]`
- 若最小的一份**≥ 2**：每份都减 1，剩下 `j-i` 仍分成 i 份 → `dp[i][j-i]`

两者相加即为答案：`dp[i][j] = dp[i-1][j-1] + dp[i][j-i]`。

以 n=7, k=3 为例验证：合法方案为 `1+1+5`、`1+2+4`、`1+3+3`、`2+2+3`，共 4 种。

**代码（DFS版）**：

```cpp
#include <bits/stdc++.h>
using namespace std;
int n, k, ans;

// rest: 剩余; cnt: 还要分几份; last: 上一份最小值
void dfs(int rest, int cnt, int last) {
    if (cnt == 0) { if (rest == 0) ans++; return; }
    for (int i = last; i * cnt <= rest; i++)  // 剪枝
        dfs(rest - i, cnt - 1, i);
}

int main() {
    cin >> n >> k;
    dfs(n, k, 1);
    cout << ans << endl;
    return 0;
}
```

**代码（DP版）**：

```cpp
#include <bits/stdc++.h>
using namespace std;
int n, k, dp[10][205];  // dp[i][j]: 把j分成i份的方案数

int main() {
    cin >> n >> k;
    dp[0][0] = 1;
    for (int i = 1; i <= k; i++)
        for (int j = i; j <= n; j++)
            dp[i][j] = dp[i - 1][j - 1] + dp[i][j - i];
    cout << dp[k][n] << endl;
    return 0;
}
```

**复杂度**：DFS 最坏 O(C(n,k))，剪枝后远小于此；DP 为 O(nk)。

### 例题2：[P2602] 数字计数

**题意**：给定两个数 a, b，求 `[a, b]` 中所有整数里，每个数字 0~9 各出现了多少次。a, b ≤ 10^12。

**分析**：典型数位DP。对每个数字 d 分别统计，用前缀和：`ans(d) = solve(b, d) - solve(a-1, d)`。

**算法选择**：记忆化搜索，状态 `(pos, cnt, limit, lead)`，其中 `cnt` 是数字 d 已出现次数。

**前导零处理要点**：统计数字 0 时，前导零的 0 不能算进去。例如 `005` 中的前两个 0 不是真正的数字位。判断条件 `(!lead || i != 0) && i == d`：

- 当 `d ≠ 0` 时：`i == d` 自动保证 `i ≠ 0`，前导零条件恒为真，不影响。
- 当 `d == 0` 时：只有 `lead=false`（已经开始真正计数）时的 0 才算。

**代码**：见 47.6 节。

**复杂度**：O(10 × 位数 × 位数) = O(10 × 12 × 12)，极快。

### 例题3：[P2657] windy 数

**题意**：windy 数定义为"不含前导零且相邻两个数字之差至少为 2"的正整数。求 `[a, b]` 中 windy 数的个数。a, b ≤ 2×10^9。

**分析**：状态需要记录"上一位数字"。前导零必须处理——前导零后的第一个非零位无需检查差值。

**算法选择**：记忆化搜索，状态 `(pos, last, limit, lead)`。

**前导零处理要点**：windy 数要求"不含前导零"，意味着 `0007` 应视为 `7`，它是一位数，没有"相邻位"概念。因此在 `lead=true` 时：

- 若当前位取 0：维持 `lead=true`，不更新 `last`，不检查差值。
- 若当前位取非零：这是"第一个真实位"，`lead` 转 false，无需检查与前一位的差值（因为根本没有前一位）。

**代码**：见 47.7 节。

**复杂度**：O(位数 × 10) = O(10 × 10)，极快。

### 三道例题对比

| 维度 | P1025 数的划分 | P2602 数字计数 | P2657 windy 数 |
|------|----------------|----------------|----------------|
| 类型 | 计数类DP | 数位DP | 数位DP |
| 状态 | `dp[i][j]` 份数+和 | `(pos, cnt)` | `(pos, last)` |
| 是否需 lead | 不适用 | ✅（数字0） | ✅（首位免检） |
| 核心技巧 | 非降序避免重复 | 前缀和 + 前导零 | 相邻差值≥2 |
| 复杂度 | O(nk) | O(10×位数²) | O(位数×10) |

通过对比可以发现：数位DP的**框架完全一致**，区别仅在于 `state` 的设计和 `lead` 的处理方式。掌握模板后，只需根据题意调整状态定义即可。

---

## 📚 本节练习

### 🟢 基础

**练习1**：[P1025] 数的划分 - https://www.luogu.com.cn/problem/P1025

> 提示：DFS + 剪枝，注意非降序约定避免重复。也可用 DP：`dp[i][j] = dp[i-1][j-1] + dp[i][j-i]`。

**练习2**：[P1048] 采药 - https://www.luogu.com.cn/problem/P1048

> 提示：01背包模板，复习背包DP（计数思想的基础）。

### 🟡 进阶

**练习3**：[P2602] 数字计数 - https://www.luogu.com.cn/problem/P2602

> 提示：对每个数字 0~9 分别做数位DP，注意前导零的处理。前缀和：`solve(R) - solve(L-1)`。

**练习4**：[P2657] windy 数 - https://www.luogu.com.cn/problem/P2657

> 提示：状态记录上一位数字 `last`，前导零后的第一个非零位无需检查差值。

### 🔴 综合

**练习5**：[P4124] 手机号码 - https://www.luogu.com.cn/problem/P4124

> 提示：需要同时记录多个状态：是否有相邻相同、是否有 4、是否有 8、上一位数字。是数位DP的进阶综合题。

**练习6**：[P4127] 同类分布 - https://www.luogu.com.cn/problem/P4127

> 提示：枚举数位之和 S，对每个 S 做数位DP，状态为当前数位之和。需要取模。

---

## 📖 本章总结

### 思维导图

- 计数类DP & 数位统计DP
  - 计数类DP（求方案总数）
    - 整数划分：完全背包（外层物品、内层容量）或 DFS+剪枝（非降序）
    - 其他计数：背包变形，dp[0]=1（空方案），转移用 `+=`
  - 数位统计DP（区间计数）
    - 前缀和思想：`ans = solve(R) - solve(L-1)`
    - 记忆化搜索：`dfs(pos, limit, lead, state)`
    - 状态设计：last / cnt / sum / ... 因题而异
    - 记忆化原则：`limit=true` 或 `lead=true` 时不记忆（临时状态）；`limit=false && lead=false` 时可记忆（通用状态）

### 核心口诀

**计数DP四句诀：**
> 计数DP求方案，转移用加不用减；
> 空方案要初始化，dp[0]等1别忘啦。

**数位DP六句诀：**
> 数位统计前缀和，solve(R)减solve(L)；
> 逐位枚举带上界，limit传递要小心；
> 前导零需单独判，lead为真不记忆；
> 自由状态才记忆，pos加state是关键。

### 速查表

| 题型 | 状态设计 | 关键技巧 | 典型题目 |
|------|----------|----------|----------|
| 整数划分 | `dp[j]` 方案数 | 完全背包，外层物品 | P1025 变体 |
| 分成k份 | `dp[i][j]` | `dp[i-1][j-1]+dp[i][j-i]` | P1025 |
| 数字计数 | `(pos, cnt)` | 前导零的0不计 | P2602 |
| windy数 | `(pos, last)` | 首个非零位免检 | P2657 |
| 数位之和 | `(pos, sum)` | 枚举目标S | P4127 |
| 多限制 | `(pos, last, flag...)` | 状态压缩 | P4124 |

### 数位DP模板检查清单

写数位DP时，逐项核对：

- [ ] `solve(x)` 是否正确拆位？（低位在前，从最高位开始递归）
- [ ] `limit` 是否正确传递？（`limit && i == up`）
- [ ] `lead` 是否正确传递？（`lead && i == 0`）
- [ ] 记忆化条件是否为 `!limit && !lead`？
- [ ] `solve(L-1)` 中 L=1 时（即 `solve(0)`）是否正确返回？
- [ ] 是否需要取模？
- [ ] 数组大小是否足够？（`a[]` 至少 20 位，`dp[]` 第二维覆盖所有状态）

!!! tip "学习建议"
    数位DP的模板高度统一，建议**背熟一个模板**，然后通过改 `state` 的含义来适配不同题目。初学时建议手画"决策树"——把 x=234 这样的具体数字画出来，标出每一位的 `limit` 和 `lead` 取值，能极大加深理解。P2602 和 P2657 是必做题，务必独立写出 AC 代码。

### 调试技巧

数位DP代码出错时，推荐以下调试方法：

1. **暴力对拍**：写一个 O(n) 的暴力枚举程序，与数位DP结果在小数据范围内比对（如 n ≤ 10000），快速定位错误。
2. **打印决策路径**：在 `dfs` 入口处打印 `pos, limit, lead, state`，手动跟踪递归过程，检查是否与手画的决策树一致。
3. **边界测试**：专门测试 `solve(0)`、`solve(1)`、`solve(9)`、`solve(10)`、`solve(99)`、`solve(100)` 等边界值。
4. **单独验证 `lead`**：若怀疑前导零处理有误，对比"有 lead"和"无 lead"两个版本的结果差异。
5. **检查记忆化清空**：每次调用 `solve` 前必须 `memset(dp, -1, sizeof(dp))`，否则上一次查询的残留数据会污染结果。

### 知识衔接

本章是DP专题的进阶篇章，与前后章节的联系如下：

- **前置知识**：第41~44章的基础DP（DP入门、背包、线性DP、区间DP）提供了状态设计与转移的思维基础；第27章的DFS与剪枝是理解数位DP记忆化搜索的前提。
- **后续拓展**：数位DP的状态设计思想可推广到"树形DP + 记忆化"（如在树上统计满足条件的路径数）；计数DP的取模技巧在组合数学中会大量使用。

---

!!! info "下一章预告"
    计数类 DP 与数位 DP 分别解决了"求方案数"和"区间计数"两类问题，核心技巧是**前缀和思想**与**记忆化搜索模板**。下一章将进入**贪心算法进阶**——从区间贪心三大模型到 Huffman 编码，再到排序不等式与推公式贪心，是 CSP-S 中"思维量不低但代码量不高"的性价比之王。
