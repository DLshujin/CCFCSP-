# 第09章 结构体与文件操作

> 💡 一句话概括：本章是编程语言筑基的收官章，掌握结构体（自定义复合类型）和文件操作两大核心技能，为后续算法题的数据管理和本地调试打下基础。

---

## 📌 学习目标

完成本章学习后，你应当能够：

1. **独立定义**包含至少 3 个成员的结构体类型，并正确使用 `.` 运算符访问成员（0 语法错误）
2. **熟练使用**结构体数组完成数据的批量读入、遍历与排序（能写出 `sort` + `cmp` 完整代码）
3. **区分**值传递与引用传递，能根据需求选择 `Student s` 或 `Student &s` 作为函数参数
4. **掌握** `freopen("in.txt", "r", stdin)` 文件重定向，能在 5 分钟内完成"文件读写 + 对拍"环境搭建
5. **了解** `ifstream`/`ofstream` 的基本用法，能写出最简文件读写程序

> 📌 要点
> - 结构体是自定义复合类型，`. ` 运算符访问成员
> - 值传递复制副本、引用传递直接改原值，各有适用场景
> - `freopen` 是竞赛文件读写的最优解，只需两行代码

---

## 💡 生活类比

> 💡 一句话概括：结构体如同学生档案卡，文件操作如同快递柜——把零散信息打包管理，让程序批量读写数据。

> **结构体就像一张「学生档案卡」**。

档案卡上印着姓名、学号、性别、年龄、三科成绩——这些信息都属于同一个学生。你拿着一张卡，就拿到了这个人的全部资料，不用在姓名本、成绩册、年龄表之间来回翻找。

如果把全班 50 个学生的档案卡装进一个**卡片盒**按顺序排好，那就是「结构体数组」。想按成绩排名？把盒子里的卡片重新排一下顺序即可。

> **文件操作就像「快递柜」**：寄件人（输入文件）把数据包裹放进柜子，程序去取件（读数据），处理完再把结果包裹放回另一个柜子（输出文件）。这样你不用守在键盘前手敲数据，程序也能批量处理。

> 📌 要点
> - 结构体 = 一张整合了全部信息的档案卡
> - 结构体数组 = 按序排列的一盒档案卡
> - 文件操作 = 快递柜式批量数据存取

---

## 核心概念

> 💡 一句话概括：结构体让你自定义数据类型，把姓名、年龄、成绩等关联信息打包成一个整体；文件操作让程序脱离键盘屏幕，实现批量数据处理。

### 结构体的定义

「结构体 struct」让你**自己定义一种新的数据类型**，把多个相关数据打包在一起。

📖 通俗解释：结构体就像你自定义的「复合盒子」。`int` 只能装一个整数，但 `struct` 可以同时装姓名（`string`）、年龄（`int`）、成绩（`int`）等多个不同类型的数据，一个变量就搞定。

```cpp
struct 类型名 {
    成员1的类型 成员1的名字;
    成员2的类型 成员2的名字;
    ...
};                        // 末尾必须有分号
```

> ⚠️ 警告：易错提醒
> 结构体定义的**右大括号 `}` 后面必须加分号 `;`**，忘了会编译报错！定义的是"类型"而非"变量"——就像 `int` 是类型，要 `int a;` 才是变量。

### 成员访问（. 运算符）

声明结构体变量后，用**点运算符 `.`** 访问其成员：

| 操作 | 语法 | 示例 |
|------|------|------|
| 声明变量 | `类型名 变量名;` | `Student s1;` |
| 访问成员 | `变量名.成员名` | `s1.name`、`s1.score` |
| 修改成员 | `变量名.成员名 = 值;` | `s1.age = 12;` |
| 数组元素访问 | `数组名[i].成员名` | `stu[i].score` |

### 文件操作一览

C++ 提供了多种文件读写方式，竞赛中最常用的是 `freopen`：

| 方式 | 头文件 | 特点 | 适用场景 |
|------|--------|------|----------|
| `freopen` 重定向 | `<cstdio>` | 把 `cin`/`cout` 重定向到文件，**代码改动最小** | ⭐ 竞赛首选 |
| `ifstream`/`ofstream` | `<fstream>` | C++ 流式文件操作，面向对象 | 了解即可 |
| `fopen`/`fclose` | `<cstdio>` | C 风格，需手动管理文件指针 | 一般不用 |

> 💡 提示：竞赛建议
> CSP-J/S 复赛中，**`freopen` 是最省事的方式**——只需在 `main` 开头加两行，原有的 `cin`/`cout` 代码一行都不用改。

> 📌 要点
> - `struct` 定义的是类型而非变量，末尾必须加分号
> - `.` 运算符是访问成员的唯一方式
> - 文件操作三种方式中，`freopen` 是竞赛首选

---

## 代码实现

> 💡 一句话概括：从结构体定义、数组成员访问、函数参数传递，到 `freopen` 文件重定向，每一步都有标准写法。

### 结构体定义与成员访问

```cpp
#include <bits/stdc++.h>
using namespace std;

struct Student {
    string name;          // 姓名
    int age;              // 年龄
    int score;            // 成绩
};                        // 注意末尾有分号！

int main() {
    Student s1;                       // 声明变量
    s1.name = "张三";
    s1.age = 12;
    s1.score = 90;

    // 三种初始化方式
    Student s2 = {"李四", 13, 85};    // 方式1：按顺序初始化
    Student s3{"王五", 12, 78};       // 方式2：C++11 列表初始化

    cout << s1.name << " " << s1.age << " " << s1.score << endl;
    // 输出：张三 12 90
    cout << s2.name << " " << s2.age << " " << s2.score << endl;
    // 输出：李四 13 85
    return 0;
}
```

### 结构体数组

```cpp
#include <bits/stdc++.h>
using namespace std;

struct Student {
    string name;
    int age;
    int score;
};

int main() {
    int n;
    cin >> n;

    Student stu[105];
    for (int i = 0; i < n; i++) {
        cin >> stu[i].name >> stu[i].age >> stu[i].score;
    }

    // 遍历输出（输入什么就输出什么）
    for (int i = 0; i < n; i++) {
        cout << stu[i].name << " " << stu[i].age << " " << stu[i].score << endl;
        // 输出：stu[i] 的姓名、年龄、成绩
    }
    return 0;
}
```

> 💡 提示：访问方式
> `stu[i].name` 表示"数组 `stu` 的第 `i` 个元素的 `name` 成员"——先取数组元素，再取成员。

### 结构体作为函数参数（值传递 vs 引用传递）

📖 通俗解释：值传递就像复印一张档案卡给你，你在复印件上修改不影响原件；引用传递就像直接把原件递给你，你改的就是原件本身。

```cpp
#include <bits/stdc++.h>
using namespace std;

struct Student {
    string name;
    int age;
    int score;
};

// 值传递：复制整个结构体，函数内修改不影响原变量
void printStudent(Student s) {
    cout << s.name << " " << s.age << " " << s.score << endl;
}

// 引用传递：直接操作原变量，修改生效
void addBonus(Student &s, int bonus) {
    s.score += bonus;
}

int main() {
    Student s1 = {"张三", 12, 85};
    printStudent(s1);              // 输出：张三 12 85

    addBonus(s1, 10);              // 加 10 分（引用传递，原变量被改）
    printStudent(s1);              // 输出：张三 12 95
    return 0;
}
```

> 💡 提示：传结构体用引用
> 结构体通常比普通变量大（含多个成员），**值传递会复制整个结构体**，浪费时间和空间。所以**传结构体通常用引用 `&`**：
>
> | 场景 | 写法 | 说明 |
> |------|------|------|
> | 需要**修改**原变量 | `Student &s` | 直接操作原变量 |
> | 只**读取**不修改 | `const Student &s` | 加 `const` 防止误改 |

### freopen 文件重定向（竞赛最常用）

📖 通俗解释：`freopen` 就像给 `cin`/`cout` 换了个「聊天对象」——本来 `cin` 跟键盘聊天、`cout` 跟屏幕聊天，加了 `freopen` 后，`cin` 改成跟文件聊天、`cout` 也改成跟文件聊天。

`freopen` 把 `cin`/`cout` 或 `scanf`/`printf` **重定向到文件**，原有读写代码完全不用改：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    freopen("in.txt", "r", stdin);       // 从 in.txt 读
    freopen("out.txt", "w", stdout);     // 写到 out.txt

    int a, b;
    cin >> a >> b;                        // 实际从 in.txt 读
    cout << a + b;                        // 实际写到 out.txt（输出 a+b 的结果）
    return 0;
}
```

| 参数 | 含义 |
|------|------|
| `"in.txt"` | 文件名（默认在程序运行目录） |
| `"r"` | read，只读模式（输入） |
| `"w"` | write，写入模式（输出，会覆盖原文件） |
| `stdin` | 标准输入（键盘） |
| `stdout` | 标准输出（屏幕） |

> ⚠️ 警告：文件位置与路径
> `freopen` 默认在**程序运行的当前目录**找文件。若不确定位置，可写**绝对路径**：
>
> ```cpp
> freopen("D:\\test\\in.txt", "r", stdin);   // 路径用双反斜杠 \\
> ```

> ⚠️ 警告：提交前务必删除 freopen
> 洛谷等在线评测系统**不允许读写文件**！本地调试用 `freopen`，**提交代码前一定要删掉或注释掉这两行**，否则会 WA 或 RE。

### ifstream / ofstream（了解即可）

C++ 提供了面向对象的文件流类，用法和 `cin`/`cout` 类似：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ifstream fin("in.txt");        // 打开输入文件（相当于 cin）
    ofstream fout("out.txt");      // 打开输出文件（相当于 cout）

    int a, b;
    fin >> a >> b;                 // 从文件读
    fout << a + b;                 // 写到文件（输出 a+b 的结果）

    fin.close();                   // 关闭输入文件
    fout.close();                  // 关闭输出文件
    return 0;
}
```

> 📌 要点：ifstream/ofstream vs freopen
> | 对比维度 | `freopen` | `ifstream`/`ofstream` |
> |----------|-----------|----------------------|
> | 改动量 | 只需加两行 | 需把 `cin` 改成 `fin`、`cout` 改成 `fout` |
> | 灵活性 | 一般 | 可同时读写多个文件 |
> | 竞赛推荐 | ⭐ 首选 | 了解即可 |

> 📌 要点
> - 结构体定义末尾分号不可省略，初始化有三种方式
> - 传结构体参数优先用引用 `&`，只读场景加 `const`
> - `freopen` 两行搞定文件读写，提交前记得注释掉

---

## 指针与引用

> 💡 一句话概括：指针存储变量的地址，引用是变量的别名——两者都是 2025 年 J 组新增考点，理解它们能写出更高效的函数传参。

> 📌 要点：2025 年新增考点（难度 5）
> 指针与引用是 C++ 的进阶语法。CSP-J 以往很少考，2025 年大纲新增为考点。掌握"地址、解引用、引用传参"即可应对初赛选择题与简单复赛题。

### 指针的概念

📖 通俗解释：每个变量都存放在内存的某个"房间"里，房间有编号（地址）。**指针**就是专门**存放地址**的变量——它不存数据本身，而存"数据在哪个房间"。

```
变量 a = 10，存放在地址 0x1000
指针 p 存放 0x1000（即 a 的地址）
通过 p 可以"顺藤摸瓜"找到 a
```

### 指针的定义与解引用

```cpp
int a = 10;
int *p = &a;        // & 取地址：p 指向 a，p 的值是 a 的地址
cout << p << endl;  // 输出 a 的地址（如 0x7ffe...）
cout << *p << endl; // * 解引用：通过 p 取出 a 的值，输出 10

*p = 20;            // 通过指针修改 a 的值
cout << a << endl;  // 输出：20（a 被改了！）
```

| 符号 | 含义 | 示例 |
|------|------|------|
| `&` | 取地址 | `&a` 得到 a 的地址 |
| `*`（声明时） | 定义指针 | `int *p;` |
| `*`（使用时） | 解引用 | `*p` 取出 p 指向的值 |

### 指针与数组的关系

数组名本身就是指向首元素的指针，指针可以像数组下标一样使用：

```cpp
int a[5] = {10, 20, 30, 40, 50};
int *p = a;             // p 指向 a[0]
cout << *p << endl;     // 输出：10
cout << *(p + 2) << endl; // 输出：30（p+2 指向 a[2]）
cout << p[2] << endl;   // 输出：30（指针也能用下标）
```

> 💡 提示：数组名 vs 指针
> `a` 和 `p` 都能当指针用，但 `a` 是常量指针（不能 `a++`），`p` 是变量指针（可以 `p++` 移动）。

### 引用的概念与定义

**引用**是变量的**别名**：给一个已有变量起另一个名字，操作引用就是操作原变量。

```cpp
int a = 10;
int &r = a;          // r 是 a 的别名（引用）
cout << r << endl;   // 输出：10
r = 20;              // 改 r 就是改 a
cout << a << endl;   // 输出：20
```

### 引用与指针的区别

| 对比项 | 指针 `int *p` | 引用 `int &r` |
|--------|--------------|--------------|
| 是否必须初始化 | ❌ 可以不初始化 | ✅ **必须**初始化 |
| 能否重新绑定 | ✅ 可指向别的变量 | ❌ **不能**，终身绑定 |
| 是否可为空 | ✅ 可为 `nullptr` | ❌ 必须绑定有效变量 |
| 安全性 | 低（易野指针） | **高**（更安全） |
| 语法 | 需 `*` 解引用 | 直接当变量用 |

> ⚠️ 警告：引用的三条铁律
> 1. 引用**必须**在定义时初始化：`int &r;` ❌（错）
> 2. 引用**不能**重新绑定到别的变量（赋值只是改原变量的值）
> 3. 引用**不能**绑定临时值（右值），除非加 `const`

### 引用在函数传参中的使用

引用最常见的用途是**函数传参**——避免拷贝大对象，同时能修改原变量。

```cpp
struct Student { string name; int score; };  // 结构体可能很大

// 值传递：拷贝整个结构体，慢且占空间
void printVal(Student s) { cout << s.name; }

// 引用传递：不拷贝，直接操作原变量，高效
void printRef(const Student &s) { cout << s.name; }  // const 防误改
```

### 代码示例：swap 函数的指针版与引用版对比

```cpp
#include <bits/stdc++.h>
using namespace std;

// 指针版：传地址
void swapPtr(int *a, int *b) {
    int t = *a;
    *a = *b;
    *b = t;
}

// 引用版：传别名（推荐！更简洁安全）
void swapRef(int &a, int &b) {
    int t = a;
    a = b;
    b = t;
}

int main() {
    int x = 3, y = 5;
    swapPtr(&x, &y);           // 传地址
    cout << x << " " << y << endl;  // 输出：5 3

    swapRef(x, y);             // 传引用，调用更简洁
    cout << x << " " << y << endl;  // 输出：3 5
    return 0;
}
```

> 💡 提示：竞赛建议
> - 传结构体/容器（vector、string）**优先用引用 `&`**，避免拷贝开销
> - 只读不修改加 `const &`，防止误改
> - 指针在 CSP-J 中考得少，理解"地址+解引用"即可；引用传参是必会技能

> 📌 要点
> - 指针存地址，`&` 取地址、`*` 解引用；数组名是指向首元素的指针
> - 引用是别名，必须初始化、不能重新绑定、更安全
> - 函数传参优先用引用 `&`，大对象加 `const &` 避免拷贝

---

## ⚠️ 常见误区与陷阱

> 💡 一句话概括：结构体分号、值引用传递、`freopen` 提交前删除——这些是初学者最高频的踩坑点，逐一攻克即可避坑。

### 常见陷阱

#### 陷阱1：结构体定义漏分号

❌ 错误写法：
```cpp
struct Student {
    string name;
    int score;
}                            // ❌ 漏了分号！
// 编译报错：expected ';' after struct
```

✅ 正确写法：右大括号后**必须加 `;`** → `};`

---

#### 陷阱2：值传递误以为能修改原值

❌ 错误写法：
```cpp
void addBonus(Student s, int bonus) {   // ❌ 值传递！
    s.score += bonus;                    // 改的是副本，原变量不变
}

int main() {
    Student s1 = {"张三", 12, 85};
    addBonus(s1, 10);
    cout << s1.score;                    // 输出：85（仍是 85，没加成！）
}
```

✅ 正确写法：想修改原变量，参数必须用**引用 `&`** → `void addBonus(Student &s, int bonus)`

---

#### 陷阱3：提交时忘记删除 freopen

❌ 错误：本地调试用了 `freopen`，提交到洛谷时忘记删除 → 程序去读不存在的文件，导致 WA 或 RE

✅ 正确：提交前注释掉 `freopen` 两行（而非删除，方便下次调试再用）：
```cpp
// freopen("in.txt", "r", stdin);
// freopen("out.txt", "w", stdout);
```

---

#### 陷阱4：排序比较函数方向写反

❌ 错误：
```cpp
bool cmp(Student a, Student b) {
    return a.score < b.score;    // ❌ 这是升序！想要降序结果搞反了
}
```

✅ 正确：
```cpp
bool cmp(Student a, Student b) {
    return a.score > b.score;    // ✅ 降序（从大到小）
}
```

| 写法 | 排序方向 |
|------|----------|
| `return a.score > b.score;` | 降序（从大到小） |
| `return a.score < b.score;` | 升序（从小到大） |

---

#### 陷阱5：忘记关闭文件流

❌ 错误：用 `ifstream`/`ofstream` 后忘记 `close()`

✅ 正确：养成 `fin.close(); fout.close();` 的习惯。虽然程序正常退出时操作系统会自动回收文件句柄，但 `close()` 能确保缓冲区数据写入磁盘，且避免复杂程序中文件句柄耗尽。

> ⚠️ 警告：注意
> `freopen` 重定向的 `stdin`/`stdout` 一般不需要手动关闭。

> 📌 要点
> - 结构体 `}` 后必须加分号；想修改原值必须用引用 `&`
> - 提交前注释 `freopen`；`cmp` 中 `>` 是降序、`<` 是升序
> - 使用文件流记得 `close()`

---

## 典型例题：学生成绩管理

> 💡 一句话概括：学生成绩管理综合了结构体定义、数组排序、多关键字 `cmp`、文件读写，是 CSP-J/S 的经典模板题。

> **题目**：输入 n 名学生的姓名、年龄、语文、数学、英语三科成绩，按总分从高到低排序输出排名；总分相同者按姓名字典序升序。同时输出最高分、最低分和平均分。

这道题综合考查**结构体定义、结构体数组、多关键字排序、文件读写**，是 CSP-J/S 的经典题型。

```cpp
#include <bits/stdc++.h>
using namespace std;

struct Student {
    string name;
    int age;
    int chinese, math, english;
    int total;                 // 总分
};

// 多关键字排序：总分降序，总分相同按姓名升序
bool cmp(const Student &a, const Student &b) {
    if (a.total != b.total) return a.total > b.total;
    return a.name < b.name;
}

int main() {
    freopen("in.txt", "r", stdin);        // 本地调试用，提交时删掉
    freopen("out.txt", "w", stdout);

    int n;
    cin >> n;

    Student stu[105];
    for (int i = 0; i < n; i++) {
        cin >> stu[i].name >> stu[i].age;
        cin >> stu[i].chinese >> stu[i].math >> stu[i].english;
        stu[i].total = stu[i].chinese + stu[i].math + stu[i].english;
    }

    sort(stu, stu + n, cmp);

    // 输出排名
    cout << "====== 成绩排行榜 ======" << endl;
    for (int i = 0; i < n; i++) {
        cout << "第" << i + 1 << "名：" << stu[i].name << " "
             << stu[i].age << "岁 总分" << stu[i].total << "分" << endl;
    }

    // 统计信息
    int maxScore = stu[0].total;          // 排序后第一个是最高
    int minScore = stu[n - 1].total;      // 最后一个是最低
    long long sum = 0;
    for (int i = 0; i < n; i++) sum += stu[i].total;
    double avg = (double)sum / n;

    cout << "========================" << endl;
    cout << "最高分：" << maxScore << endl;
    cout << "最低分：" << minScore << endl;
    cout << "平均分：" << avg << endl;
    return 0;
}
```

**输入文件 `in.txt`**：
```
5
Alice 12 90 85 88
Bob 13 78 92 80
Carol 12 90 85 88
David 14 95 90 92
Eve 13 70 65 60
```

**输出文件 `out.txt`**：
```
====== 成绩排行榜 ======
第1名：David 14岁 总分277分
第2名：Alice 12岁 总分263分
第3名：Carol 12岁 总分263分
第4名：Bob 13岁 总分250分
第5名：Eve 13岁 总分195分
========================
最高分：277
最低分：195
平均分：249.6
```

> 💡 提示：多关键字排序模板
> ```cpp
> bool cmp(Student a, Student b) {
>     if (a.主关键字 != b.主关键字) return a.主关键字 > b.主关键字;  // 降序
>     return a.次关键字 < b.次关键字;                                // 升序
> }
> ```
> 这是 CSP-S 排序题的**万能模板**，务必记牢。Alice 和 Carol 总分相同，因 `Alice < Carol`（字典序），Alice 排前面。

### 关键代码模板

#### 模板1：结构体定义 + 数组 + 排序（万能模板）

```cpp
struct Student {
    string name;
    int score;
};
Student stu[105];
bool cmp(Student a, Student b) {
    return a.score > b.score;  // 按成绩降序
}
sort(stu, stu + n, cmp);
```

#### 模板2：多关键字排序

```cpp
bool cmp(Student a, Student b) {
    if (a.主关键字 != b.主关键字) return a.主关键字 > b.主关键字;  // 第一关键字降序
    if (a.次关键字 != b.次关键字) return a.次关键字 < b.次关键字;  // 第二关键字升序
    return a.第三关键字 < b.第三关键字;                             // 第三关键字升序
}
```

#### 模板3：freopen 文件重定向（竞赛标准写法）

```cpp
int main() {
    freopen("in.txt", "r", stdin);
    freopen("out.txt", "w", stdout);
    // ... 原有 cin/cout 代码不动 ...
    return 0;
}
```

#### 模板4：结构体引用传递

```cpp
void func(Student &s) { ... }          // 需要修改原变量
void func(const Student &s) { ... }    // 只读不修改，const 防误改
```

> 📌 要点
> - 多关键字排序：`cmp` 中先判断主关键字是否相等，不等则比主关键字，相等则比次关键字
> - 排序后 `stu[0]` 是第一名（最高分），`stu[n-1]` 是最后一名（最低分）
> - 四个关键模板覆盖了结构体章节 90% 的代码场景

---

## 📚 本节练习

> 💡 一句话概括：从基础的结构体读写到进阶的多关键字排序，六道题目覆盖了 CSP-J/S 结构体与排序的全部考点。

### 🟢 基础题（巩固结构体基本用法）

**练习 1**：[P5742] 评等级 - https://www.luogu.com.cn/problem/P5742
> 输入 n 名学生的姓名和成绩，根据成绩评定等级并输出。
>
> **提示**：定义结构体 `{string name; int score;}`，读入后计算平均分，再遍历判断每人等级。重点练习**结构体数组的读入与成员访问**。

**练习 2**：[P5740] 最厉害的学生 - https://www.luogu.com.cn/problem/P5740
> 输入 n 名学生的姓名和语数英三科成绩，找出总分最高的学生，输出其姓名和三科成绩。
>
> **提示**：结构体含 `name`、`chinese`、`math`、`english`、`total` 五个成员，遍历维护最大值即可，不必排序。

### 🟡 拓展题（结构体 + 排序 / 嵌套循环）

**练习 3**：[P5741] 旗鼓相当的对手 - https://www.luogu.com.cn/problem/P5741
> 输入 n 名学生的姓名和三科成绩，找出每对"旗鼓相当"的学生（各科成绩差值均不超过 5），按字典序输出。
>
> **提示**：结构体存三科成绩，双重循环枚举所有学生对 `(i, j)`，判断三科差值绝对值是否都 ≤ 5。用 `abs()` 取绝对值。

**练习 4**：成绩排序（洛谷 B2099）- https://www.luogu.com.cn/problem/B2099
> 输入 n 名学生的姓名和成绩，按成绩从高到低排序输出；成绩相同则按输入顺序输出。
>
> **提示**：结构体加一个"输入序号"字段作为第二关键字，用 `stable_sort` 或在 `cmp` 中处理。练习 **`sort` + `cmp`** 标准写法。

### 🔴 进阶题（多关键字排序，CSP-S 真题变式）

**练习 5**：[P1781] 宇宙总统 - https://www.luogu.com.cn/problem/P1781
> n 个人竞选总统，每人有选票数。找出票数最多的人，输出其编号（从 1 开始）和票数。
>
> **提示**：结构体 `{int id; int votes;}`，遍历找最大值。也可用 `sort` 排序取第一名。注意编号从 1 开始。

**练习 6**：[P1068] 分数线划定 - https://www.luogu.com.cn/problem/P1068
> 按面试成绩从高到低取第 `m×150%`（向下取整）名所在分数为分数线，分数不低于该线者全部入选。成绩相同者编号小者优先。
>
> **提示**：NOIP2009 普及组真题。结构体含 `id` 和 `score`，`cmp` 先按 `score` 降序、相同按 `id` 升序。注意分数线可能让更多人并列入选——需从第 `k` 名往后继续检查相同分数者。

> 📌 要点
> - 基础题练结构体读写，拓展题练 `sort` + `cmp`，进阶题练多关键字排序
> - 练习 6 是 NOIP 真题，理解"向下取整"和"同分并列入选"两个关键点

---

## 📖 本章总结

> 💡 一句话概括：结构体打包数据，文件操作对接外部——这是编程语言筑基模块的最后一章，也是初赛到复赛的重要过渡。

### 核心要点回顾

- ✅ 结构体用 `struct` 定义，把**多个相关数据打包**成新类型
- ✅ 访问成员用 **`.`** 运算符：`s.name`、`stu[i].score`
- ✅ 结构体数组可管理多条结构化数据，配合 `sort` + `cmp` 排序
- ✅ 函数参数：**值传递**复制副本（不改原值），**引用传递 `&`** 直接改原值
- ✅ `freopen("in.txt", "r", stdin)` 是竞赛最常用的文件读写方式
- ✅ `ifstream`/`ofstream` 是 C++ 流式文件操作，了解即可

### 结构体速查表

| 操作 | 代码 |
|------|------|
| 定义 | `struct Student { string name; int score; };` |
| 声明变量 | `Student s;` |
| 初始化 | `Student s = {"张三", 90};` |
| 访问成员 | `s.name`、`s.score` |
| 结构体数组 | `Student stu[100];` |
| 排序 | `sort(stu, stu+n, cmp);` |
| 值传递 | `void f(Student s)` |
| 引用传递 | `void f(Student &s)` |
| 文件重定向 | `freopen("in.txt", "r", stdin);` |

### 知识脉络

- 第09章 结构体与文件
  - 结构体
    - 定义：`struct 类型名 { 成员; };`（末尾分号不可省略）
    - 访问成员：`变量名.成员名`
    - 初始化：`Student s = {"张三", 90};` 或 `Student s{"张三", 90};`
  - 结构体数组
    - 声明数组：`Student stu[100];`
    - 遍历：`for (int i = 0; i < n; i++)` + `stu[i].成员名`
    - 排序：`sort(stu, stu + n, cmp);` 配合自定义 `cmp` 函数
  - 文件操作
    - `freopen` 重定向：`freopen("in.txt", "r", stdin);` + `freopen("out.txt", "w", stdout);`
    - `ifstream` / `ofstream`：`fin >> x;` / `fout << x;`
    - `close()`：`fin.close();` / `fout.close();`
  - 函数参数传递
    - 值传递：`Student s`（复制整个结构体，副本修改不影响原值）
    - 引用传递：`Student &s`（直接操作原变量，修改生效）
    - 只读引用：`const Student &s`（防止误改，仅读取）

### 知识衔接

本章是CSP-J/S入门教材编程语言筑基模块（模块一）的收官章节，与前后章节的联系如下：

- **前置知识**：第08章「函数与递归入门」——函数定义与调用、参数传递（值传递与引用传递）、递归思维
- **本章定位**：自定义数据类型+文件I/O，掌握struct定义与多关键字排序、freopen文件重定向，是竞赛中对拍调试的基础
- **后续发展**：为第10章「算法效率与时间复杂度」铺路——模块一（编程语言筑基）全部完成，进入模块二（基础算法），从"写对代码"走向"写快代码"

> 💡 提示：下一章预告
> 模块一（编程语言筑基）已全部完成！第 10 章进入**模块二：基础算法**，先学习**算法效率与时间复杂度**——这是初赛必考、复赛必用的核心技能，掌握它你就能根据数据范围一秒选出正确算法！

> 📌 要点
> - 结构体 + 排序 + 文件操作是本章「三件套」，CSP-J/S 高频考点
> - `cmp` 多关键字排序模板和 `freopen` 两行代码是必须背熟的技能
> - 下一章进入模块二，开启算法复杂度分析与基础算法