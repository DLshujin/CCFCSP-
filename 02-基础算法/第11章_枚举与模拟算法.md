# 第11章 枚举与模拟算法

!!! info "本章内容"
    本章将让你学会两种最基础的算法思想。你将学习：

    - 枚举（暴力）的思想与实现
    - 模拟：照着题意一步步做
    - 枚举的优化技巧（减少维度、缩小范围、对称性剪枝等）
    - 经典枚举与模拟问题（百钱买百鸡、约瑟夫问题）

!!! warning "本章特点"
    枚举和模拟是 **CSP-J/S 复赛最常考** 的算法类型，也是"暴力骗分"的基础。学好它们，至少能拿到每题的部分分！

---

## 📌 学习目标

枚举与模拟是算法学习的"第一课"，也是 CSP-J/S 赛场上最稳妥的得分手段。读完本章后，你应当能够：

1. **掌握枚举四步法**：能在 10 分钟内独立写出"找素数""百钱买百鸡"等基础枚举题的完整代码，正确率 ≥ 90%
2. **应用 4 种以上枚举优化技巧**：减少维度、缩小范围、提前终止、空间换时间，能将 $O(n^3)$ 优化到 $O(n^2 \log n)$ 或更优
3. **忠实模拟题意**：能准确翻译题目描述的每一步操作，处理约瑟夫环、日期计算、扫雷等典型模拟题，**不漏步、不越界**
4. **熟练使用方向数组**：用 `dx[]`/`dy[]` 处理网格 4/8 方向问题，配合越界检查写出健壮的网格模拟代码
5. **识别并规避 4 大常见误区**：枚举范围过大超时、模拟遗漏边界条件、变量未初始化、方向数组越界访问

---

## 💡 生活类比

!!! tip "枚举像「挨个试钥匙开锁」"
    你面前有一大串钥匙，但不知道哪把能开锁。最直接的办法就是**一把一把试**：第一把不行换第二把，第二把不行换第三把……直到锁"咔哒"一声打开。

    虽然看起来"笨"，但**一定能找到答案**——只要钥匙串里有对的那把。这就是枚举的最大优势：**简单且保证正确**。

    当然，聪明的你会先观察锁孔形状，**排除明显不对的钥匙**（缩小范围），这就引出了后面的"枚举优化"。

!!! tip "模拟像「照着菜谱一步步做菜」"
    菜谱说"放油 → 放葱 → 放鸡蛋 → 翻炒 → 出锅"，你就**一步一步照做**。不需要什么巧妙的算法，关键是**不漏掉任何一步**，也不擅自加料或跳步。

    做菜漏了放盐，菜就难吃；模拟漏了一步操作，程序就出错。**忠实翻译题意**是模拟的核心。

!!! info "枚举与模拟的关系"
    枚举和模拟常常**配合使用**：先用枚举列出所有可能，再用模拟验证每种情况是否满足题意。例如百钱买百鸡——枚举公鸡母鸡数量（枚举），验证总价是否 100 元（模拟验证）。两者是 CSP-J 选手的"双刀流"。

---

## 核心概念

### 枚举思想

「枚举 enumeration」就是**把所有可能的情况都试一遍**，找到符合条件的答案。

**枚举四步法**：

1. **确定枚举对象**：枚举什么？（数字、位置、组合……）
2. **确定枚举范围**：从哪到哪？
3. **确定筛选条件**：什么样的情况是答案？
4. **写循环遍历**：用 for 循环枚举所有可能

### 模拟思想

「模拟 simulation」就是**按照题目描述的过程，一步步用代码实现**。

**模拟四要点**：

1. **仔细读题**：每一步操作都要理解清楚，不脑补、不简化
2. **用变量记录状态**：当前在哪、当前位置是什么、还剩多少
3. **按顺序执行**：严格按题目描述的步骤，不调换顺序
4. **注意边界**：数组越界、特殊情况、空输入

### 枚举优化技巧一览表

| 优化技巧 | 核心思路 | 典型应用 | 复杂度变化 |
|---------|---------|---------|-----------|
| **减少枚举维度** | 用方程关系消去变量 | 百钱买百鸡：三重 → 两重 | $O(n^3) \to O(n^2)$ |
| **缩小枚举范围** | 根据约束收紧上下界 | 公鸡最多 20 只（非 100） | 常数级优化 |
| **提前终止** | 找到答案即 `break` | 找素数发现因子即停 | 平均情况优化 |
| **空间换时间** | 数组标记已访问状态 | 判重、查表、八皇后冲突检测 | $O(n^2) \to O(n)$ |
| **对称性剪枝** | 利用对称性跳过等价情况 | 八皇后镜像对称 | 常数减半 |
| **数学优化** | 利用数学性质减少枚举 | 素数只查到 $\sqrt{n}$ | $O(n) \to O(\sqrt{n})$ |

!!! info "技巧选择"
    CSP-J 中最常用的是**前四种**。对称性剪枝和数学优化属于进阶技巧，在 CSP-S 中才能体现出明显优势。

### 枚举优化技巧详解

**技巧 1：减少枚举维度**

给定数组，找三个数使它们的和为 0。朴素三重循环 $O(n^3)$：

```cpp
for (int i = 0; i < n; i++)
    for (int j = i+1; j < n; j++)
        for (int k = j+1; k < n; k++)
            if (a[i] + a[j] + a[k] == 0) // 找到
```

**优化**：先排序，第三层用二分查找，降到 $O(n^2 \log n)$。

**技巧 2：提前终止**

找到答案就 `break`，不再继续枚举。

```cpp
bool found = false;
for (int i = 0; i < n && !found; i++) {      // 找到就停
    for (int j = 0; j < n && !found; j++) {
        if (满足条件) {
            found = true;
        }
    }
}
```

**技巧 3：空间换时间**

用数组记录已出现的状态，避免重复计算。

```cpp
// 判断数组中是否有重复元素
bool seen[100005] = {};
bool hasDup = false;
for (int i = 0; i < n; i++) {
    if (seen[a[i]]) {                // 之前出现过
        hasDup = true;
        break;
    }
    seen[a[i]] = true;
}
```

**技巧 4：对称性剪枝**

八皇后问题中，棋盘旋转 90°/180°/270° 后的解等价。可以只搜索"第一行皇后在第 1~4 列"的情况，剩下的通过对称变换得到，常数减半。CSP-S 中遇到对称问题可考虑此优化。

---

## 代码实现

### 枚举示例：判断素数

判断一个数 $n$ 是不是素数（只能被 1 和它本身整除）。

**朴素枚举**：从 2 试到 $n-1$，看有没有能整除的。

**优化**：只需枚举到 $\sqrt{n}$。原理：若 $n = a \times b$ 且 $a \leq \sqrt{n}$，则 $b \geq \sqrt{n}$，只需检查较小因子即可。

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    
    bool isPrime = true;
    for (int i = 2; i * i <= n; i++) {    // 优化：只到 sqrt(n)
        if (n % i == 0) {
            isPrime = false;
            break;                         // 提前终止
        }
    }
    
    if (isPrime && n >= 2) {
        cout << n << " 是素数";
    } else {
        cout << n << " 不是素数";
    }
    return 0;
}
```

!!! tip "i * i <= n 的妙用"
    用 `i * i <= n` 代替 `i <= sqrt(n)`，避免浮点运算，更高效更精确。复杂度从 $O(n)$ 降到 $O(\sqrt{n})$，对 $n = 10^9$ 只需约 31623 次运算！

!!! warning "注意 n < 2 的情况"
    0 和 1 不是素数，代码里要加 `n >= 2` 的判断。

### 模拟示例：日期计算

输入年月日，求这是这一年的第几天。

**模拟思路**：把前面各月份的天数累加，再加上当前日。

```cpp
#include <bits/stdc++.h>
using namespace std;

int days[] = {0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};

bool isLeap(int year) {
    if (year % 400 == 0) return true;
    if (year % 100 == 0) return false;
    if (year % 4 == 0) return true;
    return false;
}

int main() {
    int y, m, d;
    cin >> y >> m >> d;
    
    int total = 0;
    for (int i = 1; i < m; i++) {       // 累加前 m-1 个月
        total += days[i];
        if (i == 2 && isLeap(y)) {      // 闰年 2 月多一天
            total += 1;
        }
    }
    total += d;                          // 加上当月天数
    
    cout << total;
    return 0;
}
```

!!! tip "闰年规则"
    - 能被 400 整除：闰年
    - 能被 100 整除但不是 400：平年
    - 能被 4 整除但不是 100：闰年
    - 其他：平年

    简记：**四年一闰，百年不闰，四百年再闰**。

### 综合示例：扫雷游戏（简化版）

在一个 $n \times n$ 的格子里，有些格子有雷（用 `*` 表示），有些没雷（用 `?` 表示）。对每个没雷的格子，输出它周围 8 个方向上有多少个雷。

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    
    char grid[105][105];
    int ans[105][105] = {};
    
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            cin >> grid[i][j];
        }
    }
    
    // 8 个方向
    int dx[] = {-1, -1, -1, 0, 0, 1, 1, 1};
    int dy[] = {-1, 0, 1, -1, 1, -1, 0, 1};
    
    // 枚举每个格子
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            if (grid[i][j] == '*') {
                ans[i][j] = -1;              // 雷的位置标记为 -1
            } else {
                int cnt = 0;
                for (int d = 0; d < 8; d++) {
                    int ni = i + dx[d];
                    int nj = j + dy[d];
                    // 检查是否越界
                    if (ni >= 0 && ni < n && nj >= 0 && nj < n) {
                        if (grid[ni][nj] == '*') cnt++;
                    }
                }
                ans[i][j] = cnt;
            }
        }
    }
    
    // 输出结果
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++) {
            if (ans[i][j] == -1) cout << '*';
            else cout << ans[i][j];
        }
        cout << endl;
    }
    return 0;
}
```

!!! tip "方向数组的妙用"
    用 `dx[]` 和 `dy[]` 数组表示 8 个方向，比写 8 个 if 判断简洁得多。这是搜索题的常用技巧，后面的 DFS/BFS 章节会大量使用。

---

## ⚠️ 常见误区

!!! warning "误区 1：枚举范围过大导致超时"
    **错误**：百钱买百鸡用三重循环，每个变量都从 0 枚举到 100，共 $101^3 \approx 10^6$ 次。
    
    **正确**：根据约束缩小范围——公鸡最多 20 只（100/5），母鸡最多 33 只（100/3），并用 $z = 100 - x - y$ 消去一维，降到 $21 \times 34 \approx 714$ 次。
    
    **口诀**：先看约束再定范围，能消一维是一维。

!!! warning "误区 2：模拟遗漏边界条件"
    **错误**：约瑟夫环中 `pos` 初始化为 0，导致第一次报数就从 1 号开始数，少报一人。
    
    **正确**：`pos = -1`，先 `(pos+1) % n` 移动到 0 号再报数，保证从 1 号开始。
    
    **常见边界坑**：
    - 数组越界（下标 0 vs 1 混淆）
    - 取模运算后回到起点
    - 空数组、单个元素
    - 闰年 2 月 29 天
    - 字符串末尾的换行符

!!! warning "误区 3：变量未初始化"
    **错误**：`int remaining;` 后直接 `while (remaining > 1)`，remaining 是随机值，行为未定义。
    
    **正确**：`int remaining = n;` 显式初始化。
    
    **扩展**：C++ 中局部变量不初始化是"未定义行为"（UB），可能本地能跑过、评测机挂掉。**养成"声明即初始化"的习惯**。

!!! warning "误区 4：方向数组漏检越界"
    **错误**：扫雷题中 `int ni = i + dx[d]; if (grid[ni][nj] == '*')`，没检查 `ni`、`nj` 是否在范围内。
    
    **正确**：先 `if (ni >= 0 && ni < n && nj >= 0 && nj < n)` 再访问 `grid[ni][nj]`。
    
    **口诀**：访问数组前，先确认下标合法。

!!! warning "误区 5：整数溢出"
    **错误**：阶乘之和题中 `long long sum = 0; for(...) sum += factorial(i);`，但 $50!$ 远超 `long long` 范围（约 $9 \times 10^{18}$），导致结果错误。
    
    **正确**：当结果可能超过 $10^{18}$ 时，必须使用**高精度**（数组存放大数，手动实现进位）。
    
    **常见溢出场景**：
    - 连乘累积（阶乘、组合数）
    - 大数求和（$n \leq 10^6$ 时累加 int 也可能溢出）
    - 中间计算 `a * b` 即使最终结果在范围内，中间值也可能溢出
    
    **口诀**：先估算结果位数，再选数据类型；拿不准就用 `long long`，还溢出就高精度。

---

## 典型例题

### 例题 1：百钱买百鸡（枚举经典）

**题目**：公鸡 5 元一只，母鸡 3 元一只，小鸡 1 元三只。100 元买 100 只鸡，问各买几只？输出所有方案。

**分析**：
- 公鸡 $x$ 只，母鸡 $y$ 只，小鸡 $z$ 只
- 条件1：$x + y + z = 100$
- 条件2：$5x + 3y + z/3 = 100$

**优化思路**：
- 三重循环 → 两重循环：用 $z = 100 - x - y$ 直接算出 $z$
- 枚举范围缩小：公鸡最多 20 只（100/5），母鸡最多 33 只（100/3）

**完整代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    // x: 公鸡(最多20只)，y: 母鸡(最多33只)
    for (int x = 0; x <= 20; x++) {
        for (int y = 0; y <= 33; y++) {
            int z = 100 - x - y;             // 由条件1推出 z
            if (z >= 0 && z % 3 == 0) {      // 小鸡数量必须是3的倍数
                if (5 * x + 3 * y + z / 3 == 100) {   // 验证条件2
                    cout << "公鸡" << x << " 母鸡" << y << " 小鸡" << z << endl;
                }
            }
        }
    }
    return 0;
}
```

**复杂度分析**：$O(21 \times 34) \approx O(714)$，远优于三重循环的 $O(101^3) \approx O(10^6)$。

!!! tip "本题体现的两大优化"
    - **减少枚举维度**：用方程 $z = 100 - x - y$ 消去 $z$
    - **缩小枚举范围**：根据价格约束收紧 $x$、$y$ 上界

**拓展思考**：能否进一步优化到 $O(n)$？利用条件2减去条件1（注意条件2需先乘3）可得 $7x + 4y = 100$，即 $y = (100 - 7x) / 4$，于是只需枚举 $x$ 一层（$x \in \{0, 4, 8, 12\}$，且 $y \geq 0$），$z$ 由 $z = 100 - x - y$ 得到。这就是"数学优化"的威力——把两重循环降到一重。

**举一反三**：遇到多变量方程组问题，先尝试用方程消元减少变量数，再根据取值范围收紧上下界。

### 例题 2：约瑟夫问题（模拟经典）

**题目**：$n$ 个人围成一圈，从 1 号开始报数，报到 $m$ 的人出列，然后下一个人从 1 重新报数。求最后剩下的人的编号。

**模拟思路**：
- 用数组标记每个人是否还在
- 用计数器记录当前报到的数
- 报到 $m$ 的人标记为出列

**完整代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    
    bool out[1005] = {};          // 标记是否出列，初始全 false
    int remaining = n;            // 还剩多少人
    int count = 0;                // 当前报的数
    int pos = -1;                 // 当前位置，初始 -1 表示"还没开始"
    
    while (remaining > 1) {
        pos = (pos + 1) % n;      // 移到下一个人（第一次到 0 号，即 1 号）
        if (!out[pos]) {          // 如果这个人还在
            count++;
            if (count == m) {     // 报到 m 了
                out[pos] = true;  // 出列
                remaining--;
                count = 0;        // 重新从 1 开始报
            }
        }
    }
    
    // 找到最后剩下的人
    for (int i = 0; i < n; i++) {
        if (!out[i]) {
            cout << i + 1;        // 编号从 1 开始
            break;
        }
    }
    return 0;
}
```

!!! warning "数组下标"
    代码中 `pos` 从 0 开始，但题目编号通常从 1 开始，输出时要 `+1`。

!!! tip "模拟的精髓"
    - **变量记录状态**：`out[]` 标记出列、`remaining` 记录剩余、`count` 记录报数、`pos` 记录位置
    - **取模实现环形**：`pos = (pos + 1) % n` 让指针在圈内循环
    - **跳过已出列者**：`if (!out[pos])` 才计数

**复杂度分析**：每次出列需要走过至多 $m$ 个未出列者，共出列 $n-1$ 次，最坏 $O(nm)$。对 $n,m \leq 10^5$ 的数据会超时，此时需用**递推公式** $f(n) = (f(n-1) + m) \% n$ 求解，复杂度 $O(n)$。

**举一反三**：模拟题先保证"写对"，再考虑"写快"。当数据规模变大导致模拟超时，往往存在数学规律或递推公式可以优化。

---

## 📚 本节练习

### 🟢 基础题（CSP-J 难度，巩固基础）

**练习 1**：[P1009] 阶乘之和 - https://www.luogu.com.cn/problem/P1009
> 求 $S = 1! + 2! + 3! + \cdots + n!$（$n \leq 50$）。
**提示**：模拟"边算边加"的过程。注意 $n=50$ 时结果超过 65 位，**必须用高精度**（数组存放大数，手动实现进位）。是模拟 + 高精度的经典组合，考验"忠实翻译题意"。

**练习 2**：[P1042] 乒乓球 - https://www.luogu.com.cn/problem/P1042
> 模拟乒乓球比赛计分（11 分制 / 21 分制），给定比赛记录字符串，输出各局比分。
**提示**：纯模拟题。逐字符读取 `W`/`L`，分别累计两人得分；达到赛点条件（11 分且差 2 分以上 / 21 分且差 2 分以上）时输出本局比分并清零。**注意分别按 11 分制和 21 分制各输出一遍**，是"按题意分阶段模拟"的典型。

**练习 3**：[P2670] 扫雷游戏 - https://www.luogu.com.cn/problem/P2670
> $n \times m$ 网格，`*` 是雷，`?` 是空地。对每个空地输出周围 8 格的雷数。
**提示**：参考本章"综合示例：扫雷游戏"代码。**方向数组 `dx[]`/`dy[]`** 处理 8 方向，注意越界检查。是 CSP-J 高频模板题。

### 🟡 拓展题（CSP-S 入门，需思考）

**练习 4**：[P1219] 八皇后 - https://www.luogu.com.cn/problem/P1219
> 在 $8 \times 8$ 棋盘上放 8 个互不攻击的皇后，输出前 3 个解和总方案数。
**提示**：本质是**枚举所有摆放方案**（每行枚举列位置），加剪枝（同列、对角线冲突则跳过）。可用三个数组 `col[]`、`diag1[]`、`diag2[]` 标记列和两条对角线是否被占，实现 $O(1)$ 冲突检测。是"枚举 + 对称性剪枝 + 空间换时间"的综合训练。也可留到 DFS 章节深入。

**练习 5**：[P1996] 约瑟夫问题 - https://www.luogu.com.cn/problem/P1996
> $n$ 人围成一圈，从 1 号报数到 $m$ 的人出列，求出列顺序。$n,m\leq 100$。
**提示**：对照本章例题 2 的代码，把"求最后剩一人"改成"输出出列顺序"——每报到 $m$ 就输出当前编号。注意编号从 1 开始、`pos` 取模回到 0。也可留到队列章节用队列解法对比。

### 🔴 CSP-S 真题变式（进阶）

**练习 6**：[NOIP2016 提高组] 玩具谜题（洛谷 P1563） - https://www.luogu.com.cn/problem/P1563
> $n$ 个玩具围成圈，各有朝向（朝内/朝外）和职业。小人按指令（左数/右数第 $s$ 个）移动，求最终到达的玩具职业。
**提示**：纯模拟题，关键是"仔细读题不漏步"。核心技巧：朝向与指令方向相同时逆时针数、不同时顺时针数（反之亦然），位置用取模回到圈内，类似约瑟夫环的 `pos = (pos \pm s + n) \% n`。考验模拟题最核心的能力——把题意一步步翻译成代码，注意正负方向与取模处理。

!!! tip "练习建议"
    - **先写对再写快**：枚举/模拟题优先保证正确性，再考虑优化
    - **手动验证样例**：写完代码后，用纸笔模拟一遍样例，确认每一步
    - **关注数据范围**：看到 $n \leq 10^5$ 就要警惕 $O(n^2)$ 是否够用
    - **积累模板**：方向数组、环形取模、日期闰年是三大常用模板，建议背熟

---

## 📖 本章总结

### 核心要点回顾

- ✅ **枚举**：把所有可能都试一遍，简单但可能慢
- ✅ **枚举四步**：确定对象 → 范围 → 条件 → 写循环
- ✅ **枚举优化**：减少维度、缩小范围、提前终止、空间换时间、对称性剪枝、数学优化
- ✅ **模拟**：照着题意一步步实现，关键是仔细读题不漏步
- ✅ **方向数组** `dx[]`/`dy[]` 是处理网格问题的利器
- ✅ **常见误区**：范围过大、边界遗漏、变量未初始化、越界未检查、整数溢出
- ✅ 枚举和模拟是 CSP-J/S **骗分**的基础，能拿到部分分

### 易错点速查表

| 序号 | 易错点 | 触发场景 | 规避方法 |
|------|--------|---------|---------|
| 1 | 枚举范围过大 | 三重循环 $n=100$ | 先消元再缩范围 |
| 2 | 边界条件遗漏 | `pos` 初始值、空数组 | 列出所有边界单独测试 |
| 3 | 变量未初始化 | 局部 `int x;` 直接用 | 声明即赋值 |
| 4 | 数组越界 | 方向数组、下标 0/1 混用 | 访问前检查下标 |
| 5 | 整数溢出 | 阶乘、连乘、大数求和 | 估算位数，必要时高精度 |
| 6 | 下标 0/1 混淆 | 编号从 1 开始、数组从 0 存 | 统一约定，输出时转换 |
| 7 | 取模方向错误 | 玩具谜题左右数 | `(pos ± s + n) % n` 保证非负 |

### 知识脉络

- **第11章 枚举与模拟**
  - **枚举思想**
    - 四步法：确定对象 → 确定范围 → 确定条件 → 写循环
    - 典型应用：找素数（`i*i <= n`，$O(\sqrt{n})$）
    - 典型应用：百钱买百鸡（消去 z 维，两重循环）
  - **模拟思想**
    - 四要点：仔细读题 → 记录状态 → 按序执行 → 注意边界
    - 典型应用：约瑟夫问题（环形取模 `pos % n`）
    - 典型应用：日期计算（闰年规则）
    - 典型应用：扫雷游戏（方向数组 `dx[]`/`dy[]`）
  - **优化技巧**
    - 减少维度：三重 → 两重（消元）
    - 缩小范围：公鸡最多 20 只（而非 100）
    - 提前终止：找到即 `break`
    - 空间换时间：数组标记已访问状态
    - 对称性剪枝：八皇后镜像对称
    - 数学优化：素数只查 $\sqrt{n}$
  - **典型例题**
    - 百钱买百鸡：两重循环枚举经典
    - 约瑟夫问题：环形模拟经典
  - **常见误区**
    - 枚举范围过大 → 先消元再缩范围
    - 边界条件遗漏 → 列出所有边界单独测试
    - 变量未初始化 → 声明即赋值
    - 方向数组越界 → 访问前检查下标
    - 整数溢出 → 估算位数，必要时高精度
  - **洛谷练习**
    - 🟢 P1009 阶乘之和（模拟 + 高精度）
    - 🟢 P1042 乒乓球（字符串模拟）
    - 🟢 P2670 扫雷游戏（方向数组）
    - 🟡 P1219 八皇后（枚举 + 剪枝）
    - 🟡 P1996 约瑟夫问题（环形模拟）
    - 🔴 P1563 玩具谜题（取模模拟，CSP-S 真题）

### 洛谷练习对照表

| 题号 | 题目 | 难度 | 考点 | 对应章节 |
|------|------|------|------|---------|
| P1009 | 阶乘之和 | 🟢 | 模拟 + 高精度 | 模拟思想 |
| P1042 | 乒乓球 | 🟢 | 字符串模拟 | 模拟思想 |
| P2670 | 扫雷游戏 | 🟢 | 方向数组 | 模拟 + 网格 |
| P1219 | 八皇后 | 🟡 | 枚举 + 剪枝 | 枚举优化 |
| P1996 | 约瑟夫问题 | 🟡 | 环形模拟 | 模拟思想 |
| P1563 | 玩具谜题 | 🔴 | 取模模拟 | CSP-S 真题 |

### 学习路径建议

1. **入门**：先吃透例题 1（百钱买百鸡）和例题 2（约瑟夫问题），理解枚举四步与模拟四要点
2. **巩固**：完成 🟢 三道基础题（P1009、P1042、P2670），重点练模拟与方向数组
3. **提升**：挑战 🟡 两道拓展题（P1219、P1996），体会枚举优化与环形模拟
4. **进阶**：拿下 🔴 P1563 玩具谜题，磨炼"忠实翻译题意"的核心能力

### 知识衔接

本章是CSP-J/S入门教材模块二（基础算法）的第二章，与前后章节的联系如下：

- **前置知识**：第10章「算法效率与时间复杂度」——时间复杂度分析、数据范围估算法，用于判断枚举是否会超时
- **本章定位**：最基础的算法思想，掌握枚举（暴力搜索所有可能）与模拟（按题意逐步模拟过程），是竞赛中"骗分"的利器
- **后续发展**：为第12章「排序算法」铺路——下一章将学习竞赛高频基础算法，排序是后续二分查找、贪心等算法的前置基础

---

!!! tip "下一章预告"
    下一章我们将学习**排序与二分查找**——排序是算法的基础，二分查找是 $O(\log n)$ 的查找神器。学会 `sort` 函数和二分思想，你的解题能力会上一个台阶！
