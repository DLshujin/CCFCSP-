# 第04章 分支结构：if / else / switch

> 💡 一句话概括：分支结构让程序拥有"判断力"——根据条件选择不同路径，`if` 管"是否"，`switch` 管"等于哪个"，`&&`/`||` 管"组合条件"。

> 💡 本章导读
>
> 顺序结构只能让程序"一条道走到黑"。真正让程序拥有"判断力"的，是分支结构。
> 本章将带你掌握 CSP-J/S 必考的 `if / else / else if / switch` 四大分支利器，
> 并彻底搞懂初学者最容易踩的坑——`if (a = 5)` 到底是判断还是赋值。

---

## 📌 学习目标

> 🤔 想一想：你有没有想过，为什么程序需要"判断"的能力？如果程序只能按顺序执行，能解决哪些问题，又无法解决哪些问题？

读完本章，你应当能够：

1. **准确默写** `if`、`if-else`、`if-else if-else`、`switch` 四种语法结构，无语法错误。
2. **能区分** 条件表达式 `a == b`（判断）与赋值表达式 `a = b`（赋值），并能解释 `if (a = 5)` 为何永远为真。
3. **能独立完成**「闰年判断」程序，写出正确的逻辑表达式 `(year % 4 == 0 && year % 100 != 0) || year % 400 == 0`。
4. **能规范缩进** 嵌套分支代码（≥2 层），代码可读性达到比赛复盘标准。
5. **能独立 AC** 洛谷 P5709、P5710、P5711 三道分支结构基础题。

---

## 💡 生活类比

> 🤔 想一想：你有没有想过，生活中你每天要做多少次"判断"？从早上决定穿什么衣服，到晚上决定几点睡觉，其实你每天都在使用"分支结构"！

> **分支就像"十字路口选方向"**。

你站在十字路口，根据手中的地图决定走哪条路：

- 如果目的地在**北边** → 走北边那条路（`if`）
- 否则如果目的地在**南边** → 走南边那条路（`else if`）
- 否则 → 原地不动（`else`）

每个"如果"都是一个**条件判断**，程序根据条件的真假选择执行路径，就像你根据地图选路一样。`switch` 则更像是**电梯按钮**——你按几楼，电梯就停几楼，一一对应、简单明了。

> 💡 一句话记忆
>
> `if` 是"如果……就……"，`else` 是"否则就……"，`switch` 是"按下几号按钮，就执行几号操作"。

---

## 核心概念

> 🤔 想一想：你有没有想过，`if`、`else if`、`switch` 虽然都能做判断，但它们的"分工"其实完全不同？选对了结构，代码能少写一半！

### 3.1 四大分支结构定义

> 💡 一句话概括：`if` 是最基础的判断，`if-else` 处理二选一，`if-else if` 处理多区间，`switch` 处理离散值匹配。

| 结构 | 关键字 | 作用 | 适用场景 |
|------|--------|------|----------|
| 单分支 | `if` | 条件为真时执行一段代码 | 满足条件才做某事 |
| 双分支 | `if-else` | 二选一，必走其一 | 非此即彼的两种情况 |
| 多分支 | `if-else if-else` | 从上到下依次判断，命中即停 | 区间判断（如成绩分级） |
| 开关分支 | `switch-case` | 按变量值跳转到对应分支 | 离散值匹配（如星期几） |

📌 本节要点：
- 四种结构各有适用场景，选错结构会让代码变复杂
- 区间判断首选 `if-else if`，离散值匹配首选 `switch`
- `if-else if-else` 从上往下判断，命中即停

### 3.2 关系运算符（构造条件的基础）

> 💡 一句话概括：关系运算符的结果是 `bool`，六种比较操作覆盖了所有判断需求。

| 运算符 | 含义 | 示例 | 结果 |
|--------|------|------|------|
| `==` | 等于（判断） | `5 == 5` | `true` |
| `!=` | 不等于 | `5 != 3` | `true` |
| `>` | 大于 | `5 > 3` | `true` |
| `<` | 小于 | `5 < 3` | `false` |
| `>=` | 大于等于 | `5 >= 5` | `true` |
| `<=` | 小于等于 | `5 <= 3` | `false` |

> ⚠️ 重中之重：`==` 与 `=` 的区别
>
> - `==` 是**关系运算符**，作用是"判断两边的值是否相等"，结果是 `bool`。
> - `=` 是**赋值运算符**，作用是"把右边的值赋给左边"，整个表达式的值就是被赋的值。
> - 这两个符号只差一个等号，却是初学者最容易踩的坑（详见第 5 节误区框）。

### 3.3 逻辑运算符（组合多个条件）

> 💡 一句话概括：`&&` 要求全部满足，`||` 要求至少一个满足，`!` 取反——三者组合能表达任意复杂条件。

| 运算符 | 含义 | 口诀 |
|--------|------|------|
| `&&` | 逻辑与（AND） | 两个都真才真 |
| `||` | 逻辑或（OR） | 有一个真就真 |
| `!` | 逻辑非（NOT） | 真假互换 |

优先级：`!` > `&&` > `||`。拿不准时**多加括号**，比如 `(a || b) && c`。

📖 通俗解释：`a > 10 && a < 20` 就像说"年龄既要大于 10 岁，又要小于 20 岁"，两个条件都满足才算数。`a < 10 || a > 60` 就像说"小孩或老人半价"，满足其中一个就行。

---

## 代码实现

> 🤔 想一想：你有没有想过，同样一个判断逻辑，用不同的分支结构写出来，代码长度和可读性可能天差地别？

### 4.1 单分支 if

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int score;
    cin >> score;
    if (score >= 90) {
        cout << "太棒了，你是优秀生！" << endl;
    }
    cout << "程序结束" << endl;
    return 0;
}
// 输入 95：输出"太棒了，你是优秀生！" + "程序结束"
// 输入 80：只输出"程序结束"
```

### 4.2 双分支 if-else

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int age;
    cin >> age;
    if (age >= 18) {
        cout << "你成年了" << endl;
    } else {
        cout << "你还未成年" << endl;
    }
    return 0;
}
// 输入 20：输出"你成年了"
// 输入 15：输出"你还未成年"
```

### 4.3 多分支 if-else if-else

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int score;
    cin >> score;
    if (score >= 90) {
        cout << "优秀" << endl;
    } else if (score >= 80) {
        cout << "良好" << endl;
    } else if (score >= 60) {
        cout << "及格" << endl;
    } else {
        cout << "不及格" << endl;
    }
    return 0;
}
// 输入 85：输出"良好"（先判断 >=90 不成立，再判断 >=80 成立，命中即停）
// 输入 55：输出"不及格"
```

> ⚠️ 执行顺序
>
> `if-else if-else` **从上往下**判断，**只要有一个条件满足就执行对应代码，然后跳出整个结构**。所以写区间条件时**要么从大到小、要么从小到大**，不能乱序。

### 4.4 switch-case

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int day;
    cin >> day;
    switch (day) {
        case 1: cout << "星期一" << endl; break;
        case 2: cout << "星期二" << endl; break;
        case 3: cout << "星期三" << endl; break;
        case 4: cout << "星期四" << endl; break;
        case 5: cout << "星期五" << endl; break;
        case 6: cout << "星期六" << endl; break;
        case 7: cout << "星期日" << endl; break;
        default: cout << "输入有误" << endl; break;
    }
    return 0;
}
// 输入 3：输出"星期三"
// 输入 8：输出"输入有误"
```

**利用穿透合并相同情况**（工作日 vs 周末）：

```cpp
switch (day) {
    case 1: case 2: case 3: case 4: case 5:
        cout << "工作日" << endl; break;
    case 6: case 7:
        cout << "周末" << endl; break;
    default:
        cout << "输入有误" << endl; break;
}
// 输入 3：输出"工作日"
// 输入 6：输出"周末"
```

> ⚠️ switch 的两条铁律
>
> 1. **每个 `case` 后必须加 `break`**，否则会"穿透"一直往下执行。
> 2. **`switch` 的变量只能是整型或字符型**（`char` 本质是整数），**不能是浮点数或字符串**；`case` 后也必须是常量。

### 4.5 三目运算符（简化二选一）

```cpp
int a = 5, b = 10;
int maxVal = (a > b) ? a : b;   // 等价于 if-else，简洁但只适合简单场景
// 输出 maxVal：10
```

---

## ⚠️ 常见误区

> 🤔 想一想：你有没有想过，为什么 `if (a = 5)` 是最"致命"的 bug？它不会报错，编译器也不会警告，但你的程序结果却永远不对——这就是 C++ 给初学者挖的最大的坑！

### 误区 1：`if (a = 5)` 是赋值而非判断（最致命的坑）

| 写法 | 含义 | 结果 |
|------|------|------|
| ❌ `if (a = 5)` | 把 5 赋给 a，表达式值为 5（真） | **永远为真** |
| ✅ `if (a == 5)` | 判断 a 是否等于 5 | 由 a 的值决定 |
| ❌ `if (a = 0)` | 把 0 赋给 a，表达式值为 0（假） | **永远为假** |

**为什么 `if (a = 5)` 永远执行？** 因为 `a = 5` 是赋值表达式，它的值就是被赋的值 `5`。在 C++ 中**任何非 0 值都视为真**，所以条件永远成立。

> 💡 防御性写法：常量写左边
>
> 习惯写成 `if (5 == a)`，如果手误漏了一个等号变成 `if (5 = a)`，编译器会直接报错（5 不能被赋值），把运行时 bug 变成编译期错误。

### 误区 2：省略大括号导致逻辑错乱

| 写法 | 后果 |
|------|------|
| ❌ 不加括号 | 第二行不受 `if` 控制，永远执行 |
| ✅ 加大括号 | 两行都在 `if` 控制范围内 |

```cpp
// ❌ 错误：第二行不在 if 里，永远会执行！
if (score >= 60)
    cout << "及格了" << endl;
    cout << "继续努力" << endl;     // 缩进是骗人的，这行不受 if 控制

// ✅ 正确：加大括号
if (score >= 60) {
    cout << "及格了" << endl;
    cout << "继续努力" << endl;
}
```

> 💡 铁律：永远加大括号
>
> 哪怕 `if` 里只有一行代码，也**必须加大括号**。这是省去无数调试时间的最简单习惯。

### 误区 3：数学式连写 `60 <= score < 90`

| 写法 | 后果 |
|------|------|
| ❌ `if (60 <= score < 90)` | 先算 `60 <= score` 得 0/1，再和 90 比，**恒为真** |
| ✅ `if (score >= 60 && score < 90)` | 正确判断区间 |

### 误区 4：`switch` 忘记 `break` 导致穿透

| 写法 | 后果 |
|------|------|
| ❌ case 后忘写 `break` | 穿透到下一个 case，多个分支都执行 |
| ✅ 每个 case 后加 `break` | 执行完匹配分支后立即跳出 |

```cpp
switch (day) {
    case 1: cout << "星期一";    // ❌ 忘了 break！
    case 2: cout << "星期二";    // 也会执行！
    case 3: cout << "星期三"; break;
}
// 输入 1，输出"星期一星期二星期三"
```

### 误区 5：嵌套分支缩进不规范

| 写法 | 后果 |
|------|------|
| ❌ 不缩进 | 分不出层级，调试找 bug 困难 |
| ✅ 每层缩进 4 空格 | 层级一目了然 |

```cpp
// ❌ 错误缩进：分不出层级
if (age >= 18) {
if (gender == 'M') {
cout << "成年男性" << endl;
} else {
cout << "成年女性" << endl;
}
}

// ✅ 正确缩进：每层缩进 4 个空格
if (age >= 18) {
    if (gender == 'M') {
        cout << "成年男性" << endl;
    } else {
        cout << "成年女性" << endl;
    }
} else {
    if (gender == 'M') {
        cout << "未成年男性" << endl;
    } else {
        cout << "未成年女性" << endl;
    }
}
```

> 💡 缩进规范
>
> - 每进入一层 `{}`，缩进 **4 个空格**（CSP 比赛惯例）。
> - `}` 单独成行，与对应的 `if`/`else` 对齐。
> - 嵌套**超过 3 层**就要考虑用 `else if` 或逻辑运算符重构。

---

## 典型例题：闰年判断

> 🤔 想一想：你有没有想过，为什么 1900 年不是闰年，但 2000 年却是？这背后是公历闰年规则的精妙设计——"四年一闰，百年不闰，四百年再闰"。

> **题目**：输入一个年份 `year`，判断它是否为闰年。是闰年输出 `yes`，否则输出 `no`。

**闰年规则**（公历）：
- 能被 4 整除但不能被 100 整除，**或者**能被 400 整除。

**分析**：
- "能被 4 整除" → `year % 4 == 0`
- "但不能被 100 整除" → `year % 100 != 0`
- "或者能被 400 整除" → `year % 400 == 0`

把这三个条件用 `&&` 和 `||` 组合起来：
```text
(year % 4 == 0 && year % 100 != 0) || year % 400 == 0
```

**完整代码**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int year;
    cin >> year;
    if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {
        cout << "yes" << endl;
    } else {
        cout << "no" << endl;
    }
    return 0;
}
// 输入 2000：输出 yes
// 输入 1900：输出 no
// 输入 2004：输出 yes
// 输入 2023：输出 no
```

**测试用例**：

| 输入 | 输出 | 说明 |
|------|------|------|
| `2000` | `yes` | 能被 400 整除，闰年 |
| `1900` | `no` | 能被 100 整除但不能被 400 整除，平年 |
| `2004` | `yes` | 能被 4 整除但不能被 100 整除，闰年 |
| `2023` | `no` | 不能被 4 整除，平年 |

> 💡 本题考点
>
> - 逻辑运算符 `&&`、`||` 的组合使用。
> - **括号**的位置：`&&` 优先级高于 `||`，但加括号可读性更强，不易出错。
> - 对应洛谷题目：[P5711 闰年判断](https://www.luogu.com.cn/problem/P5711)。

---

## 📚 本节练习

> 🤔 想一想：你有没有想过，为什么刷题时第一遍 AC 不算真正的掌握？真正的掌握是——三天后不看笔记，还能独立写出来！

以下 6 道洛谷题按难度分为 🟢🟡🔴 三档，请按顺序完成。

### 🟢 基础题（CSP-J 难度，巩固语法）

**练习 1：[P5709] Apples Prologue** — https://www.luogu.com.cn/problem/P5709

> 已知小S有 $s$ 个苹果，每天吃一个，但周天（`t % 7 == 0`）不吃。求 $t$ 天后还剩几个苹果。

**考点**：`if` 条件判断、取模运算。注意边界：苹果吃完就为 0，不能为负数。

**练习 2：[P5710] 数的性质** — https://www.luogu.com.cn/problem/P5710

> 给定一个整数 $x$，判断它是否同时满足"能被 2 整除"且"能被 3 或 5 整除"等性质组合，输出对应的判断结果。

**考点**：逻辑运算符 `&&`、`||` 的组合，建议每个子条件都加括号。

**练习 3：[P5711] 闰年判断** — https://www.luogu.com.cn/problem/P5711

> 输入年份 $year$，判断是否为闰年。

**考点**：经典闰年表达式 `(year%4==0 && year%100!=0) || year%400==0`，可直接套用本章典型例题代码 AC。

### 🟡 拓展题（CSP-S 入门，需思考）

**练习 4：[P5715] 三位数排序** — https://www.luogu.com.cn/problem/P5715

> 输入三个整数，按从小到大排序输出。

**考点**：`if-else` 嵌套比较。提示：可以用三目运算符 `?:` 求三个数的最大/最小值，也可以直接写 3 个 `if` 交换。

**练习 5：[P1085] 不高兴的 Jinjin** — https://www.luogu.com.cn/problem/P1085

> Jinjin 每天上学的烦恼度 = 上课时间 + 补习时间 − 8。若某天烦恼度 > 8，Jinjin 就不高兴。求一周中最不高兴的是第几天（烦恼度最大且 > 8）。

**考点**：循环 + 分支，找出最大值并判断是否超过阈值。

### 🔴 进阶题（CSP-S 思维题）

**练习 6：[P1424] 小鱼的航程(改进版)** — https://www.luogu.com.cn/problem/P1424

> 小鱼从周 $x$ 开始，每天游 250 公里，周末（周六周日）休息。求 $n$ 天后共游了多少公里。

**考点**：用 `switch` 或取模 `today % 7` 判断是否为工作日，结合循环累加。注意周日的编号约定（周日是 7 还是 1）。

> 💡 刷题建议
>
> - 🟢 三题务必**全部 AC**，这是分支结构的"地基"。
> - 🟡 两题至少 AC 一题，锻炼多条件组合思维。
> - 🔴 题建议先自己想 15 分钟，想不出再看题解。
> - 每题提交前**手动构造 2~3 组测试数据**（含边界值），这是比赛必备习惯。

---

## 📖 本章总结

> 🤔 想一想：你有没有想过，分支结构虽然简单，但它和循环结构结合后，就能解决 CSP-J 复赛中 80% 的题目？打好这一章的基础，后面的路会越走越顺！

### 核心要点速记

- ✅ `if` 根据条件决定是否执行，**条件必须用 `()` 括起来**。
- ✅ 判断相等用 **`==`**，赋值用 `=`，**新手最易混淆**——`if (a = 5)` 永远为真！
- ✅ `if-else if-else` 判断区间时**顺序很重要**（从大到小或从小到大）。
- ✅ `switch` 适合"等于某个具体值"的多路分支，**只能用于整型/字符**。
- ✅ `switch` 的 `case` 后**必须加 `break`**，否则会穿透。
- ✅ 逻辑运算符：`&&`（都真才真）、`||`（有真即真）、`!`（取反）。
- ✅ **永远加大括号**，即使只有一行代码。
- ✅ 嵌套分支**每层缩进 4 空格**，超过 3 层要重构。

### 常见陷阱

- ❌ `if (a = 5)` 是赋值，永远为真 → ✅ 判断用 `if (a == 5)`
- ❌ 省略大括号 → 第二行脱离 if 控制 → ✅ 永远加大括号
- ❌ `60 <= x < 90` 数学式连写 → 恒为真 → ✅ 用 `x >= 60 && x < 90`
- ❌ `switch` 忘 `break` → case 穿透 → ✅ 每个 case 后加 `break`
- ❌ 嵌套不缩进 → 自己都看不懂 → ✅ 每层缩进 4 空格

### 分支结构选型决策

| 场景 | 推荐结构 | 示例 |
|------|---------|------|
| 判断"区间"（成绩分级） | `if-else if` | `if (score >= 90)` |
| 判断"等于哪个值"（星期几） | `switch-case` | `switch (day)` |
| 简单二选一（求最大值） | 三目 `?:` | `(a > b) ? a : b` |
| 多条件组合（闰年） | `&&` / `||` | `(year%4==0 && year%100!=0) || year%400==0` |

### 知识脉络

#### 核心概念
- if 系列
  - `if`：单分支，条件为真才执行
  - `if-else`：双分支，二选一
  - `if-else if-else`：多分支，从上往下命中即停，区间判断首选
  - 嵌套 if：每层缩进 4 空格，≤ 3 层为佳
- switch
  - `case`：值匹配，只能用于整型/字符
  - `default`：兜底分支
  - 穿透：忘写 `break` 会一直往下执行
- 逻辑运算符
  - `&&`：都真才真
  - `||`：有真即真
  - `!`：取反

#### 常见陷阱
- ❌ `if (a = 5)` 是赋值，永远为真 → ✅ 判断用 `==`
- ❌ 省略大括号 → ✅ 永远加大括号
- ❌ `60 <= x < 90` 数学式连写 → ✅ 用 `&&` 连接
- ❌ `switch` 忘 `break` → ✅ 每个 case 后加 `break`
- ❌ 嵌套不缩进 → ✅ 每层缩进 4 空格

### 洛谷练习对照表

| 题号 | 题目 | 难度 | 核心考点 |
|------|------|------|----------|
| P5709 | Apples Prologue | 🟢 | `if` + 取模 |
| P5710 | 数的性质 | 🟢 | `&&` / `||` 组合 |
| P5711 | 闰年判断 | 🟢 | 经典闰年表达式 |
| P5715 | 三位数排序 | 🟡 | `if` 嵌套比较 |
| P1085 | 不高兴的 Jinjin | 🟡 | 循环 + 分支求最值 |
| P1424 | 小鱼的航程 | 🔴 | `switch` / 取模判工作日 |

### 知识衔接

本章是CSP-J/S入门教材的基础语法章节，与前后章节的联系如下：

- **前置知识**：第03章「输入输出与基本运算」——cin/cout输入输出、算术运算、关系运算符与逻辑运算符
- **本章定位**：程序有了"判断力"，掌握if/else/switch分支结构，是编程逻辑的第一步
- **后续发展**：为第05章「循环结构」铺路——下一章将让程序学会"重复做某件事"，分支与循环结合即可解决绝大多数基础竞赛题

---

> 💡 下一章预告
>
> 本章让程序学会了"做选择"。下一章我们将学习**循环结构**——让程序学会"重复做某件事"，
> 比如打印 100 次 `Hello`、计算 1 到 100 的和。循环是编程中最强大、也最容易出 bug 的结构，
> 分支与循环结合，就能解决 CSP-J/S 中绝大多数基础题。准备好迎接 `for`、`while`、`do-while` 三剑客吧！

---
## 📝 本章修改摘要
- 新增引导性问题 8 处
- 统一提示框格式：`!!! info` → `> 💡 提示`，`!!! warning` → `> ⚠️ 警告`，`!!! tip` → `> 💡 提示`
---