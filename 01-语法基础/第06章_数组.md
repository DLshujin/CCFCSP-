# 第06章 数组：一维与二维

> 💡 一句话概括：数组是 C++ 最基础的数据结构，掌握一维/二维数组的声明、初始化、遍历和常见操作，是后续学习所有数据结构的基石。

> 💡 提示：本章内容
> 
> 数组是 C++ 中最基础的数据结构，也是后续所有数据结构（栈、队列、链表、树）的"底层土壤"。
> 
> 本章你将学习：
> - 一维数组的声明、初始化、访问与遍历
> - 二维数组的"先行后列"遍历与矩阵转置
> - 数组越界陷阱与内存连续存储原理
> - 数组作为函数参数时"退化为指针"的机制
> - 用数组模拟"整数集合"的增删查——为后续数据结构铺路

---

## 📌 学习目标

> ❓ 想一想：你有没有想过，为什么数组是 C++ 里最基础的数据结构？它和普通变量有什么本质不同？

> 💡 一句话概括：通过本章学习，你将掌握数组的核心操作和内存模型，为 CSP-J 的数据结构题打下坚实基础。

读完本章后，你应当能够：

1. **独立声明并初始化** 一维 / 二维数组，能说出 4 种初始化方式的区别（全初始化、部分初始化、省略大小、全 0），代码无越界、无未初始化使用。
2. **画出** 大小为 N 的数组在内存中的连续存储布局，能解释 `a[5]`（大小为 5）访问 `a[5]` 为什么是越界、会踩到谁的内存。
3. **默写** 数组 6 种基本操作（按下标读 / 写 / 遍历 / 求和 / 求最值 / 顺序查找）的代码模板，并能说出每种操作的时间复杂度（O(1) 或 O(n)）。
4. **说清** 数组作为函数参数传递时"退化为指针、丢失长度信息"的现象，能用 `int a[]` + `int n` 的双参数写法封装数组函数。
5. **用数组模拟** 一个简单的"整数集合"，实现插入、删除、查找三种操作，为后续学习 `vector`、`set` 等容器打下思维基础。

> 📌 要点
> - 本章是后续所有数据结构（栈、队列、链表、树）的**地基**，务必掌握扎实。
> - 学习重点：初始化方式、越界理解、退化传参、模拟数据结构。

---

## 💡 生活类比

> ❓ 想一想：你有没有想过，为什么数组访问任何一个元素都是"瞬间完成"的？生活中有什么东西也具备这种"报编号就立刻找到"的特性？

> 💡 一句话概括：数组就像一排连号储物柜——编号连续、从 0 开始，报编号即可瞬间存取。

> 数组就像一排**连号储物柜**：柜子紧挨着排成一列，每格都有编号（0 号、1 号、2 号……），编号是**连续的、从 0 开始**的。你只要报出编号，就能立刻打开对应柜子存取东西——不用从头一个一个找。

储物柜编号与存储内容的关系：
- 编号 0：存 90
- 编号 1：存 85
- 编号 2：存 78
- 编号 3：存 92
- 编号 4：存 88
- 编号从 0 开始，报"3 号柜"立刻拿到 92

- **连号** = 内存连续存储，所以"报编号"就能直接算出位置，速度极快（O(1)）。
- **编号从 0 开始** = 这是 C/C++ 的铁律，新手最容易踩的坑就是误以为从 1 开始。
- **柜子数量固定** = 声明时就定死大小，不能中途"加柜子"（这是数组与后续 `vector` 的核心区别）。

> 📌 要点
> - 数组的核心优势：**下标 O(1) 随机访问**，源于内存连续存储。
> - 数组的核心限制：**大小固定**，声明后不能增减。

---

## 核心概念

> ❓ 想一想：你有没有想过，C++ 数组和 Python 列表的最大区别是什么？为什么数组越界了程序却不报错？

> 💡 一句话概括：数组是连续存储、大小固定、下标从 0 开始的同类型数据集合——理解内存布局和越界原理是安全使用数组的关键。

### 1. 数组的定义

📖 通俗解释：把数组想象成学校走廊里一排编号的储物柜。每个柜子只能放同一种东西（都是 int 或都是 double），柜子号从 0 开始排，你喊一声"3号柜"就能立刻找到它——这就是数组。

「数组 array」是一种**在内存中连续存放、大小固定、元素类型相同**的数据集合。

每个元素通过「下标 index」访问，下标从 **0** 开始。

```cpp
类型名 数组名[大小];          // 大小必须是编译期常量！
```

```cpp
int  score[5];      // 5 个 int，下标 0~4
double temp[12];    // 12 个 double
char name[20];      // 20 个 char（C 风格字符串）
int  matrix[3][4];  // 3 行 4 列的二维数组
```

> ⚠️ 警告：下标从 0 开始（铁律）
> 
> `int a[5]` 声明了 5 个元素，有效下标是 **0、1、2、3、4**，**没有 `a[5]`**！访问 `a[5]` 就是数组越界。

### 2. 内存中的连续存储（为什么越界很危险）

📖 通俗解释：数组元素在内存中就像一列紧挨着的储物柜，中间没有缝隙。因为挨得紧，所以算位置快（O(1)）；但也因为挨得紧，超出范围的柜子其实是别人的地盘，你硬要打开就会出大事。

数组元素在内存中是**紧挨着**存放的。假设 `int a[5]`，每个 `int` 占 4 字节，内存布局如下：

- **数组 a 的地盘（地址 100 ~ 116）**：
  - 地址 100：`a[0]` = 90
  - 地址 104：`a[1]` = 85
  - 地址 108：`a[2]` = 78
  - 地址 112：`a[3]` = 92
  - 地址 116：`a[4]` = 88
- **越界区（地址 120+）**：
  - 地址 120：别人的内存（???）
  - 地址 124：别人的内存（???）
- **有效下标**：0, 1, 2, 3, 4
- **越界后果**：`a[5]` 会访问地址 120，**踩到别人的内存**！

> ⚠️ 警告：越界的后果
> 
> 访问 `a[5]` 时，程序**不会报错**，而是直接读写地址 120 处的内容——那里可能是其他变量、甚至程序自身的代码。
> 
> - 轻则：数据莫名其妙被改，出现"灵异 bug"。
> - 重则：程序崩溃（段错误 Segmentation Fault），或在洛谷上返回 **RE（Runtime Error）**。
> 
> C++ 不会帮你检查越界，**下标范围必须由你自己保证**。

### 3. 数组操作速查表（含时间复杂度）

| 操作 | 代码模板 | 时间复杂度 | 说明 |
|------|---------|-----------|------|
| **按下标读** | `x = a[i];` | **O(1)** | 直接算地址，瞬间定位 |
| **按下标写** | `a[i] = x;` | **O(1)** | 同上 |
| **遍历全部** | `for (int i=0; i<n; i++)` | **O(n)** | 每个元素看一遍 |
| **求和** | `sum += a[i]` 循环 | **O(n)** | 累加器从 0 开始 |
| **求最值** | `maxVal = a[0]` 再遍历比较 | **O(n)** | 初值必须用 `a[0]` |
| **顺序查找** | `if (a[i]==x)` 循环 | **O(n)** | 找到 `break` |
| **在末尾插入** | `a[n++] = x;` | **O(1)** | 仅当还有空位时 |
| **在中间插入** | 后移再赋值 | **O(n)** | 后面元素全部后移 |
| **在末尾删除** | `n--;` | **O(1)** | 缩短逻辑长度即可 |
| **在中间删除** | 前移覆盖 | **O(n)** | 后面元素全部前移 |

> 💡 提示：记住这个规律
> 
> 数组的"超能力"是 **O(1) 按下标随机访问**；"软肋"是 **中间插入 / 删除要 O(n)**，因为要搬运后面所有元素。这个特点决定了它适合"读多写少"的场景。

### 4. 数组的初始化方式

```cpp
// 方式1：声明时全部初始化
int a[5] = {90, 85, 78, 92, 88};

// 方式2：部分初始化（剩下的自动补 0）
int a[5] = {90, 85};          // 等价于 {90, 85, 0, 0, 0}

// 方式3：不写大小，让编译器数
int a[] = {90, 85, 78, 92, 88};   // 编译器自动算出 5

// 方式4：全部初始化为 0（最常用！）
int a[100] = {};               // 全 0
int a[100] = {0};              // 同上
```

| 初始化方式 | 写法 | 效果 | 适用场景 |
|-----------|------|------|---------|
| 全初始化 | `int a[5] = {90,85,78,92,88};` | 每个元素都有明确值 | 已知所有数据 |
| 部分初始化 | `int a[5] = {90,85};` | 前两个赋值，其余自动补 0 | 只需前几个有值 |
| 省略大小 | `int a[] = {90,85,78,92,88};` | 编译器自动推断大小为 5 | 数据量恰好确定 |
| 全 0 初始化 | `int a[100] = {};` | 全部 100 个元素为 0 | 计数数组、标记数组 |

> ⚠️ 警告：未初始化 = 随机垃圾值
> 
> ```cpp
> int a[5];              // 没初始化
> cout << a[0];          // 输出什么？不确定！可能是任意值
> // 输出：？？？（垃圾值，每次运行可能不同）
> ```
> 
> 局部数组不初始化时，里面是内存残留的"垃圾数据"。**计数数组、标记数组务必先清零**。

### 常见陷阱

| 陷阱 | ❌ 错误写法 | ✅ 正确写法 | 说明 |
|------|------------|------------|------|
| 越界访问 | `int a[5]; a[5]=10;` | `int a[5]; a[4]=10;` | 最大下标是 N-1 |
| 循环越界 | `for(i=0; i<=5; i++)` | `for(i=0; i<5; i++)` | 循环条件永远 `i < n` |
| 未初始化 | `int cnt[10]; cnt[3]++;` | `int cnt[10]={}; cnt[3]++;` | 计数数组必须先清零 |
| 最值初值 | `int maxVal = 0;` | `int maxVal = a[0];` | 数据可能全是负数 |
| VLA 变长数组 | `int n; cin>>n; int a[n];` | `const int N=1005; int a[N];` | C++ 标准不支持 VLA |

### 关键代码模板

**模板 1：一维数组遍历 + 求和 + 最值**

```cpp
int a[1005], n;
long long sum = 0;
int maxVal;

cin >> n;
for (int i = 0; i < n; i++) {
    cin >> a[i];
    sum += a[i];
    if (i == 0) maxVal = a[i];          // 初始化为 a[0]
    else if (a[i] > maxVal) maxVal = a[i];
}
```

**模板 2：二维数组遍历（先行后列）**

```cpp
for (int i = 0; i < rows; i++) {        // 外层：行
    for (int j = 0; j < cols; j++) {    // 内层：列
        cout << a[i][j] << " ";
    }
    cout << endl;
}
```

**模板 3：数组函数传参（退化为指针）**

```cpp
// 必须同时传数组名和长度！
void func(int a[], int n) {  // a[] 本质是 int* a
    for (int i = 0; i < n; i++) {
        // 处理 a[i]
    }
}
```

> 📌 要点
> - 数组下标从 0 开始，有效范围是 `0 ~ N-1`，越界不报错但极其危险。
> - 4 种初始化方式中，**全 0 初始化 `= {}`** 是计数数组的标配写法。
> - 数组操作的核心分两类：O(1) 的按下标访问，和 O(n) 的遍历/查找/插入删除。

---

## 代码实现

> ❓ 想一想：你有没有想过，为什么数组传给函数时必须同时传长度？函数内部难道不能自己算出来吗？

> 💡 一句话概括：通过一维数组遍历求最值、二维数组矩阵转置、以及数组作为函数参数的退化为指针机制，掌握数组的核心编程模式。

### 一维数组：读入、遍历、求最值

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    int a[1005];                   // 题目说 n<=1000，多开 5 个防越界
    long long sum = 0;
    int maxVal;

    for (int i = 0; i < n; i++) {
        cin >> a[i];
        sum += a[i];
        if (i == 0) maxVal = a[i];          // 第一个作初值
        else if (a[i] > maxVal) maxVal = a[i];
    }

    // 正序遍历
    for (int i = 0; i < n; i++) cout << a[i] << " ";
    cout << endl;

    // 倒序遍历
    for (int i = n - 1; i >= 0; i--) cout << a[i] << " ";
    cout << endl;

    cout << "总和：" << sum << endl;
    cout << "平均：" << (double)sum / n << endl;
    cout << "最大：" << maxVal << endl;
    return 0;
    // 输入：4
    //       90 85 92 88
    // 输出：
    // 90 85 92 88
    // 88 92 85 90
    // 总和：355
    // 平均：88.75
    // 最大：92
}
```

**变量值追踪表**（输入 `n=4, 值: 90 85 92 88`）：

| 循环 i | a[i] | sum | maxVal | 判断 |
|--------|------|-----|--------|------|
| 0 | 90 | 90 | 90 | i==0，初始化 maxVal |
| 1 | 85 | 175 | 90 | 85 < 90，不更新 |
| 2 | 92 | 267 | 92 | 92 > 90，更新 |
| 3 | 88 | 355 | 92 | 88 < 92，不更新 |

> 💡 提示：求最值的初值套路
> 
> 初值**必须用 `a[0]`**，不能用 `0` 或 `-1`——因为数据可能全是负数！
> ```cpp
> int maxVal = 0;      // ✗ 如果数据全是负数，最大值永远显示 0
> int maxVal = a[0];   // ✓ 正确
> ```

### 二维数组：读入、遍历、矩阵转置

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int a[3][3] = {
        {1, 2, 3},
        {4, 5, 6},
        {7, 8, 9}
    };

    // 遍历输出（外层行、内层列）
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++) {
            cout << a[i][j] << " ";
        }
        cout << endl;
    }
    // 输出：
    // 1 2 3
    // 4 5 6
    // 7 8 9

    // 矩阵转置：行列互换，a[i][j] -> b[j][i]
    int b[3][3];
    for (int i = 0; i < 3; i++)
        for (int j = 0; j < 3; j++)
            b[j][i] = a[i][j];

    // 转置后输出验证
    for (int i = 0; i < 3; i++) {
        for (int j = 0; j < 3; j++) {
            cout << b[i][j] << " ";
        }
        cout << endl;
    }
    // 输出：
    // 1 4 7
    // 2 5 8
    // 3 6 9

    return 0;
}
```

> 💡 提示：记忆口诀：先行后列
> 
> `a[i][j]` 中 `i` 是**行号**、`j` 是**列号**。遍历时**外层循环行、内层循环列**，这是几乎所有二维数组题的通用模板。

### 数组作为函数参数（退化为指针）

📖 通俗解释：把数组传给函数时，C++ 不会把整个数组复制一份（太费内存），而是只告诉你数组在内存的"门牌号"（首地址）。所以函数里不知道数组有多大，必须额外告诉它长度。

数组传给函数时，**不会复制整个数组**，而是只传首地址——这叫「退化 decay」。

因此函数内部**无法用 `sizeof` 获取数组长度**，必须额外传一个长度参数。

```cpp
#include <bits/stdc++.h>
using namespace std;

// 数组作参数：写成 int a[]，本质是 int* a（退化为指针）
// 必须额外传长度 n，因为函数内 sizeof(a) 得到的是指针大小而非数组大小
void printArray(int a[], int n) {
    for (int i = 0; i < n; i++) {
        cout << a[i] << " ";
    }
    cout << endl;
}

int findMax(int a[], int n) {
    int maxVal = a[0];
    for (int i = 1; i < n; i++)
        if (a[i] > maxVal) maxVal = a[i];
    return maxVal;
}

int main() {
    int a[5] = {90, 85, 78, 92, 88};
    printArray(a, 5);            // 传入数组名 + 长度
    // 输出：90 85 78 92 88
    cout << "最大值：" << findMax(a, 5) << endl;
    // 输出：最大值：92
    return 0;
}
```

> ⚠️ 警告：退化陷阱
> 
> ```cpp
> void badFunc(int a[]) {
>     int len = sizeof(a) / sizeof(a[0]);   // ✗ 结果不是数组长度！
>     // sizeof(a) = 指针大小(4或8)，sizeof(a[0]) = 4
>     // len 永远是 1 或 2，而不是真正的元素个数
> }
> ```
> 
> **结论**：数组作参数时，长度信息丢失，必须**额外传 `int n`**。这是 C/C++ 数组函数的标准写法。

> 📌 要点
> - 一维数组遍历三个核心操作：正序、倒序、求最值（初值用 `a[0]`）。
> - 二维数组遍历铁律：**外层行、内层列**，矩阵转置就是 `b[j][i] = a[i][j]`。
> - 数组传参必带长度：`void func(int a[], int n)`，函数内 `sizeof(a)` 无效。

---

## ⚠️ 常见误区

> ❓ 想一想：你有没有想过，写 `int a[n]` 这种变长数组，为什么在一些编译器能过，在另一些却报错？这背后隐藏着什么危险？

> 💡 一句话概括：VLA 变长数组、越界访问、未初始化、最值初值错误、数组开太小——这五大误区是 CSP-J 考试中最常见的失分点。

> ⚠️ 警告：误区一：`int a[n];` 变长数组 VLA（C++ 不支持）
> 
> ```cpp
> int n;
> cin >> n;
> int a[n];          // ✗ n 是变量，这是"变长数组 VLA"
> ```
> 
> **为什么错？** C++ 标准中，数组大小必须是**编译期常量**（如 `5`、`1005`、`const int N=1005`）。
> 
> `int a[n]` 中 `n` 是运行时才确定的变量，属于 C99 的 VLA（Variable-Length Array）特性，**C++ 标准并不支持**。
> 
> - 在洛谷（GCC 编译器）上可能"碰巧能过"，但在 MSVC、部分严格编译器上直接**编译报错**。
> - VLA 分配在栈上，`n` 很大时会导致**栈溢出 Stack Overflow**。
> - 竞赛中是**禁忌写法**，CSP-J/S 评测环境不保证支持。
> 
> **正确写法**：开一个比最大数据范围稍大的常量数组：
> ```cpp
> const int MAXN = 1005;
> int a[MAXN];       // ✓ 大小是常量，安全
> // 用 a[0] ~ a[n-1]，n 从键盘读入
> ```

> ⚠️ 警告：误区二：数组越界（下标超出 0 ~ N-1）
> 
> ```cpp
> int a[5];
> a[5] = 10;                      // ✗ 最大下标是 4
> for (int i = 0; i <= 5; i++)    // ✗ 应该 i < 5
>     cin >> a[i];                // i=5 时越界，踩到别人的内存
> ```
> 
> 越界不会报错，而是**悄悄修改相邻内存**，导致"灵异 bug"或在洛谷上返回 **RE**。
> 
> **防范**：循环条件永远写 `i < n`，数组大小多开 5~10 个。

> ⚠️ 警告：误区三：未初始化就使用
> 
> ```cpp
> int cnt[10];          // 没初始化
> cnt[3]++;             // ✗ cnt[3] 是垃圾值，++ 后还是垃圾值
> ```
> 
> 计数数组、标记数组**必须先清零**：`int cnt[10] = {};`

> ⚠️ 警告：误区四：求最值初值设成 0 或 -1
> 
> ```cpp
> int maxVal = 0;       // ✗ 数据全是负数时，最大值显示 0（错！）
> int maxVal = a[0];    // ✓ 正确
> ```

> ⚠️ 警告：误区五：数组开太小
> 
> 题目说 `n ≤ 1000`，你开 `int a[100]`——数据一大就越界。
> 
> **习惯**：题目说 N，就开 `N + 5`，永远多留几个空位防越界。

### 常见陷阱速查

| 误区 | ❌ 错误 | ✅ 正确 |
|------|--------|--------|
| VLA 变长数组 | `int a[n];`（n 是变量） | `const int N=1005; int a[N];` |
| 数组越界 | `for(i=0; i<=N; i++)` | `for(i=0; i<N; i++)` |
| 未初始化 | `int cnt[10]; cnt[3]++;` | `int cnt[10]={}; cnt[3]++;` |
| 最值初值 | `int maxVal = 0;` | `int maxVal = a[0];` |
| 数组太小 | 题目 N=1000，开 `a[100]` | 开 `a[1005]`（N+5） |

> 📌 要点
> - VLA 是 C++ 竞赛的**禁忌写法**，始终用 `const int MAXN` 定义常量大小。
> - 越界是"沉默的杀手"——不报错但后果严重，**循环条件永远 `i < n`**。
> - 计数和标记数组务必 `= {}` 清零，最值初值务必用 `a[0]`。

---

## 典型例题：用数组模拟"整数集合"

> ❓ 想一想：你有没有想过，C++ 的 `vector` 和 `set` 底层是怎么实现的？其实它们就是从数组这种最基础的结构一步步演化而来的！

> 💡 一句话概括：用数组 + 逻辑长度 `size_` 模拟整数集合的增删查操作，是理解后续数据结构（vector、set、哈希表）的重要启蒙。

> **题目**：维护一个整数集合（元素互不相同），支持三种操作：①插入一个数；②删除一个数；③查询某数是否存在。用一维数组实现，为后续学习 `set`、`vector` 等容器做铺垫。
>
> 这道题是后续数据结构（栈、队列、链表）的"启蒙题"——理解了用数组手动管理一个集合，你才能真正体会后续容器的价值。

> 💡 提示：本例题使用了函数封装，函数的完整讲解见第08章。初学者可先跳过函数定义部分，关注 main 函数中的逻辑，学完第08章后再回看完整代码。

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 1005;      // 集合最大容量

// 用数组的 [0, size) 区间存放元素，size 是"逻辑长度"
int data_[MAXN];
int size_ = 0;              // 当前元素个数（加下划线避免与 std::size 混淆）

// 插入：先查重，不存在则追加到末尾（O(n) 查重 + O(1) 追加）
void insert(int x) {
    if (size_ >= MAXN) return;  // 容量已满，防止越界
    for (int i = 0; i < size_; i++) {
        if (data_[i] == x) return;     // 已存在，不重复插入
    }
    data_[size_++] = x;                // 追加到末尾，长度+1
}

// 删除：找到位置后，用最后一个元素覆盖它，再缩短长度（O(n)）
void remove(int x) {
    for (int i = 0; i < size_; i++) {
        if (data_[i] == x) {
            data_[i] = data_[size_ - 1];   // 用末尾元素覆盖
            size_--;                        // 长度-1
            return;
        }
    }
}

// 查找：顺序遍历（O(n)）
bool find(int x) {
    for (int i = 0; i < size_; i++) {
        if (data_[i] == x) return true;
    }
    return false;
}

// 打印集合
void printSet() {
    cout << "{ ";
    for (int i = 0; i < size_; i++) cout << data_[i] << " ";
    cout << "}" << endl;
}

int main() {
    insert(3);  insert(1);  insert(4);  insert(1);  // 1 重复，不插入
    printSet();                          // 输出：{ 3 1 4 }

    cout << "查找 4：" << (find(4) ? "存在" : "不存在") << endl;   // 输出：查找 4：存在
    cout << "查找 5：" << (find(5) ? "存在" : "不存在") << endl;   // 输出：查找 5：不存在

    remove(1);
    printSet();                          // 输出：{ 3 4 }（用末尾覆盖，顺序会变）
    return 0;
}
```

**操作过程追踪**：

| 步骤 | 操作 | data_ 数组内容 | size_ | 说明 |
|------|------|---------------|-------|------|
| 初始 | — | `[]` | 0 | 空集合 |
| 1 | insert(3) | `[3]` | 1 | 追加到末尾 |
| 2 | insert(1) | `[3, 1]` | 2 | 追加到末尾 |
| 3 | insert(4) | `[3, 1, 4]` | 3 | 追加到末尾 |
| 4 | insert(1) | `[3, 1, 4]` | 3 | 1 已存在，跳过 |
| 5 | remove(1) | `[3, 4]` | 2 | 用末尾 4 覆盖位置 1 |

> 💡 提示：这道题的数据结构思维
> 
> - **`size_` 逻辑长度**：数组物理大小是 `MAXN`，但真正用了几个由 `size_` 控制——这是所有"数组模拟数据结构"的核心思想。
> - **末尾覆盖删除法**：把末尾元素搬过来覆盖要删的元素，再 `size--`，避免 O(n) 的整体搬移。代价是**不保序**——后续学"无序集合"时会再次用到。
> - 这个"集合"的查找是 O(n)，后续学**哈希表**能优化到 O(1)，学**平衡树**能优化到 O(log n)——数组是理解它们的起点。

> 📌 要点
> - **逻辑长度 `size_`** 是数组模拟数据结构的核心概念——物理大小 ≠ 逻辑大小。
> - **末尾覆盖删除法**用 O(1) 替代 O(n) 搬移，代价是不保序，适合无序集合场景。
> - 数组集合的查找是 O(n)，这是它的瓶颈——后续容器正是为了解决这个问题。

---

## 📚 本节练习

> ❓ 想一想：你有没有想过，刷题时最怕的不是"不会做"，而是"代码写对了但莫名其妙 RE"——数组越界就是最常见的元凶之一？

> 💡 一句话概括：通过 6 道洛谷真题（基础→拓展→进阶），巩固数组的遍历、计数、标记、倒序、区间求和等核心操作。

### 🟢 基础题（CSP-J 难度，巩固一维数组）

**练习 1**：[P1046] 陶陶摘苹果 - https://www.luogu.com.cn/problem/P1046
> 已知 10 个苹果的高度和陶陶伸手能达的最大高度（含 30cm 板凳），问陶陶能摘到几个苹果。
**提示**：用一维数组 `int h[10]` 存 10 个苹果高度，遍历时 `if (h[i] <= 能达高度) cnt++`。复习"遍历 + 计数"两个基本操作。

**练习 2**：[P1427] 小鱼的数字游戏 - https://www.luogu.com.cn/problem/P1427
> 小鱼依次念出一串数字（以 0 结尾），请你把她念的数字**倒序**输出。
**提示**：用 `while (cin >> x && x != 0)` 循环读入并存入数组，记录个数 `n`，再用 `for (int i = n-1; i >= 0; i--)` 倒序输出。复习"读入数组"和"倒序遍历"。

### 🟡 拓展题（CSP-J 提高，需思考）

**练习 3**：[P5727] 冰雹猜想 - https://www.luogu.com.cn/problem/P5727
> 给定正整数 n，按冰雹猜想规则（偶数除 2、奇数乘 3 加 1）反复变换直到变成 1，**倒序输出**变换过程中经历的所有数。
**提示**：用一个数组存下每一步的值（包括初始 n 和最后的 1），变换完成后倒序输出。这题同时考查"数组存储"和"while 循环"，注意数组要开够大。

**练习 4**：[P1047] 校门外的树 - https://www.luogu.com.cn/problem/P1047
> 马路长度 L（编号 0 到 L），有 M 个区域要砍树（每个区域给出起点和终点），求最后还剩多少棵树。
**提示**：开 `bool tree[L+5]` 全部初始化为 1（表示有树），对每个砍树区域用双重循环把 `tree[i] = 0`，最后遍历统计还有多少个 1。这是经典的**数组标记法**，复习"计数统计"思路。

### 🔴 进阶题（CSP-S 入门，综合应用）

**练习 5**：[P2141] 珠心算测验 - https://www.luogu.com.cn/problem/P2141
> 给定 n 个不同的正整数，问其中有多少个数，恰好等于另外两个（不同的）数之和。
**提示**：用**标记数组** `bool sum[]`，先双重循环枚举所有两数之和并标记 `sum[a[i]+a[j]] = true`，再遍历原数组统计 `sum[a[k]]` 为 true 的个数。这题考查"用数组下标当哈希键"的技巧，是本章"整数集合"思想的延伸。

**练习 6**：[P1614] 爱与愁的心痛 - https://www.luogu.com.cn/problem/P1614
> 给定 n 个数和区间长度 m，求所有长度为 m 的连续子段中，和的最小值。
**提示**：先用 `for` 双重循环（或前缀和）求出每个长度为 m 的子段和，再用求最小值的套路（初值设为第一个子段和）找最小。这题综合考查"数组遍历 + 区间求和 + 求最值"，是后续"前缀和与差分"（第 30 章）的预热。

### 练习难度对比

| 难度 | 题号 | 核心考点 | 数组操作 |
|------|------|---------|---------|
| 🟢 基础 | P1046 陶陶摘苹果 | 遍历 + 计数 | 读入、遍历、条件判断 |
| 🟢 基础 | P1427 小鱼数字游戏 | 倒序遍历 | 读入到 0 为止、倒序输出 |
| 🟡 拓展 | P5727 冰雹猜想 | 数组存储 + while 循环 | 动态存入、倒序输出 |
| 🟡 拓展 | P1047 校门外的树 | 数组标记法 | 布尔标记、区间覆盖 |
| 🔴 进阶 | P2141 珠心算测验 | 下标作哈希键 | 枚举 + 标记数组 |
| 🔴 进阶 | P1614 爱与愁心痛 | 区间求和 + 最值 | 子段遍历、最小值 |

> 📌 要点
> - 基础题练的是"遍历 + 计数 + 倒序"三板斧，是 CSP-J 第一题的常见模式。
> - 拓展题引入"数组标记法"，用布尔数组标记状态是解决区间问题的利器。
> - 进阶题将数组下标当作"哈希键"使用，是数组思维向高级数据结构的过渡。

---

## 📖 本章总结

> ❓ 想一想：你有没有想过，学完数组后，你已经掌握了数据结构最核心的两种思维——连续存储和下标访问——后续所有数据结构都是在这两种思维上的变体？

> 💡 一句话概括：本章核心——数组的连续存储与 O(1) 随机访问、五大常见误区、退化为指针的传参机制、以及用 `size_` 逻辑长度模拟数据结构的思维。

### 核心要点回顾

- ✅ 数组 = **连续存储 + 大小固定 + 同类型 + 下标从 0 开始**
- ✅ 按下标访问是 **O(1)**，遍历 / 查找 / 中间插入删除是 **O(n)**
- ✅ **4 种初始化**：全初始化、部分初始化（余 0）、省略大小、全 0（`= {}`）
- ✅ 未初始化的局部数组是**垃圾值**，计数数组务必 `= {}` 清零
- ✅ 数组越界**不报错**但会踩内存，循环条件永远写 `i < n`
- ✅ 求最值初值用 `a[0]`，不能用 `0` 或 `-1`
- ✅ 二维数组遍历"**先行后列**"：外层行、内层列
- ✅ 数组作函数参数会**退化为指针**，必须额外传长度 `n`
- ✅ `int a[n]`（VLA 变长数组）C++ 不支持，用 `const int MAXN` 代替
- ✅ `size_` 逻辑长度 + 末尾覆盖删除 = 数组模拟数据结构的核心套路

### 知识脉络

- **数组总览**
  - 核心属性：连续存储、大小固定、下标从 0 开始
  - 一维数组
    - 声明：`int a[N];`（N 必须是编译期常量）
    - 初始化：4 种方式（全初始化、部分初始化补 0、省略大小、全 0 用 `= {}`）
    - 访问：`a[i]`，O(1) 按下标随机访问
    - 基本操作
      - 遍历 O(n)：正序 `for(i=0; i<n; i++)`、倒序 `for(i=n-1; i>=0; i--)`
      - 求和 O(n)：`sum += a[i]`
      - 最值 O(n)：初值用 `a[0]`，遍历比较更新
      - 查找 O(n)：顺序遍历，找到 `break`
    - 插入与删除
      - 末尾插入 O(1)：`a[n++] = x`
      - 末尾删除 O(1)：`n--`
      - 中间插入 O(n)：后移元素再赋值
      - 中间删除 O(n)：前移覆盖
  - 二维数组
    - 声明：`int a[rows][cols];`
    - 遍历铁律：外层行（i）、内层列（j），即"先行后列"
    - 矩阵转置：`b[j][i] = a[i][j]`
  - 函数传参
    - 退化为指针：`int a[]` 本质是 `int* a`
    - 丢失长度信息：`sizeof(a)` 得到的是指针大小
    - 标准写法：`void func(int a[], int n)`，必须额外传长度
  - 五大常见误区
    - ① `int a[n]` VLA 变长数组 → 用 `const int MAXN` 常量
    - ② 越界 `a[N]` → 循环条件永远 `i < N`
    - ③ 未初始化用垃圾值 → 计数数组 `= {}` 清零
    - ④ 最值初值设 0 或 -1 → 用 `a[0]`
    - ⑤ 数组开太小 → 题目 N 就开 `N+5`
  - 数组模拟数据结构
    - 核心概念：`size_` 逻辑长度，物理大小 ≠ 逻辑大小
    - 末尾覆盖删除法：O(1) 删除，不保序
    - 用途：模拟整数集合（插入、删除、查找）

### 操作复杂度速查表

| 操作 | 时间复杂度 | 代码 |
|------|-----------|------|
| 按下标读写 | O(1) | `a[i]` |
| 遍历 | O(n) | `for (int i=0; i<n; i++)` |
| 求和 / 最值 / 查找 | O(n) | 循环 + 累加 / 比较 |
| 末尾插入 / 删除 | O(1) | `a[n++]=x` / `n--` |
| 中间插入 / 删除 | O(n) | 后移 / 前移覆盖 |

### 知识衔接

本章是CSP-J/S入门教材的基础语法章节，与前后章节的联系如下：

- **前置知识**：第05章「循环结构」——for/while/do-while循环、嵌套循环、循环变量与边界控制
- **本章定位**：批量数据的存储与处理，掌握一维/二维数组的声明、遍历与基本操作（求和、最值、查找），是CSP-J核心数据结构
- **后续发展**：为第07章「字符串」铺路——字符串本质是字符数组，数组+字符串是CSP-J的"双基石"

---

> 💡 提示：下一章预告
> 
> 下一章我们将学习**字符串**——本质就是"字符数组"，但 C++ 提供了更强大的 `string` 类型。学会字符串后，你就能处理文字、密码、回文等真实问题。数组 + 字符串是 CSP-J 的"双基石"！

---

> 📝 **修改摘要（2026-08-08）**
> 
> 1. 在每个 `##` 二级标题小节开头增加了"❓ 想一想"引导性问题，共 8 个（学习目标、生活类比、核心概念、代码实现、常见误区、典型例题、本节练习、本章总结）
> 2. 将 `!!! info "本章内容"` 改为 `> 💡 提示：本章内容`
> 3. 将 `!!! tip "..."` 共 5 处改为 `> 💡 提示：...`（记住这个规律、求最值的初值套路、记忆口诀：先行后列、这道题的数据结构思维、下一章预告）
> 4. 将 `!!! warning "..."` 共 3 处改为 `> ⚠️ 警告：...`（下标从 0 开始、未初始化 = 随机垃圾值、退化陷阱）
> 5. 将 `!!! danger "..."` 共 6 处改为 `> ⚠️ 警告：...`（越界的后果、误区一~五）
> 6. 内存连续存储布局已为缩进层级列表格式，无需修改
> 7. 所有代码块、练习链接、表格、总结内容保持不变