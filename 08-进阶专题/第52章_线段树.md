# 第52章 线段树（Segment Tree）

`[CSP-S 核心考点]` 难度7 | 线段树是最通用的区间数据结构，支持区间修改与区间查询

!!! info "本章内容"
    线段树（Segment Tree）是一种基于分治思想的二叉树结构，每个节点代表数组中的一个区间。它功能强大：区间修改、区间查询、区间最值统统支持，是 CSP-S 最核心的数据结构。本章你将学习：

    - 线段树的基本结构：每个节点代表区间 $[l, r]$，左儿子 $[l, mid]$，右儿子 $[mid+1, r]$
    - `build` 建树：$O(n)$ 递归构建
    - 单点修改 `update`：$O(\log n)$
    - 区间查询 `query`：$O(\log n)$
    - lazy 标记（懒标记/延迟标记）：区间修改的核心优化
    - `pushdown` 操作：下传 lazy 标记
    - 区间修改 + 区间查询的完整实现
    - 双 lazy 标记（乘法 + 加法）：P3373 难点
    - 线段树与树状数组的对比
    - 扫描线算法简介（线段树的经典应用）

---

## 📌 学习目标

学完本章，你应当能够：

1. **画出** $n=10$ 时线段树的完整树形结构图，说出每个节点代表的区间范围
2. **独立默写**线段树的 `build`、`pushup`、`update`（单点）、`query` 四个函数，代码无 bug
3. **准确解释** lazy 标记的作用与 `pushdown` 的执行时机，理解"延迟修改"的思想
4. **独立默写**区间修改 + 区间查询的完整线段树模板（含 `lazy` 和 `pushdown`）
5. **掌握**双 lazy 标记（乘法 + 加法）的下传顺序，理解"先乘后加"的原因
6. **独立 AC** 洛谷 P3372（线段树 1）和 P3373（线段树 2），理解线段树与树状数组的适用场景差异

---

## 💡 生活类比

!!! tip "生活类比：公司管理层"
    把线段树想象成一家**公司**：

    ```
                    CEO（管全公司 [1, n]）
                   /                      \
            部门经理A                    部门经理B
        （管左半 [1, mid]）          （管右半 [mid+1, n]）
           /        \                  /        \
        组长1      组长2           组长3       组长4
     ([1,...])  ([...])         ([...])    ([...,n])
        / \        / \            / \        / \
      员工 员工  员工 员工      员工 员工   员工 员工
    ```

    - **查询"1~7 号员工总绩效"**：CEO 把任务分给部门经理 → 部门经理分给组长 → 组长汇总员工数据层层上报，**只需问 $O(\log n)$ 个人**
    - **"给 3~6 号员工统一加薪 500"**：CEO 发现 3~6 正好覆盖某个组长的管辖范围，就**只告诉那个组长"你的人每人加 500"**（打上 lazy 标记），不需要一层层通知到每个员工——这就是**懒标记**的精髓
    - 等到真正需要查询那个组长的下属时，组长才把"加薪 500"的指令**下传**给员工——这就是 **pushdown**

    一句话：**线段树是"分治 + 延迟执行"的区间管理架构**。

---

## 第一节 线段树的基本结构

### 1.1 核心思想

线段树将数组区间 $[1, n]$ 不断**二分**，每个节点代表一个区间 $[l, r]$：

- 若 $l = r$（叶子节点）：代表单个元素 $a[l]$
- 若 $l < r$（内部节点）：设 $mid = \lfloor (l+r)/2 \rfloor$，左儿子代表 $[l, mid]$，右儿子代表 $[mid+1, r]$

### 1.2 结构示例

以 $n = 5$，数组 $a = [1, 5, 4, 2, 3]$（下标从 1 开始）为例：

```
                  [1,5] sum=15
                 /          \
          [1,3] sum=10      [4,5] sum=5
          /      \           /      \
     [1,2] s=6  [3,3] s=4  [4,4] s=2  [5,5] s=3
     /     \
[1,1] s=1  [2,2] s=5
```

- 根节点代表整个区间 $[1, 5]$，存储区间和 15
- 每个内部节点存储**左右儿子之和**（`pushup` 操作）
- 叶子节点存储单个元素的值

### 1.3 数组存储与空间

线段树通常用**数组模拟**，根节点编号为 1，节点 $rt$ 的左儿子为 $rt \times 2$（即 `rt<<1`），右儿子为 $rt \times 2 + 1$（即 `rt<<1|1`）。

!!! warning "空间要开 4 倍"
    线段树数组空间需要开 **$4n$**。虽然线段树节点数约为 $2n$，但由于数组存储的编号方式，最坏情况下需要 $4n$ 的空间才不会越界。

```cpp
const int N = 100005;
long long sum[N << 2];    // 区间和，开 4 倍空间
long long lazy[N << 2];  // 懒标记，开 4 倍空间
long long a[N];           // 原数组
// 输入：无（全局数组声明）
// 输出：无（sum 存区间和，lazy 存懒标记）
```

---

## 第二节 build 建树

### 2.1 递归建树

从根节点开始，递归构建左右子树，回溯时用 `pushup` 合并子节点信息。

```cpp
// pushup：用子节点信息更新当前节点
void pushup(int rt) {
    sum[rt] = sum[rt << 1] + sum[rt << 1 | 1];
}

// build(rt, l, r)：建树，rt 为当前节点编号，管理区间 [l, r]
void build(int rt, int l, int r) {
    if (l == r) {              // 叶子节点
        sum[rt] = a[l];
        return;
    }
    int mid = (l + r) >> 1;
    build(rt << 1, l, mid);       // 左儿子
    build(rt << 1 | 1, mid + 1, r); // 右儿子
    pushup(rt);                   // 回溯合并
}
// 输入：节点编号 rt，管理的区间 [l, r]；原数组 a[1]~a[n]
// 输出：无（建好线段树，sum[rt] 存区间和）
```

### 2.2 复杂度

建树过程访问每个节点恰好一次，节点总数 $O(n)$，故建树复杂度为 **$O(n)$**。

---

## 第三节 单点修改

将 $a[p]$ 修改为 $v$（或加上 $v$），从根节点递归到叶子节点 $[p, p]$，修改后回溯更新所有祖先节点。

```cpp
void update(int rt, int l, int r, int p, long long v) {
    if (l == r) {           // 找到叶子节点
        sum[rt] = v;        // 或 sum[rt] += v;
        return;
    }
    int mid = (l + r) >> 1;
    if (p <= mid) update(rt << 1, l, mid, p, v);
    else update(rt << 1 | 1, mid + 1, r, p, v);
    pushup(rt);             // 回溯更新
}
// 输入：节点编号 rt，区间 [l,r]，修改位置 p，新值 v
// 输出：无（更新 a[p] 并回溯维护所有祖先节点）
```

从根到叶子路径长度为 $O(\log n)$，故单点修改复杂度为 **$O(\log n)$**。

---

## 第四节 区间查询

查询区间 $[L, R]$ 的和：递归遍历，将查询区间拆分为若干个线段树节点的并集。

```cpp
long long query(int rt, int l, int r, int L, int R) {
    if (L <= l && r <= R)    // 当前区间完全被查询区间包含
        return sum[rt];
    int mid = (l + r) >> 1;
    long long res = 0;
    if (L <= mid) res += query(rt << 1, l, mid, L, R);
    if (R > mid) res += query(rt << 1 | 1, mid + 1, r, L, R);
    return res;
}
// 输入：节点编号 rt，区间 [l,r]，查询区间 [L,R]
// 输出：区间 [L,R] 的元素之和
```

**执行示例**：$n=5$，查询 `query(1, 1, 5, 2, 4)`（即 $a_2 + a_3 + a_4$）：

```
[1,5] → mid=3，L=2≤3 查左，R=4>3 查右
  ├─ [1,3] → mid=2，L=2≤2 查左，R=4>2 查右
  │   ├─ [1,2] → mid=1，L=2>1 不查左，R=4>1 查右
  │   │   └─ [2,2] → 完全包含，返回 a[2]=5
  │   └─ [3,3] → 完全包含，返回 a[3]=4
  └─ [4,5] → mid=4，L=2≤4 查左，R=4>4? 否，不查右
      └─ [4,4] → 完全包含，返回 a[4]=2

结果 = 5 + 4 + 2 = 11 ✓
```

!!! tip "区间查询的关键条件"
    - `L <= l && r <= R`：当前节点区间 $[l,r]$ 完全在查询区间 $[L,R]$ 内，直接返回，**不再往下递归**
    - `L <= mid`：查询区间与左子树有交集，递归左子树
    - `R > mid`：查询区间与右子树有交集，递归右子树

区间查询最多访问 $O(\log n)$ 个节点，复杂度为 **$O(\log n)$**。

---

## 第五节 lazy 标记与 pushdown

### 5.1 为什么需要 lazy 标记

**朴素区间修改**：要把 $[L, R]$ 内每个数都加 $k$，递归到每个叶子节点修改，复杂度 $O(n)$——太慢了！

**lazy 标记思想**：如果某个节点 $[l, r]$ **完全被**修改区间 $[L, R]$ 包含，不往下递归修改每个叶子，而是：

1. 直接更新该节点的 `sum`：`sum[rt] += k * (r - l + 1)`
2. 在该节点打上 lazy 标记：`lazy[rt] += k`（表示"我的子节点们还没加 $k$"）
3. **暂时不管子节点**，等以后需要访问子节点时再下传

### 5.2 pushdown 操作

当下次需要递归进入子节点时（在 `update` 或 `query` 中往下走之前），调用 `pushdown` 把 lazy 标记下传给两个子节点：

```cpp
// pushdown：将 rt 的 lazy 标记下传给两个子节点
// len 是当前节点 rt 代表的区间长度
void pushdown(int rt, int len) {
    if (lazy[rt]) {
        // 左子树下传
        lazy[rt << 1] += lazy[rt];
        sum[rt << 1] += lazy[rt] * (len - (len >> 1));  // 左子树长度
        // 右子树下传
        lazy[rt << 1 | 1] += lazy[rt];
        sum[rt << 1 | 1] += lazy[rt] * (len >> 1);     // 右子树长度
        // 清空当前标记
        lazy[rt] = 0;
    }
}
// 输入：节点编号 rt，区间长度 len
// 输出：无（将 lazy[rt] 下传给两个子节点并清空）
```

**理解 `len - (len >> 1)` 和 `len >> 1`**：

- 当前节点区间长度为 `len`
- 左子树长度 = `len - len/2`（向上取整，如 len=5 时左=3 右=2）
- 右子树长度 = `len/2`（向下取整）

### 5.3 lazy 标记的执行流程

```
区间修改 update(rt, l, r, L, R, v)：

1. 若 [l,r] 完全包含在 [L,R] 中：
   - sum[rt] += v * (r-l+1)    ← 立即更新当前节点
   - lazy[rt] += v              ← 打标记，告诉子节点"你们欠我 v"
   - return                     ← 不往下走！

2. 否则需要往下递归：
   - pushdown(rt, r-l+1)        ← 下传之前的 lazy 标记（关键！）
   - 递归左/右子树
   - pushup(rt)                 ← 回溯更新
```

!!! warning "pushdown 的时机"
    **只要需要往下递归，就必须先 pushdown！** 这是线段树区间修改的核心。忘记 pushdown 是最常见的错误。

---

## 第六节 区间修改 + 区间查询完整实现

以下是带 lazy 标记的完整线段树模板，支持区间加法修改和区间求和查询：

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100005;
long long a[N], sum[N << 2], lazy[N << 2];
int n, m;

void pushup(int rt) {
    sum[rt] = sum[rt << 1] + sum[rt << 1 | 1];
}

void pushdown(int rt, int len) {
    if (lazy[rt]) {
        lazy[rt << 1] += lazy[rt];
        sum[rt << 1] += lazy[rt] * (len - (len >> 1));
        lazy[rt << 1 | 1] += lazy[rt];
        sum[rt << 1 | 1] += lazy[rt] * (len >> 1);
        lazy[rt] = 0;
    }
}

void build(int rt, int l, int r) {
    lazy[rt] = 0;
    if (l == r) { sum[rt] = a[l]; return; }
    int mid = (l + r) >> 1;
    build(rt << 1, l, mid);
    build(rt << 1 | 1, mid + 1, r);
    pushup(rt);
}

// 区间 [L,R] 加 v
void update(int rt, int l, int r, int L, int R, long long v) {
    if (L <= l && r <= R) {
        sum[rt] += v * (r - l + 1);
        lazy[rt] += v;
        return;
    }
    pushdown(rt, r - l + 1);
    int mid = (l + r) >> 1;
    if (L <= mid) update(rt << 1, l, mid, L, R, v);
    if (R > mid) update(rt << 1 | 1, mid + 1, r, L, R, v);
    pushup(rt);
}

// 查询区间 [L,R] 的和
long long query(int rt, int l, int r, int L, int R) {
    if (L <= l && r <= R) return sum[rt];
    pushdown(rt, r - l + 1);
    int mid = (l + r) >> 1;
    long long res = 0;
    if (L <= mid) res += query(rt << 1, l, mid, L, R);
    if (R > mid) res += query(rt << 1 | 1, mid + 1, r, L, R);
    return res;
}

int main() {
    scanf("%d%d", &n, &m);
    for (int i = 1; i <= n; i++) scanf("%lld", &a[i]);
    build(1, 1, n);
    while (m--) {
        int op, x, y;
        long long k;
        scanf("%d", &op);
        if (op == 1) {
            scanf("%d%d%lld", &x, &y, &k);
            update(1, 1, n, x, y, k);
        } else {
            scanf("%d%d", &x, &y);
            printf("%lld\n", query(1, 1, n, x, y));
        }
    }
    return 0;
}
// 输入：
// 5 5
// 1 5 4 2 3
// 2 2 4
// 1 2 3 2
// 2 3 4
// 1 1 5 1
// 2 1 4
// 输出：
// 11
// 8
// 20
```

**样例验证**：

```
初始：a = [1, 5, 4, 2, 3]
2 2 4     → sum(2,4) = 5+4+2 = 11
1 2 3 2   → a[2..3] += 2 → a = [1, 7, 6, 2, 3]
2 3 4     → sum(3,4) = 6+2 = 8
1 1 5 1   → a[1..5] += 1 → a = [2, 8, 7, 3, 4]
2 1 4     → sum(1,4) = 2+8+7+3 = 20
```

---

## 第七节 双 lazy 标记（乘法 + 加法）

### 7.1 问题背景

当需要同时支持**区间乘**和**区间加**时，需要两个 lazy 标记：`mul`（乘法标记）和 `add`（加法标记）。

### 7.2 下传顺序：先乘后加

考虑一个节点当前值为 $v$，有标记 `mul = m, add = a`，含义是：子节点的真实值应为 $v \times m + a$。

如果再来一个新的乘法 $k$：$(v \times m + a) \times k = v \times (m \times k) + a \times k$，所以：

- `mul *= k`
- `add *= k`（加法标记也要乘！）

如果再来一个新的加法 $k$：$(v \times m + a) + k = v \times m + (a + k)$，所以：

- `add += k`

**pushdown 实现**：

```cpp
// 子节点更新：sum = sum * mul + add * len
// 子标记更新：mul' = mul * parent_mul, add' = add * parent_mul + parent_add
void pushdown(int rt, int len) {
    sum[rt << 1] = (sum[rt << 1] * mul[rt] + add[rt] * (len - (len >> 1))) % mod;
    sum[rt << 1 | 1] = (sum[rt << 1 | 1] * mul[rt] + add[rt] * (len >> 1)) % mod;

    mul[rt << 1] = (mul[rt << 1] * mul[rt]) % mod;
    add[rt << 1] = (add[rt << 1] * mul[rt] + add[rt]) % mod;

    mul[rt << 1 | 1] = (mul[rt << 1 | 1] * mul[rt]) % mod;
    add[rt << 1 | 1] = (add[rt << 1 | 1] * mul[rt] + add[rt]) % mod;

    mul[rt] = 1;
    add[rt] = 0;
}
// 输入：节点编号 rt，区间长度 len；mul[rt] 和 add[rt] 为当前标记
// 输出：无（将乘法/加法标记按"先乘后加"下传给子节点并清空）
```

!!! danger "关键易错点：乘法时 add 也要乘"
    执行区间乘法时，**加法标记 `add` 也要乘上乘数**！因为加法标记表示"每个元素加 $a$"，乘了之后变成"每个元素加 $a \times k$"。

---

## 第八节 线段树 vs 树状数组对比

| 特性 | 树状数组（第51章） | 线段树 |
|------|-------------------|--------|
| 单点修改 | $O(\log n)$ | $O(\log n)$ |
| 区间查询 | $O(\log n)$ | $O(\log n)$ |
| 区间修改 | 差分 $O(\log n)$（仅单点查询） | $O(\log n)$（lazy 标记） |
| 区间修改 + 区间查询 | 需双树状数组，较复杂 | 直接支持 |
| 区间最值 | 较难实现 | 直接支持 |
| 区间乘 + 区间加 | 难以实现 | 双 lazy 标记 |
| 代码量 | 约 10 行 | 约 40 行 |
| 常数 | 小 | 较大 |
| 空间 | $O(n)$ | $O(4n)$ |
| 灵活性 | 低（仅前缀型操作） | 高（支持各种区间操作） |

!!! info "选择建议"
    - **能用树状数组就用树状数组**：代码短、常数小、不易写错
    - **需要区间修改 + 区间查询、区间最值、区间乘加** → 线段树
    - **CSP-S 考试中**：线段树模板必须背熟，是最高频的数据结构之一

---

## 第九节 扫描线简介

### 9.1 经典问题：矩形面积并

给定平面上 $n$ 个矩形，求它们的**并集面积**。

### 9.2 扫描线思路

1. 把每个矩形拆成两条**垂直边**：左边界（下边）和右边界（上边）
2. 按 $x$ 坐标排序所有边
3. 从左到右扫描，用线段树维护 $y$ 轴方向上**当前被覆盖的长度**
4. 相邻两条边之间的面积 = 覆盖长度 $\times$ $x$ 方向间距

```
矩形1和矩形2的面积并：

  ┌──────┐     
  │      │  ┌──────┐
  │  矩1 │  │  矩2 │
  │      │  │      │
  └──────┘  └──────┘
  ←扫描线从左到右移动→
```

线段树的作用：维护 $y$ 轴上每段被覆盖了几次，高效计算被覆盖的总长度。

!!! tip "扫描线是线段树的高级应用"
    扫描线 + 线段树是 CSP-S/SNOI 的进阶考点。核心是线段树维护"区间覆盖次数"，查询"被覆盖的长度"。这里只做简介，掌握基础线段树模板是前提。

---

## 常见误区

!!! danger "误区1：空间只开 2 倍"
    **错误**：`long long sum[N * 2]`
    **原因**：线段树数组存储时，节点编号可能超过 $2n$，最坏达 $4n$。
    **正确**：开 **4 倍空间**，即 `N << 2`。

!!! danger "误区2：忘记 pushdown"
    **错误**：在 `update` 和 `query` 中递归子节点前不调用 `pushdown`。
    **原因**：父节点的 lazy 标记未下传，子节点的 `sum` 值是旧的。
    **正确**：**每次递归子节点前必须先 `pushdown`**，回溯后必须 `pushup`。

!!! danger "误区3：pushdown 中左右子树长度算错"
    **错误**：`sum[rt<<1] += lazy[rt] * (len >> 1)`（左右搞反）
    **正确**：左子树长度 = `len - (len >> 1)`（较大），右子树长度 = `len >> 1`（较小）。

!!! danger "误区4：区间乘法时忘记更新 add 标记"
    **错误**：`updateMul` 中只更新 `mul` 和 `sum`，不更新 `add`。
    **原因**：乘法作用于整个区间，加法标记表示的"每个元素加 $a$"也应变成"加 $a \times k$"。
    **正确**：`add[rt] = (add[rt] * v) % mod;`（乘法时 add 也要乘）。

!!! danger "误区5：lazy 标记没有取模"
    **错误**：双 lazy 标记中不取模，导致溢出。
    **正确**：`sum`、`mul`、`add` 的每次更新都要 `% mod`。

!!! danger "误区6：mid 计算用 (l+r)/2 导致溢出"
    **错误**：`int mid = (l + r) / 2`，当 $l, r$ 很大时溢出。
    **正确**：用 `int mid = (l + r) >> 1`，或 `l + (r - l) / 2`（实际竞赛中 `l + r` 不会溢出 int，但养成好习惯）。

---

## 典型例题

### 【P3372】线段树 1（区间修改 + 区间查询）

**题目**：已知数列，支持两种操作：将区间 $[x, y]$ 内每个数加上 $k$；查询区间 $[x, y]$ 的和。

**分析**：标准的线段树区间修改 + 区间查询模板。使用 lazy 标记延迟修改，`pushdown` 下传标记。

**样例验证**：

```
初始：a = [1, 5, 4, 2, 3]
2 2 4     → sum(2,4) = 5+4+2 = 11
1 2 3 2   → a[2..3] += 2 → a = [1, 7, 6, 2, 3]
2 3 4     → sum(3,4) = 6+2 = 8
1 1 5 1   → a[1..5] += 1 → a = [2, 8, 7, 3, 4]
2 1 4     → sum(1,4) = 2+8+7+3 = 20
```

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100005;
long long a[N], sum[N << 2], lazy[N << 2];
int n, m;

void pushup(int rt) {
    sum[rt] = sum[rt << 1] + sum[rt << 1 | 1];
}

void pushdown(int rt, int len) {
    if (lazy[rt]) {
        lazy[rt << 1] += lazy[rt];
        sum[rt << 1] += lazy[rt] * (len - (len >> 1));
        lazy[rt << 1 | 1] += lazy[rt];
        sum[rt << 1 | 1] += lazy[rt] * (len >> 1);
        lazy[rt] = 0;
    }
}

void build(int rt, int l, int r) {
    lazy[rt] = 0;
    if (l == r) { sum[rt] = a[l]; return; }
    int mid = (l + r) >> 1;
    build(rt << 1, l, mid);
    build(rt << 1 | 1, mid + 1, r);
    pushup(rt);
}

void update(int rt, int l, int r, int L, int R, long long v) {
    if (L <= l && r <= R) {
        sum[rt] += v * (r - l + 1);
        lazy[rt] += v;
        return;
    }
    pushdown(rt, r - l + 1);
    int mid = (l + r) >> 1;
    if (L <= mid) update(rt << 1, l, mid, L, R, v);
    if (R > mid) update(rt << 1 | 1, mid + 1, r, L, R, v);
    pushup(rt);
}

long long query(int rt, int l, int r, int L, int R) {
    if (L <= l && r <= R) return sum[rt];
    pushdown(rt, r - l + 1);
    int mid = (l + r) >> 1;
    long long res = 0;
    if (L <= mid) res += query(rt << 1, l, mid, L, R);
    if (R > mid) res += query(rt << 1 | 1, mid + 1, r, L, R);
    return res;
}

int main() {
    scanf("%d%d", &n, &m);
    for (int i = 1; i <= n; i++) scanf("%lld", &a[i]);
    build(1, 1, n);
    while (m--) {
        int op, x, y;
        long long k;
        scanf("%d", &op);
        if (op == 1) {
            scanf("%d%d%lld", &x, &y, &k);
            update(1, 1, n, x, y, k);
        } else {
            scanf("%d%d", &x, &y);
            printf("%lld\n", query(1, 1, n, x, y));
        }
    }
    return 0;
}
// 输入：
// 5 5
// 1 5 4 2 3
// 2 2 4
// 1 2 3 2
// 2 3 4
// 1 1 5 1
// 2 1 4
// 输出：
// 11
// 8
// 20
```

---

### 【P3373】线段树 2（区间乘 + 区间加 + 区间查询）

**题目**：已知数列，支持三种操作：区间乘 $k$；区间加 $k$；查询区间和对 $m$ 取模。

**分析**：需要**双 lazy 标记**（`mul` 和 `add`）。关键在于 pushdown 的下传顺序——**先乘后加**：子节点的值 = `sum * parent_mul + parent_add * len`。

**样例验证**（模 38）：

```
初始：a = [1, 5, 4, 2, 3]
2 1 4 1   → a[1..4] += 1 → a = [2, 6, 5, 3, 3]
3 2 5     → sum(2,5) = 6+5+3+3 = 17
1 2 4 2   → a[2..4] *= 2 → a = [2, 12, 10, 6, 3]
2 3 5 5   → a[3..5] += 5 → a = [2, 12, 15, 11, 8]
3 1 4     → sum(1,4) = 2+12+15+11 = 40, 40 % 38 = 2
```

```cpp
#include <bits/stdc++.h>
using namespace std;

const int N = 100005;
long long a[N], sum[N << 2], add[N << 2], mul[N << 2];
int n, q;
long long mod;

void pushup(int rt) {
    sum[rt] = (sum[rt << 1] + sum[rt << 1 | 1]) % mod;
}

void pushdown(int rt, int len) {
    // 子节点：sum = sum * mul + add * len（先乘后加）
    sum[rt << 1] = (sum[rt << 1] * mul[rt] % mod + add[rt] * (len - (len >> 1))) % mod;
    sum[rt << 1 | 1] = (sum[rt << 1 | 1] * mul[rt] % mod + add[rt] * (len >> 1)) % mod;
    // 子标记更新
    mul[rt << 1] = mul[rt << 1] * mul[rt] % mod;
    add[rt << 1] = (add[rt << 1] * mul[rt] + add[rt]) % mod;
    mul[rt << 1 | 1] = mul[rt << 1 | 1] * mul[rt] % mod;
    add[rt << 1 | 1] = (add[rt << 1 | 1] * mul[rt] + add[rt]) % mod;
    // 清空当前标记
    mul[rt] = 1;
    add[rt] = 0;
}

void build(int rt, int l, int r) {
    mul[rt] = 1; add[rt] = 0;
    if (l == r) { sum[rt] = a[l] % mod; return; }
    int mid = (l + r) >> 1;
    build(rt << 1, l, mid);
    build(rt << 1 | 1, mid + 1, r);
    pushup(rt);
}

// 区间乘
void updateMul(int rt, int l, int r, int L, int R, long long v) {
    if (L <= l && r <= R) {
        sum[rt] = sum[rt] * v % mod;
        mul[rt] = mul[rt] * v % mod;
        add[rt] = add[rt] * v % mod;   // 加法标记也要乘！
        return;
    }
    pushdown(rt, r - l + 1);
    int mid = (l + r) >> 1;
    if (L <= mid) updateMul(rt << 1, l, mid, L, R, v);
    if (R > mid) updateMul(rt << 1 | 1, mid + 1, r, L, R, v);
    pushup(rt);
}

// 区间加
void updateAdd(int rt, int l, int r, int L, int R, long long v) {
    if (L <= l && r <= R) {
        sum[rt] = (sum[rt] + v * (r - l + 1)) % mod;
        add[rt] = (add[rt] + v) % mod;
        return;
    }
    pushdown(rt, r - l + 1);
    int mid = (l + r) >> 1;
    if (L <= mid) updateAdd(rt << 1, l, mid, L, R, v);
    if (R > mid) updateAdd(rt << 1 | 1, mid + 1, r, L, R, v);
    pushup(rt);
}

long long query(int rt, int l, int r, int L, int R) {
    if (L <= l && r <= R) return sum[rt];
    pushdown(rt, r - l + 1);
    int mid = (l + r) >> 1;
    long long res = 0;
    if (L <= mid) res = (res + query(rt << 1, l, mid, L, R)) % mod;
    if (R > mid) res = (res + query(rt << 1 | 1, mid + 1, r, L, R)) % mod;
    return res;
}

int main() {
    scanf("%d%d%lld", &n, &q, &mod);
    for (int i = 1; i <= n; i++) scanf("%lld", &a[i]);
    build(1, 1, n);
    while (q--) {
        int op, x, y;
        long long k;
        scanf("%d", &op);
        if (op == 1) {
            scanf("%d%d%lld", &x, &y, &k);
            updateMul(1, 1, n, x, y, k);
        } else if (op == 2) {
            scanf("%d%d%lld", &x, &y, &k);
            updateAdd(1, 1, n, x, y, k);
        } else {
            scanf("%d%d", &x, &y);
            printf("%lld\n", query(1, 1, n, x, y));
        }
    }
    return 0;
}
// 输入：
// 5 5 38
// 1 5 4 2 3
// 2 1 4 1
// 3 2 5
// 1 2 4 2
// 2 3 5 5
// 3 1 4
// 输出：
// 17
// 2
```

!!! tip "双 lazy 标记的记忆口诀"
    - **pushdown 顺序**：先算 sum（`sum * mul + add * len`），再更新子标记（`mul' = mul * parent_mul, add' = add * parent_mul + parent_add`）
    - **区间乘时**：`sum` 乘 $v$、`mul` 乘 $v$、**`add` 也乘 $v$**
    - **区间加时**：`sum` 加 $v \times len$、`add` 加 $v$、`mul` 不变
    - **初始值**：`mul = 1, add = 0`

---

## 📖 本章总结

### 核心要点回顾

- ✅ **线段树结构**：每个节点代表区间 $[l, r]$，左儿子 $[l, mid]$，右儿子 $[mid+1, r]$，数组存储开 $4n$ 空间
- ✅ **build 建树**：$O(n)$，递归到叶子后 `pushup` 回溯
- ✅ **单点修改 / 区间查询**：$O(\log n)$，递归 + 回溯
- ✅ **lazy 标记**：区间完全包含时不往下递归，打标记延迟修改
- ✅ **pushdown**：递归子节点前下传 lazy 标记，左子树长度 `len-(len>>1)`，右子树长度 `len>>1`
- ✅ **双 lazy 标记**：先乘后加，乘法时 `add` 也要乘，所有操作取模

### 知识脉络

- **线段树（最通用的区间数据结构）**
  - **基础结构**
    - 每个节点代表区间 $[l,r]$，左儿子 $[l,mid]$，右儿子 $[mid+1,r]$
    - 数组存储：根为 1，左 `rt<<1`，右 `rt<<1|1`，空间开 $4n$
  - **核心操作（均 $O(\log n)$）**
    - `build` 建树：$O(n)$，递归 + `pushup` 回溯
    - `update` 单点修改：递归到叶子 + `pushup`
    - `query` 区间查询：拆分为若干节点并集
  - **lazy 标记（区间修改核心）**
    - 完全包含时打标记，不往下递归
    - `pushdown`：递归子节点前下传标记
    - `pushup`：回溯时合并子节点信息
  - **双 lazy 标记（乘法 + 加法）**
    - pushdown 顺序：先乘后加（`sum * mul + add * len`）
    - 区间乘时 `add` 也要乘
    - 初始 `mul=1, add=0`
  - **线段树 vs 树状数组**
    - 树状数组：代码短、常数小，仅前缀型操作
    - 线段树：功能强，支持区间修改 + 区间查询、最值、乘加
  - **高级应用**
    - 扫描线（矩形面积并）：线段树维护覆盖长度
    - 权值线段树 / 可持久化线段树（主席树）

### 题目速查表

| 题目 | 难度 | 考点 | 模板 |
|------|------|------|------|
| P3372 线段树 1 | 🟢 | 区间修改+区间查询 | lazy + pushdown |
| P3373 线段树 2 | 🟡 | 区间乘+区间加+区间查询 | 双 lazy 标记 |
| P1531 I Hate It | 🟡 | 区间最值 | sum 改 max |
| 扫描线（矩形面积并） | 🔴 | 线段树 + 离散化 | 维护覆盖长度 |

### 线段树模板速记

```
建树：build(1, 1, n) → 叶子赋值 + pushup 回溯
单点修改：update(1,1,n,p,v) → 递归到叶子 + pushup
区间查询：query(1,1,n,L,R) → 完全包含返回 + 部分交集递归
区间修改：update(1,1,n,L,R,v) → 完全包含打lazy + 否则pushdown+递归+pushup
pushdown：子节点 sum += lazy*len，子 lazy 继承，自身清零
```

---

### 知识衔接

本章是考纲补强卷的第二章，与前后章节的联系如下：

- **前置知识**：第51章树状数组（对比理解两种区间数据结构的优劣）、第25章二叉树（线段树的树形结构基础）、第08章函数与递归入门（递归建树与递归查询的核心实现）
- **本章定位**：讲解区间修改+区间查询的通用利器，通过lazy标记实现高效区间操作，是CSP-S最核心的进阶数据结构之一
- **后续发展**：本章为第53章强连通分量与差分约束铺路，从数据结构转向图论进阶，同时线段树也是第54章扫描线算法的配合工具

!!! info "下一章预告"
    线段树解决了区间修改与查询问题，但图论中还有一类重要问题：**强连通分量（SCC）**与**差分约束系统**。下一章我们将学习 Tarjan 算法求强连通分量、缩点技术在 DAG 上的 DP 应用，以及差分约束系统如何将"不等式组"转化为"最短路"问题。这些是 CSP-S 图论进阶的核心考点！
